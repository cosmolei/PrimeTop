# 客户端 — 数据导出与账户注销页面架构与交互设计

> **文档版本**：v1.0  
> **最后更新**：2026-06-06  
> **依赖文档**：
> - [端到端流程设计-数据导出与账户注销完整链路](端到端流程设计-数据导出与账户注销完整链路-详细设计.md)
> - [学习数据导出与账户注销服务-详细设计](学习数据导出与账户注销服务-详细设计.md)
> - [客户端-设置页面与偏好配置交互设计](客户端-设置页面与偏好配置交互设计.md)
> - [客户端-我的页面架构与个人中心交互设计](客户端-我的页面架构与个人中心交互设计.md)

---

## 1. 概述

### 1.1 设计目标

为 PrimeTop 用户提供清晰、安全、可信赖的数据导出和账户注销交互体验，满足《个人信息保护法》合规要求，同时降低因误操作导致的用户流失。

### 1.2 核心原则

| 原则 | 说明 |
|------|------|
| **透明可控** | 用户清楚知道哪些数据会被导出/删除，每一步都有确认 |
| **可逆优先** | 注销设置 15 天冷静期，导出支持随时取消 |
| **安全兜底** | 关键操作需要身份验证（短信验证码 / 生物识别） |
| **低龄适配** | 未成年用户操作需家长代理确认 |
| **状态可见** | 异步任务提供进度反馈，完成后主动通知 |

### 1.3 页面清单

| 页面 | 路由 | 说明 |
|------|------|------|
| 隐私与数据管理页 | `/settings/privacy` | 入口页，汇聚数据导出和账户注销 |
| 数据导出主页 | `/settings/privacy/export` | 选择导出范围和格式 |
| 导出模块选择页 | `/settings/privacy/export/modules` | 细粒度勾选要导出的数据模块 |
| 导出进度页 | `/settings/privacy/export/progress/:taskId` | 导出任务进度和下载 |
| 导出历史页 | `/settings/privacy/export/history` | 历史导出记录列表 |
| 账户注销引导页 | `/settings/privacy/deactivate` | 注销须知和挽留 |
| 注销身份验证页 | `/settings/privacy/deactivate/verify` | 二次身份验证 |
| 注销权益检查页 | `/settings/privacy/deactivate/check` | 展示未消耗权益 |
| 注销确认页 | `/settings/privacy/deactivate/confirm` | 最终确认（含冷静期说明） |
| 注销状态页 | `/settings/privacy/deactivate/status` | 注销进行中/冷静期状态 |
| 家长代理确认页 | `/settings/privacy/parent-confirm` | 未成年操作的家长授权弹窗 |

---

## 2. 入口与导航设计

### 2.1 入口路径

```
我的 → 设置 → 隐私与数据 → [数据导出 | 账户注销]
                      ↑
             （设置页新增"隐私与数据"入口）
```

### 2.2 入口组件

```dart
/// 在设置页中新增入口项
class _SettingsPageState extends State<SettingsPage> {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        // ... 已有设置项 ...

        const SettingsSectionHeader(title: '安全与隐私'),
        SettingsListTile(
          icon: Icons.shield_outlined,
          title: '隐私与数据管理',
          subtitle: '导出数据、注销账户',
          onTap: () => context.push('/settings/privacy'),
          trailing: const Icon(Icons.chevron_right, size: 20),
        ),

        // ... 其他设置项 ...
      ],
    );
  }
}
```

---

## 3. 隐私与数据管理页（入口页）

### 3.1 页面布局

```
┌──────────────────────────────────┐
│  ←  隐私与数据管理               │
├──────────────────────────────────┤
│                                  │
│  ┌────────────────────────────┐  │
│  │ 📦 导出我的数据            │  │
│  │                            │  │
│  │ 将你的学习记录、错题本、   │  │
│  │ 对话历史等数据打包下载     │  │
│  │                            │  │
│  │ 上次导出：2026-05-20       │  │
│  │                        >   │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ ⚠️ 注销账户               │  │
│  │                            │  │
│  │ 永久删除账户及所有数据     │  │
│  │ 注销后不可恢复             │  │
│  │                            │  │
│  │                    注销 >  │  │
│  └────────────────────────────┘  │
│                                  │
│  ─── 法律条款 ───                │
│                                  │
│  📄 隐私政策                     │
│  📄 用户协议                     │
│  📄 儿童隐私保护声明             │
│                                  │
└──────────────────────────────────┘
```

### 3.2 关键交互逻辑

```dart
class PrivacyDataPage extends StatefulWidget {
  @override
  State<PrivacyDataPage> createState() => _PrivacyDataPageState();
}

class _PrivacyDataPageState extends State<PrivacyDataPage> {
  DateTime? _lastExportDate;
  bool _isLoading = true;

  @override
  void initState() {
    super.initState();
    _loadLastExportDate();
  }

  Future<void> _loadLastExportDate() async {
    final result = await DataExportApi.getLastExportSummary();
    if (mounted) {
      setState(() {
        _lastExportDate = result.lastExportDate;
        _isLoading = false;
      });
    }
  }

  void _onExportTap() {
    // 检查是否未成年 → 需要家长代理
    if (UserScope.isMinor()) {
      _showParentConfirmDialog(
        action: 'DATA_EXPORT',
        onConfirmed: () => context.push('/settings/privacy/export'),
      );
    } else {
      context.push('/settings/privacy/export');
    }
  }

  void _onDeactivateTap() {
    // 注销入口始终需要二次确认
    _showDeactivateWarningDialog();
  }

  void _showDeactivateWarningDialog() {
    showCupertinoDialog(
      context: context,
      builder: (ctx) => CupertinoAlertDialog(
        title: const Text('确认要注销账户？'),
        content: const Text(
          '注销后你的所有学习数据、错题本、会员权益等将被永久删除，'
          '且无法恢复。建议先导出数据备份。',
        ),
        actions: [
          CupertinoDialogAction(
            child: const Text('先导出数据'),
            onPressed: () {
              Navigator.pop(ctx);
              context.push('/settings/privacy/export');
            },
          ),
          CupertinoDialogAction(
            isDestructiveAction: true,
            child: const Text('继续注销'),
            onPressed: () {
              Navigator.pop(ctx);
              if (UserScope.isMinor()) {
                _showParentConfirmDialog(
                  action: 'ACCOUNT_DEACTIVATE',
                  onConfirmed: () => context.push('/settings/privacy/deactivate'),
                );
              } else {
                context.push('/settings/privacy/deactivate');
              }
            },
          ),
          CupertinoDialogAction(
            isDefaultAction: true,
            child: const Text('取消'),
            onPressed: () => Navigator.pop(ctx),
          ),
        ],
      ),
    );
  }
}
```

---

## 4. 数据导出主页

### 4.1 页面布局

```
┌──────────────────────────────────┐
│  ←  导出我的数据                 │
├──────────────────────────────────┤
│                                  │
│  选择导出范围                    │
│                                  │
│  ┌─ ○ 全部数据 ──────────────┐  │
│  │  导出所有个人数据          │  │
│  │  预计文件较大（约 50MB）   │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌─ ○ 选择性导出 ───────────┐   │
│  │  勾选需要导出的数据类型    │   │
│  │  已选 4/12 个模块      >  │   │
│  └────────────────────────────┘  │
│                                  │
│  ─── 导出格式 ───                │
│                                  │
│  [HTML 可读格式  ✅]             │  ← 推荐，浏览器直接打开
│  [JSON 机器格式    ]             │
│  [CSV 表格格式     ]             │
│                                  │
│  ─── 说明 ───                    │
│                                  │
│  ℹ️ 导出文件为加密 ZIP，密码将通过  │
│  短信发送到你的手机。             │
│  ℹ️ 导出过程约需 5~30 分钟。      │
│  ℹ️ 导出文件保留 7 天后自动删除。  │
│                                  │
│  ┌──────────────────────────┐    │
│  │      开始导出             │    │
│  └──────────────────────────┘    │
│                                  │
│  导出历史 (3)                 >  │
│                                  │
└──────────────────────────────────┘
```

### 4.2 数据结构

```dart
/// 导出请求模型
class ExportRequest {
  final ExportType exportType; // FULL | PARTIAL
  final List<ExportModule>? selectedModules;
  final ExportFormat format;   // HTML | JSON | CSV
}

enum ExportType { full, partial }

enum ExportFormat { html, json, csv }

/// 导出模块定义（与 UI 卡片一一对应）
class ExportModuleItem {
  final ExportModule module;
  final String title;
  final String description;
  final IconData icon;
  final bool isRecommended; // 推荐导出的模块
}

/// 预定义模块列表
const kExportModules = [
  ExportModuleItem(
    module: ExportModule.profile,
    title: '基本信息',
    description: '昵称、头像、年级、教材版本',
    icon: Icons.person_outline,
    isRecommended: true,
  ),
  ExportModuleItem(
    module: ExportModule.learningRecords,
    title: '学习记录',
    description: '学习时长、章节进度、完成情况',
    icon: Icons.timeline,
    isRecommended: true,
  ),
  ExportModuleItem(
    module: ExportModule.mistakeBook,
    title: '错题本',
    description: '错题记录、错因标签、订正记录',
    icon: Icons.error_outline,
    isRecommended: true,
  ),
  ExportModuleItem(
    module: ExportModule.exerciseData,
    title: '练习数据',
    description: '答题记录、正确率统计、题型分布',
    icon: Icons.assignment,
    isRecommended: true,
  ),
  ExportModuleItem(
    module: ExportModule.aiConversations,
    title: 'AI 对话记录',
    description: '与 AI 的所有辅导对话历史',
    icon: Icons.chat_bubble_outline,
    isRecommended: false,
  ),
  ExportModuleItem(
    module: ExportModule.studyPlans,
    title: '学习计划',
    description: '学习目标、每日任务、计划完成率',
    icon: Icons.event_note,
    isRecommended: false,
  ),
  ExportModuleItem(
    module: ExportModule.notesFavorites,
    title: '笔记与收藏',
    description: '收藏的题目、知识点笔记',
    icon: Icons.bookmark_outline,
    isRecommended: false,
  ),
  ExportModuleItem(
    module: ExportModule.ordersPayments,
    title: '订单与支付',
    description: '购买记录、发票信息',
    icon: Icons.receipt_long,
    isRecommended: false,
  ),
  ExportModuleItem(
    module: ExportModule.membership,
    title: '会员信息',
    description: '订阅状态、权益记录',
    icon: Icons.card_membership,
    isRecommended: false,
  ),
  ExportModuleItem(
    module: ExportModule.achievements,
    title: '成就与勋章',
    description: '获得的勋章、成就记录',
    icon: Icons.emoji_events_outlined,
    isRecommended: false,
  ),
  ExportModuleItem(
    module: ExportModule.settings,
    title: '设置与偏好',
    description: '个人偏好、通知设置',
    icon: Icons.settings_outlined,
    isRecommended: false,
  ),
  ExportModuleItem(
    module: ExportModule.feedback,
    title: '反馈记录',
    description: '历史意见反馈和客服记录',
    icon: Icons.feedback_outlined,
    isRecommended: false,
  ),
];
```

### 4.3 核心交互

```dart
class DataExportPage extends StatefulWidget {
  @override
  State<DataExportPage> createState() => _DataExportPageState();
}

class _DataExportPageState extends State<DataExportPage> {
  ExportType _type = ExportType.full;
  ExportFormat _format = ExportFormat.html;
  Set<ExportModule> _selectedModules = {};
  bool _isSubmitting = false;

  // 推荐模块（默认选中）
  final _recommendedModules = kExportModules
      .where((m) => m.isRecommended)
      .map((m) => m.module)
      .toSet();

  @override
  void initState() {
    super.initState();
    _selectedModules = {..._recommendedModules};
  }

  Future<void> _startExport() async {
    if (_isSubmitting) return;

    // 前置校验
    if (_type == ExportType.partial && _selectedModules.isEmpty) {
      showToast('请至少选择一个导出模块');
      return;
    }

    setState(() => _isSubmitting = true);

    try {
      final request = ExportRequest(
        exportType: _type,
        selectedModules: _type == ExportType.partial
            ? _selectedModules.toList()
            : null,
        format: _format,
      );

      final task = await DataExportApi.createExportTask(request);

      if (!mounted) return;

      // 跳转到进度页
      context.replace('/settings/privacy/export/progress/${task.taskId}');
    } on ApiException catch (e) {
      _handleExportError(e);
    } finally {
      if (mounted) setState(() => _isSubmitting = false);
    }
  }

  void _handleExportError(ApiException e) {
    switch (e.code) {
      case 'EXPORT_TASK_LIMIT_EXCEEDED':
        showDialog(
          context: context,
          builder: (ctx) => AlertDialog(
            title: const Text('导出任务过多'),
            content: const Text('你已有正在进行的导出任务，请等待完成后再试。'),
            actions: [
              TextButton(
                onPressed: () {
                  Navigator.pop(ctx);
                  context.push('/settings/privacy/export/history');
                },
                child: const Text('查看导出记录'),
              ),
            ],
          ),
        );
        break;
      case 'ACCOUNT_NOT_VERIFIED':
        showToast('请先完成手机号验证');
        context.push('/settings/account/verify');
        break;
      default:
        showToast('导出请求失败，请稍后重试');
    }
  }
}
```

---

## 5. 导出模块选择页

### 5.1 页面布局

```
┌──────────────────────────────────┐
│  ←  选择导出内容                 │
├──────────────────────────────────┤
│                                  │
│  已选择 4/12 个模块              │
│                                  │
│  ── 推荐 ────────────────────    │
│                                  │
│  ✅ 📝 基本信息                  │
│     昵称、头像、年级、教材版本    │
│                                  │
│  ✅ 📊 学习记录                  │
│     学习时长、章节进度、完成情况  │
│                                  │
│  ✅ ❌ 错题本                    │
│     错题记录、错因标签、订正记录  │
│                                  │
│  ✅ 📋 练习数据                  │
│     答题记录、正确率统计          │
│                                  │
│  ── 可选 ────────────────────    │
│                                  │
│  ☐  💬 AI 对话记录              │
│     与 AI 的所有辅导对话历史      │
│                                  │
│  ☐  📅 学习计划                 │
│     学习目标、每日任务            │
│                                  │
│  ☐  🔖 笔记与收藏              │
│     收藏的题目、知识点笔记        │
│                                  │
│  ... (更多模块)                  │
│                                  │
│  ┌──────────────────────────┐    │
│  │  确认选择 (4 个模块)      │    │
│  └──────────────────────────┘    │
│                                  │
└──────────────────────────────────┘
```

### 5.2 组件实现

```dart
class ExportModuleSelectPage extends StatefulWidget {
  final Set<ExportModule> initialSelection;

  @override
  State<ExportModuleSelectPage> createState() => _State();
}

class _State extends State<ExportModuleSelectPage> {
  late Set<ExportModule> _selected;

  @override
  void initState() {
    super.initState();
    _selected = {...widget.initialSelection};
  }

  @override
  Widget build(BuildContext context) {
    final recommended =
        kExportModules.where((m) => m.isRecommended).toList();
    final optional =
        kExportModules.where((m) => !m.isRecommended).toList();

    return Scaffold(
      appBar: AppBar(title: const Text('选择导出内容')),
      body: Column(
        children: [
          // 计数器
          _buildCounter(),
          // 推荐模块
          Expanded(
            child: ListView(
              children: [
                const SectionHeader(title: '推荐导出'),
                ...recommended.map((m) => _buildModuleTile(m)),
                const SectionHeader(title: '可选导出'),
                ...optional.map((m) => _buildModuleTile(m)),
              ],
            ),
          ),
          // 底部按钮
          _buildBottomBar(),
        ],
      ),
    );
  }

  Widget _buildCounter() {
    return Container(
      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
      color: Theme.of(context).colorScheme.surfaceVariant,
      child: Row(
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: [
          Text('已选择 ${_selected.length}/${kExportModules.length} 个模块'),
          if (_selected.isNotEmpty)
            TextButton(
              onPressed: () => setState(() => _selected.clear()),
              child: const Text('清空'),
            ),
        ],
      ),
    );
  }

  Widget _buildModuleTile(ExportModuleItem item) {
    final isSelected = _selected.contains(item.module);
    return CheckboxListTile(
      secondary: Icon(item.icon),
      title: Text(item.title),
      subtitle: Text(item.description),
      value: isSelected,
      onChanged: (v) {
        setState(() {
          if (v == true) {
            _selected.add(item.module);
          } else {
            _selected.remove(item.module);
          }
        });
      },
    );
  }

  Widget _buildBottomBar() {
    return SafeArea(
      child: Padding(
        padding: const EdgeInsets.all(16),
        child: FilledButton(
          onPressed: _selected.isEmpty ? null : () => _confirm(),
          child: Text('确认选择（${_selected.length} 个模块）'),
        ),
      ),
    );
  }

  void _confirm() {
    Navigator.pop(context, _selected);
  }
}
```

---

## 6. 导出进度页

### 6.1 页面布局

```
┌──────────────────────────────────┐
│  ←  数据导出                     │
├──────────────────────────────────┤
│                                  │
│         ┌──────────┐             │
│         │ 进度动画 │             │
│         │  ▓▓▓▓░░  │             │
│         │   65%    │             │
│         └──────────┘             │
│                                  │
│  正在打包你的数据...             │
│  当前：聚合 AI 对话记录          │
│                                  │
│  ┌────────────────────────────┐  │
│  │ ✅ 基本信息       已完成   │  │
│  │ ✅ 学习记录       已完成   │  │
│  │ ✅ 错题本         已完成   │  │
│  │ 🔄 AI 对话记录    进行中   │  │
│  │ ⏳ 练习数据       等待中   │  │
│  │ ⏳ 笔记与收藏     等待中   │  │
│  └────────────────────────────┘  │
│                                  │
│  预计剩余时间：约 8 分钟         │
│  开始时间：2026-06-06 07:15      │
│                                  │
│  ──────────────────────────────  │
│                                  │
│  ⚠️ 导出期间请勿关闭应用         │
│     完成后将推送通知             │
│                                  │
│  ┌──────────────────────────┐    │
│  │      取消导出             │    │
│  └──────────────────────────┘    │
│                                  │
└──────────────────────────────────┘
```

### 6.2 进度状态机

```dart
/// 导出任务状态枚举（与服务端对齐）
enum ExportTaskStatus {
  queued,       // 排队中
  aggregating,  // 数据聚合中
  packaging,    // 打包中
  encrypting,   // 加密中
  uploading,    // 上传到存储中
  completed,    // 已完成
  failed,       // 失败
  cancelled,    // 已取消
  expired,      // 已过期（下载链接失效）
}

/// 单个模块状态
enum ModuleExportStatus {
  pending,    // 等待中
  running,    // 进行中
  completed,  // 已完成
  failed,     // 失败
  skipped,    // 跳过（无数据）
}
```

### 6.3 轮询与状态管理

```dart
class ExportProgressPage extends StatefulWidget {
  final String taskId;
  const ExportProgressPage({required this.taskId, super.key});

  @override
  State<ExportProgressPage> createState() => _ExportProgressPageState();
}

class _ExportProgressPageState extends State<ExportProgressPage> {
  ExportTaskDetail? _task;
  Timer? _pollTimer;
  bool _isCancelling = false;

  // 轮询间隔策略：5s → 10s → 15s（上限）
  static const _pollIntervals = [5, 10, 15];
  int _pollIndex = 0;

  @override
  void initState() {
    super.initState();
    _loadTask();
  }

  @override
  void dispose() {
    _pollTimer?.cancel();
    super.dispose();
  }

  Future<void> _loadTask() async {
    final task = await DataExportApi.getExportTask(widget.taskId);
    if (!mounted) return;
    setState(() => _task = task);

    if (task.status == ExportTaskStatus.completed) {
      _onExportCompleted(task);
    } else if (task.status == ExportTaskStatus.failed) {
      _onExportFailed(task);
    } else if (_isInProgress(task.status)) {
      _scheduleNextPoll();
    }
  }

  void _scheduleNextPoll() {
    final seconds = _pollIntervals[_pollIndex.clamp(0, _pollIntervals.length - 1)];
    _pollTimer = Timer(Duration(seconds: seconds), () {
      _pollIndex = (_pollIndex + 1).clamp(0, _pollIntervals.length - 1);
      _loadTask();
    });
  }

  bool _isInProgress(ExportTaskStatus s) {
    return const [
      ExportTaskStatus.queued,
      ExportTaskStatus.aggregating,
      ExportTaskStatus.packaging,
      ExportTaskStatus.encrypting,
      ExportTaskStatus.uploading,
    ].contains(s);
  }

  void _onExportCompleted(ExportTaskDetail task) {
    // 展示成功状态 + 下载按钮
    // 发送本地通知
    LocalNotificationService.show(
      title: '数据导出完成',
      body: '你的数据已打包完成，点击下载',
      payload: '/settings/privacy/export/progress/${task.taskId}',
    );
  }

  void _onExportFailed(ExportTaskDetail task) {
    showCupertinoDialog(
      context: context,
      builder: (ctx) => CupertinoAlertDialog(
        title: const Text('导出失败'),
        content: Text(task.errorMessage ?? '数据导出过程中出现错误，请重试。'),
        actions: [
          CupertinoDialogAction(
            child: const Text('重试'),
            onPressed: () {
              Navigator.pop(ctx);
              _retryExport();
            },
          ),
          CupertinoDialogAction(
            isDefaultAction: true,
            child: const Text('确定'),
            onPressed: () => Navigator.pop(ctx),
          ),
        ],
      ),
    );
  }

  Future<void> _cancelExport() async {
    final confirmed = await showConfirmDialog(
      context: context,
      title: '取消导出',
      message: '确定取消当前导出任务？已导出的数据将被丢弃。',
    );
    if (!confirmed) return;

    setState(() => _isCancelling = true);
    try {
      await DataExportApi.cancelExportTask(widget.taskId);
      if (mounted) {
        showToast('已取消导出');
        context.pop();
      }
    } catch (_) {
      if (mounted) showToast('取消失败，请重试');
    } finally {
      if (mounted) setState(() => _isCancelling = false);
    }
  }

  Future<void> _downloadExport() async {
    if (_task?.downloadUrl == null) return;

    // 调用系统分享/下载
    final result = await FileDownloadService.downloadAndSave(
      url: _task!.downloadUrl!,
      fileName: 'PrimeTop_数据导出_${DateTime.now().yyyyMMdd()}.zip',
      passwordHint: '解压密码已发送至你的手机 ${_maskPhone(UserScope.phone)}',
    );

    if (result == DownloadResult.success && mounted) {
      showToast('文件已保存');
    }
  }

  String _maskPhone(String? phone) {
    if (phone == null || phone.length < 7) return '***';
    return '${phone.substring(0, 3)}****${phone.substring(phone.length - 4)}';
  }

  Future<void> _retryExport() async {
    // 重新发起导出（回到导出主页参数复用）
    context.pop();
  }

  @override
  Widget build(BuildContext context) {
    // ... 根据 _task 状态渲染不同 UI
    // 完成态：进度100% + 下载按钮 + 有效期倒计时
    // 进行中：进度动画 + 模块列表 + 取消按钮
    // 失败态：错误信息 + 重试按钮
    return Scaffold(/* ... */);
  }
}
```

### 6.4 完成态布局

```
┌──────────────────────────────────┐
│  ←  数据导出                     │
├──────────────────────────────────┤
│                                  │
│         ✅ 导出完成              │
│                                  │
│  文件大小：23.5 MB               │
│  包含模块：4 个                  │
│  完成时间：2026-06-06 07:28      │
│                                  │
│  ⚠️