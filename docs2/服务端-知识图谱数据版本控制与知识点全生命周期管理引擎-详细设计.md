# 知识图谱数据版本控制与知识点全生命周期管理引擎 详细设计

## 1. 概述

### 1.1 模块定位

知识图谱是 PrimeTop 教育平台的底层数据骨架——题目标注、教材章节映射、学习路径规划、学情诊断、AI 辅导上下文注入等核心业务全部依赖知识点体系的正确性与稳定性。本引擎负责管理知识点的**实体生命周期状态**与知识图谱的**数据版本控制**，确保知识图谱在任何时点都处于一致、可追溯、可回滚的状态。

### 1.2 核心职责

| 职责 | 说明 |
| --- | --- |
| 知识点生命周期状态机 | 管理 KnowledgePoint 实体从草稿到归档的完整状态流转 |
| 图谱快照版本管理 | 对知识图谱进行定期/触发式快照，支持任意历史版本回溯查询 |
| 变更审计与影响分析 | 记录每一次知识点/边的变更，自动计算变更对下游资源的影响范围 |
| 向后兼容与引用迁移 | 知识点废弃/合并/拆分时，自动处理题目、课程、学生数据中的旧引用 |
| 时序查询支持 | 支持按时间点/版本号查询图谱拓扑，满足历史数据分析与合规审计需求 |

### 1.3 依赖关系

```
                    ┌──────────────────────────┐
                    │   本引擎 (KG Versioning)  │
                    └──────────┬───────────────┘
         ┌─────────────┬──────┴───────┬──────────────┐
         ▼             ▼              ▼              ▼
  ┌──────────────┐ ┌──────────┐ ┌──────────┐ ┌───────────────┐
  │ 知识点体系    │ │ 题目服务 │ │ 内容服务 │ │ 学情分析服务  │
  │ 与教材映射    │ │          │ │           │               │
  └──────────────┘ └──────────┘ └──────────┘ └───────────────┘
         │                                             │
         ▼                                             ▼
  ┌──────────────┐                          ┌───────────────────┐
  │ 图数据库      │                          │ 学生掌握度数据     │
  │ (Neo4j/Janus)│                          │ (MySQL/ClickHouse) │
  └──────────────┘                          └───────────────────┘
```

**上游依赖：**
- 知识点体系与教材映射引擎：提供知识点基础数据
- 管理后台：提供人工操作入口（创建、审核、发布、废弃知识点）

**下游影响：**
- 题目服务：题目关联的知识点 ID 变更需要同步
- 内容服务：教材章节关联的知识点变更需要同步
- 学情分析服务：学生掌握度数据中的知识点引用需要迁移
- AI 辅导服务：RAG 检索使用的知识点图谱需要保持一致
- 学习路径推荐：路径规划依赖的图谱拓扑需要匹配当前版本

### 1.4 设计原则

1. **不可变历史**：已发布的图谱版本不可被修改，只能产生新版本
2. **兼容优先**：知识点废弃时必须提供替代项或迁移路径
3. **影响可控**：任何变更必须经过影响分析，高风险变更需人工审批
4. **最终一致**：下游服务通过事件通知 + 异步迁移达到最终一致

---

## 2. 数据模型

### 2.1 核心实体定义

#### 2.1.1 KnowledgePoint（知识点实体）

```typescript
interface KnowledgePoint {
  // === 基础信息 ===
  id: string;                    // 全局唯一 ID，格式：kp_{ulid}
  code: string;                  // 知识点编码，如 "MATH.6.RF.01" (数学6年级分数01)
  name: string;                  // 知识点名称，如 "分数的基本性质"
  aliases: string[];             // 别名列表，如 ["等值分数", "分数相等"]
  description: string;           // 详细描述
  subject: SubjectCode;          // 学科：CHINESE|MATH|ENGLISH|PHYSICS|...
  stage: StageCode;              // 学段：KINDERGARTEN|PRIMARY|JUNIOR|SENIOR
  gradeBand: GradeBand[];        // 适用年级段：["G5","G6"]

  // === 分类与标签 ===
  category: KnowledgeCategory;   // 类别：CONCEPT|SKILL|METHOD|APPLICATION
  cognitiveLevel: CognitiveLevel;// 认知层级：REMEMBER|UNDERSTAND|APPLY|ANALYZE|EVALUATE|CREATE
  difficulty: number;            // 基础难度系数 1-5
  tags: string[];                // 业务标签：["中考高频","易错点"]

  // === 关键词与检索 ===
  keywords: string[];            // 检索关键词，用于RAG匹配
  examples: string[];            // 典型例子描述

  // === 生命周期 ===
  status: KnowledgePointStatus;  // 状态：DRAFT|IN_REVIEW|PUBLISHED|DEPRECATED|ARCHIVED
  supersedesId?: string;         // 替代的旧知识点ID（新知识点指向被废弃的旧知识点）
  supersededById?: string;       // 被哪个新知识点替代（旧知识点指向新知识点）
  mergeTargetId?: string;        // 合并到的目标知识点ID
  splitFromId?: string;          // 从哪个知识点拆分而来

  // === 版本控制 ===
  currentVersion: number;        // 当前数据版本号，从1开始递增
  graphVersionId: string;        // 所属图谱版本ID

  // === 元数据 ===
  createdAt: string;             // 创建时间 ISO 8601
  updatedAt: string;             // 最后修改时间
  publishedAt?: string;          // 发布时间
  deprecatedAt?: string;         // 废弃时间
  archivedAt?: string;           // 归档时间

  // === 审计 ===
  createdBy: string;             // 创建人（管理后台用户ID）
  updatedBy: string;             // 最后修改人
  reviewedBy?: string;           // 审核人
}
```

#### 2.1.2 知识点状态枚举

```typescript
/**
 * 知识点生命周期状态
 *
 * 状态流转图：
 *
 *  ┌──────┐  提交审核   ┌───────────┐  审核通过  ┌───────────┐  废弃操作  ┌────────────┐  定期清理 ┌──────────┐
 *  │ DRAFT │ ─────────► │ IN_REVIEW  │ ────────► │ PUBLISHED │ ────────► │ DEPRECATED │ ────────► │ ARCHIVED │
 *  └──────┘            └───────────┘           └───────────┘           └────────────┘          └──────────┘
 *      │                     │                      │ ▲                     │
 *      │     审核驳回         │                      │ │    发布新版本        │ 回退（仅在
 *      │     (退回修改)       │                      │ │    (版本号+1)        │ 窗口期内）
 *      ▼                     ▼                      │ │                     ▼
 *  ┌──────┐            ┌───────────┐                │ │              ┌────────────┐
 *  │ DRAFT │            │ IN_REVIEW │◄───────────────┘ │              │ superseded │
 *  └──────┘            └─┬─────────┘  驳回后修改重新提交│              │   状态      │
 *                          │                        │              └────────────┘
 *                      审核拒绝                       │
 *                          │               ┌──────────┴─────┐
 *                          ▼               │  并行版本：     │
 *                    ┌──────────┐          │  PUBLISHED_V2  │ 替代旧版本
 *                    │ REJECTED │          │  与 PUBLISHED  │
 *                    └──────────┘          │  并存过渡期     │
 *                                          └────────────────┘
 */
type KnowledgePointStatus =
  | 'DRAFT'        // 草稿：刚创建，仅创建人可见，可自由编辑
  | 'IN_REVIEW'    // 审核中：已提交审核，等待教研团队审批
  | 'PUBLISHED'    // 已发布：对所有服务可见，参与图谱查询
  | 'DEPRECATED'   // 已废弃：不再推荐使用，但保留引用兼容
  | 'ARCHIVED'     // 已归档：只读历史记录，不再参与任何业务查询
  | 'REJECTED';    // 已拒绝：审核未通过，退回创建人修改
```

#### 2.1.3 KnowledgeEdge（知识点关系边）

```typescript
interface KnowledgeEdge {
  id: string;                      // 边ID：ke_{ulid}
  sourceKpId: string;              // 源知识点ID
  targetKpId: string;              // 目标知识点ID
  edgeType: KnowledgeEdgeType;     // 关系类型
  weight: number;                  // 关系强度 0.0-1.0
  metadata: Record<string, any>;   // 扩展元数据
  status: EdgeStatus;              // ACTIVE|INACTIVE
  graphVersionId: string;          // 所属图谱版本
  createdAt: string;
  updatedAt: string;
}

/**
 * 知识点关系类型
 */
type KnowledgeEdgeType =
  | 'PREREQUISITE'         // 前置知识：学习target前需先掌握source
  | 'RELATED_TO'           // 相关联：两个知识点有交叉内容
  | 'PART_OF'             // 包含关系：source是target的子知识点
  | 'DERIVED_FROM'         // 衍生关系：source从target发展而来
  | 'SIMILAR_TO'           // 相似知识点：易混淆概念
  | 'LEADS_TO'             // 进阶关系：掌握source后可学习target
  | 'CROSS_SUBJECT'        // 跨学科关联
  | 'SAME_AS';             // 同义知识点（合并时使用）
```

#### 2.1.4 GraphVersion（图谱快照版本）

```typescript
interface GraphVersion {
  id: string;                      // 版本ID：gv_{ulid}
  versionNumber: number;           // 语义化版本号，如 2026.06.1
  subject: SubjectCode | 'ALL';    // 学科范围
  snapshotType: SnapshotType;      // SCHEDULED|EVENT_TRIGGERED|MANUAL
  description: string;             // 版本说明
  status: GraphVersionStatus;      // DRAFT|FROZEN|ACTIVE|SUPERSEDED

  // === 快照统计 ===
  knowledgePointCount: number;     // 知识点总数
  edgeCount: number;               // 关系边总数
  changeSummary: ChangeSummary;    // 相对上一版本的变更摘要

  // === 关联 ===
  previousVersionId?: string;      // 上一版本ID
  nextVersionId?: string;          // 下一版本ID

  // === 时间 ===
  frozenAt?: string;               // 冻结时间
  activatedAt?: string;            // 生效时间
  supersededAt?: string;           // 被替代时间

  createdBy: string;
  createdAt: string;
}

interface ChangeSummary {
  addedKpCount: number;            // 新增知识点数
  modifiedKpCount: number;         // 修改知识点数
  deletedKpCount: number;          // 删除知识点数
  addedEdgeCount: number;          // 新增边数
  removedEdgeCount: number;        // 移除边数
  affectedSubjects: SubjectCode[]; // 受影响学科
  affectedDownstream: number;      // 受影响下游资源数（题目、课程等）
  riskLevel: RiskLevel;            // 风险等级
}

type RiskLevel = 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL';
```

#### 2.1.5 ChangeRecord（变更记录）

```typescript
interface ChangeRecord {
  id: string;                      // cr_{ulid}
  graphVersionId: string;          // 所属图谱版本
  entityType: 'KNOWLEDGE_POINT' | 'KNOWLEDGE_EDGE';
  entityId: string;                // 知识点或边的ID
  changeType: ChangeType;          // CREATE|UPDATE|DELETE|MERGE|SPLIT|DEPRECATE
  beforeSnapshot?: string;         // 变更前数据快照 (JSON)
  afterSnapshot?: string;          // 变更后数据快照 (JSON)
  diff: DiffEntry[];               // 字段级差异
  impactAnalysis: ImpactAnalysis;  // 影响分析结果
  approvedBy?: string;             // 审批人（高风险变更需要）
  appliedAt: string;               // 变更应用时间
  appliedBy: string;               // 变更操作人
  reason: string;                  // 变更原因
}

interface DiffEntry {
  field: string;                   // 变更字段
  oldValue?: any;                  // 旧值
  newValue?: any;                  // 新值
  changeType: 'SET' | 'UPDATE' | 'UNSET' | 'ARRAY_ADD' | 'ARRAY_REMOVE';
}

interface ImpactAnalysis {
  // 影响范围
  affectedQuestions: number;       // 受影响题目数
  affectedCourses: number;         // 受影响课程数
  affectedChapters: number;        // 受影响章节数
  affectedStudentRecords: number;  // 受影响学生记录数
  affectedLearningPaths: number;   // 受影响学习路径数
  affectedAiPrompts: number;       // 受影响AI Prompt模板数

  // 风险评估
  riskLevel: RiskLevel;
  riskReasons: string[];           // 风险原因说明
  requiresApproval: boolean;       // 是否需要人工审批
  estimatedMigrationTime?: number; // 预估迁移耗时（秒）
}
```

### 2.2 数据库表结构

#### 2.2.1 MySQL 主库表（OLTP 操作）

```sql
-- ============================================================
-- 知识点主表
-- ============================================================
CREATE TABLE kp_knowledge_point (
    id              VARCHAR(40)   PRIMARY KEY COMMENT '知识点ID kp_{ulid}',
    code            VARCHAR(100)  NOT NULL COMMENT '知识点编码',
    name            VARCHAR(200)  NOT NULL COMMENT '知识点名称',
    aliases         JSON          COMMENT '别名列表',
    description     TEXT          COMMENT '详细描述',
    subject         VARCHAR(20)   NOT NULL COMMENT '学科代码',
    stage           VARCHAR(20)   NOT NULL COMMENT '学段代码',
    grade_band      JSON          NOT NULL COMMENT '适用年级段',
    category        VARCHAR(30)   NOT NULL DEFAULT 'CONCEPT' COMMENT '类别',
    cognitive_level VARCHAR(30)   NOT NULL DEFAULT 'UNDERSTAND' COMMENT '认知层级',
    difficulty      TINYINT       NOT NULL DEFAULT 3 COMMENT '难度系数1-5',
    tags            JSON          COMMENT '业务标签',
    keywords        JSON          COMMENT '检索关键词',
    examples        JSON          COMMENT '典型例子',
    status          VARCHAR(20)   NOT NULL DEFAULT 'DRAFT' COMMENT '生命周期状态',
    supersedes_id   VARCHAR(40)   COMMENT '替代的旧知识点ID',
    superseded_by_id VARCHAR(40)  COMMENT '被替代的目标ID',
    merge_target_id VARCHAR(40)   COMMENT '合并目标ID',
    split_from_id   VARCHAR(40)   COMMENT '拆分来源ID',
    current_version INT           NOT NULL DEFAULT 1 COMMENT '数据版本号',
    graph_version_id VARCHAR(40)  NOT NULL COMMENT '所属图谱版本',
    created_at      DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at      DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    published_at    DATETIME(3)   NULL,
    deprecated_at   DATETIME(3)   NULL,
    archived_at     DATETIME(3)   NULL,
    created_by      VARCHAR(40)   NOT NULL,
    updated_by      VARCHAR(40)   NOT NULL,
    reviewed_by     VARCHAR(40)   NULL,

    UNIQUE KEY uk_code (code),
    INDEX idx_subject_stage (subject, stage),
    INDEX idx_status (status),
    INDEX idx_graph_version (graph_version_id),
    INDEX idx_supersedes (supersedes_id),
    INDEX idx_updated (updated_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识点主表';

-- ============================================================
-- 知识点版本历史表（每次发布保存快照）
-- ============================================================
CREATE TABLE kp_version_history (
    id              BIGINT        PRIMARY KEY AUTO_INCREMENT,
    kp_id           VARCHAR(40)   NOT NULL COMMENT '知识点ID',
    version_number  INT           NOT NULL COMMENT '版本号',
    snapshot        JSON          NOT NULL COMMENT '完整数据快照',
    change_type     VARCHAR(20)   NOT NULL COMMENT 'CREATE|UPDATE|DEPRECATE|MERGE|SPLIT',
    change_reason   VARCHAR(500)  COMMENT '变更原因',
    changed_by      VARCHAR(40)   NOT NULL,
    changed_at      DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3),

    INDEX idx_kp_version (kp_id, version_number),
    INDEX idx_changed_at (changed_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识点版本历史';

-- ============================================================
-- 知识点关系边表
-- ============================================================
CREATE TABLE kp_edge (
    id              VARCHAR(40)   PRIMARY KEY COMMENT '边ID ke_{ulid}',
    source_kp_id    VARCHAR(40)   NOT NULL COMMENT '源知识点ID',
    target_kp_id    VARCHAR(40)   NOT NULL COMMENT '目标知识点ID',
    edge_type       VARCHAR(30)   NOT NULL COMMENT '关系类型',
    weight          DECIMAL(3,2)  NOT NULL DEFAULT 1.00 COMMENT '关系强度',
    metadata        JSON          COMMENT '扩展元数据',
    status          VARCHAR(20)   NOT NULL DEFAULT 'ACTIVE' COMMENT 'ACTIVE|INACTIVE',
    graph_version_id VARCHAR(40)  NOT NULL,
    created_at      DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at      DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),

    UNIQUE KEY uk_edge (source_kp_id, target_kp_id, edge_type),
    INDEX idx_source (source_kp_id),
    INDEX idx_target (target_kp_id),
    INDEX idx_type (edge_type),
    INDEX idx_graph_version (graph_version_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识点关系边';

-- ============================================================
-- 图谱版本表
-- ============================================================
CREATE TABLE kp_graph_version (
    id              VARCHAR(40)   PRIMARY KEY COMMENT '版本ID gv_{ulid}',
    version_number  VARCHAR(30)   NOT NULL COMMENT '语义版本号',
    subject         VARCHAR(20)   NOT NULL DEFAULT 'ALL' COMMENT '学科范围',
    snapshot_type   VARCHAR(20)   NOT NULL COMMENT 'SCHEDULED|EVENT_TRIGGERED|MANUAL',
    description     VARCHAR(500)  COMMENT '版本说明',
    status          VARCHAR(20)   NOT NULL DEFAULT 'DRAFT' COMMENT 'DRAFT|FROZEN|ACTIVE|SUPERSEDED',
    kp_count        INT           NOT NULL DEFAULT 0 COMMENT '知识点总数',
    edge_count      INT           NOT NULL DEFAULT 0 COMMENT '边总数',
    change_summary  JSON          COMMENT '变更摘要',
    previous_version_id VARCHAR(40) COMMENT '上一版本ID',
    next_version_id VARCHAR(40)   COMMENT '下一版本ID',
    frozen_at       DATETIME(3)   NULL,
    activated_at    DATETIME(3)   NULL,
    superseded_at   DATETIME(3)   NULL,
    created_by      VARCHAR(40)   NOT NULL,
    created_at      DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3),

    UNIQUE KEY uk_version_number (version_number),
    INDEX idx_status (status),
    INDEX idx_subject (subject),
    INDEX idx_previous (previous_version_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='图谱版本表';

-- ============================================================
-- 变更记录表
-- ============================================================
CREATE TABLE kp_change_record (
    id              VARCHAR(40)   PRIMARY KEY COMMENT 'cr_{ulid}',
    graph_version_id VARCHAR(40)  NOT NULL,
    entity_type     VARCHAR(20)   NOT NULL COMMENT 'KNOWLEDGE_POINT|KNOWLEDGE_EDGE',
    entity_id       VARCHAR(40)   NOT NULL,
    change_type     VARCHAR(20)   NOT NULL COMMENT 'CREATE|UPDATE|DELETE|MERGE|SPLIT|DEPRECATE',
    before_snapshot JSON          COMMENT '变更前快照',
    after_snapshot  JSON          COMMENT '变更后快照',
    diff            JSON          NOT NULL COMMENT '字段级差异',
    impact_analysis JSON          NOT NULL COMMENT '影响分析结果',
    approved_by     VARCHAR(40)   COMMENT '审批人',
    applied_at      DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    applied_by      VARCHAR(40)   NOT NULL,
    reason          VARCHAR(500)  NOT NULL COMMENT '变更原因',

    INDEX idx_graph_version (graph_version_id),
    INDEX idx_entity (entity_type, entity_id),
    INDEX idx_change_type (change_type),
    INDEX idx_applied_at (applied_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='变更记录表';

-- ============================================================
-- 知识点引用注册表（跟踪哪些资源引用了该知识点）
-- ============================================================
CREATE TABLE kp_reference_registry (
    id              BIGINT        PRIMARY KEY AUTO_INCREMENT,
    kp_id           VARCHAR(40)   NOT NULL COMMENT '知识点ID',
    ref_type        VARCHAR(30)   NOT NULL COMMENT 'QUESTION|COURSE|CHAPTER|STUDENT_MASTERY|LEARNING_PATH|AI_PROMPT',
    ref_id          VARCHAR(40)   NOT NULL COMMENT '引用方实体ID',
    ref_field       VARCHAR(50)   COMMENT '引用字段名',
    migration_status VARCHAR(20)  NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING|MIGRATED|SKIPPED',
    migrated_at     DATETIME(3)   NULL,
    created_at      DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3),

    INDEX idx_kp (kp_id),
    INDEX idx_ref (ref_type, ref_id),
    INDEX idx_migration (migration_status, kp_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识点引用注册表';

-- ============================================================
-- 审批流表
-- ============================================================
CREATE TABLE kp_change_approval (
    id              VARCHAR(40)   PRIMARY KEY,
    change_record_id VARCHAR(40)  NOT NULL COMMENT '关联变更记录',
    approval_type   VARCHAR(30)   NOT NULL COMMENT 'PUBLISH|DEPRECATE|MERGE|SPLIT|BULK_IMPORT',
    risk_level      VARCHAR(20)   NOT NULL,
    status          VARCHAR(20)   NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING|APPROVED|REJECTED',
    requested_by    VARCHAR(40)   NOT NULL,
    reviewed_by     VARCHAR(40)   NULL,
    review_comment  VARCHAR(500)  NULL,
    requested_at    DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    reviewed_at     DATETIME(3)   NULL,

    INDEX idx_change (change_record_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='变更审批流';
```

#### 2.2.2 图数据库 Schema (Neo4j Cypher)

```cypher
// ============================================================
// 知识点节点（图数据库视角，用于图查询）
// 注意：图数据库是 MySQL 的只读投影，数据从 MySQL 同步
// ============================================================

// 创建约束
CREATE CONSTRAINT kp_id_unique IF NOT EXISTS
FOR (n:KnowledgePoint) REQUIRE n.id IS UNIQUE;

CREATE CONSTRAINT kp_code_unique IF NOT EXISTS
FOR (n:KnowledgePoint) REQUIRE n.code IS UNIQUE;

// 知识点节点属性
// (:KnowledgePoint {
//   id, code, name, subject, stage, gradeBand,
//   category, cognitiveLevel, difficulty, tags,
//   status, currentVersion, graphVersionId
// })

// 关系类型
// [:PREREQUISITE {weight, graphVersionId}]
// [:RELATED_TO {weight, graphVersionId}]
// [: