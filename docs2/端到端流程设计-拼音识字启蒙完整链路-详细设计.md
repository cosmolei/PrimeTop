# 端到端流程设计 - 拼音识字启蒙完整链路

> **文档定位**：从幼儿/小学低年级用户打开拼音识字启蒙模块到完成一轮学习的完整数据链路、服务调用、状态流转与异常处理设计。
> **原始需求来源**：`docs/design/启硕-PrimeTop-全学段AI辅助学习软件项目设计文档.md` §5.1, §6.5
> **关联文档**：
> - `拼音识字启蒙-详细设计.md`（模块级详细设计）
> - `客户端-拼音识字启蒙页面架构与交互设计.md`（客户端页面架构）
> - `语音服务(ASR-TTS)-详细设计.md`（ASR/TTS 能力）
> - `亲子共学模式与双角色交互设计-详细设计.md`（家长管控）
> - `间隔重复算法与遗忘曲线复习调度引擎-详细设计.md`（复习调度）
> - `客户端-拼音识字启蒙页面架构与交互设计.md`（客户端交互）
> - `服务端-用户额度管控与功能门控引擎-详细设计.md`（额度校验）
> - `多模态输入统一处理与智能路由引擎-详细设计.md`（语音输入处理）
> **优先级**：P1（V1.5 阶段）
> **覆盖学段**：幼儿（4-6 岁）、小学一二年级（6-8 岁）

---

## 1. 链路概述

### 1.1 核心场景

拼音识字启蒙模块覆盖以下核心学习场景，每个场景构成一条独立但相互衔接的端到端链路：

| 链路编号 | 场景 | 触发入口 | 核心路径 |
|----------|------|----------|----------|
| L01 | 拼音声韵母学习 | 启蒙主页 → 拼音入口 | 选拼音 → 发音演示 → 跟读录音 → 评分反馈 → 下一个 |
| L02 | 拼读组合练习 | 拼音学习 → 拼读练习 | 选声母+韵母 → 拼读动画 → 跟读 → 评分 |
| L03 | 声调练习 | 拼音学习 → 声调练习 | 听音 → 选调/标调 → 反馈 |
| L04 | 汉字卡片识记 | 启蒙主页 → 识字入口 | 浏览字库 → 卡片翻转 → 笔画动画 → 跟读 → 掌握标记 |
| L05 | 笔画描红练习 | 汉字卡片 → 描红 | 笔顺演示 → 描红书写 → 笔画检测 → 评分 |
| L06 | 配对连线小游戏 | 启蒙主页 → 游戏入口 | 选游戏类型 → 配对 → 计时计分 → 结算奖励 |
| L07 | 发音评测与纠音 | 跟读练习入口 | 朗读 → 录音 → ASR评分 → 纠音建议 → 重试 |
| L08 | 每日任务打卡 | 启蒙主页/每日任务卡片 | 查看任务 → 完成学习 → 打卡 → 奖励发放 |
| L09 | 家长查看报告 | 家长入口 | 身份验证 → 查看进度报告 → 调整学习计划 |

### 1.2 链路全景时序

```
用户(幼儿/家长)          客户端              BFF/网关            学习服务           AI/语音服务          数据服务
    │                    │                    │                   │                    │                   │
    │── 打开启蒙模块 ──→│                    │                   │                    │                   │
    │                    │── 查询学习进度 ──→│── GET /literacy/  │                    │                   │
    │                    │                    │    progress       │── 查询进度数据 ──→│                   │
    │                    │                    │                   │←── 返回进度 ──────│                   │
    │                    │←── 渲染启蒙主页 ──│                   │                    │                   │
    │                    │                    │                   │                    │                   │
    │── 点击"学拼音" ──→│                    │                   │                    │                   │
    │                    │── 获取拼音列表 ──→│── GET /literacy/  │                    │                   │
    │                    │                    │    pinyin/list    │── 查询拼音库 ────→│                   │
    │                    │                    │                   │←── 拼音数据 ──────│                   │
    │                    │←── 渲染拼音网格 ──│                   │                    │                   │
    │                    │                    │                   │                    │                   │
    │── 点击拼音"b" ──→│                    │                   │                    │                   │
    │                    │── 播放发音音频 ──→│                   │                    │                   │
    │                    │── 展示口型动画 ──→│                   │                    │                   │
    │                    │                    │                   │                    │                   │
    │── 点击"跟读" ───→│                    │                   │                    │                   │
    │                    │── 开始录音 ──────→│                   │                    │                   │
    │                    │── 上传音频 ──────→│── POST /literacy/ │                    │                   │
    │                    │                    │    pronunciation/ │── 调用ASR评测 ───→│── ASR+评分 ────→│
    │                    │                    │    evaluate       │←── 评分结果 ──────│                   │
    │                    │                    │                   │── 记录学习行为 ──→│                   │
    │                    │←── 返回评分+反馈 ─│                   │                    │                   │
    │←── 展示评分动画 ──│                    │                   │                    │                   │
    │                    │                    │                   │                    │                   │
    │── 继续下一个 ────→│                    │                   │                    │                   │
    │                   ...                  ...                 ...                  ...                 ...
```

---

## 2. L01 - 拼音声韵母学习完整链路

### 2.1 流程状态机

```
                    ┌─────────────┐
                    │  进入拼音    │
                    │  总览页面    │
                    └──────┬──────┘
                           │ 用户选择具体拼音
                           ▼
                    ┌─────────────┐
                    │  拼音学习页  │
                    │  发音演示    │
                    └──────┬──────┘
                           │ 播放完成
                           ▼
              ┌────────────────────────┐
              │  用户选择操作           │
              ├──────┬──────┬──────────┤
              │ 再听  │ 跟读  │ 下一个  │
              │ 一次  │ 练习  │  拼音   │
              └──┬───┘──┬──┘────┬─────┘
                 │      │       │
                 │      ▼       │
                 │ ┌─────────┐ │
                 │ │ 录音中   │ │
                 │ └────┬────┘ │
                 │      │ 录音完成
                 │      ▼       │
                 │ ┌─────────┐ │
                 │ │ ASR评测  │ │
                 │ │ 评分反馈 │ │
                 │ └────┬────┘ │
                 │      │       │
                 │  ┌───┴──────┴───┐
                 │  │ 评分结果判断   │
                 │  ├────┬─────────┤
                 │  │≥80 │ <80     │
                 │  │通过 │ 需重试  │
                 │  └─┬──┴────┬────┘
                 │    │       │
                 │    ▼       │ 重试(最多3次)
                 │ ┌───────┐  │
                 │ │ 标记   │  │
                 │ │ 已掌握 │  │
                 │ └───┬───┘  │
                 │     │      │
                 └─────┼──────┘
                       │
                       ▼
                ┌─────────────┐
                │ 学习进度更新 │
                │ 推荐下一个   │
                └─────────────┘
```

### 2.2 客户端关键交互

```dart
/// 拼音学习页面控制器
class PinyinLearningController extends GetxController {
  final PinyinItem currentPinyin;
  final AudioPlayer _audioPlayer = AudioPlayer();
  final PronunciationEvaluator _evaluator = PronunciationEvaluator();

  // 状态：idle / playing / recording / evaluating / result
  final state = LearningState.idle.obs;
  final score = 0.obs;
  final retryCount = 0.obs;
  final maxRetry = 3;

  /// 步骤1: 播放标准发音
  Future<void> playDemo() async {
    state.value = LearningState.playing;
    await _audioPlayer.play(UrlSource(currentPinyin.audioUrl));
    state.value = LearningState.idle;
  }

  /// 步骤2: 开始跟读录音
  Future<void> startRecording() async {
    state.value = LearningState.recording;
    await _evaluator.startRecording(maxDuration: const Duration(seconds: 5));
  }

  /// 步骤3: 停止录音并上传评测
  Future<void> stopAndEvaluate() async {
    state.value = LearningState.evaluating;
    final audioPath = await _evaluator.stopRecording();
    final result = await _literacyApi.evaluatePronunciation(
      pinyinId: currentPinyin.id,
      audioFilePath: audioPath,
      expectedPinyin: currentPinyin.pinyin,
    );
    score.value = result.score;
    state.value = LearningState.result;
  }

  /// 步骤4: 处理评分结果
  Future<void> handleResult() async {
    if (score.value >= 80) {
      // 标记掌握，播放成功动画
      await _literacyApi.markMastered(
        pinyinId: currentPinyin.id,
        score: score.value,
        retryCount: retryCount.value,
      );
      _showSuccessAnimation();
    } else {
      retryCount.value++;
      if (retryCount.value >= maxRetry) {
        // 超过重试次数，降低难度提示后继续
        _showEncouragement();
        await _literacyApi.markNeedsReview(pinyinId: currentPinyin.id);
      }
    }
  }
}
```

### 2.3 服务端 API 接口

#### 2.3.1 获取拼音学习列表

```
GET /api/v1/literacy/pinyin/list
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| type | string | 否 | 拼音类型过滤：shengmu / yunmu / zhengti，为空返回全部 |
| gradeLevel | string | 否 | 学段过滤：K / G1A / G1B，默认根据用户学段 |
| page | int | 否 | 页码，默认 1 |
| pageSize | int | 否 | 每页数量，默认 50 |

**响应：**

```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": 1001,
        "pinyin": "b",
        "type": "shengmu",
        "audioUrl": "https://cdn.primetop.edu/audio/pinyin/b.mp3",
        "mouthImgUrl": "https://cdn.primetop.edu/img/mouth/b.png",
        "exampleChar": "玻",
        "exampleWord": "波浪",
        "exampleImgUrl": "https://cdn.primetop.edu/img/wave.png",
        "tips": "双唇紧闭，然后突然放开，让气流冲出",
        "mastered": false,
        "learningStatus": "new"
      }
    ],
    "total": 63,
    "masteredCount": 15,
    "totalCount": 63
  }
}
```

#### 2.3.2 发音评测

```
POST /api/v1/literacy/pronunciation/evaluate
Content-Type: multipart/form-data
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| audioFile | file | 是 | 录音文件（WAV/PCM，≤ 10s） |
| pinyinId | long | 是 | 目标拼音ID |
| expectedPinyin | string | 是 | 预期拼音字符串 |
| retryCount | int | 否 | 当前重试次数 |

**响应：**

```json
{
  "code": 0,
  "data": {
    "score": 85,
    "level": "excellent",
    "feedback": "发音很标准！继续保持！",
    "detail": {
      "pronunciationAccuracy": 88,
      "toneAccuracy": 82,
      "fluency": 85,
      "weakPoints": []
    },
    "audioUrl": "https://cdn.primetop.edu/audio/user/pronounce/xxx.wav",
    "tryAgainSuggestion": null
  }
}
```

#### 2.3.3 标记掌握状态

```
POST /api/v1/literacy/pinyin/mastery
```

**请求参数：**

```json
{
  "pinyinId": 1001,
  "score": 85,
  "retryCount": 1,
  "timeSpentMs": 12000
}
```

**响应：**

```json
{
  "code": 0,
  "data": {
    "masteryId": "m_20260602_001",
    "status": "mastered",
    "nextReviewDate": "2026-06-04",
    "earnedStars": 2,
    "earnedBadges": ["pinyin_master_5"],
    "totalProgress": {
      "masteredCount": 16,
      "totalCount": 63,
      "percentage": 25.4
    }
  }
}
```

### 2.4 服务端内部处理流程

```
客户端请求
    │
    ▼
BFF层 ──── 参数校验 ──── 额度检查 ──── 用户学段校验
    │
    ▼
LiteracyService.evaluatePronunciation()
    │
    ├── 1. 保存录音文件到 OSS
    │
    ├── 2. 调用 ASR 评测服务
    │       │
    │       ▼
    │   PronunciationEvaluationService.evaluate()
    │       ├── 音频预处理（降噪、格式转换）
    │       ├── 调用语音评测模型 API
    │       │   └── 模型选择：
    │       │       ├── 标准模式：通用 ASR + 拼音对齐
    │       │       └── 精准模式：教育专用发音评测引擎
    │       ├── 评分计算
    │       │   ├── 准确度分 = 发音匹配度 × 0.4
    │       │   ├── 声调分 = 声调匹配度 × 0.3
    │       │   └── 流利度分 = 语速/停顿评估 × 0.3
    │       └── 生成反馈文本
    │           ├── ≥ 90: "太棒了！发音非常标准！"
    │           ├── ≥ 80: "很好！发音很清晰！"
    │           ├── ≥ 60: "不错哦，再试一次会更好！"
    │           └── < 60: "加油！仔细听示范再试试看"
    │
    ├── 3. 记录学习行为
    │       ├── 写入 literacy_learning_log
    │       ├── 更新 pinyin_mastery_status
    │       └── 发送学习行为事件 → 事件总线
    │
    ├── 4. 触发下游处理（异步）
    │       ├── 更新学习进度缓存
    │       ├── 检查成就解锁
    │       ├── 更新间隔重复复习计划
    │       └── 更新学习画像（幼儿版）
    │
    └── 5. 返回评测结果
```

### 2.5 数据库操作

```sql
-- 拼音掌握记录表
INSERT INTO pinyin_mastery_log (
    id, user_id, pinyin_id, score, retry_count,
    time_spent_ms, audio_url, status, created_at
) VALUES (
    ?, ?, ?, 85, 1, 12000, 'https://...', 'mastered', NOW()
);

-- 更新用户拼音掌握汇总
INSERT INTO user_pinyin_progress (
    user_id, pinyin_id, status, best_score, attempt_count,
    last_practiced_at, next_review_at, updated_at
) VALUES (?, ?, 'mastered', 85, 3, NOW(), '2026-06-04', NOW())
ON DUPLICATE KEY UPDATE
    status = VALUES(status),
    best_score = GREATEST(best_score, VALUES(best_score)),
    attempt_count = attempt_count + VALUES(attempt_count),
    last_practiced_at = VALUES(last_practiced_at),
    next_review_at = VALUES(next_review_at),
    updated_at = VALUES(updated_at);
```

### 2.6 异常处理

| 异常场景 | 客户端处理 | 服务端处理 |
|----------|-----------|-----------|
| 录音权限被拒 | 引导到系统设置页开启权限 | 不涉及 |
| 录音超时（>10s） | 自动停止录音，提示"时间到" | 服务端超时限制 15s |
| 音频上传失败 | 本地缓存，离线队列稍后重试 | 返回 `AUDIO_UPLOAD_FAILED` |
| ASR 服务不可用 | 降级为本地基础评分（仅音量/时长判断） | 返回 `ASR_SERVICE_UNAVAILABLE`，降级标记 |
| 评分接口超时 | 显示"评测中..."超时提示，允许重试 | 30s 超时，超时返回 `EVALUATION_TIMEOUT` |
| 用户学段不匹配 | 禁止访问，提示"该内容不适合当前年级" | BFF 层校验学段，拒绝请求 |
| 亲子模式时长耗尽 | 弹窗提示"今天的学习时间到啦～" | 服务端校验每日学习时长 |

---

## 3. L02 - 拼读组合练习完整链路

### 3.1 业务流程

拼读组合练习是拼音学习的关键进阶环节，用户需要将声母和韵母组合拼读出正确音节。

```
┌──────────────────────────────────────────────────────────┐
│                    拼读组合练习流程                         │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  1. 选择声母 ──→ 展示声母网格（已学声母优先）              │
│       │                                                  │
│       ▼                                                  │
│  2. 选择韵母 ──→ 展示韵母网格                             │
│       │                                                  │
│       ▼                                                  │
│  3. 组合展示 ──→ 显示 "b" + "ā" = "bā"                   │
│       │           拼读动画：声母滑入 → 韵母滑入 → 合体      │
│       │                                                  │
│       ▼                                                  │
│  4. 听标准拼读 ──→ 播放合成音频                            │
│       │                                                  │
│       ▼                                                  │
│  5. 跟读练习 ──→ 录音 → ASR 评测 → 反馈                   │
│       │                                                  │
│       ▼                                                  │
│  6. 组词扩展 ──→ 展示对应汉字和词语                        │
│       │           如 "bā" → 爸 → 爸爸                     │
│       │                                                  │
│       ▼                                                  │
│  7. 记录进度 → 更新拼读掌握状态                            │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 3.2 API 接口

#### 获取拼读组合

```
GET /api/v1/literacy/pinyin/blend/options
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| shengmuId | long | 否 | 指定声母ID，为空则推荐 |
| difficulty | string | 否 | easy/medium/hard，默认根据用户进度 |
| count | int | 否 | 返回组合数量，默认 10 |

**响应：**

```json
{
  "code": 0,
  "data": {
    "shengmu": {
      "id": 1001,
      "pinyin": "b",
      "audioUrl": "https://cdn.primetop.edu/audio/pinyin/b.mp3"
    },
    "combinations": [
      {
        "shengmu": "b",
        "yunmu": "ā",
        "syllable": "bā",
        "tone": 1,
        "audioUrl": "https://cdn.primetop.edu/audio/pinyin/ba1.mp3",
        "characters": [
          {
            "char": "爸",
            "word": "爸爸",
            "imgUrl": "https://cdn.primetop.edu/img/dad.png"
          },
          {
            "char": "巴",
            "word": "尾巴",
            "imgUrl": "https://cdn.primetop.edu/img/tail.png"
          }
        ],
        "difficulty": "easy",
        "learned": false
      }
    ],
    "recommended": true,
    "reason": "声母 b 的常见组合，适合当前进度"
  }
}
```

### 3.3 拼读评测逻辑

```java
/**
 * 拼读评测服务 - 处理声韵母组合的发音评测
 */
@Service
public class PinyinBlendEvaluationService {

    @Autowired
    private AsrEvaluationClient asrClient;

    @Autowired
    private LiteracyLearningLogRepository logRepo;

    @Autowired
    private ApplicationEventPublisher eventPublisher;

    /**
     * 评测拼读发音
     *
     * @param request 包含音频、声母、韵母、声调信息
     * @return 评测结果
     */
    public PinyinBlendEvalResult evaluate(PinyinBlendEvalRequest request) {
        // 1. 参数校验
        validateRequest(request);

        // 2. 调用ASR评测
        AsrEvalResult asrResult = asrClient.evaluatePronunciation(
            AsrEvalRequest.builder()
                .audioUrl(request.getAudioUrl())
                .expectedText(request.getExpectedSyllable()) // 如 "bā"
                .language("zh-CN")
                .mode("pinyin_blend")          // 拼读评测专用模式
                .gradeLevel(request.getGradeLevel())
                .build()
        );

        // 3. 计算综合评分
        int totalScore = calculateBlendScore(asrResult, request);

        // 4. 生成适龄反馈
        String feedback = generateFeedback(totalScore, request.getGradeLevel());

        // 5. 记录学习日志
        LiteracyLearningLog log = LiteracyLearningLog.builder()
            .userId(request.getUserId())
            .learnType("pinyin_blend")
            .targetId(request.getShengmuId() + "_" + request.getYunmuId())
            .score(totalScore)
            .timeSpentMs(request.getTimeSpentMs())
            .detail(buildDetail(asrResult, request))
            .build();
        logRepo.save(log);

        // 6. 发布学习事件（异步处理下游）
        eventPublisher.publishEvent(new PinyinLearnedEvent(
            request.getUserId(), "blend",
            request.getShengmuId(), request.getYunmuId(),
            totalScore
        ));

        return PinyinBlendEvalResult.builder()
            .score(totalScore)
            .level(getScoreLevel(totalScore))
            .feedback(feedback)
            .pronunciationAccuracy(asrResult.getAccuracy())
            .toneAccuracy(asrResult.getToneAccuracy())
            .blendSmoothness(asrResult.getFluency())
            .build();
    }

    /**
     * 拼读综合评分算法
     * 声母清晰度 30% + 韵母准确度 30% + 声调 20% + 拼读流畅度 20%
     */
    private int calculateBlendScore(AsrEvalResult asrResult,
                                     PinyinBlendEvalRequest request) {
        double shengmuScore = asrResult.getInitialConsonantScore() * 0.30;
        double yunmuScore = asrResult.getVowelScore() * 0.30;
        double toneScore = asrResult.getToneAccuracy() * 0.20;
        double fluencyScore = asrResult.getFluency() * 0.20;
        return (int) Math.round(shengmuScore + yunmuScore + toneScore + fluencyScore);
    }

    private String generateFeedback(int score, String gradeLevel) {
        if ("K".equals(gradeLevel)) {
            // 幼儿版反馈：更多鼓励性语言
            if (score >= 80) return "太好啦！你读得真好听！🌟";
            if (score >= 60) return "很棒哦！再来一次就更厉害啦！💪";
            return "加油加油！听听声音再试试～🎵";
        } else {
            // 小学版反馈：更有指导性
            if (score >= 80) return "发音很标准！继续保持！";
            if (score >= 60) return "不错！注意声调会更好哦";
            return "再仔细听一遍示范，然后试着跟读";
        }
    }
}
```

---

## 4. L04 - 汉字卡片识记完整链路

### 4.1 业务流程

```
┌───────────────────────────────────────────────────────────────┐
│                   汉字识记完整流程                               │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  用户进入识字入口                                              │
│       │                                                       │
│       ▼                                                       │
│  ┌─────────────┐                                              │
│  │ 字库选择     │ ←── 按教材 / 按主题 / 按偏旁 / 智能推荐      │
│  └──────┬──────┘                                              │