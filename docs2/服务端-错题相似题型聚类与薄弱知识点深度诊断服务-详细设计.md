# 服务端 - 错题相似题型聚类与薄弱知识点深度诊断服务 详细设计

> 版本: v1.0 | 更新日期: 2026-06-07 | 状态: 初稿

## 1. 概述

### 1.1 模块定位

错题相似题型聚类与薄弱知识点深度诊断服务（Error Pattern Clustering & Deep Diagnosis Service, EPC-DDS）是 PrimeTop 学习闭环中"诊断归因"环节的核心引擎。它从学生积累的错题记录中，识别出系统性错误模式，区分"概念性薄弱"与"表面性失误"，并生成可操作的深度诊断报告与补救路径。

**核心职责：**
- 对学生错题进行多维度相似性聚类，发现高频错误模式
- 区分概念性错误（需要重新学习）与操作性失误（需要增加练习）
- 识别跨章节、跨知识点的隐性关联薄弱点
- 生成薄弱知识点深度诊断报告
- 为每位学生维护动态更新的「错误模式画像」
- 推荐针对性的补救学习路径

**不在本模块范围内：**
- 错题收录与管理 → 见 `错题整理-详细设计.md`
- 错题订正与复习追踪 → 见 `服务端-错题订正效果评估与复习成效追踪服务-详细设计.md`
- 复习调度（遗忘曲线） → 见 `间隔重复算法与遗忘曲线复习调度引擎-详细设计.md`
- 学习计划生成 → 见 `学习规划-详细设计.md`
- 题目推荐 → 见 `服务端-题目智能推荐与个性化练习编排服务-详细设计.md`

### 1.2 触发场景

| 场景 | 触发方 | 说明 |
|------|--------|------|
| 错题累积触发 | 错题服务 | 新增错题达到阈值（≥5 道）或距上次诊断超过 7 天 |
| 手动诊断 | 学生/家长 | 用户主动请求「学情深度诊断」 |
| 定期批量 | 定时任务 | 每周日凌晨批量对所有活跃用户执行增量诊断 |
| 学段切换 | 升迁服务 | 学生升入新学段时对历史错题进行回溯分析 |
| 考前分析 | 考试系统 | 考试模拟完成后自动触发针对性薄弱点诊断 |
| 家长报告 | 报告服务 | 生成周报/月报时附带最新诊断结论 |

### 1.3 设计目标

| 目标 | 指标 |
|------|------|
| 聚类准确性 | 同一聚类内题目错误根因一致率 ≥ 85% |
| 诊断时效 | 增量诊断 < 3s，全量诊断 < 10s |
| 误判率低 | 概念性错误 vs 操作性失误分类准确率 ≥ 90% |
| 可操作性 | 每条诊断结论必须附带 ≥1 条可执行补救建议 |
| 覆盖度 | 对累积 ≥10 道错题的学生，诊断覆盖率 ≥ 95% |

---

## 2. 核心概念与数据模型

### 2.1 核心概念

```
┌───────────────────────────────────────────────────────┐
│                     错误模式画像                        │
│  (ErrorPatternProfile)                                │
│                                                       │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐  │
│  │ 错误聚类组   │  │ 薄弱知识点   │  │ 诊断报告     │  │
│  │ ErrorCluster │  │ WeakKP      │  │ Diagnosis    │  │
│  └─────────────┘  └─────────────┘  └──────────────┘  │
│                                                       │
│  ┌─────────────┐  ┌─────────────┐                    │
│  │ 错误根因标签 │  │ 补救路径     │                    │
│  │ RootCause   │  │ RemedyPath  │                    │
│  └─────────────┘  └─────────────┘                    │
└───────────────────────────────────────────────────────┘
```

| 概念 | 说明 |
|------|------|
| **ErrorCluster（错误聚类组）** | 将多道错题按错误根因归为一组，同一组内的题目具有相似的错误模式 |
| **RootCause（错误根因）** | 对错误聚类组的归因标签，如"概念不清"、"审题失误"、"计算错误"等 |
| **WeakKP（薄弱知识点）** | 通过聚类分析识别出的掌握不足的知识点，附带严重程度和趋势 |
| **Diagnosis（诊断报告）** | 一次诊断的完整结果，包含聚类结果、薄弱点列表、补救建议 |
| **RemedyPath（补救路径）** | 针对诊断结果生成的学习补救方案 |
| **ErrorPatternProfile（错误模式画像）** | 学生长期累积的错误模式综合画像，持续更新 |

### 2.2 错误根因分类体系

```typescript
/**
 * 错误根因一级分类
 */
enum ErrorRootCause {
  /** 概念性错误 - 对知识点理解有偏差或缺失 */
  CONCEPTUAL = 'CONCEPTUAL',
  /** 操作性失误 - 理解正确但执行出错 */
  PROCEDURAL = 'PROCEDURAL',
  /** 审题失误 - 未能正确理解题意 */
  READING = 'READING',
  /** 策略性错误 - 解题方法选择不当 */
  STRATEGIC = 'STRATEGIC',
  /** 知识关联缺失 - 未能将多个知识点综合运用 */
  INTEGRATION = 'INTEGRATION',
}

/**
 * 概念性错误二级分类
 */
enum ConceptualErrorType {
  CONCEPT_MISSING = 'CONCEPT_MISSING',           // 概念完全缺失
  CONCEPT_CONFUSION = 'CONCEPT_CONFUSION',         // 概念混淆
  CONCEPT_MISAPPLICATION = 'CONCEPT_MISAPPLICATION', // 概念误用
  CONCEPT_INCOMPLETE = 'CONCEPT_INCOMPLETE',       // 概念理解不完整
}

/**
 * 操作性失误二级分类
 */
enum ProceduralErrorType {
  CALCULATION = 'CALCULATION',           // 计算失误
  SIGN_ERROR = 'SIGN_ERROR',             // 符号错误
  UNIT_ERROR = 'UNIT_ERROR',             // 单位换算错误
  FORMAT_ERROR = 'FORMAT_ERROR',         // 格式/书写规范错误
  CARELESS = 'CARELESS',                 // 粗心大意（抄错、漏看等）
}

/**
 * 审题失误二级分类
 */
enum ReadingErrorType {
  MISREAD_CONDITION = 'MISREAD_CONDITION',   // 漏看/误读条件
  WRONG_QUESTION_GOAL = 'WRONG_QUESTION_GOAL', // 理解错问题目标
  IGNORE_KEYWORD = 'IGNORE_KEYWORD',         // 忽略关键限制词
  OVERLOOK_NEGATION = 'OVERLOOK_NEGATION',   // 忽略否定词
}

/**
 * 薄弱程度等级
 */
enum WeaknessLevel {
  CRITICAL = 'CRITICAL',   // 严重薄弱 - 正确率 < 30%
  SEVERE = 'SEVERE',       // 较重薄弱 - 正确率 30-50%
  MODERATE = 'MODERATE',   // 中度薄弱 - 正确率 50-70%
  MILD = 'MILD',           // 轻度薄弱 - 正确率 70-85%
}
```

### 2.3 数据库表结构

#### 2.3.1 错误聚类组表 `error_cluster`

```sql
CREATE TABLE error_cluster (
    id                  BIGSERIAL PRIMARY KEY,
    cluster_id          VARCHAR(36) NOT NULL,          -- 聚类唯一标识 (UUID)
    student_id          BIGINT NOT NULL,               -- 学生 ID
    subject_id          BIGINT NOT NULL,               -- 学科 ID
    root_cause          VARCHAR(30) NOT NULL,          -- 一级根因: ErrorRootCause
    sub_cause           VARCHAR(40),                   -- 二级根因
    error_count         INT NOT NULL DEFAULT 0,        -- 聚类内错题数量
    first_occurred_at   TIMESTAMPTZ NOT NULL,          -- 最早出错时间
    last_occurred_at    TIMESTAMPTZ NOT NULL,          -- 最近出错时间
    recurrence_count    INT NOT NULL DEFAULT 0,        -- 同类错误重复出现次数（订正后仍错）
    description         TEXT,                          -- AI 生成的聚类描述
    confidence_score    DECIMAL(5,4),                  -- 聚类置信度 (0-1)
    status              VARCHAR(20) NOT NULL DEFAULT 'ACTIVE', -- ACTIVE / RESOLVED / MERGED
    resolved_at         TIMESTAMPTZ,                   -- 解消时间
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    CONSTRAINT uk_cluster_id UNIQUE (cluster_id)
);

CREATE INDEX idx_error_cluster_student ON error_cluster (student_id, subject_id);
CREATE INDEX idx_error_cluster_status ON error_cluster (student_id, status);
CREATE INDEX idx_error_cluster_root_cause ON error_cluster (root_cause, sub_cause);
```

#### 2.3.2 聚类-错题关联表 `error_cluster_item`

```sql
CREATE TABLE error_cluster_item (
    id                  BIGSERIAL PRIMARY KEY,
    cluster_id          VARCHAR(36) NOT NULL,          -- 关联 error_cluster.cluster_id
    mistake_record_id   BIGINT NOT NULL,               -- 关联错题记录 ID
    similarity_score    DECIMAL(5,4),                  -- 与聚类中心的相似度 (0-1)
    assigned_at         TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    CONSTRAINT uk_cluster_mistake UNIQUE (cluster_id, mistake_record_id)
);

CREATE INDEX idx_eci_cluster ON error_cluster_item (cluster_id);
CREATE INDEX idx_eci_mistake ON error_cluster_item (mistake_record_id);
```

#### 2.3.3 薄弱知识点诊断表 `weak_knowledge_point`

```sql
CREATE TABLE weak_knowledge_point (
    id                  BIGSERIAL PRIMARY KEY,
    student_id          BIGINT NOT NULL,
    knowledge_point_id  BIGINT NOT NULL,               -- 关联知识点 ID
    subject_id          BIGINT NOT NULL,
    weakness_level      VARCHAR(20) NOT NULL,          -- WeaknessLevel
    error_cluster_ids   JSONB,                         -- 关联的错误聚类组 ID 列表 ["uuid1", "uuid2"]
    total_attempts      INT NOT NULL DEFAULT 0,        -- 该知识点总答题次数
    total_errors        INT NOT NULL DEFAULT 0,        -- 该知识点总错误次数
    accuracy_rate       DECIMAL(5,4),                  -- 当前正确率 (0-1)
    trend               VARCHAR(10),                   -- IMPROVING / STABLE / DECLINING
    depth_score         DECIMAL(5,4),                  -- 薄弱深度分 (0-1, 越高越严重)
    is_conceptual       BOOLEAN NOT NULL DEFAULT FALSE, -- 是否为概念性薄弱
    last_diagnosed_at   TIMESTAMPTZ NOT NULL,
    first_diagnosed_at  TIMESTAMPTZ NOT NULL,
    resolved_at         TIMESTAMPTZ,
    status              VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',

    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    CONSTRAINT uk_student_kp UNIQUE (student_id, knowledge_point_id)
);

CREATE INDEX idx_wkp_student ON weak_knowledge_point (student_id, subject_id, status);
CREATE INDEX idx_wkp_level ON weak_knowledge_point (student_id, weakness_level);
```

#### 2.3.4 诊断报告表 `diagnosis_report`

```sql
CREATE TABLE diagnosis_report (
    id                  BIGSERIAL PRIMARY KEY,
    report_id           VARCHAR(36) NOT NULL,          -- 报告唯一标识 (UUID)
    student_id          BIGINT NOT NULL,
    subject_id          BIGINT,                        -- NULL 表示全科诊断
    diagnosis_type      VARCHAR(30) NOT NULL,          -- INCREMENTAL / FULL / MANUAL / PRE_EXAM
    trigger_source      VARCHAR(30) NOT NULL,          -- AUTO / USER / CRON / EXAM
    cluster_count       INT NOT NULL DEFAULT 0,        -- 本次发现的聚类数
    new_weak_kps        INT NOT NULL DEFAULT 0,        -- 新发现的薄弱知识点数
    resolved_kps        INT NOT NULL DEFAULT 0,        -- 本轮解消的薄弱知识点数
    summary             TEXT NOT NULL,                  -- AI 生成的诊断摘要
    detail_json         JSONB NOT NULL,                -- 完整诊断结果 JSON
    ai_model_used       VARCHAR(50),                   -- 使用的 AI 模型标识
    processing_time_ms  INT,                           -- 诊断处理耗时
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    CONSTRAINT uk_report_id UNIQUE (report_id)
);

CREATE INDEX idx_diagnosis_report_student ON diagnosis_report (student_id, created_at DESC);
CREATE INDEX idx_diagnosis_report_type ON diagnosis_report (diagnosis_type, created_at DESC);
```

#### 2.3.5 补救路径表 `remedy_path`

```sql
CREATE TABLE remedy_path (
    id                  BIGSERIAL PRIMARY KEY,
    remedy_id           VARCHAR(36) NOT NULL,
    student_id          BIGINT NOT NULL,
    diagnosis_report_id VARCHAR(36),                   -- 关联诊断报告
    weak_kp_ids         JSONB NOT NULL,                -- 针对的薄弱知识点列表
    remedy_type         VARCHAR(30) NOT NULL,          -- 类型: CONCEPT_REVIEW / TARGETED_PRACTICE / GUIDED_LEARNING
    priority            INT NOT NULL DEFAULT 5,        -- 优先级 1-10
    estimated_effort    VARCHAR(20),                   -- 预估用时: "15min" / "30min" / "1h" / "2h"
    steps               JSONB NOT NULL,                -- 补救步骤详情
    status              VARCHAR(20) NOT NULL DEFAULT 'PENDING', -- PENDING / IN_PROGRESS / COMPLETED / SKIPPED
    progress            DECIMAL(5,4) DEFAULT 0,        -- 完成进度 0-1
    started_at          TIMESTAMPTZ,
    completed_at        TIMESTAMPTZ,
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    CONSTRAINT uk_remedy_id UNIQUE (remedy_id)
);

CREATE INDEX idx_remedy_student ON remedy_path (student_id, status, priority);
```

### 2.4 核心数据结构定义

#### 2.4.1 诊断请求

```typescript
interface DiagnosisRequest {
  studentId: number;
  subjectId?: number;              // 不传则全科诊断
  diagnosisType: 'INCREMENTAL' | 'FULL' | 'MANUAL' | 'PRE_EXAM';
  triggerSource: 'AUTO' | 'USER' | 'CRON' | 'EXAM';
  /** 增量诊断时传入上次诊断时间，仅分析新增错题 */
  sinceTimestamp?: string;
  /** 强制重新全量分析 */
  forceFull?: boolean;
  /** 限定分析范围（如考前诊断仅分析特定章节） */
  scope?: {
    chapterIds?: number[];
    knowledgePointIds?: number[];
  };
}
```

#### 2.4.2 错误特征向量

```typescript
interface ErrorFeatureVector {
  mistakeRecordId: number;
  /** 知识点 ID 列表 */
  knowledgePointIds: number[];
  /** 一级学科 */
  subjectId: number;
  /** 题型 */
  questionType: string;
  /** 难度等级 1-5 */
  difficulty: number;
  /** 错误根因（AI 标注或用户自标） */
  rootCause?: string;
  subCause?: string;
  /** 错误文本特征（题干关键词提取） */
  textFeatures: number[];          // 嵌入向量 (dim=128)
  /** 是否已订正成功 */
  corrected: boolean;
  /** 订正后是否再次犯错 */
  recurred: boolean;
  /** 出错时间 */
  occurredAt: string;
}
```

#### 2.4.3 诊断报告详情 JSON 结构

```typescript
interface DiagnosisReportDetail {
  /** 错误概览 */
  overview: {
    totalErrors: number;
    analyzedErrors: number;
    clusterCount: number;
    newWeakKps: number;
    resolvedKps: number;
    improvementTrend: 'IMPROVING' | 'STABLE' | 'DECLINING';
  };
  /** 错误聚类详情 */
  clusters: Array<{
    clusterId: string;
    rootCause: string;
    subCause?: string;
    description: string;
    errorCount: number;
    recurrenceRate: number;          // 订正后再次出错的比例
    confidenceScore: number;
    relatedKnowledgePoints: Array<{
      kpId: number;
      kpName: string;
      weaknessLevel: string;
    }>;
    sampleMistakes: Array<{
      mistakeId: number;
      questionSummary: string;
      studentAnswer: string;
      correctAnswer: string;
    }>;
  }>;
  /** 薄弱知识点排名 */
  weakKnowledgePoints: Array<{
    kpId: number;
    kpName: string;
    subjectName: string;
    chapterName: string;
    weaknessLevel: string;
    accuracyRate: number;
    trend: string;
    depthScore: number;
    isConceptual: boolean;
    relatedClusterIds: string[];
  }>;
  /** 补救建议 */
  remedySuggestions: Array<{
    priority: number;
    weakKpIds: number[];
    remedyType: string;
    description: string;
    estimatedEffort: string;
    steps: Array<{
      step: number;
      action: string;
      targetUrl?: string;            // 跳转到对应学习内容
      resourceType?: string;
    }>;
  }>;
  /** AI 诊断摘要 */
  aiSummary: string;
}
```

---

## 3. 系统架构

### 3.1 整体架构

```
┌──────────────────────────────────────────────────────────────────┐
│                         调用方                                    │
│  错题服务  │  学情分析  │  家长报告  │  考试系统  │  定时任务       │
└─────┬──────┴─────┬──────┴─────┬──────┴─────┬──────┴────┬─────────┘
      │            │            │            │           │
      └────────────┴────────────┴─────┬──────┴───────────┘
                                      │
                    ┌─────────────────▼──────────────────┐
                    │   EPC-DDS API (诊断入口)             │
                    │   POST /diagnosis                   │
                    │   GET  /diagnosis/{reportId}        │
                    │   GET  /weak-points                 │
                    │   GET  /error-pattern-profile       │
                    └─────────────────┬──────────────────┘
                                      │
              ┌───────────────────────┼───────────────────────┐
              │                       │                       │
    ┌─────────▼──────────┐ ┌─────────▼──────────┐ ┌─────────▼──────────┐
    │  特征提取模块        │ │  聚类分析模块        │ │  诊断报告生成模块    │
    │  FeatureExtractor   │ │  ClusterAnalyzer   │ │  ReportGenerator   │
    │                     │ │                    │ │                    │
    │  - 错题特征向量化    │ │  - DBSCAN 聚类     │ │  - 报告组装        │
    │  - 知识点关联提取    │ │  - 层次聚类        │ │  - AI 摘要生成     │
    │  - 根因预标注       │ │  - 聚类合并/分裂   │ │  - 补救路径推荐    │
    └─────────┬──────────┘ └─────────┬──────────┘ └─────────┬──────────┘
              │                       │                       │
    ┌─────────▼──────────────────────▼───────────────────────▼──────────┐
    │                        共享依赖层                                   │
    │  知识点服务 │ 题目服务 │ AI模型调用 │ 向量检索 │ 错题服务          │
    └────────────────────────────────────────────────────────────────────┘
```

### 3.2 模块职责

| 模块 | 职责 | 关键算法 |
|------|------|----------|
| **FeatureExtractor** | 从错题记录中提取多维特征向量，包括知识点、文本语义、题型、难度、根因标签 | TF-IDF + Sentence Embedding |
| **ClusterAnalyzer** | 对特征向量进行聚类分析，将相似错误归组，识别高频错误模式 | DBSCAN + 层次聚类 + 自定义合并策略 |
| **RootCauseClassifier** | 对聚类结果进行根因分类，区分概念性错误与操作性失误 | 规则引擎 + AI 辅助分类 |
| **WeaknessDetector** | 从聚类结果中提取薄弱知识点，评估严重程度和趋势 | 统计分析 + 时序趋势检测 |
| **ReportGenerator** | 组装诊断报告，调用 AI 生成自然语言摘要和补救建议 | Prompt 编排 + LLM |
| **ProfileManager** | 维护学生错误模式画像的持续更新 | 增量更新策略 |

---

## 4. API 接口设计

### 4.1 触发诊断

```
POST /api/v1/diagnosis
```

**请求体：**

```json
{
  "subjectId": 3,
  "diagnosisType": "INCREMENTAL",
  "triggerSource": "AUTO",
  "sinceTimestamp": "2026-05-30T00:00:00.000Z"
}
```

**响应体（异步模式）：**

```json
{
  "code": 0,
  "data": {
    "reportId": "diag-20260607-abc123",
    "status": "PROCESSING",
    "estimatedWaitSeconds": 5
  }
}
```

**说明：**
- 诊断请求采用异步处理，立即返回 `reportId`
- 调用方通过轮询或 WebSocket 获取结果
- 同一学生同一学科 10 分钟内不允许重复触发

### 4.2 查询诊断报告

```
GET /api/v1/diagnosis/{reportId}
```

**响应体：**

```json
{
  "code": 0,
  "data": {
    "reportId": "diag-20260607-abc123",
    "studentId": 10001,
    "subjectId": 3,
    "diagnosisType": "INCREMENTAL",
    "status": "COMPLETED",
    "overview": {
      "totalErrors": 23,
      "analyzedErrors": 8,
      "clusterCount": 3,
      "newWeakKps": 1,
      "resolvedKps": 0,
      "improvementTrend": "STABLE"
    },
    "clusters": [
      {
        "clusterId": "clu-001",
        "rootCause": "CONCEPTUAL",
        "subCause": "CONCEPT_CONFUSION",
        "description": "混淆「充分条件」与「必要条件」的判断逻辑，在充要条件判定题中反复出错",
        "errorCount": 4,
        "recurrenceRate": 0.75,
        "confidenceScore": 0.92,
        "relatedKnowledgePoints": [
          {
            "kpId": 3015,
            "kpName": "充分条件与必要条件",
            "weaknessLevel": "SEVERE"
          }
        ],
        "sampleMistakes": [
          {
            "mistakeId": 50023,
            "questionSummary": "判断 p: x²>4 是否为 q: x>2 的充分条件",
            "studentAnswer": "是充分条件",
            "correctAnswer": "不是充分条件（x<-2 也满足 p 但不满足 q）"
          }
        ]
      }
    ],
    "weakKnowledgePoints": [
      {
        "kpId": 3015,
        "kpName": "充分条件与必要条件",
        "subjectName": "数学",
        "chapterName": "常用逻辑用语",
        "weaknessLevel": "SEVERE",
        "accuracyRate": 0.35,
        "trend": "DECLINING",
        "depthScore": 0.82,
        "isConceptual": true,
        "relatedClusterIds": ["clu-001"]
      }
    ],
    "remedySuggestions": [
      {
        "priority": 1,
        "weakKpIds": [3015],
        "remedyType": "CONCEPT_REVIEW",
        "description": "建议先通过 AI 辅导重新理解充分条件与必要条件的定义，再进行专项练习",
        "estimatedEffort": "30min",
        "steps": [
          {
            "step": 1,
            "action": "通过 AI 对话复习充分条件、必要条件、充要条件的定义",
            "resourceType": "AI_DIALOG"
          },
          {
            "step": 2,
            "action": "完成 5 道基础判断题（判定给定命题的条件关系）",
            "resourceType": "PRACTICE"
          },
          {
            "step": 3,
            "action": "完成 3 道综合应用题（结合集合、不等式等知识）",
            "resourceType": "PRACTICE"
          }
        ]
      }
    ],
    "aiSummary": "近期在「充分条件与必要条件」相关题目中反复出现概念混淆，4 道错题中有 3 道在订正后仍出现类似错误。建议优先重新理解定义，再进行专项训练。整体学习状态保持稳定，其他知识点表现正常。",
    "createdAt": "2026-06-07T04:30:00.000Z"
  }
}
