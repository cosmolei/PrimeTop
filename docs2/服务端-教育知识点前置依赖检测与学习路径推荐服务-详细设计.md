# 服务端 - 教育知识点前置依赖检测与学习路径推荐服务

## 1. 模块概述

### 1.1 功能定位

本服务基于知识图谱中的知识点依赖关系（如"二次方程"依赖"一次方程"），当学生在某个知识点上表现不佳时，自动检测其前置知识是否掌握，并生成个性化的补学路径推荐。核心解决的问题是：**学生不会某道题，往往不是因为当前知识点不会，而是前置知识存在缺口**。

### 1.2 核心能力

1. **前置知识检测**：根据知识图谱的依赖边，递归查询当前知识点的前置知识链，结合学生的掌握度数据，定位真正的薄弱根因。
2. **学习路径生成**：基于学生的当前能力和目标知识点，生成一条从"当前掌握的知识"到"目标知识"的最优学习路径。
3. **缺口诊断报告**：输出一份结构化的知识缺口报告，包含缺口层级、严重程度、推荐补学顺序。
4. **路径动态调整**：随着学生补学进度的推进，实时更新推荐路径，跳过已掌握的中间节点。

### 1.3 在系统中的位置

```
知识图谱构建引擎 ──→ 知识点依赖关系数据 ──→ 本服务
                                               │
用户学习画像 ──────→ 知识点掌握度数据 ──────→ 本服务
                                               │
AI辅导引擎 ────────→ "学生不懂二次方程求根" ──→ 本服务
                                               ├──→ 缺口诊断报告（返回给 AI 辅导引擎）
                                               ├──→ 补学路径（返回给学习规划模块）
                                               └──→ 前置知识列表（返回给同步课堂模块）
```

### 1.4 与其他模块的关系

| 关联模块 | 交互方式 | 说明 |
|----------|----------|------|
| 知识点图数据库建模与图查询服务 | 读取 | 获取知识点依赖关系图 |
| 用户学习画像与能力维度模型 | 读取 | 获取学生各知识点的掌握度 |
| AI辅导全链路请求处理与编排设计 | 被调用 | AI辅导发现学生不理解时请求前置分析 |
| 学习规划模块 | 被调用 | 将补学路径转化为学习任务 |
| 同步课堂学习模块 | 被调用 | 推荐学生应先复习的前置章节 |
| 错题整理模块 | 被调用 | 根据错题归因触发前置检测 |
| 练习与测评系统 | 被调用 | 根据前置掌握度推荐练习 |

---

## 2. 数据模型

### 2.1 知识点依赖关系（存储于图数据库）

依赖关系由知识图谱构建引擎维护，本服务只读取。

```cypher
// 图数据库中的节点和边结构（已有，本服务查询用）
(:KnowledgePoint {
  id: "kp_001",
  name: "一元二次方程求根公式",
  subject: "math",
  grade_level: 8,           // 初二年级
  difficulty: 0.65,         // 难度系数 0-1
  textbook_id: "tb_pep_8",
  chapter_id: "ch_16_2"
})

// PREREQUISITE 边：A 是 B 的前置知识
(:KnowledgePoint {id: "kp_001"})-[:PREREQUISITE {
  strength: 0.9,            // 依赖强度 0-1
  mandatory: true           // 是否必须掌握的前置
}]->(:KnowledgePoint {id: "kp_002"})
```

### 2.2 学生知识点掌握度（由学习画像模块维护）

```sql
-- 表：student_knowledge_mastery（已有表，本服务读取）
CREATE TABLE student_knowledge_mastery (
    id              BIGSERIAL PRIMARY KEY,
    student_id      BIGINT NOT NULL,
    knowledge_id    VARCHAR(64) NOT NULL,
    mastery_level   DECIMAL(5,4) NOT NULL DEFAULT 0,  -- 掌握度 0~1
    confidence      DECIMAL(5,4) NOT NULL DEFAULT 0,  -- 置信度 0~1（基于答题次数）
    last_updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(student_id, knowledge_id)
);

CREATE INDEX idx_skm_student ON student_knowledge_mastery(student_id);
```

### 2.3 前置检测任务记录

```sql
-- 表：prerequisite_detection_log（新增）
CREATE TABLE prerequisite_detection_log (
    id              BIGSERIAL PRIMARY KEY,
    student_id      BIGINT NOT NULL,
    target_kp_id    VARCHAR(64) NOT NULL,           -- 目标知识点
    trigger_source  VARCHAR(32) NOT NULL,            -- 触发来源：ai_tutor / error_book / practice / manual
    trigger_context JSONB,                           -- 触发上下文（如对话ID、错题ID）
    detection_depth INTEGER NOT NULL DEFAULT 3,      -- 检测深度（递归层数）
    gaps_found      INTEGER NOT NULL DEFAULT 0,      -- 发现的缺口数
    gap_details     JSONB,                           -- 缺口详情（见 GapReport 结构）
    path_generated  JSONB,                           -- 生成的补学路径
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_pdl_student_time ON prerequisite_detection_log(student_id, created_at DESC);
CREATE INDEX idx_pdl_target ON prerequisite_detection_log(target_kp_id);
```

### 2.4 补学路径跟踪

```sql
-- 表：learning_path_tracking（新增）
CREATE TABLE learning_path_tracking (
    id              BIGSERIAL PRIMARY KEY,
    student_id      BIGINT NOT NULL,
    path_id         VARCHAR(64) NOT NULL,            -- 路径唯一ID
    target_kp_id    VARCHAR(64) NOT NULL,            -- 终点知识点
    status          VARCHAR(16) NOT NULL DEFAULT 'active',  -- active / completed / abandoned / expired
    path_nodes      JSONB NOT NULL,                  -- 有序知识点节点列表（含状态）
    current_step    INTEGER NOT NULL DEFAULT 1,      -- 当前步骤（从1开始）
    total_steps     INTEGER NOT NULL,                -- 总步骤数
    started_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    completed_at    TIMESTAMPTZ,
    expires_at      TIMESTAMPTZ,                     -- 路径过期时间
    UNIQUE(path_id)
);

CREATE INDEX idx_lpt_student ON learning_path_tracking(student_id, status);
CREATE INDEX idx_lpt_path_id ON learning_path_tracking(path_id);
```

### 2.5 核心 DTO 定义

```typescript
// ===== 前置知识缺口 =====
interface KnowledgeGap {
  knowledgeId: string;           // 知识点ID
  knowledgeName: string;         // 知识点名称
  depth: number;                 // 距离目标知识点的层级（1=直接前置, 2=前置的前置...）
  masteryLevel: number;          // 学生当前掌握度 0~1
  masteryThreshold: number;      // 达标阈值（通常 0.6）
  isGap: boolean;                // 是否判定为缺口
  mandatory: boolean;            // 是否为必须前置
  prerequisiteStrength: number;  // 依赖强度 0~1
  confidence: number;            // 掌握度置信度（答题次数太少时置信度低）
  subject: string;
  gradeLevel: number;
  textbookId: string;
  chapterId: string;
}

// ===== 缺口诊断报告 =====
interface GapDiagnosisReport {
  studentId: number;
  targetKnowledgeId: string;
  targetKnowledgeName: string;
  triggerSource: 'ai_tutor' | 'error_book' | 'practice' | 'manual';
  detectionTimestamp: string;        // ISO 8601
  maxDepth: number;
  totalPrerequisites: number;        // 检测的前置总数
  gapsFound: number;
  criticalGaps: number;              // 必须掌握但未掌握的缺口数
  gaps: KnowledgeGap[];              // 按层级排序的缺口列表
  rootCauses: string[];              // 最底层根因知识点ID列表
  recommendation: DiagnosisRecommendation;
}

interface DiagnosisRecommendation {
  action: 'immediate_prerequisite' | 'deep_remedy' | 'continue_with_support' | 'no_action';
  priorityGaps: string[];            // 优先补学的知识点ID（按优先级排序）
  estimatedEffort: number;           // 预估补学时长（分钟）
  suggestedLearningMode: 'review' | 'relearn' | 'guided_practice';
  message: string;                   // 给 AI 辅导引擎的建议话术
}

// ===== 学习路径 =====
interface LearningPath {
  pathId: string;
  studentId: number;
  targetKnowledgeId: string;
  targetKnowledgeName: string;
  nodes: LearningPathNode[];
  totalSteps: number;
  estimatedMinutes: number;          // 预估总时长
  createdAt: string;
  expiresAt: string;
}

interface LearningPathNode {
  step: number;                      // 步骤序号
  knowledgeId: string;
  knowledgeName: string;
  status: 'pending' | 'in_progress' | 'mastered' | 'skipped';
  masteryLevel: number;
  masteryThreshold: number;
  isCritical: boolean;               // 是否为关键路径节点
  suggestedActivities: LearningActivity[];
}

interface LearningActivity {
  type: 'review_chapter' | 'watch_explanation' | 'practice_basic' | 'practice_intermediate';
  description: string;
  estimatedMinutes: number;
  resourceRefs: {                    // 关联资源引用
    chapterId?: string;
    questionPoolId?: string;
    aiPromptTemplate?: string;
  }[];
}
```

---

## 3. API 接口设计

### 3.1 前置知识缺口检测

```
POST /api/v1/prerequisite/detect
```

**请求体：**
```json
{
  "studentId": 10086,
  "targetKnowledgeId": "kp_quadratic_formula",
  "maxDepth": 3,
  "triggerSource": "ai_tutor",
  "triggerContext": {
    "conversationId": "conv_abc123",
    "questionText": "用求根公式解方程 x² - 5x + 6 = 0"
  },
  "masteryThreshold": 0.6
}
```

**响应体：**
```json
{
  "code": 0,
  "data": {
    "studentId": 10086,
    "targetKnowledgeId": "kp_quadratic_formula",
    "targetKnowledgeName": "一元二次方程求根公式",
    "triggerSource": "ai_tutor",
    "detectionTimestamp": "2026-06-05T06:00:00.000Z",
    "maxDepth": 3,
    "totalPrerequisites": 8,
    "gapsFound": 3,
    "criticalGaps": 2,
    "gaps": [
      {
        "knowledgeId": "kp_quadratic_concept",
        "knowledgeName": "一元二次方程概念",
        "depth": 1,
        "masteryLevel": 0.3,
        "masteryThreshold": 0.6,
        "isGap": true,
        "mandatory": true,
        "prerequisiteStrength": 0.95,
        "confidence": 0.85,
        "subject": "math",
        "gradeLevel": 8,
        "textbookId": "tb_pep_8",
        "chapterId": "ch_16_1"
      },
      {
        "knowledgeId": "kp_linear_equation",
        "knowledgeName": "一元一次方程",
        "depth": 2,
        "masteryLevel": 0.45,
        "masteryThreshold": 0.6,
        "isGap": true,
        "mandatory": true,
        "prerequisiteStrength": 0.85,
        "confidence": 0.9,
        "subject": "math",
        "gradeLevel": 7,
        "textbookId": "tb_pep_7",
        "chapterId": "ch_3_2"
      },
      {
        "knowledgeId": "kp_square_root",
        "knowledgeName": "平方根与算术平方根",
        "depth": 2,
        "masteryLevel": 0.55,
        "masteryThreshold": 0.6,
        "isGap": true,
        "mandatory": false,
        "prerequisiteStrength": 0.6,
        "confidence": 0.7,
        "subject": "math",
        "gradeLevel": 7,
        "textbookId": "tb_pep_7",
        "chapterId": "ch_6_1"
      }
    ],
    "rootCauses": ["kp_linear_equation", "kp_square_root"],
    "recommendation": {
      "action": "deep_remedy",
      "priorityGaps": ["kp_linear_equation", "kp_quadratic_concept", "kp_square_root"],
      "estimatedEffort": 45,
      "suggestedLearningMode": "relearn",
      "message": "该生在求解一元二次方程时遇到困难，检测发现其前置知识存在缺口：一元一次方程和方程基础概念掌握不牢固。建议先复习一元一次方程的解法，再学习一元二次方程概念，最后学习求根公式。"
    }
  }
}
```

### 3.2 生成补学路径

```
POST /api/v1/prerequisite/learning-path
```

**请求体：**
```json
{
  "studentId": 10086,
  "targetKnowledgeId": "kp_quadratic_formula",
  "maxSteps": 8,
  "includeOptionalPrerequisites": false,
  "gapReportReference": "det_log_12345"
}
```

**响应体：**
```json
{
  "code": 0,
  "data": {
    "pathId": "path_a1b2c3d4",
    "studentId": 10086,
    "targetKnowledgeId": "kp_quadratic_formula",
    "targetKnowledgeName": "一元二次方程求根公式",
    "totalSteps": 4,
    "estimatedMinutes": 50,
    "createdAt": "2026-06-05T06:00:00.000Z",
    "expiresAt": "2026-06-12T06:00:00.000Z",
    "nodes": [
      {
        "step": 1,
        "knowledgeId": "kp_linear_equation",
        "knowledgeName": "一元一次方程",
        "status": "pending",
        "masteryLevel": 0.45,
        "masteryThreshold": 0.6,
        "isCritical": true,
        "suggestedActivities": [
          {
            "type": "review_chapter",
            "description": "复习一元一次方程概念与解法",
            "estimatedMinutes": 15,
            "resourceRefs": [
              { "chapterId": "ch_3_2" }
            ]
          },
          {
            "type": "practice_basic",
            "description": "一元一次方程基础练习（5题）",
            "estimatedMinutes": 10,
            "resourceRefs": [
              { "questionPoolId": "pool_linear_basic" }
            ]
          }
        ]
      },
      {
        "step": 2,
        "knowledgeId": "kp_quadratic_concept",
        "knowledgeName": "一元二次方程概念",
        "status": "pending",
        "masteryLevel": 0.3,
        "masteryThreshold": 0.6,
        "isCritical": true,
        "suggestedActivities": [
          {
            "type": "watch_explanation",
            "description": "AI讲解一元二次方程的定义与标准形式",
            "estimatedMinutes": 10,
            "resourceRefs": [
              { "aiPromptTemplate": "quadratic_concept_intro" }
            ]
          },
          {
            "type": "practice_basic",
            "description": "识别一元二次方程练习",
            "estimatedMinutes": 5,
            "resourceRefs": [
              { "questionPoolId": "pool_quadratic_identify" }
            ]
          }
        ]
      },
      {
        "step": 3,
        "knowledgeId": "kp_direct_formula",
        "knowledgeName": "直接开平方法",
        "status": "pending",
        "masteryLevel": 0.5,
        "masteryThreshold": 0.6,
        "isCritical": false,
        "suggestedActivities": [
          {
            "type": "review_chapter",
            "description": "复习直接开平方法解一元二次方程",
            "estimatedMinutes": 5,
            "resourceRefs": [
              { "chapterId": "ch_16_2_1" }
            ]
          }
        ]
      },
      {
        "step": 4,
        "knowledgeId": "kp_quadratic_formula",
        "knowledgeName": "一元二次方程求根公式",
        "status": "pending",
        "masteryLevel": 0.15,
        "masteryThreshold": 0.6,
        "isCritical": true,
        "suggestedActivities": [
          {
            "type": "watch_explanation",
            "description": "AI推导并讲解求根公式",
            "estimatedMinutes": 10,
            "resourceRefs": [
              { "aiPromptTemplate": "quadratic_formula_derive" }
            ]
          },
          {
            "type": "practice_intermediate",
            "description": "求根公式综合练习（5题）",
            "estimatedMinutes": 10,
            "resourceRefs": [
              { "questionPoolId": "pool_quadratic_formula" }
            ]
          }
        ]
      }
    ]
  }
}
```

### 3.3 查询补学路径进度

```
GET /api/v1/prerequisite/learning-path/{pathId}
```

**响应体：** 同 3.2，但各节点的 `status` 会反映实时进度。

### 3.4 更新补学路径步骤状态

```
PATCH /api/v1/prerequisite/learning-path/{pathId}/steps/{step}
```

**请求体：**
```json
{
  "status": "mastered",
  "masteryLevel": 0.75,
  "actualMinutes": 12
}
```

**响应体：**
```json
{
  "code": 0,
  "data": {
    "pathId": "path_a1b2c3d4",
    "currentStep": 3,
    "completedSteps": 2,
    "totalSteps": 4,
    "pathStatus": "active",
    "nextStep": {
      "step": 3,
      "knowledgeId": "kp_direct_formula",
      "knowledgeName": "直接开平方法"
    }
  }
}
```

### 3.5 批量前置检测（用于学习规划）

```
POST /api/v1/prerequisite/batch-detect
```

**请求体：**
```json
{
  "studentId": 10086,
  "targetKnowledgeIds": [
    "kp_quadratic_formula",
    "kp_similarity_triangle",
    "kp_quadratic_function"
  ],
  "maxDepth": 2,
  "triggerSource": "learning_plan"
}
```

**响应体：**
```json
{
  "code": 0,
  "data": {
    "studentId": 10086,
    "results": {
      "kp_quadratic_formula": {
        "gapsFound": 3,
        "criticalGaps": 2,
        "estimatedEffort": 45,
        "priorityGaps": ["kp_linear_equation", "kp_quadratic_concept"]
      },
      "kp_similarity_triangle": {
        "gapsFound": 1,
        "criticalGaps": 0,
        "estimatedEffort": 15,
        "priorityGaps": ["kp_parallel_line_properties"]
      },
      "kp_quadratic_function": {
        "gapsFound": 5,
        "criticalGaps": 3,
        "estimatedEffort": 90,
        "priorityGaps": ["kp_linear_equation", "kp_quadratic_concept", "kp_coordinate_system"]
      }
    },
    "mergedPriorityGaps": [
      "kp_linear_equation",
      "kp_quadratic_concept",
      "kp_coordinate_system",
      "kp_parallel_line_properties"
    ],
    "totalEstimatedEffort": 120,
    "dependencyOverlap": {
      "kp_linear_equation": ["kp_quadratic_formula", "kp_quadratic_function"],
      "kp_quadratic_concept": ["kp_quadratic_formula", "kp_quadratic_function"]
    }
  }
}
```

### 3.6 获取知识点的前置依赖链（调试/展示用）

```
GET /api/v1/prerequisite/dependency-chain/{knowledgeId}?maxDepth=5
```

**响应体：**
```json
{
  "code": 0,
  "data": {
    "knowledgeId": "kp_quadratic_formula",
    "knowledgeName": "一元二次方程求根公式",
    "maxDepth": 5,
    "chain": [
      {
        "depth": 1,
        "nodes": [
          { "knowledgeId": "kp_quadratic_concept", "name": "一元二次方程概念", "mandatory": true, "strength": 0.95 },
          { "knowledgeId": "kp_direct_formula", "name": "直接开平方法", "mandatory": true, "strength": 0.8 },
          { "knowledgeId": "kp_factoring", "name": "因式分解法", "mandatory": false, "strength": 0.6 }
        ]
      },
      {
        "depth": 2,
        "nodes": [
          { "knowledgeId": "kp_linear_equation", "name": "一元一次方程", "mandatory": true, "strength": 0.85 },
          { "knowledgeId": "kp_square_root", "name": "平方根与算术平方根", "mandatory": true, "strength": 0.9 },
          { "knowledgeId": "kp_algebraic_expression", "name": "代数式", "mandatory": true, "strength": 0.7 },
          { "knowledgeId": "kp_polynomial_factor", "name": "多项式因式分解", "mandatory": true, "strength": 0.85 }
        ]
      }
    ]
  }
}
```

---

## 4. 核心算法

### 4.1 前置知识缺口检测算法

```python
class PrerequisiteDetector:
    """
    前置知识缺口检测器
    
    核心思路：BFS/DFS 遍历知识图谱的 PREREQUISITE 边，
    结合学生的掌握度数据，识别未达标的前置知识点。
    """
    
    def __init__(
        self,
        graph_client: GraphDatabaseClient,    # 图数据库客户端
        mastery_repo: MasteryRepository,       # 掌握度数据仓库
        config: DetectionConfig,
    ):
        self.graph = graph_client
        self.mastery = mastery_repo
        self.config = config
    
    async def detect(
        self,
        student_id: int,
        target_kp_id: str,
        max_depth: int = 3,
        mastery_threshold: float = 0.6,
        min_confidence: float = 0.3,
    ) -> GapDiagnosisReport:
        """
        执行前置知识缺口检测。
        
        算法：带剪枝的广度优先遍历
        1. 从目标知识点出发，沿 PREREQUISITE 边向外扩展
        2. 每层检查学生的掌握度是否低于阈值
        3. 如果某个前置知识点已掌握且为唯一路径，则剪枝不再深入
        4. 如果某个前置知识点未掌握但置信度太低（答题次数不足），
           标记为"待验证"而非确定缺口
        """
        # Step 1: 批量获取学生在相关知识点上的掌握度
        # 先用图查询获取所有可能涉及的知识点
        all_prereqs = await self.graph.traverse_prerequisites(
            target_kp_id, max_depth=max_depth
        )
        
        kp_ids = {target_kp_id}
        for depth_nodes in all_prereqs.values():
            kp_ids.update(n['knowledgeId'] for n in depth_nodes)
        
        # 批量获取掌握度（减少DB查询次数）
        mastery_map = await self.mastery.batch_get_mastery(
            student_id, list(kp_ids)
        )
        
        # Step 2: BFS 遍历，标记缺口
        gaps: list[KnowledgeGap] = []
        visited: set[str] = set()
        root_causes: list[str] = []
        
        # 队列：(当前知识点ID, 当前深度)
        queue = deque()
        queue.append((target_kp_id, 0))
        visited.add(target_kp_id)
        
        while queue:
            current_kp, depth = queue.popleft()
            
            if depth >= max_depth:
                continue
            
            # 获取当前知识点的直接前置
            direct_prereqs = all_prereqs.get(depth + 1, [])
            prereqs_for_current = [
                p for p in direct_prereqs
                # 这里简化了，实际需要根据图结构确定哪些是 current_kp 的直接前置
            ]
            
            for prereq in prereqs_for_current:
                if prereq['knowledgeId'] in visited:
                    continue
                visited.add(prereq['knowledgeId'])
                
                # 获取掌握度
                mastery_data = mastery_map.get(prereq['knowledgeId'])
                mastery_level = mastery_data.mastery_level if mastery_data else 0.0
                confidence = mastery_data.confidence if mastery_data else 0.0
                
                is_gap = mastery_level < mastery_threshold
                
                gap = KnowledgeGap(
                    knowledge_id=prereq['knowledgeId'],
                    knowledge_name=prereq['name'],
                    depth=depth + 1,
                    mastery_level=mastery_level,
                    mastery_threshold=mastery_threshold,
                    is_gap=is_gap,
                    mandatory=prereq.get('mandatory', True),
                    prerequisite_strength=prereq.get('strength', 1.0),
                    confidence=confidence,
                    subject=prereq.get('subject', ''),
                    grade_level=prereq.get('grade_level', 0),
                    textbook_id=prereq.get('textbook_id', ''),
                    chapter_id=prereq.get('chapter_id', ''),
                )
                
                if is_gap:
                    gaps.append(gap)
                    
                    # 判断是否为根因：该缺口没有更深层的前置缺口
                    deeper_prereqs = all_prereqs.get(depth + 2, [])
                    has_deeper_gap = any(
                        dp['knowledgeId'] not in visited
                        for dp in deeper_prereqs
                    )
                    if not has_deeper_gap or depth + 1 == max_depth:
                        root_causes.append(prereq['knowledgeId'])
                    
                    # 未掌握的前置需要继续深入检查其前置
                    queue.append((prereq['knowledgeId'], depth + 1))
                else:
                    # 已掌握的前置：如果是 mandatory 且已充分掌握，可以剪枝
                    # 但如果是 non-mandatory 的，不剪枝（可能还有其他路径）
                    if prereq.get('mandatory', True) and confidence >= min_confidence:
                        pass  # 剪枝，不继续深入
                    else:
                        queue.append((prereq['knowledgeId'], depth + 1))
        
        # Step 3: 生成推荐
        recommendation = self._generate_recommendation(
            gaps, root_causes, mastery_threshold
        )
        
        return GapDiagnosisReport(
            student_id=student_id,
            target_knowledge_id=target_kp_id,
            target_knowledge_name=await self.graph.get_kp_name(target_kp_id),
            trigger_source='',  # 由调用方填充
            detection_timestamp=datetime.utcnow().isoformat(),
            max_depth=max_depth,
            total_prerequisites=len(visited) - 1,  # 排除目标知识点自身
            gaps_found=len(gaps),
            critical_gaps=len([g for g in gaps if g.mandatory]),
            gaps=sorted(gaps, key=lambda g: (g.depth, -g.prerequisite_strength)),
            root_causes=root_causes,
            recommendation=recommendation,
        )
    
    def _generate_recommendation(
        self,
        gaps: list[KnowledgeGap],
        root_causes: list[str],
        threshold: float,
    ) -> DiagnosisRecommendation:
        """
        根据缺口情况生成推荐动作。
        
        决策逻辑：
        - 无缺口 → no_action
        - 仅有1层浅层缺口且数量≤2 → immediate_prerequisite
        - 缺口数量多或有多层深层缺口 → deep_remedy
        - 缺口是非强制性的且掌握度接近阈值 → continue_with_support
        """
        if not gaps:
            return DiagnosisRecommendation(
                action='no_action',
                priority_gaps=[],
                estimated_effort=0,
                suggested_learning_mode='review',
                message='未发现前置知识缺口，可以继续学习当前知识点。',
            )
        
        critical_gaps = [g for g in gaps if g.mandatory]
        shallow_gaps = [g for g in gaps if g.depth == 1]
        deep_gaps = [g for g in gaps if g.depth > 1]
        
        # 按优先级排序：先深层（根因），再浅层；同层按依赖强度降序
        priority_gaps = sorted(
            gaps,
            key=lambda g: (-g.depth, -g.prerequisite_strength, g.mastery_level),
        )
        priority_gap_ids = [g.knowledge_id for g in priority_gaps]
        
        # 预估补学时长：每