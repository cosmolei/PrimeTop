# 服务端-大模型流式输出实时安全过滤中间件与动态拦截替换引擎-详细设计

## 1. 概述

### 1.1 模块定位

大模型流式输出实时安全过滤中间件（Streaming Output Safety Filter，简称 **SOSF**）是部署在 AI 服务层与客户端之间的流式内容安全网关。当大模型以 SSE（Server-Sent Events）或 WebSocket 方式流式输出回答时，SOSF 负责在每个 token chunk 到达客户端之前进行**毫秒级实时安全检测**，在不破坏用户流式阅读体验的前提下，拦截和替换不适宜内容。

### 1.2 核心挑战

| 挑战 | 说明 |
| --- | --- |
| **部分文本匹配** | 敏感词可能被拆分到多个 SSE chunk 中（如"考" + "试答案"），需要跨 chunk 滑动窗口 |
| **超低延迟要求** | 每个 chunk 的过滤延迟必须控制在 50ms 以内，否则影响流式阅读体验 |
| **语义级检测** | 单纯关键词无法识别变体表达、暗语、隐喻等语义层面的不当内容 |
| **优雅中断** | 检测到违规内容时需要平滑中断流并输出替换文本，不能让客户端出现明显断裂 |
| **上下文感知** | 同一表述在不同学段、学科场景下合规性不同（如高中生物 vs 小学对话） |
| **流恢复** | 中断或替换后，需要尽可能保持后续内容的连贯性 |

### 1.3 与现有模块的边界

| 现有模块 | 职责 | 与本模块关系 |
| --- | --- | --- |
| AI输入安全与教育对话护栏引擎 | 输入侧意图识别与 prompt 注入安全 | **上游**：输入安全通过后才进入模型推理 |
| 教育场景敏感词多层次过滤引擎 | 离线/批量内容的多层敏感词过滤 | **共享词库**：复用其敏感词库与分类体系 |
| AI对话安全审计与敏感内容自动上报 | 事后审计与上报 | **下游消费者**：SOSF 的拦截事件发送给它做审计 |
| AI回答后处理与智能优化管线 | 非流式场景的后处理 | **互补**：非流式场景由其处理，流式场景由 SOSF 处理 |
| AI幻觉检测与教育事实校验引擎 | 事实性校验 | **并行管道**：幻觉检测可以异步执行，不影响流式输出 |
| SSE流式响应与AI增量渲染引擎 | SSE 协议管理与客户端渲染 | **传输层**：SOSF 作为 SSE 管道中的中间件 |

### 1.4 设计目标

```
┌─────────────────────────────────────────────────────────────┐
│                      性能目标                                │
├─────────────────────────────────────────────────────────────┤
│  单 chunk 过滤延迟：   < 20ms (P99)                         │
│  端到端 SSE 延迟增加：  < 50ms (P99)                        │
│  吞吐量：             ≥ 10,000 chunks/s (单节点)           │
│  内存占用：           < 512MB (单连接滑动窗口)              │
│  规则数支持：         ≥ 100,000 条敏感规则                  │
│  误拦截率：           < 0.1%                                │
│  漏拦截率：           < 0.5%                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 架构设计

### 2.1 整体架构

```
                         客户端 (APP / Web)
                              ▲
                              │ SSE / WebSocket
                              ▼
                    ┌─────────────────┐
                    │  API Gateway     │
                    └────────┬────────┘
                             │
                              ▼
               ┌──────────────────────────┐
               │   AI 服务编排层            │
               │   (Prompt → LLM API)     │
               └────────────┬─────────────┘
                            │ 流式 chunk
                            ▼
          ┌─────────────────────────────────────┐
          │      SOSF 流式安全过滤中间件          │
          │                                     │
          │  ┌───────┐  ┌────────┐  ┌────────┐ │
          │  │ L1:   │→│ L2:    │→│ L3:    │ │
          │  │ 关键词 │  │ 规则  │  │ 语义  │ │
          │  │ AC自动│  │ 引擎  │  │ 分类  │ │
          │  │ 机匹配│  │       │  │ 异步  │ │
          │  └───────┘  └────────┘  └────────┘ │
          │       │          │         │       │
          │       ▼          ▼         ▼       │
          │  ┌──────────────────────────────┐  │
          │  │    决策仲裁器 (Decision)      │  │
          │  └──────────────┬───────────────┘  │
          │                 │                  │
          │     ┌───────────┼───────────┐      │
          │     ▼           ▼           ▼      │
          │  [通过]     [替换]      [中断]     │
          │     │           │           │      │
          │     ▼           ▼           ▼      │
          │  ┌──────────────────────────────┐  │
          │  │    输出缓冲区 (Output Buffer) │  │
          │  └──────────────────────────────┘  │
          │                 │                  │
          └─────────────────┼──────────────────┘
                            │
                            ▼
                    SSE / WebSocket → 客户端
```

### 2.2 三层过滤管道

SOSF 采用**三层级联过滤**架构，按延迟从低到高、精度从粗到细排列：

| 层级 | 名称 | 延迟 | 机制 | 拦截类型 |
| --- | --- | --- | --- | --- |
| **L1** | 关键词层 | < 1ms | AC 自动机多模式匹配 | 精确敏感词、变体词 |
| **L2** | 规则引擎层 | < 5ms | 正则 + 规则模板 + 上下文条件 | 格式化违规、答案直接泄露、联系方式 |
| **L3** | 语义分类层 | < 20ms (异步) | 轻量分类模型 + 置信度判断 | 隐喻、暗示、语义不当内容 |

**L3 异步设计**：由于语义分类可能偶尔超过 20ms，L3 采用**延迟判定 + 回溯拦截**策略：
- 正常情况下 L3 在 chunk 到达输出缓冲区之前完成
- 如果 L3 处理超时，chunk 暂不释放，等待 L3 结果（最多等 50ms）
- 如果 50ms 内 L3 仍未完成，放行当前 chunk，异步标记为"待复审"
- 后续如果 L3 判定违规，对后续 chunk 执行拦截并触发告警

### 2.3 核心组件

```java
// 核心组件接口定义
public interface StreamingSafetyFilter {

    /**
     * 初始化一个流式过滤会话
     * @param context 会话上下文（学段、学科、场景等）
     * @return 会话ID
     */
    String initSession(FilterContext context);

    /**
     * 对流入的 chunk 进行安全过滤
     * @param sessionId 会话ID
     * @param chunk 模型输出的文本片段
     * @return 过滤结果（通过/替换内容/中断信号）
     */
    FilterResult filter(String sessionId, String chunk);

    /**
     * 流结束时清理资源
     */
    void closeSession(String sessionId);
}

public class FilterContext {
    private String userId;
    private SchoolPhase phase;       // 学段: KINDERGARTEN/PRIMARY/JUNIOR/SENIOR
    private String subject;          // 学科
    private SceneType sceneType;     // 场景: AI_TUTORING/PHOTO_SEARCH/ESSAY/etc.
    private boolean answerControlEnabled;  // 是否开启答案管控
    private List<String> blockedCategories; // 额外屏蔽类别
}

public class FilterResult {
    private FilterAction action;     // PASS / REPLACE / INTERRUPT / BUFFER
    private String content;          // 替换后的内容（REPLACE 时有效）
    private String reason;           // 拦截原因
    private ViolationLevel level;    // LOW / MEDIUM / HIGH / CRITICAL
    private List<String> matchedRules; // 命中的规则ID列表
    private boolean needsReview;     // 是否需要异步人工复审
}
```

---

## 3. 数据模型

### 3.1 敏感词库设计

#### 3.1.1 敏感词分类体系

```
敏感词分类（与"敏感词多层次过滤引擎"共享）
├── 01_政治敏感 (POLITICAL)
│   ├── 0101_违禁言论
│   ├── 0102_政治人物不当提及
│   └── 0103_政治事件
├── 02_色情低俗 (PORN_VULGAR)
│   ├── 0201_直白色情描写
│   ├── 0202_低俗暗示
│   └── 0203_不适宜未成年人性内容
├── 03_暴力血腥 (VIOLENCE)
│   ├── 0301_暴力描写
│   ├── 0302_自残自杀诱导
│   └── 0303_违法犯罪方法
├── 04_教育场景特有 (EDUCATION_SPECIFIC)
│   ├── 0401_直接给出考试答案（答案管控场景）
│   ├── 0402_代写承诺（"我帮你写"）
│   ├── 0403_鼓励抄袭
│   └── 0404_不当教育价值观
├── 05_个人隐私 (PRIVACY)
│   ├── 0501_诱导学生提供个人信息
│   ├── 0502_家庭住址/联系方式泄露
│   └── 0503_诱导添加社交账号
├── 06_商业引流 (COMMERCIAL)
│   ├── 0601_竞品推荐
│   ├── 0602_外部链接引流
│   └── 0603_广告内容
├── 07_心理危害 (PSYCHOLOGICAL)
│   ├── 0701_负面心理暗示
│   ├── 0702_不当比较贬低
│   └── 0703_焦虑诱导
└── 08_学段特有 (PHASE_SPECIFIC)
    ├── 0801_超出幼儿认知的复杂概念
    ├── 0802_小学阶段不宜的暴力/复杂社会议题
    └── 0803_初高中阶段特有的敏感内容
```

#### 3.1.2 数据库表结构

```sql
-- 敏感词主表
CREATE TABLE `safety_keyword` (
    `id`             BIGINT       NOT NULL AUTO_INCREMENT,
    `keyword`        VARCHAR(256) NOT NULL COMMENT '敏感词原文',
    `keyword_hash`   VARCHAR(64)  NOT NULL COMMENT 'SHA-256哈希，用于去重',
    `category_code`  VARCHAR(10)  NOT NULL COMMENT '分类编码，如0201',
    `severity`       TINYINT      NOT NULL DEFAULT 2 COMMENT '严重程度 1-4',
    `phase_scope`    VARCHAR(50)  DEFAULT 'ALL' COMMENT '适用学段，逗号分隔',
    `subject_scope`  VARCHAR(50)  DEFAULT 'ALL' COMMENT '适用学科',
    `action_type`    VARCHAR(20)  NOT NULL DEFAULT 'REPLACE' COMMENT '拦截动作: REPLACE/INTERRUPT/MASK',
    `replacement`    VARCHAR(512) COMMENT '替换文本',
    `variants`       JSON         COMMENT '变体列表：谐音、拆字、拼音、英文等',
    `status`         TINYINT      NOT NULL DEFAULT 1 COMMENT '0=禁用 1=启用',
    `created_at`     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_hash` (`keyword_hash`),
    KEY `idx_category` (`category_code`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='敏感词库';

-- 规则模板表
CREATE TABLE `safety_rule` (
    `id`             BIGINT       NOT NULL AUTO_INCREMENT,
    `rule_name`      VARCHAR(128) NOT NULL COMMENT '规则名称',
    `rule_type`      VARCHAR(30)  NOT NULL COMMENT 'REGEX/TEMPLATE/COMPOSITE',
    `pattern`        TEXT         NOT NULL COMMENT '正则表达式或模板',
    `category_code`  VARCHAR(10)  NOT NULL,
    `severity`       TINYINT      NOT NULL DEFAULT 2,
    `conditions`     JSON         COMMENT '触发条件：学段、学科、场景等',
    `action_type`    VARCHAR(20)  NOT NULL DEFAULT 'REPLACE',
    `replacement`    VARCHAR(512),
    `description`    VARCHAR(512),
    `priority`       INT          NOT NULL DEFAULT 100 COMMENT '优先级，越小越先执行',
    `status`         TINYINT      NOT NULL DEFAULT 1,
    `created_at`     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_type_status` (`rule_type`, `status`),
    KEY `idx_priority` (`priority`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='安全规则库';

-- 拦截事件日志表
CREATE TABLE `safety_filter_log` (
    `id`              BIGINT       NOT NULL AUTO_INCREMENT,
    `log_id`          VARCHAR(36)  NOT NULL COMMENT 'UUID',
    `session_id`      VARCHAR(64)  NOT NULL COMMENT 'SOSF会话ID',
    `user_id`         BIGINT       NOT NULL,
    `conversation_id` VARCHAR(64)  COMMENT 'AI对话ID',
    `original_chunk`  TEXT         NOT NULL COMMENT '被拦截的原始内容',
    `filtered_chunk`  TEXT         COMMENT '替换后的内容',
    `layer`           VARCHAR(10)  NOT NULL COMMENT '拦截层: L1/L2/L3',
    `matched_keyword` VARCHAR(256) COMMENT '命中的敏感词',
    `matched_rule_id` BIGINT       COMMENT '命中的规则ID',
    `category_code`   VARCHAR(10)  NOT NULL,
    `severity`        TINYINT      NOT NULL,
    `action`          VARCHAR(20)  NOT NULL COMMENT 'REPLACE/INTERRUPT/MASK',
    `phase`           VARCHAR(20)  COMMENT '学段',
    `subject`         VARCHAR(20)  COMMENT '学科',
    `scene_type`      VARCHAR(30)  COMMENT '场景',
    `chunk_index`     INT          NOT NULL COMMENT 'chunk序号',
    `latency_ms`      INT          NOT NULL COMMENT '过滤耗时(ms)',
    `created_at`      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_log_id` (`log_id`),
    KEY `idx_session` (`session_id`),
    KEY `idx_user_time` (`user_id`, `created_at`),
    KEY `idx_category_time` (`category_code`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='流式过滤拦截日志';

-- 人工复审队列表
CREATE TABLE `safety_review_queue` (
    `id`             BIGINT       NOT NULL AUTO_INCREMENT,
    `log_id`         VARCHAR(36)  NOT NULL COMMENT '关联日志ID',
    `session_id`     VARCHAR(64)  NOT NULL,
    `original_chunk` TEXT         NOT NULL,
    `context_before` TEXT         COMMENT '前文上下文',
    `context_after`  TEXT         COMMENT '后文上下文',
    `review_status`  TINYINT      NOT NULL DEFAULT 0 COMMENT '0=待审 1=确认违规 2=误报 3=忽略',
    `reviewer_id`    BIGINT       COMMENT '审核员ID',
    `reviewed_at`    DATETIME     COMMENT '审核时间',
    `review_note`    VARCHAR(512) COMMENT '审核备注',
    `created_at`     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_status` (`review_status`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='安全过滤人工复审队列';
```

### 3.2 Redis 数据结构

```
# 敏感词 AC 自动机 Trie（序列化后存储，启动时加载到内存）
SOSF:ac_trie:v{version}  → Protobuf序列化的 Trie 数据结构
TTL: 无（通过版本号管理）

# 规则缓存
SOSF:rules:v{version}    → JSON序列化的规则列表
TTL: 无

# 会话上下文（每个流式过滤会话）
SOSF:session:{sessionId} → Hash {
    userId: "123456",
    phase: "PRIMARY",
    subject: "MATH",
    sceneType: "AI_TUTORING",
    answerControl: "true",
    chunkIndex: "42",
    slidingWindow: "...最近N个chunk的拼接文本...",
    violationCount: "0",
    createdAt: "2026-07-12T00:46:00Z"
}
TTL: 600s (10分钟，流式会话一般不超过10分钟)

# 热点拦截统计（实时计数，用于监控告警）
SOSF:stats:realtime     → Hash {
    total_chunks_today: "15234567",
    blocked_chunks_today: "1234",
    blocked_rate: "0.008%",
    l1_blocked: "890",
    l2_blocked: "234",
    l3_blocked: "110",
    avg_latency_ms: "3.2"
}
TTL: 86400s (每天重置)

# 用户级实时频率控制（防止恶意触发大量拦截）
SOSF:user_violation:{userId} → String (计数器)
TTL: 3600s (1小时窗口)
```

---

## 4. 详细设计

### 4.1 L1：AC 自动机关键词匹配层

#### 4.1.1 核心算法：改进的 Aho-Corasick 多模式匹配

选择 AC 自动机的原因：
- **多模式同时匹配**：一次扫描可同时匹配 10 万+ 敏感词
- **O(n) 时间复杂度**：扫描耗时仅与文本长度相关，与词库大小无关
- **成熟可靠**：工业界广泛使用的字符串匹配算法

#### 4.1.2 变体处理策略

```java
public class KeywordVariantExpander {

    // 变体生成策略
    public Set<String> expandVariants(String keyword) {
        Set<String> variants = new HashSet<>();
        variants.add(keyword);

        // 1. 谐音变体（中文特有）
        variants.addAll(homophoneVariants(keyword));
        // 示例："答案" → "答暗", "哒案"

        // 2. 拆字变体
        variants.addAll(splitCharVariants(keyword));
        // 示例："考试" → "考  试", "考.试", "考-试"

        // 3. 拼音变体
        variants.addAll(pinyinVariants(keyword));
        // 示例："答案" → "da'an", "da an"

        // 4. 全角/半角混淆
        variants.addAll(fullWidthVariants(keyword));
        // 示例："abc" → "ａｂｃ"

        // 5. 星号/符号混淆
        variants.addAll(symbolObfuscationVariants(keyword));
        // 示例："微信号" → "微☆信号", "微*信号"

        // 6. 英文大小写变体（针对英文敏感词）
        variants.addAll(caseVariants(keyword));

        return variants;
    }

    private Set<String> homophoneVariants(String keyword) {
        // 基于预构建的谐音字典生成同音字替换
        // 字典示例：答→[答,哒,搭], 案→[案,暗,按]
        Set<String> result = new HashSet<>();
        char[] chars = keyword.toCharArray();
        generateHomophones(chars, 0, new StringBuilder(), result);
        return result;
    }
}
```

#### 4.1.3 跨 Chunk 滑动窗口

这是流式过滤的**核心技术难点**。敏感词可能被拆分到多个 SSE chunk 中：

```
Chunk 1: "这道题的正确答"
Chunk 2: "案是C，因为..."
                    ↑ "答案" 跨越了两个 chunk
```

**解决方案：滑动窗口缓冲器**

```java
public class SlidingWindowBuffer {

    private final int windowSize;           // 窗口大小（字节），默认 256
    private final StringBuilder buffer;     // 累积缓冲区
    private int flushedPosition;            // 已安全刷新的位置

    /**
     * 写入新 chunk，返回可以被安全输出的文本段
     * 
     * 策略：
     * 1. 将新 chunk 追加到 buffer
     * 2. 在 buffer 的 [flushedPosition, end] 范围内执行 AC 匹配
     * 3. 找到所有匹配后，确定可安全输出的范围：
     *    - 从 flushedPosition 到「最后一个匹配结束位置之后」或「buffer 尾部减窗口余量」
     *    - 保留尾部 windowSize 字节作为下一轮的窗口前缀
     * 4. 返回可安全输出的文本，更新 flushedPosition
     */
    public WindowFlushResult flush(String newChunk, ACTrieMatcher matcher) {
        buffer.append(newChunk);
        int totalLen = buffer.length();

        // 在未刷新区域执行匹配
        String unchecked = buffer.substring(flushedPosition);
        List<Match> matches = matcher.match(unchecked);

        // 计算安全可输出边界
        int safeEndOffset = calculateSafeBoundary(unchecked.length(), matches, windowSize);

        // 提取可输出内容
        String flushable = unchecked.substring(0, safeEndOffset);
        String remaining = unchecked.substring(safeEndOffset);

        // 更新状态
        flushedPosition += safeEndOffset;

        // 检查缓冲区是否需要压缩
        if (flushedPosition > 4096) {
            buffer.delete(0, flushedPosition);
            flushedPosition = 0;
        }

        return new WindowFlushResult(
            flushable,    // 可安全输出的文本
            remaining,    // 暂留窗口中的文本
            matches       // 在本次匹配中发现的违规项
        );
    }

    private int calculateSafeBoundary(int uncheckedLen, List<Match> matches, int windowSize) {
        if (matches.isEmpty()) {
            // 无匹配：可输出到「总长度 - 窗口保留区」
            return Math.max(0, uncheckedLen - windowSize);
        }

        // 有匹配：输出到最后一个匹配起点之前
        // 匹配点及之后的内容需要被处理（替换/中断）
        Match lastMatch = matches.get(matches.size() - 1);
        return lastMatch.getStart();
    }
}
```

#### 4.1.4 AC 自动机构建与热更新

```java
public class ACTrieManager {

    private volatile ACTrie trie;        // 当前生效的 Trie
    private volatile long version;       // 当前版本号
    private final ScheduledExecutorService scheduler;

    /**
     * 构建新的 AC 自动机（全量重建）
     * 触发条件：词库变更量超过阈值（新增/修改 ≥ 100 条）
     */
    public void rebuildTrie(List<SafetyKeyword> keywords) {
        // 1. 构建新的 Trie
        ACTrie newTrie = new ACTrie();
        for (SafetyKeyword kw : keywords) {
            if (kw.getStatus() != 1) continue;

            // 添加原始词
            newTrie.add(kw.getKeyword(), kw);

            // 添加变体
            if (kw.getVariants() != null) {
                for (String variant : kw.getVariants()) {
                    newTrie.add(variant, kw);
                }
            }
        }
        newTrie.buildFailureLinks(); // 构建 failure 链接

        // 2. 原子切换
        long newVersion = version + 1;
        // 序列化到 Redis
        redis.set("SOSF:ac_trie:v" + newVersion, serialize(newTrie));
        // 内存切换
        this.trie = newTrie;
        this.version = newVersion;
        // 更新缓存版本标记
        redis.set("SOSF:ac_trie:current_version", String.valueOf(newVersion));
    }

    /**
     * 增量更新（少量词变更时使用）
     * 注意：AC 自动机不支持真正的增量更新，这里的增量是指：
     * 1. 收集变更词列表
     * 2. 在低峰期异步重建
     * 3. 平滑切换
     */
    @Scheduled(fixedDelay = 60000) // 每分钟检查一次
    public void checkAndIncrementalUpdate() {
        long lastVersion = Long.parseLong(redis.get("SOSF:ac_trie:current_version"));
        if (lastVersion > this.version) {
            // 发现新版本，从 Redis 加载
            byte[] data = redis