# 服务端-作文素材库管理与主题素材智能推荐服务 - 详细设计

> 本文档基于 `docs/design/启硕-PrimeTop-全学段AI辅助学习软件项目设计文档.md` 第 6.10 节（作文辅导模块 - 素材推荐功能）进行细化，聚焦服务端作文素材库的数据建模、内容管理、检索服务和智能推荐引擎的实现方案，为开发人员提供可直接编码的设计规范。

---

## 1. 概述

### 1.1 文档目的

定义 PrimeTop 作文素材库的数据结构、内容生产管线、分类标签体系、检索引擎和智能推荐算法，使开发人员能够据此实现完整的素材管理与推荐服务。

### 1.2 范围

| 范围内 | 范围外 |
| --- | --- |
| 素材数据模型与存储设计 | 作文批改评分引擎（见《服务端-作文批改评分引擎与写作能力评估服务》） |
| 素材分类标签体系 | 作文辅导页面交互（见《客户端-作文辅导页面架构与批改交互设计》） |
| 素材内容生产与审核流程 | 富文本编辑器（见《客户端-富文本编辑器与作文书写引擎》） |
| 全文检索与语义检索 | AI Prompt 编排系统（见《AI-Prompt编排与场景模板系统》） |
| 主题匹配推荐算法 | 向量数据库基础设施 |
| 学段适龄化过滤 | 搜索服务通用基础设施 |
| 个性化推荐（基于写作历史） | 通用推荐引擎框架 |
| 素材使用效果追踪 | 支付与会员权益校验 |
| 管理后台素材管理 API | 内容安全审核通用服务 |

### 1.3 术语定义

| 术语 | 含义 |
| --- | --- |
| Material | 素材，作文素材库中的单条内容单元 |
| MaterialType | 素材类型：quote（名言警句）、story（事例典故）、expression（表达句式）、vocabulary（好词好句）、opening（开头模板）、ending（结尾模板）、transition（过渡衔接） |
| Theme | 主题标签，素材关联的写作主题，如"坚持""成长""亲情""环保" |
| SceneTag | 场景标签，素材适用的写作场景，如"议论文论据""记叙文开头""描写抒情" |
| MaterialCollection | 素材合集，按主题或用途组织的素材集合 |
| WritingProfile | 写作能力画像，用于个性化推荐的数据基础 |

### 1.4 设计原则

1. **分类精细**：素材按类型、主题、场景、学段、文体等多维度标签化，支持精准匹配。
2. **适龄过滤**：高中素材不应出现在小学生推荐中，确保内容认知水平匹配。
3. **主题驱动**：以作文题目主题为核心匹配维度，优先推荐与主题强相关的素材。
4. **个性化补充**：基于用户历史写作数据和薄弱维度，补充个性化推荐。
5. **质量可控**：所有入库素材需经过审核，AI 生成素材需人工复核。
6. **扩展友好**：素材类型和标签体系可动态扩展，不依赖硬编码枚举。

---

## 2. 系统架构

### 2.1 服务定位

素材库服务属于内容服务域，在整体架构中的位置：

```
┌──────────────────────────────────────────────────────────────┐
│                        API 网关层                             │
├──────────────────────────────────────────────────────────────┤
│  BFF 聚合层  ──→  作文素材服务 (EssayMaterialService)          │
│                      │                                        │
│                      ├── 素材存储模块 (MaterialRepository)     │
│                      │                                        │
│                      ├── 素材检索模块 (MaterialSearchEngine)   │
│                      │   ├── 关键词检索 (ES)                   │
│                      │   └── 语义检索 (向量库)                  │
│                      │                                        │
│                      ├── 推荐引擎 (MaterialRecommender)       │
│                      │   ├── 主题匹配器                        │
│                      │   ├── 适龄过滤器                        │
│                      │   ├── 个性化排序器                      │
│                      │   └── 多样性打散器                      │
│                      │                                        │
│                      ├── 素材生产管线 (MaterialProducer)       │
│                      │   ├── AI 批量生成                       │
│                      │   ├── 外部导入                          │
│                      │   └── 用户沉淀（好词好句提取）            │
│                      │                                        │
│                      └── 效果追踪模块 (UsageTracker)           │
│                                                                │
├──────────────────────────────────────────────────────────────┤
│  MySQL  │  Elasticsearch  │  向量库(pgvector)  │  Redis       │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 与其他模块的交互

| 交互模块 | 交互方式 | 说明 |
| --- | --- | --- |
| 作文辅导模块 | 同步调用 | 根据作文题目获取推荐素材 |
| 作文批改引擎 | 异步事件 | 接收好词好句提取结果入库 |
| AI 服务 | 同步调用 | 素材语义向量化、AI 生成素材 |
| 内容管理后台 | REST API | 素材 CRUD、审核、批量操作 |
| 用户画像服务 | 同步调用 | 获取写作能力画像用于个性化推荐 |
| 搜索服务 | 内部调用 | 全文检索能力复用 |

---

## 3. 数据模型

### 3.1 素材主表 `essay_materials`

```sql
CREATE TABLE essay_materials (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '素材ID',
    -- 内容
    material_type   VARCHAR(20) NOT NULL COMMENT '素材类型: quote/story/expression/vocabulary/opening/ending/transition',
    title           VARCHAR(200) NOT NULL COMMENT '素材标题（用于展示和检索）',
    content         TEXT NOT NULL COMMENT '素材正文',
    source          VARCHAR(200) COMMENT '出处/作者（名言类必填，如 "苏轼《赤壁赋》"）',
    source_type     VARCHAR(20) COMMENT '来源类型: classic（古籍经典）/ modern（近现代）/ foreign（外国）/ ai_generated（AI生成）/ user_extracted（用户沉淀）',
    
    -- 分类标签
    themes          JSON NOT NULL COMMENT '主题标签 ["坚持","奋斗","成长"]',
    scene_tags      JSON COMMENT '场景标签 ["议论文论据","开头引入","结尾升华"]',
    genres          JSON COMMENT '适用文体 ["narrative","argumentative","expository"]',
    
    -- 学段适配
    min_stage       VARCHAR(10) NOT NULL DEFAULT 'primary' COMMENT '最低适用学段: kindergarten/primary/junior/senior',
    max_stage       VARCHAR(10) NOT NULL DEFAULT 'senior' COMMENT '最高适用学段',
    min_grade       INT COMMENT '最低适用年级（1-12，为空表示不限制）',
    max_grade       INT COMMENT '最高适用年级',
    difficulty_level TINYINT NOT NULL DEFAULT 1 COMMENT '理解难度: 1=简单 2=中等 3=较深',
    
    -- 统计
    usage_count     INT NOT NULL DEFAULT 0 COMMENT '被引用次数',
    like_count      INT NOT NULL DEFAULT 0 COMMENT '收藏次数',
    avg_score       DECIMAL(3,2) COMMENT '平均评分（用户反馈 1-5）',
    
    -- 状态
    status          TINYINT NOT NULL DEFAULT 0 COMMENT '状态: 0=草稿 1=待审核 2=已上架 3=已下架 4=审核拒绝',
    audit_remark    VARCHAR(500) COMMENT '审核备注',
    audited_by      BIGINT COMMENT '审核人ID',
    audited_at      DATETIME COMMENT '审核时间',
    
    -- 元数据
    source_id       VARCHAR(100) COMMENT '外部来源ID（导入时标记去重）',
    vector_status   TINYINT NOT NULL DEFAULT 0 COMMENT '向量化状态: 0=未处理 1=处理中 2=已完成 3=失败',
    vector_task_id  VARCHAR(100) COMMENT '向量化任务ID',
    
    created_by      BIGINT COMMENT '创建人ID（后台管理员/系统）',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_type_status (material_type, status),
    INDEX idx_stage (min_stage, max_stage),
    INDEX idx_usage (usage_count DESC),
    INDEX idx_created (created_at DESC),
    INDEX idx_source_id (source_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='作文素材库';
```

### 3.2 主题词表 `essay_material_themes`

```sql
CREATE TABLE essay_material_themes (
    id              INT PRIMARY KEY AUTO_INCREMENT,
    theme_name      VARCHAR(50) NOT NULL COMMENT '主题名称',
    theme_category  VARCHAR(30) NOT NULL COMMENT '主题分类: personal（个人品质）/ social（社会关系）/ nature（自然）/ culture（文化）/ philosophy（哲理）/ life（生活）',
    parent_id       INT COMMENT '父主题ID（支持层级，如"亲情"→"爱"）',
    synonym_names   JSON COMMENT '同义主题词 ["努力","拼搏","不屈"]',
    description     VARCHAR(500) COMMENT '主题描述',
    material_count  INT NOT NULL DEFAULT 0 COMMENT '关联素材数（冗余统计）',
    sort_order      INT NOT NULL DEFAULT 0 COMMENT '排序权重',
    is_hot          TINYINT NOT NULL DEFAULT 0 COMMENT '是否热门主题',
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '1=启用 0=停用',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_name (theme_name),
    INDEX idx_category (theme_category),
    INDEX idx_parent (parent_id),
    INDEX idx_hot (is_hot, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='作文素材主题词表';
```

### 3.3 素材与主题关联表 `essay_material_theme_rel`

```sql
CREATE TABLE essay_material_theme_rel (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    material_id     BIGINT NOT NULL COMMENT '素材ID',
    theme_id        INT NOT NULL COMMENT '主题ID',
    relevance_score DECIMAL(3,2) NOT NULL DEFAULT 0.80 COMMENT '相关性分数 0-1（人工标注或算法计算）',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_material_theme (material_id, theme_id),
    INDEX idx_theme (theme_id),
    INDEX idx_relevance (theme_id, relevance_score DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='素材-主题关联';
```

### 3.4 素材合集表 `essay_material_collections`

```sql
CREATE TABLE essay_material_collections (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    collection_name VARCHAR(100) NOT NULL COMMENT '合集名称',
    description     VARCHAR(500) COMMENT '合集描述',
    cover_image     VARCHAR(500) COMMENT '封面图URL',
    collection_type VARCHAR(20) NOT NULL DEFAULT 'theme' COMMENT '合集类型: theme（主题合集）/ exam（考试专题）/ skill（技能提升）/ system（系统推荐）',
    tags            JSON COMMENT '标签 ["中考","高考","必背"]',
    
    -- 适用范围
    min_stage       VARCHAR(10) COMMENT '适用学段',
    max_stage       VARCHAR(10) COMMENT '适用学段上限',
    
    -- 统计
    material_count  INT NOT NULL DEFAULT 0 COMMENT '素材数量',
    subscribe_count INT NOT NULL DEFAULT 0 COMMENT '订阅/收藏数',
    sort_order      INT NOT NULL DEFAULT 0,
    
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '1=启用 0=停用',
    created_by      BIGINT COMMENT '创建人',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_type_status (collection_type, status),
    INDEX idx_sort (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='素材合集';
```

### 3.5 素材使用记录表 `essay_material_usage_log`

```sql
CREATE TABLE essay_material_usage_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    material_id     BIGINT NOT NULL COMMENT '素材ID',
    task_id         BIGINT COMMENT '关联作文任务ID',
    usage_type      VARCHAR(20) NOT NULL COMMENT '使用类型: view（浏览）/ collect（收藏）/ quote（引用入文）/ share（分享）',
    recommend_scene VARCHAR(30) COMMENT '推荐场景: topic_match/personalized/hot/collection/search',
    position        INT COMMENT '在推荐列表中的位置（用于排序效果分析）',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_user (user_id, created_at DESC),
    INDEX idx_material (material_id, created_at DESC),
    INDEX idx_scene (recommend_scene, created_at DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='素材使用记录';
```

### 3.6 用户素材收藏表 `essay_material_favorites`

```sql
CREATE TABLE essay_material_favorites (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    material_id     BIGINT NOT NULL COMMENT '素材ID',
    folder_name     VARCHAR(50) NOT NULL DEFAULT 'default' COMMENT '收藏夹名称',
    note            VARCHAR(500) COMMENT '用户备注',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_user_material (user_id, material_id),
    INDEX idx_user_folder (user_id, folder_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户素材收藏';
```

### 3.7 Elasticsearch 索引设计

```json
{
  "essay_materials": {
    "mappings": {
      "properties": {
        "id": { "type": "long" },
        "materialType": { "type": "keyword" },
        "title": {
          "type": "text",
          "analyzer": "ik_max_word",
          "search_analyzer": "ik_smart",
          "fields": {
            "keyword": { "type": "keyword" }
          }
        },
        "content": {
          "type": "text",
          "analyzer": "ik_max_word",
          "search_analyzer": "ik_smart"
        },
        "source": {
          "type": "text",
          "analyzer": "ik_max_word"
        },
        "themes": { "type": "keyword" },
        "sceneTags": { "type": "keyword" },
        "genres": { "type": "keyword" },
        "minStage": { "type": "keyword" },
        "maxStage": { "type": "keyword" },
        "difficultyLevel": { "type": "integer" },
        "usageCount": { "type": "integer" },
        "status": { "type": "integer" },
        "createdAt": { "type": "date" },
        "suggest": {
          "type": "completion",
          "analyzer": "ik_max_word"
        }
      }
    }
  }
}
```

### 3.8 向量化存储（pgvector）

```sql
-- 在 PostgreSQL 中创建向量表
CREATE TABLE essay_material_vectors (
    id              BIGINT PRIMARY KEY,
    material_id     BIGINT NOT NULL REFERENCES essay_materials(id),
    embedding       vector(1024) NOT NULL COMMENT '素材内容语义向量',
    embedding_model VARCHAR(50) NOT NULL COMMENT '向量化使用的模型',
    created_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    
    INDEX idx_material (material_id)
);

-- 创建 HNSW 索引加速相似度检索
CREATE INDEX idx_embedding_cosine ON essay_material_vectors 
    USING hnsw (embedding vector_cosine_ops);
```

---

## 4. 素材标签体系设计

### 4.1 主题标签分类

素材主题按六大类别组织，支持两级层级：

| 一级分类 | 说明 | 二级示例 |
| --- | --- | --- |
| personal（个人品质） | 个人成长、品质修养 | 坚持、勇敢、诚信、勤奋、创新、乐观、自律、责任 |
| social（社会关系） | 人际、家国、社会 | 亲情、友情、师生、团结、合作、爱国、奉献、感恩 |
| nature（自然） | 自然环境、科技 | 环保、四季、山水、星辰、探索、科技、生态 |
| culture（文化） | 文化传承、艺术 | 读书、传统、历史、艺术、语言、文明、礼仪 |
| philosophy（哲理） | 思辨、哲理、人生 | 时间、选择、变化、得失、平凡、自由、幸福、价值 |
| life（生活） | 日常生活、体验 | 成长、梦想、青春、旅行、美食、运动、校园 |

### 4.2 场景标签

| 场景标签 | 说明 | 适用素材类型 |
| --- | --- | --- |
| `argue_evidence` | 议论文论据 | story, quote |
| `narrative_opening` | 记叙文开头 | opening |
| `narrative_ending` | 记叙文结尾 | ending |
| `descriptive_detail` | 描写细节 | expression, vocabulary |
| `transition_connect` | 过渡衔接 | transition |
| `emotional_expression` | 情感抒发 | expression, quote |
| `argument_opening` | 议论文开头 | opening |
| `argument_ending` | 议论文结尾 | ending |
| `rhetoric_device` | 修辞手法 | expression |
| `contrast_reference` | 对比引用 | story, quote |

### 4.3 学段-难度映射

```
kindergarten (幼儿)  → difficulty_level = 1
primary_low (小学低)  → difficulty_level = 1-2
primary_high (小学高) → difficulty_level = 1-2
junior (初中)         → difficulty_level = 1-3
senior (高中)         → difficulty_level = 1-3
```

具体映射规则：
- 小学低年级（1-3年级）：只推荐 difficulty_level=1 的素材，语言通俗简短
- 小学高年级（4-6年级）：推荐 difficulty_level=1-2，引入中等长度素材
- 初中（7-9年级）：推荐 difficulty_level=1-3，鼓励引用经典名句
- 高中（10-12年级）：全量可用，侧重思辨性和深度素材

---

## 5. API 接口设计

### 5.1 根据作文题目推荐素材（核心接口）

```
POST /api/v1/essay-materials/recommend
```

**请求体：**
```json
{
  "essayTitle": "那一次，我懂得了坚持",
  "genre": "narrative",
  "keywords": ["坚持", "成长", "克服困难"],
  "stage": "junior",
  "grade": 8,
  "subject": "chinese",
  "limit": 10,
  "materialTypes": ["quote", "story", "opening", "ending"],
  "excludeIds": [12345, 67890],
  "needExplanation": true
}
```

**响应体：**
```json
{
  "code": 0,
  "data": {
    "themeAnalysis": {
      "primaryTheme": "坚持",
      "relatedThemes": ["成长", "勇气", "毅力"],
      "themeId": 15
    },
    "recommendations": [
      {
        "materialId": 100001,
        "materialType": "quote",
        "title": "锲而不舍，金石可镂",
        "content": "锲而舍之，朽木不折；锲而不舍，金石可镂。",
        "source": "荀子《劝学》",
        "sourceType": "classic",
        "themes": ["坚持", "勤奋"],
        "sceneTags": ["argue_evidence", "argument_opening"],
        "relevanceScore": 0.95,
        "matchReason": "主题高度匹配"坚持"，适合用作议论文论据或文章开头引入",
        "usageTip": "可用于开头引入主题，如：古人云'锲而不舍，金石可镂'，那一次的经历让我真正理解了这句话的含义。"
      },
      {
        "materialId": 100052,
        "materialType": "story",
        "title": "司马迁忍辱著《史记》",
        "content": "司马迁遭受宫刑后，忍辱负重，历时十余年，终于完成了被誉为"史家之绝唱，无韵之离骚"的《史记》...",
        "source": "《汉书·司马迁传》",
        "sourceType": "classic",
        "themes": ["坚持", "逆境", "责任"],
        "sceneTags": ["argue_evidence"],
        "relevanceScore": 0.91,
        "matchReason": "经典坚持主题事例，叙事性强",
        "usageTip": "可用于文中举例论证，突出在极端困难中坚持的力量。"
      }
    ],
    "total": 42,
    "hasMore": true
  }
}
```

### 5.2 素材搜索

```
GET /api/v1/essay-materials/search
```

**参数：**
| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| keyword | string | 否 | 搜索关键词 |
| materialType | string[] | 否 | 素材类型筛选 |
| themes | string[] | 否 | 主题标签筛选 |
| sceneTags | string[] | 否 | 场景标签筛选 |
| genres | string[] | 否 | 文体筛选 |
| stage | string | 否 | 学段筛选 |
| difficultyMax | int | 否 | 最大难度等级 |
| sortBy | string | 否 | 排序: relevance/hot/latest |
| page | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量（默认20，最大50） |

### 5.3 素材详情

```
GET /api/v1/essay-materials/{materialId}
```

**响应体（关键字段）：**
```json
{
  "code": 0,
  "data": {
    "materialId": 100001,
    "materialType": "quote",
    "title": "锲而不舍，金石可镂",
    "content": "锲而舍之，朽木不折；锲而不舍，金石可镂。",
    "fullContent": "积土成山，风雨兴焉；积水成渊，蛟龙生焉；积善成德，而神明自得，圣心备焉。故不积跬步，无以至千里；不积小流，无以成江海。骐骥一跃，不能十步；驽马十驾，功在不舍。锲而舍之，朽木不折；锲而不舍，金石可镂。",
    "source": "荀子《劝学》",
    "sourceType": "classic",
    "themes": ["坚持", "勤奋", "积累"],
    "sceneTags": ["argue_evidence", "argument_opening"],
    "genres": ["argumentative", "expository"],
    "difficultyLevel": 2,
    "minStage": "primary",
    "maxStage": "senior",
    "usageCount": 12580,
    "likeCount": 892,
    "avgScore": 4.7,
    "relatedMaterials": [
      { "materialId": 100002, "title": "...", "relevance": 0.88 }
    ],
    "usageExamples": [
      {
        "scene": "议论文开头引入",
        "text": "古人云：'锲而不舍，金石可镂。'坚持的力量，往往在平凡中日积月累，最终绽放出惊人的光芒。"
      }
    ]
  }
}
```

### 5.4 收藏/取消收藏素材

```
POST /api/v1/essay-materials/{materialId}/favorite
DELETE /api/v1/essay-materials/{materialId}/favorite
```

**请求体（收藏时）：**
```json
{
  "folderName": "中考必备",
  "note": "适合议论文开头"
}
```

### 5.5 获取用户素材收藏列表

```
GET /api/v1/essay-materials/favorites
```

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| folderName | string | 收藏夹筛选（为空返回全部） |
| materialType | string | 素材类型筛选 |
| page | int | 页码 |
| pageSize | int | 每页数量 |

### 5.6 素材使用反馈

```
POST /api/v1/essay-materials/{materialId}/feedback
```

```json
{
  "action": "quote",
  "taskId": 12345,
  "rating": 4,
  "feedback": "素材很好用，帮我在作文中增加了论据"
}
```

### 5.7 获取热门主题与素材合集

```
GET /api/v1/essay-materials/themes/hot
GET /api/v1/essay-materials/collections
GET /api/v1/essay-materials/collections/{collectionId}/materials
```

### 5.8 管理后台 API

```
POST   /api/admin/essay-materials               # 创建素材
PUT    /api/admin/essay-materials/{id}           # 更新素材
POST   /api/admin/essay-materials/batch          # 批量导入素材
PUT    /api/admin/essay-materials/{id}/audit     # 审核素材
POST   /api/admin/essay-materials/generate       # AI 批量生成素材
GET    /api/admin/essay-materials/stats           # 素材库统计
POST   /api/admin/essay-materials/reindex        # 触发重建索引
```

---

## 6. 核心算法：素材推荐引擎

### 6.1 推荐流程总览

```
作文题目 + 用户上下文
        │
        ▼
  ┌─────────────────┐
  │ 1. 主题解析     │  提取题目核心主题词
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │ 2. 候选召回     │  多路召回：主题匹配 + 语义召回 + 热门兜底
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │ 3. 适龄过滤     │  根据学段、年级、难度过滤不适宜素材
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │ 4. 精排打分     │  综合相关性、质量分、个性化偏好排序
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │ 5. 多样性打散   │  类型打散 + 主题打散，避免推荐同质化
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │ 6. 用法生成     │  为 Top-N 素材生成使用建议
  └────────┬────────┘
           │
           ▼
       推荐结果
```

### 6.2 主题解析算法

对作文题目进行结构化分析，提取核心主题：

```java
public class ThemeAnalyzer {
    
    /**
     * 分析作文题目，提取主题词和匹配信息
     */
    public ThemeAnalysisResult analyze(String essayTitle, String genre, List<String> keywords) {
        ThemeAnalysisResult result = new ThemeAnalysisResult();
        
        // 1. 关键词直接匹配主题词表
        List<ThemeMatch> directMatches = matchThemesByKeywords(
            mergeKeywords(essayTitle, keywords)
        );
        
        // 2. 语义相似度匹配（通过向量库）
        List<ThemeMatch> semanticMatches = matchThemesBySemantic(essayTitle);
        
        // 3. 合并去重，按匹配分数排序
        List<ThemeMatch> merged = mergeAndDeduplicate(directMatches, semanticMatches);
        
        // 4. 取 Top-3 主