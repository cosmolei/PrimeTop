# 设备指纹与反作弊风控引擎 - 详细设计

## 1. 概述

### 1.1 背景

启硕 PrimeTop 作为付费教育产品，面临多重作弊与滥用风险：
- AI 大模型 API 调用成本高昂，需防止自动化刷量与爬虫批量调用
- 会员订阅制度下，需防止账号共享与倒卖
- 拍题 OCR、语音识别等能力存在被批量盗用的风险
- 支付环节需防范欺诈与恶意退款
- 未成年人账号需确保不被成人冒用绕过家长管控

### 1.2 设计目标

1. **精准识别**：通过多维度设备指纹和行为分析，准确识别作弊与滥用行为
2. **低误判率**：对正常用户的影响 < 0.1%，避免阻断合法学习活动
3. **实时响应**：风控决策延迟 < 50ms（P99），不阻塞核心学习链路
4. **渐进式处置**：从验证码、限流到封禁，分级别处置
5. **隐私合规**：符合《个人信息保护法》和《儿童个人信息网络保护规定》，数据最小化采集

### 1.3 适用范围

本设计覆盖客户端 SDK、服务端风控引擎、规则管理系统、风险运营后台四大子系统。

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     客户端 SDK                            │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌──────────┐ │
│  │ 设备信息采集 │ │ 行为埋点上报 │ │ 环境安全检测 │ │ 挑战响应  │ │
│  └─────┬─────┘ └─────┬─────┘ └─────┬─────┘ └────┬─────┘ │
└────────┼─────────────┼─────────────┼─────────────┼───────┘
         │             │             │             │
         ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────┐
│                   风控接入层 (Gateway)                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                 │
│  │ 签名校验   │ │ 会话关联   │ │ 频率计数   │                 │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘                 │
└───────┼─────────────┼─────────────┼──────────────────────┘
        │             │             │
        ▼             ▼             ▼
┌─────────────────────────────────────────────────────────┐
│                  风控决策引擎                              │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐            │
│  │ 规则引擎 │ │ 评分引擎 │ │ 情报中心 │ │ 处置中心 │            │
│  └───┬────┘ └───┬────┘ └───┬────┘ └───┬────┘            │
└──────┼──────────┼──────────┼──────────┼─────────────────┘
       │          │          │          │
       ▼          ▼          ▼          ▼
┌─────────────────────────────────────────────────────────┐
│                   数据存储层                               │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                   │
│  │ Redis │ │MySQL │ │Click │ │ ES   │                   │
│  │(实时) │ │(持久)│ │House │ │(日志)│                   │
│  └──────┘ └──────┘ └──────┘ └──────┘                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 核心数据结构

### 3.1 设备指纹表 (device_fingerprints)

```sql
CREATE TABLE device_fingerprints (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    fingerprint_id  VARCHAR(64) NOT NULL COMMENT '设备指纹ID (SHA-256)',
    user_id         BIGINT DEFAULT NULL COMMENT '当前关联用户ID',
    -- 硬件特征
    platform        VARCHAR(20) NOT NULL COMMENT 'android/ios/web',
    device_model    VARCHAR(100) COMMENT '设备型号',
    os_version      VARCHAR(50) COMMENT '操作系统版本',
    screen_width    INT,
    screen_height   INT,
    screen_dpi      INT,
    cpu_arch        VARCHAR(20) COMMENT 'arm64-v8a等',
    memory_total_mb INT,
    -- 网络特征
    network_type    VARCHAR(20) COMMENT 'wifi/4g/5g',
    carrier         VARCHAR(50),
    ip_address      VARCHAR(45),
    ip_province     VARCHAR(20),
    ip_city         VARCHAR(20),
    -- 软件环境
    app_version     VARCHAR(20),
    sdk_version     VARCHAR(20),
    root_status     TINYINT COMMENT '0-未知 1-未root 2-已root',
    emulator_status TINYINT COMMENT '0-未知 1-真机 2-模拟器',
    debugger_status TINYINT COMMENT '0-未知 1-无 2-检测到',
    vpn_status      TINYINT COMMENT '0-未知 1-无 2-有VPN',
    -- 统计
    first_seen_at   DATETIME NOT NULL,
    last_seen_at    DATETIME NOT NULL,
    bind_user_count INT DEFAULT 1 COMMENT '绑定过的用户数',
    risk_score      DECIMAL(5,2) DEFAULT 0 COMMENT '0-100',
    risk_level      VARCHAR(10) DEFAULT 'low' COMMENT 'low/medium/high/blocked',
    status          TINYINT DEFAULT 1 COMMENT '0-禁用 1-正常 2-受限',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_fingerprint (fingerprint_id),
    INDEX idx_user (user_id),
    INDEX idx_ip (ip_address),
    INDEX idx_risk (risk_level, updated_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.2 设备-用户关联表 (device_user_bindings)

```sql
CREATE TABLE device_user_bindings (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    fingerprint_id  VARCHAR(64) NOT NULL,
    user_id         BIGINT NOT NULL,
    first_bind_at   DATETIME NOT NULL,
    last_active_at  DATETIME NOT NULL,
    active_days     INT DEFAULT 0,
    total_sessions  INT DEFAULT 0,
    is_primary      TINYINT DEFAULT 0 COMMENT '是否主要设备',
    share_suspect   TINYINT DEFAULT 0 COMMENT '0-无 1-疑似 2-确认',
    share_evidence  JSON,
    status          TINYINT DEFAULT 1 COMMENT '0-解绑 1-正常',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_device_user (fingerprint_id, user_id),
    INDEX idx_user (user_id),
    INDEX idx_suspect (share_suspect, updated_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.3 风险事件表 (risk_events)

```sql
CREATE TABLE risk_events (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    event_id        VARCHAR(36) NOT NULL,
    event_type      VARCHAR(50) NOT NULL COMMENT '事件类型',
    event_source    VARCHAR(20) NOT NULL COMMENT 'sdk/gateway/rule/manual',
    user_id         BIGINT DEFAULT NULL,
    fingerprint_id  VARCHAR(64) DEFAULT NULL,
    ip_address      VARCHAR(45) DEFAULT NULL,
    risk_score      DECIMAL(5,2) NOT NULL,
    risk_tags       JSON COMMENT '风险标签数组',
    rule_ids        JSON COMMENT '触发的规则ID',
    evidence        JSON COMMENT '证据详情',
    action_taken    VARCHAR(30) COMMENT '处置措施',
    is_false_positive TINYINT DEFAULT NULL COMMENT 'NULL-未审 0-真实 1-误报',
    request_path    VARCHAR(200),
    server_time     DATETIME NOT NULL,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_time (user_id, created_at),
    INDEX idx_fp_time (fingerprint_id, created_at),
    INDEX idx_type_time (event_type, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.4 处置记录表 (risk_actions)

```sql
CREATE TABLE risk_actions (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    action_id       VARCHAR(36) NOT NULL,
    action_type     VARCHAR(30) NOT NULL COMMENT 'captcha/throttle/block/freeze等',
    user_id         BIGINT DEFAULT NULL,
    fingerprint_id  VARCHAR(64) DEFAULT NULL,
    event_ids       JSON,
    action_config   JSON NOT NULL,
    reason          VARCHAR(500) NOT NULL,
    source          VARCHAR(20) NOT NULL COMMENT 'auto/manual',
    operator_id     BIGINT DEFAULT NULL,
    start_at        DATETIME NOT NULL,
    expire_at       DATETIME DEFAULT NULL COMMENT 'NULL=永久',
    status          TINYINT DEFAULT 1 COMMENT '0-撤销 1-生效 2-过期',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user (user_id, status),
    INDEX idx_fp (fingerprint_id, status),
    INDEX idx_expire (expire_at, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.5 风控规则表 (risk_rules)

```sql
CREATE TABLE risk_rules (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    rule_id         VARCHAR(36) NOT NULL,
    rule_name       VARCHAR(100) NOT NULL,
    rule_group      VARCHAR(50) NOT NULL COMMENT 'environment/velocity/account/behavior/payment',
    condition_type  VARCHAR(20) NOT NULL COMMENT 'simple/compound/velocity/statistical',
    condition_expr  JSON NOT NULL,
    risk_score      DECIMAL(5,2) NOT NULL,
    risk_tags       JSON,
    auto_action     VARCHAR(30),
    action_config   JSON,
    priority        INT DEFAULT 100,
    enabled         TINYINT DEFAULT 1,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_rule_id (rule_id),
    INDEX idx_group (rule_group, priority, enabled)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 4. 设备指纹采集

### 4.1 指纹生成算法

分层哈希生成，确保单维度变更不导致指纹完全改变：

```
fingerprint_id = SHA-256(
    SHA-256(hardware_features) +   // 型号/CPU/屏幕/内存/存储
    SHA-256(software_features) +   // OS主版本
    SHA-256(device_identifier)     // Android ID / IDFV
)
```

**稳定特征（权重 80%）**：

| 特征 | Android | iOS | 说明 |
|------|---------|-----|------|
| 设备型号 | `Build.MODEL` | `deviceModel` | 硬件型号 |
| 制造商 | `Build.MANUFACTURER` | - | 厂商 |
| CPU架构 | `Build.SUPPORTED_ABIS` | `cpuType` | 芯片 |
| 屏幕分辨率 | `DisplayMetrics` | `screenBounds` | 物理像素 |
| 总内存 | `MemoryInfo.totalMem` | `physicalMemory` | RAM |
| 持久存储 | `StatFs.totalBytes` | `volumeCapacity` | 存储 |
| Android ID | `Settings.Secure` | - | Android专用 |
| IDFV | - | `identifierForVendor` | iOS专用 |

### 4.2 环境安全检测

| 检测项 | 检测方式 | 风险权重 |
|--------|----------|----------|
| Root/越狱 | su/sudo存在 + 沙箱写入测试 | 30分 |
| 模拟器 | CPU特征+传感器+硬件属性综合 | 25分 |
| 调试器 | ptrace/DebugProbe检测 | 20分 |
| Hook框架 | Frida/Xposed特征字符串扫描 | 15分 |
| VPN/代理 | 网络接口检测+DNS泄漏测试 | 5分 |
| 自动化工具 | AccessibilityService滥用检测 | 10分 |
| 多开/虚拟空间 | 进程/文件系统特征检测 | 15分 |
| 重打包 | APK/IPA签名校验 | 20分 |

环境可信度 = max(0, 1 - sum(detected × weight) / 100)

- ≥ 0.8：安全放行
- 0.5~0.8：可疑，加强监控
- < 0.5：高危，触发挑战验证

### 4.3 客户端 SDK 接口（TypeScript 定义）

```typescript
interface RiskSDKConfig {
  gatewayUrl: string;
  appId: string;
  reportInterval: number;      // 上报间隔秒，默认30
  enableEnvironmentCheck: boolean;
  enableBehaviorCollection: boolean;
}

interface RiskSDK {
  init(config: RiskSDKConfig): Promise<void>;
  getFingerprintId(): string;
  getEnvironmentReport(): {
    isRooted: boolean;
    isEmulator: boolean;
    isDebugger: boolean;
    hasVpn: boolean;
    hasHook: boolean;
    confidence: number;         // 0-1
  };
  signRequest(params: Record<string, any>): {
    fingerprintId: string;
    timestamp: number;
    nonce: string;
    signature: string;          // HMAC-SHA256
  };
  reportBehavior(event: BehaviorEvent): void;
  respondChallenge(challenge: ServerChallenge): Promise<ChallengeResponse>;
  destroy(): void;
}

type BehaviorEventType =
  | 'page_view' | 'button_click' | 'ai_submit' | 'photo_submit'
  | 'payment_init' | 'login_attempt' | 'voice_start' | 'voice_end'
  | 'screenshot' | 'app_background' | 'app_foreground';

interface BehaviorEvent {
  eventType: BehaviorEventType;
  timestamp: number;
  metadata?: Record<string, any>;
}
```

### 4.4 Flutter 集成示例

```dart
class RiskInterceptor extends Interceptor {
  @override
  void onRequest(RequestOptions options, RequestInterceptorHandler handler) {
    final risk = RiskService.instance;
    final headers = risk.signHeaders(options.data ?? {});
    options.headers.addAll(headers);

    // 高风险接口检查环境
    if (_isHighRisk(options.path) && !risk.isEnvironmentSafe) {
      handler.reject(DioException(
        requestOptions: options,
        error: 'Environment not safe',
      ));
      return;
    }
    handler.next(options);
  }

  @override
  void onError(DioException err, ErrorInterceptorHandler handler) {
    if (err.response?.statusCode == 403) {
      final body = err.response?.data;
      if (body?['code'] == 40301) {
        // 风控挑战
        final challenge = ServerChallenge.fromJson(body['data']['challenge']);
        _handleChallenge(challenge, err.requestOptions, handler);
        return;
      }
    }
    handler.next(err);
  }

  Future<void> _handleChallenge(
    ServerChallenge challenge,
    RequestOptions original,
    ErrorInterceptorHandler handler,
  ) async {
    final result = await Navigator.pushNamed(
      AppRouter.instance.navigatorKey.currentContext!,
      '/risk_challenge',
      arguments: challenge,
    );
    if (result != null && result is ChallengeResponse) {
      // 验证通过后重试原请求
      original.headers['X-Challenge-Token'] = result.token;
      final dio = Dio();
      final response = await dio.fetch(original);
      handler.resolve(response);
    } else {
      handler.next(DioException(
        requestOptions: original,
        error: 'Challenge failed',
      ));
    }
  }
}
```

---

## 5. 服务端风控引擎

### 5.1 风控接入层（网关拦截器）

```python
class RiskInterceptor:
    async def intercept(self, request) -> RiskDecision:
        fp = request.header('X-Device-Fingerprint')
        ts = int(request.header('X-Request-Timestamp'))
        nonce = request.header('X-Request-Nonce')
        sig = request.header('X-Request-Signature')

        # 签名校验（防篡改）
        expected = hmac_sha256(SECRET, f"{fp}|{ts}|{nonce}")
        if sig != expected:
            return RiskDecision(block=True, reason='INVALID_SIGNATURE')

        # 时间窗口（防重放，5分钟）
        if abs(now_ms() - ts) > 300_000:
            return RiskDecision(block=True, reason='EXPIRED_REQUEST')

        # Nonce去重
        if await redis.exists(f"nonce:{nonce}"):
            return RiskDecision(block=True, reason='REPLAY')
        await redis.setex(f"nonce:{nonce}", 300, "1")

        # 频率计数
        key = f"rate:{fp}:{request.path}"
        count = await redis.incr(key)
        if count == 1:
            await redis.expire(key, 60)

        # 构建上下文，执行规则
        ctx = RiskContext(
            fingerprint_id=fp, user_id=request.user_id,
            ip=request.client_ip, path=request.path,
            rate_1min=count,
        )
        return await self.engine.evaluate(ctx)
```

### 5.2 规则引擎

#### 规则类型与示例

**简单规则**（单条件）：
```json
{
  "condition_expr": {
    "field": "device.emulator_status",
    "operator": "equals",
    "value": 2
  },
  "risk_score": 30,
  "auto_action": "challenge",
  "action_config": {"type": "captcha"}
}
```

**复合规则**（多条件 AND/OR）：
```json
{
  "condition_expr": {
    "logic": "AND",
    "conditions": [
      {"field": "device.bind_user_count_7d", "operator": "gt", "value": 3},
      {"field": "user.membership_type", "operator": "in", "value": ["monthly","annual"]}
    ]
  },
  "risk_score": 40,
  "risk_tags": ["account_sharing"]
}
```

**速率规则**（时间窗口频率）：
```json
{
  "condition_expr": {
    "field": "api.ai_chat.call_count",
    "window": "10m",
    "threshold": 50,
    "operator": "gt"
  },
  "risk_score": 25,
  "auto_action": "throttle",
  "action_config": {"limit_per_min": 3}
}
```

**统计规则**（历史行为偏差）：
```json
{
  "condition_expr": {
    "metric": "photo_submit_rate",
    "baseline_window": "30d",
    "detection_window": "1h",
    "deviation_sigma": 3.0,
    "min_samples": 10
  },
  "risk_score": 20
}
```

#### 内置规则清单

| ID | 规则名 | 分组 | 说明 |
|----|--------|------|------|
| R001 | 模拟器检测 | environment | 模拟器立即挑战 |
| R002 | Root/越狱 | environment | 标记高风险 |
| R003 | 调试器 | environment | 阻断 |
| R004 | Hook框架 | environment | 标记 |
| R010 | AI问答高频 | velocity | 10分钟>50次 |
| R011 | 拍题高频 | velocity | 10分钟>30次 |
| R012 | 登录高频 | velocity | 5分钟>10次 |
| R013 | 注册高频 | velocity | 1h同IP>20次 |
| R020 | 账号共享嫌疑 | account | 7天>3用户同设备 |
| R021 | 异地登录 | account | 短时间跨省 |
| R022 | 设备频繁切换 | account | 24h>5设备 |
| R023 | 新设备高危操作 | account | 新设备支付/改绑 |
| R030 | 批量注册 | registration | 同IP段批量 |
| R031 | 机器行为 | behavior | 操作间隔过规律 |
| R040 | 支付欺诈 | payment | 多次购买-退款 |

### 5.3 评分引擎

```
final_score = base_score
            + sum(rule_hit_scores)     // 规则命中累加
            + device_risk_bonus        // 设备历史风险加权
            + behavior_anomaly_bonus   // 行为异常加权
            - trust_discount           // 信用历史折扣

trust_discount = min(20, active_trust_days × 0.5)
```

| 风险分 | 等级 | 处置 |
|--------|------|------|
| 0-20 | low | 正常放行 |
| 21-50 | medium | 监控，必要时验证码 |
| 51-80 | high | 严格限流，二次验证 |
| 81-100 | blocked | 阻断，账号受限 |

### 5.4 核心检测场景

#### 账号共享检测

| 维度 | 阈值 |
|------|------|
| 7天设备绑定数 | > 5 |
| 7天同设备多账号 | > 3 |
| 同时在线设备 | > 2 |
| 短时间跨城市(>500km) | < 2h |
| 24h使用时段覆盖 | > 18h |

处置：疑似→设备确认提示；确认→限并发1；严重→冻结

#### 爬虫检测

| 维度 | 说明 |
|------|------|
| 请求间隔方差 | 机器请求方差极小 |
| 操作路径覆盖 | 爬虫只打API不访问页面 |
| TLS指纹(JA3) | 非APP的TLS特征 |
| 交互事件 | 缺少自然交互 |
| API/页面比 | 爬虫比值接近100% |

#### 支付欺诈检测

| 维度 | 说明 |
|------|------|
| 新设备支付 | 首次即支付，结合注册时间 |
| 退款频率 | 多次购买后退款 |
| 支付工具 | 同一工具绑定多账号>5 |
| 设备黑名单 | 关联历史欺诈设备 |
| IP信誉 | 命中代理/黑名单库 |

---

## 6. API 接口设计

### 6.1 设备注册/上报

```
POST /api/v1/risk/device/report
```

请求：
```json
{
  "fingerprint_id": "sha256_hash",
  "platform": "android",
  "hardware": {
    "device_model": "Pixel 7", "manufacturer": "Google",
    "os_version": "14", "screen_width": 1080, "screen_height": 2400,
    "cpu_arch": "arm64-v8a", "memory_total_mb": 8192
  },
  "network": {"type": "wifi", "carrier": "中国移动"},
  "environment": {
    "root_status": 1, "emulator_status": 1,
    "debugger_status": 1, "confidence": 0.95
  },
  "sdk_version": "1.0.0", "app_version": "1.0.0"
}
```

响应：
```json
{
  "code": 0,
  "data": {
    "fingerprint_id": "sha256_hash",
    "risk_level": "low",
    "session_token": "encrypted_token",
    "challenge": null,
    "config": {
      "report_interval_sec": 30,
      "collect_behaviors": true,
      "sensitive_paths": ["/api/v1/ai/chat", "/api/v1/photo/solve"]
    }
  }
}
```

### 6.2 风控预检查

```
POST /api/v1/risk/precheck
```

请求：
```json
{"action": "ai_chat", "context": {"subject": "数学"}}
```

正常响应：
```json
{"code": 0, "data": {"allowed": true, "challenge": null, "remaining_quota": 15}}
```

需挑战响应：
```json
{
  "code": 0,
  "data": {
    "allowed": false,
    "challenge": {
      "challenge_id": "uuid",
      "type": "slider",
      "payload": {
        "background_url": "https://cdn.primetop.com/captcha/bg.png",
        "slider_url": "https://cdn.primetop.com/captcha/sl.png"
      },
      "expires_at": 1716518400
    }
  }
}
```

### 6.3 挑战验证

```
POST /api/v1/risk/challenge/verify
```

请求：
```json
{
  "challenge_id": "uuid",
  "type": "slider",
  "response": {
    "x_offset": 128,
    "trace": [{"t":0,"x":0,"y":0},{"t":50,"x":30,"y":2},{"t":150,"x":128,"y":0}],
    "duration_ms": 320
  }
}
```

响应：
```json
{
  "code": 0,
  "data": {
    "verified": true,
    "granted_actions": ["ai_chat"],
    "granted_duration_sec": 1800
  }
}
```

### 6.4 行为批量上报

```
POST /api/v1/risk/behavior/batch
```

```json
{
  "behaviors": [
    {"event_type": "page_view", "timestamp": 1716518400000, "metadata": {"page": "ai_chat"}},
    {"event_type": "ai_submit", "timestamp": 1716518405000, "metadata": {"subject": "数学"}}
  ]
}
```

### 6.5 运营后台接口

```
GET  /api/v1/admin/risk/events?page=1&size=20&risk_level=high
GET  /api/v1/admin/risk/devices/{fingerprint_id}
GET  /api/v1/admin/risk/users/{user_id}/profile
POST /api/v1/admin/risk/actions
POST /api/v1/admin/risk/actions/{action_id}/revoke
POST /api/v1/admin/risk/events/{event_id}/review
GET  /api/v1/admin/risk/rules
POST /api/v1/admin/risk/rules
PUT  /api/v1/admin/risk/rules/{rule_id}
GET  /api/v1/admin/risk/dashboard?range=7d
```

---

## 7. 关键流程

### 7.1 登录风控流程

```
用户发起登录
    │
    ▼
签名校验 + 环境检测
    │
    ├──[签名无效]──▶ 拒绝(403)
    │
    ▼
设备指纹匹配/注册
    │
    ▼
加载设备绑定记录 ──▶ 绑定用户数>5? ──▶ 标记共享嫌疑
    │
    ▼
异地登录检测 ──▶ 跨省且<2h? ──▶ 触发短信验证
    │
    ▼
执行风控规则集
    │
    ├──[low]──▶ 正常放行
    ├──[medium]──▶ 放行 + 弹出滑块验证
    ├──[high]──▶ 放行 + 短信验证 + 标记监控
    └──[blocked]──▶ 拒绝登录 + 通知运营
```

### 7.2 AI 问答风控流程

```
客户端调用 precheck(action=ai_chat)
    │
    ▼
频率检查 ──▶ 10min内>50次? ──▶ 触发挑战(验证码)
    │
    ▼
设备风险检查 ──▶ risk_level=high? ──▶ 拒绝 + 提示
    │
    ▼
额度检查 ──▶ 超出会员额度? ──▶ 拒绝 + 引导升级
    │
    ▼
放行，返回 remaining_quota
    │
    ▼
客户端发起实际 ai/chat 请求
    │
    ▼
网关风控二次校验(防止绕过precheck)
    │
    ├──[通过]──▶ 转发到AI服务
    └──[拦截]──▶ 返回403 + 挑战
```

### 7.3 支付风控流程

```
用户发起支付
    │
    ▼
设备环境检查 ──▶ 新设备? ──▶ 短信验证
    │
    ▼
支付历史检查 ──▶ 多次退款? ──▶ 拒绝 + 运营审核
    │
    ▼
IP/代理检查 ──▶ 命中黑名单? ──▶ 拒绝
    │
    ▼
支付工具检查 ──▶ 同工具>5账号? ──▶ 限