# 客户端 - 知识图谱浏览与关联学习页面架构与交互设计

> **版本**: v1.0 | **日期**: 2026-05-28 | **状态**: 初稿
> **原始需求来源**: `docs/design/启硕-PrimeTop-全学段AI辅助学习软件项目设计文档` §6.9 考点梳理模块 — 知识图谱
> **服务端文档**: `知识图谱构建与可视化交互引擎-详细设计.md`、`知识点体系与教材映射引擎-详细设计.md`
> **关联模块**: 客户端架构与前端框架、客户端状态管理架构、客户端组件库与设计系统、客户端路由与深链接系统、学情分析-详细设计、学习数据可视化与图表渲染引擎、考点梳理-详细设计、自适应学习与个性化推荐引擎

---

## 1. 模块概述

### 1.1 功能定位

知识图谱浏览页面是 PrimeTop 的核心差异化功能入口，通过力导向图（Force-Directed Graph）将知识点之间的前置关系、关联关系和掌握度状态可视化呈现。学生可以直观地发现知识脉络、定位薄弱环节、探索关联知识点，并一键进入学习。本文档聚焦 **Flutter 客户端**的页面架构、Canvas 渲染引擎、手势交互、状态管理和性能优化，让开发人员可直接编码。

### 1.2 目标用户与分龄策略

| 学段 | 信息密度 | 核心诉求 | 交互特点 |
|------|----------|----------|----------|
| 小学 (1-6年级) | 低 | 知识点连线、颜色卡通化、进度星星 | 大节点、圆角卡片、彩色渐变、动画丰富 |
| 初中 (7-9年级) | 中 | 考点关联、薄弱环节可视化、学科全景 | 中等节点、掌握度色阶、简洁布局 |
| 高中 (10-12年级) | 高 | 全考点拓扑、推荐路径、多维筛选 | 紧凑节点、热力叠加、路径高亮、统计面板 |

### 1.3 页面清单

| 页面 | 路由 | 优先级 | 说明 |
|------|------|--------|------|
| 图谱全景页 | `/knowledge-graph` | P1 | 学科/章节选择 + 力导向图渲染 + 掌握度叠加 |
| 图谱详情面板 | 底部 Sheet (非独立路由) | P1 | 节点详情 + 掌握度 + AI讲解入口 + 关联题目 |
| 推荐路径页 | `/knowledge-graph/path` | P2 | 可视化推荐学习路径 + 路径进度 |
| 薄弱点图谱页 | `/knowledge-graph/weak` | P1 | 仅展示薄弱知识点子图 + 传播路径高亮 |
| 图谱搜索页 | `/knowledge-graph/search` | P2 | 图谱内搜索定位 + 结果高亮 |
| 图谱统计页 | `/knowledge-graph/stats` | P2 | 学科掌握度总览 + 覆盖率 + 分维度统计 |

### 1.4 依赖的客户端基础模块

| 模块 | 使用方式 |
|------|----------|
| `客户端状态管理架构` | Riverpod 2.x 异步 Provider |
| `客户端路由与深链接系统` | GoRouter 路由注册 |
| `客户端组件库与设计系统` | 分龄主题 Token、通用组件 |
| `学习数据可视化与图表渲染引擎` | 掌握度色阶、统计图表 |
| `SSE流式响应与AI增量渲染引擎` | 节点AI讲解流式渲染 |
| `客户端网络请求治理与弱网适配方案` | Dio 请求层 + 图谱数据缓存 |
| `客户端-题目展示与答题交互组件库` | 跳转练习时复用 |

---

## 2. 路由注册

### 2.1 GoRouter 配置

```dart
// lib/app/router/knowledge_graph_routes.dart

import 'package:go_router/go_router.dart';
import '../../pages/knowledge_graph/*.dart';

List<RouteBase> knowledgeGraphRoutes = [
  GoRoute(
    path: '/knowledge-graph',
    name: 'knowledgeGraph',
    builder: (context, state) => const KnowledgeGraphPage(),
    routes: [
      GoRoute(
        path: 'path',
        name: 'knowledgeGraphPath',
        builder: (context, state) {
          final subjectId = state.uri.queryParameters['subjectId'];
          final chapterId = state.uri.queryParameters['chapterId'];
          return KnowledgeGraphPathPage(
            subjectId: int.tryParse(subjectId ?? ''),
            chapterId: int.tryParse(chapterId ?? ''),
          );
        },
      ),
      GoRoute(
        path: 'weak',
        name: 'knowledgeGraphWeak',
        builder: (context, state) {
          final subjectId = state.uri.queryParameters['subjectId'];
          return KnowledgeGraphWeakPage(
            subjectId: int.tryParse(subjectId ?? ''),
          );
        },
      ),
      GoRoute(
        path: 'search',
        name: 'knowledgeGraphSearch',
        builder: (context, state) => const KnowledgeGraphSearchPage(),
      ),
      GoRoute(
        path: 'stats',
        name: 'knowledgeGraphStats',
        builder: (context, state) {
          final subjectId = state.uri.queryParameters['subjectId'];
          return KnowledgeGraphStatsPage(
            subjectId: int.tryParse(subjectId ?? ''),
          );
        },
      ),
    ],
  ),
];
```

### 2.2 深链接支持

| 深链接格式 | 目标 | 示例 |
|-----------|------|------|
| `primetop://knowledge-graph?subjectId=2&chapterId=45` | 图谱全景（定位到指定章节） | 通知点击跳转 |
| `primetop://knowledge-graph/weak?subjectId=3` | 薄弱点图谱 | 学情报告跳转 |
| `primetop://knowledge-graph/path?subjectId=2` | 推荐学习路径 | 自适应学习跳转 |
| `primetop://knowledge-graph/stats?subjectId=5` | 图谱统计 | 学习报告嵌入 |

---

## 3. 核心数据模型

### 3.1 图谱节点模型 (Graph Node)

```dart
// lib/features/knowledge_graph/models/graph_node.dart

import 'package:flutter/material.dart';

/// 图谱节点类型
enum GraphNodeType {
  knowledgePoint,  // 知识点
  chapter,         // 章节
  examPoint,       // 考点
  skill,           // 能力维度
}

/// 掌握度等级
enum MasteryLevel {
  unknown,   // 未学习 (灰色)
  weak,      // 薄弱 (红色)
  learning,  // 学习中 (橙色)
  fair,      // 一般 (黄色)
  good,      // 良好 (浅绿)
  mastered,  // 已掌握 (深绿)
}

/// 图谱节点
class GraphNode {
  final int id;
  final int kpId;           // 关联知识点ID
  final String name;         // 节点显示名称
  final GraphNodeType type;
  final int subjectId;
  final int? chapterId;
  final MasteryLevel mastery;
  final double masteryScore; // 0.0 ~ 1.0
  final bool isHighFreqExam; // 是否高频考点
  final bool isWeakPoint;    // 是否薄弱点
  final int relatedQuestionCount; // 关联题目数量
  final List<String> tags;   // 标签列表

  // 渲染属性 (由布局引擎计算)
  Offset position;           // 当前渲染位置
  double radius;             // 节点半径

  GraphNode({
    required this.id,
    required this.kpId,
    required this.name,
    required this.type,
    required this.subjectId,
    this.chapterId,
    this.mastery = MasteryLevel.unknown,
    this.masteryScore = 0.0,
    this.isHighFreqExam = false,
    this.isWeakPoint = false,
    this.relatedQuestionCount = 0,
    this.tags = const [],
    this.position = Offset.zero,
    this.radius = 24.0,
  });

  /// 获取掌握度对应颜色
  Color get masteryColor {
    switch (mastery) {
      case MasteryLevel.unknown:  return const Color(0xFFBDBDBD);
      case MasteryLevel.weak:     return const Color(0xFFEF5350);
      case MasteryLevel.learning: return const Color(0xFFFF9800);
      case MasteryLevel.fair:     return const Color(0xFFFFEB3B);
      case MasteryLevel.good:     return const Color(0xFF66BB6A);
      case MasteryLevel.mastered: return const Color(0xFF2E7D32);
    }
  }
}
```

### 3.2 图谱边模型 (Graph Edge)

```dart
// lib/features/knowledge_graph/models/graph_edge.dart

/// 边类型
enum GraphEdgeType {
  prerequisite,  // 前置关系 (A是B的前置)
  related,       // 关联关系
  contains,      // 包含关系 (章节→知识点)
  examRelated,   // 考点关联
}

/// 图谱边
class GraphEdge {
  final int id;
  final int sourceId;        // 起始节点ID
  final int targetId;        // 目标节点ID
  final GraphEdgeType type;
  final double weight;       // 关系权重 0.0~1.0
  final String? label;       // 边标签 (如 "前置", "延伸")

  GraphEdge({
    required this.id,
    required this.sourceId,
    required this.targetId,
    required this.type,
    this.weight = 1.0,
    this.label,
  });
}
```

### 3.3 图谱数据聚合模型

```dart
// lib/features/knowledge_graph/models/knowledge_graph_data.dart

/// 图谱数据 — 服务端单次返回的完整子图
class KnowledgeGraphData {
  final List<GraphNode> nodes;
  final List<GraphEdge> edges;
  final int subjectId;
  final int? chapterId;
  final String? chapterName;
  final int totalCount;     // 全图总节点数 (分页用)
  final double overallMastery; // 整体掌握度 0.0~1.0
  final List<GraphNode> weakNodes; // 薄弱节点TOP N

  KnowledgeGraphData({
    required this.nodes,
    required this.edges,
    required this.subjectId,
    this.chapterId,
    this.chapterName,
    this.totalCount = 0,
    this.overallMastery = 0.0,
    this.weakNodes = const [],
  });

  /// 根据节点ID获取节点
  GraphNode? nodeById(int id) {
    for (final n in nodes) {
      if (n.id == id) return n;
    }
    return null;
  }

  /// 获取指定节点的邻接节点
  List<GraphNode> neighborsOf(int nodeId) {
    final neighborIds = <int>{};
    for (final e in edges) {
      if (e.sourceId == nodeId) neighborIds.add(e.targetId);
      if (e.targetId == nodeId) neighborIds.add(e.sourceId);
    }
    return nodes.where((n) => neighborIds.contains(n.id)).toList();
  }
}
```

---

## 4. 服务端 API 接口

### 4.1 获取图谱子图

```
GET /api/v1/knowledge-graph/subgraph
```

**请求参数:**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| subjectId | int | ✅ | 学科ID |
| chapterId | int | ❌ | 章节ID (不传=学科全景) |
| graphType | string | ❌ | CHAPTER / EXAM / SKILL, 默认 CHAPTER |
| depth | int | ❌ | 从目标节点展开深度, 默认 2, 最大 4 |
| includeMastery | bool | ❌ | 是否叠加掌握度, 默认 true |
| maxNodes | int | ❌ | 最大返回节点数, 默认 50, 上限 200 |

**响应示例:**

```json
{
  "code": 0,
  "data": {
    "nodes": [
      {
        "id": 1024,
        "kpId": 50023,
        "name": "一元二次方程",
        "type": "knowledgePoint",
        "subjectId": 2,
        "chapterId": 45,
        "mastery": "fair",
        "masteryScore": 0.55,
        "isHighFreqExam": true,
        "isWeakPoint": false,
        "relatedQuestionCount": 238,
        "tags": ["代数", "方程"]
      }
    ],
    "edges": [
      {
        "id": 1,
        "sourceId": 1022,
        "targetId": 1024,
        "type": "prerequisite",
        "weight": 0.9,
        "label": "前置"
      }
    ],
    "subjectId": 2,
    "chapterId": 45,
    "chapterName": "二次根式与方程",
    "totalCount": 87,
    "overallMastery": 0.62,
    "weakNodes": [
      { "id": 1030, "name": "韦达定理", "mastery": "weak", "masteryScore": 0.25 }
    ]
  }
}
```

### 4.2 获取节点详情

```
GET /api/v1/knowledge-graph/nodes/{nodeId}
```

**响应示例:**

```json
{
  "code": 0,
  "data": {
    "id": 1024,
    "kpId": 50023,
    "name": "一元二次方程",
    "description": "形如 ax²+bx+c=0 (a≠0) 的方程...",
    "subjectId": 2,
    "chapterId": 45,
    "chapterName": "二次根式与方程",
    "mastery": "fair",
    "masteryScore": 0.55,
    "isHighFreqExam": true,
    "relatedQuestionCount": 238,
    "prerequisites": [
      { "id": 1022, "name": "一元一次方程", "mastery": "mastered" },
      { "id": 1023, "name": "因式分解", "mastery": "good" }
    ],
    "dependents": [
      { "id": 1030, "name": "韦达定理", "mastery": "weak" },
      { "id": 1031, "name": "二次函数", "mastery": "learning" }
    ],
    "examFrequency": "high",
    "lastStudyAt": "2026-05-25T14:30:00Z",
    "studyDurationMinutes": 45,
    "aiExplanationAvailable": true
  }
}
```

### 4.3 获取薄弱点传播路径

```
GET /api/v1/knowledge-graph/weak-propagation
```

**请求参数:**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| subjectId | int | ✅ | 学科ID |
| maxDepth | int | ❌ | 传播路径最大深度, 默认 3 |

**响应示例:**

```json
{
  "code": 0,
  "data": {
    "weakNodes": [
      {
        "id": 1030,
        "name": "韦达定理",
        "masteryScore": 0.25,
        "propagationPaths": [
          {
            "path": [1030, 1024, 1031],
            "pathNames": ["韦达定理", "一元二次方程", "二次函数"],
            "impactScore": 0.85
          }
        ],
        "rootCauseCandidates": [
          { "id": 1023, "name": "因式分解", "mastery": "fair", "isPrerequisite": true }
        ]
      }
    ],
    "stats": {
      "totalWeak": 5,
      "totalAffected": 12,
      "maxImpactScore": 0.92
    }
  }
}
```

### 4.4 获取推荐学习路径

```
GET /api/v1/knowledge-graph/learning-path
```

**请求参数:**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| subjectId | int | ✅ | 学科ID |
| chapterId | int | ❌ | 限定章节范围 |
| targetNodeIds | int[] | ❌ | 目标节点ID列表 |
| maxSteps | int | ❌ | 最大步骤数, 默认 10 |

**响应:**

```json
{
  "code": 0,
  "data": {
    "pathId": "lp_20260528_001",
    "steps": [
      {
        "order": 1,
        "nodeId": 1023,
        "name": "因式分解",
        "reason": "前置知识，掌握度仅0.45",
        "estimatedMinutes": 20,
        "mastery": "fair"
      },
      {
        "order": 2,
        "nodeId": 1024,
        "name": "一元二次方程",
        "reason": "核心考点，高频",
        "estimatedMinutes": 30,
        "mastery": "fair"
      }
    ],
    "totalEstimatedMinutes": 50,
    "currentStep": 1
  }
}
```

### 4.5 图谱搜索

```
GET /api/v1/knowledge-graph/search
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| subjectId | int | ✅ | 学科ID |
| keyword | string | ✅ | 搜索关键词 |
| limit | int | ❌ | 最大返回数, 默认 20 |

---

## 5. 页面架构设计

### 5.1 整体架构

```
lib/features/knowledge_graph/
├── models/
│   ├── graph_node.dart           # 节点模型
│   ├── graph_edge.dart           # 边模型
│   ├── knowledge_graph_data.dart # 图谱聚合数据
│   └── node_detail.dart          # 节点详情
├── providers/
│   ├── graph_data_provider.dart  # 图谱数据获取
│   ├── graph_layout_provider.dart# 布局状态管理
│   ├── node_selection_provider.dart # 节点选中状态
│   └── graph_filter_provider.dart  # 筛选/显示选项
├── engine/
│   ├── force_layout_engine.dart  # 力导向布局引擎
│   ├── graph_renderer.dart       # Canvas 图谱渲染器
│   ├── gesture_handler.dart      # 手势处理 (缩放/平移/点击)
│   └── node_style_resolver.dart  # 分龄节点样式解析
├── pages/
│   ├── knowledge_graph_page.dart       # 图谱全景主页
│   ├── knowledge_graph_weak_page.dart  # 薄弱点图谱
│   ├── knowledge_graph_path_page.dart  # 推荐路径
│   ├── knowledge_graph_search_page.dart # 图谱搜索
│   └── knowledge_graph_stats_page.dart  # 图谱统计
├── widgets/
│   ├── graph_canvas.dart         # Canvas 交互组件
│   ├── node_detail_sheet.dart    # 节点详情底部弹窗
│   ├── graph_filter_bar.dart     # 筛选条件栏
│   ├── graph_legend.dart         # 图例组件
│   ├── mastery_overlay.dart      # 掌握度叠加层
│   ├── path_progress_bar.dart    # 路径进度条
│   └── mini_graph_card.dart      # 迷你图谱卡片 (首页用)
└── services/
    ├── graph_cache_service.dart  # 图谱本地缓存
    └── graph_api_service.dart    # 网络请求
```

### 5.2 Provider 依赖图

```
┌──────────────────────┐
│ graphFilterProvider   │ ← 用户选择学科/章节/图谱类型/显示选项
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐     ┌──────────────────────┐
│ graphDataProvider    │────▶│ graphLayoutProvider   │
│ (AsyncNotifier)      │     │ (布局状态机)           │
│ 请求服务端子图数据    │     │ 计算力导向布局         │
└──────────────────────┘     └──────────┬───────────┘
                                        │
                                        ▼
                             ┌──────────────────────┐
                             │ nodeSelectionProvider │ ← 用户点击节点
                             │ (选中节点+详情状态)    │
                             └──────────────────────┘
```

---

## 6. 力导向布局引擎

### 6.1 引擎核心

力导向布局是图谱渲染的核心，需在移动端保持 60fps 流畅度。

```dart
// lib/features/knowledge_graph/engine/force_layout_engine.dart

import 'dart:math';
import '../models/graph_node.dart';
import '../models/graph_edge.dart';

/// 力导向布局引擎 — 基于 Fruchterman-Reingold 算法简化版
class ForceLayoutEngine {
  final List<GraphNode> nodes;
  final List<GraphEdge> edges;

  /// 布局参数
  double area;              // 布局区域面积
  double gravity;           // 重力系数 (防止节点飞散)
  double repulsion;         // 斥力系数
  double attraction;        // 引力系数
  double damping;           // 衰减系数
  double cooling;           // 温度冷却率
  double temperature;       // 当前温度 (控制位移幅度)

  int _iteration = 0;
  static const int maxIterations = 300;
  static const double minTemperature = 0.01;
  static const double epsilon = 0.001;

  ForceLayoutEngine({
    required this.nodes,
    required this.edges,
    required this.area,
    this.gravity = 0.1,
    this.repulsion = 5000.0,
    this.attraction = 0.005,
    this.damping = 0.85,
    this.cooling = 0.95,
  }) : temperature = sqrt(area) / 10;

  /// 初始化节点位置 — 按章节/层级环形排布
  void initializePositions() {
    if (nodes.isEmpty) return;

    // 按章节分组，同章节节点聚簇
    final chapterGroups = <int?, List<GraphNode>>{};
    for (final node in nodes) {
      chapterGroups.putIfAbsent(node.chapterId, () => []).add(node);
    }

    final centerX = sqrt(area) / 2;
    final centerY = sqrt(area) / 2;
    final groupCount = chapterGroups.length;
    var groupIndex = 0;

    for (final entry in chapterGroups.entries) {
      final groupNodes = entry.value;
      final groupAngle = (2 * pi * groupIndex) / groupCount;
      final groupRadius = sqrt(area) * 0.3;
      final groupCenterX = centerX + groupRadius * cos(groupAngle);
      final groupCenterY = centerY + groupRadius * sin(groupAngle);

      // 组内节点绕组中心圆形分布
      final innerRadius = 30.0 * sqrt(groupNodes.length.toDouble());
      for (var i = 0; i < groupNodes.length; i++) {
        final angle = (2 * pi * i) / groupNodes.length;
        groupNodes[i].position = Offset(
          groupCenterX + innerRadius * cos(angle),
          groupCenterY + innerRadius * sin(angle),
        );
      }
      groupIndex++;
    }
  }

  /// 执行单步布局计算
  /// 返回 true 表示布局仍在收敛中，false 表示已稳定
  bool step() {
    if (temperature < minTemperature || _iteration >= maxIterations) {
      return false;
    }

    final displacements = List.generate(nodes.length, (_) => Offset.zero);

    // 1. 节点间斥力 (库仑力)
    for (var i = 0; i < nodes.length; i++) {
      for (var j = i + 1; j < nodes.length; j++) {
        final dx = nodes[i].position.dx - nodes[j].position.dx;
        final dy = nodes[i].position.dy - nodes[j].position.dy;
        final dist = max(sqrt(dx * dx + dy * dy), epsilon);
        final force = repulsion / (dist * dist);
        final fx = (dx / dist) * force;
        final fy = (dy / dist) * force;
        displacements[i] = displacements[i] + Offset(fx, fy);
        displacements[j] = displacements[j] - Offset(fx, fy);
      }
    }

    // 2. 边引力 (弹簧力)
    for (final edge in edges) {
      final sourceIdx = nodes.indexWhere((n) => n.id == edge.sourceId);
      final targetIdx = nodes.indexWhere((n) => n.id == edge.targetId);
      if (sourceIdx == -1 || targetIdx == -1) continue;

      final dx = nodes[targetIdx].position.dx - nodes[sourceIdx].position.dx;
      final dy = nodes[targetIdx].position.dy - nodes[sourceIdx].position.dy;
      final dist = max(sqrt(dx * dx + dy * dy), epsilon);
      final force = attraction * dist * edge.weight;
      final fx = (dx / dist) * force;
      final fy = (dy / dist) * force;
      displacements[sourceIdx] = displacements[sourceIdx] + Offset(fx, fy);
      displacements[targetIdx] = displacements[targetIdx] - Offset(fx, fy);
    }

    // 3. 重力 (向中心聚集)
    final centerX = sqrt(area) / 2;
    final centerY = sqrt(area) / 2;
    for (var i = 0; i < nodes.length; i++) {
      final dx = centerX - nodes[i].position.dx;
      final dy = centerY - nodes[i].position.dy;
      displacements[i] = displacements[i] + Offset(dx * gravity, dy * gravity);
    }

    // 4. 施加位移 (受温度限制)
    for (var i = 0; i < nodes.length; i++) {
      final dist = max(
        sqrt(displacements[i].dx * displacements[i].dx +
            displacements[i].dy * displacements[i].dy),
        epsilon,
      );
      final cappedDist = min(dist, temperature);
      nodes[i].position = Offset(
        nodes[i].position.dx + (displacements[i].dx / dist) * cappedDist,
        nodes[i].position.dy + (displacements[i].dy / dist) * cappedDist,
      );
    }

    // 5. 冷却
    temperature *= cooling;
    _iteration++;
    return true;
  }

  /// 计算总动能 (用于判断布局稳定)
  double kineticEnergy() {
    // 简化：返回当前温度即可
    return temperature;
  }

  /// 重置布局
  void reset() {
    _iteration = 0;
    temperature = sqrt(area) / 10;
    initializePositions();
  }
}
```

### 6.2 布局策略分龄适配

```dart
// lib/features/knowledge_graph/engine/node_style_resolver.dart

import 'package:flutter/material.dart';
import '../models/graph_node.dart';
import '../../shared/providers/grade_provider.dart';

/// 分龄节点样式解析器
class NodeStyleResolver {
  final GradeSegment gradeSegment;

  NodeStyleResolver({required this.gradeSegment});

  /// 节点半径
  double resolveRadius(GraphNode node) {
    double baseRadius;
    switch (gradeSegment) {
      case GradeSegment.primary:
        baseRadius = 30.0; // 大节点，便于儿童点击
        break;
      case GradeSegment.junior:
        baseRadius = 24.0;
        break;
      case GradeSegment.senior:
        baseRadius = 20.0; // 紧凑，更多节点可见
        break;
      default:
        baseRadius = 24.0;
    }
    // 高频考点节点稍大
    if (node.isHighFreqExam) baseRadius *= 1.2;
    return baseRadius;
  }

  /// 节点填充色
  Color resolveFillColor(GraphNode node) {
    if (gradeSegment == GradeSegment.primary) {
      // 小学：卡通色彩映射
      return _cartoonMasteryColor(node.mastery);
    }
    return node.masteryColor;
  }

  /// 节点边框
  BorderSide resolveBorder(GraphNode node) {
    if (node.isWeakPoint) {
      return const BorderSide(color: Colors.redAccent, width: 2.5);
    }
    if (node.isHighFreqExam) {
      return BorderSide(color: Colors.amber.shade700, width: 2.0);
    }
    return BorderSide.none;
  }

  /// 节点内文字样式
  TextStyle resolveTextStyle(GraphNode node) {
    final baseSize = gradeSegment == GradeSegment.primary ? 13.0 : 11.0;
    return TextStyle(
      fontSize: baseSize,
      fontWeight: node.isHighFreqExam ? FontWeight.bold : FontWeight.w500,
      color: Colors.white,
    );
  }

  /// 获取节点最大显示文字长度
  int get maxLabelLength {
    switch (gradeSegment) {
      case GradeSegment.primary: return 4;
      case GradeSegment.junior: return 6;
      case GradeSegment.senior: return 8;
      default: return 6;
    }
  }

  Color _cartoonMasteryColor(MasteryLevel mastery) {
    switch (mastery) {
      case MasteryLevel.unknown:  return const Color(0xFFB0BEC5);
      case MasteryLevel.weak:     return const Color(0xFFFF7043);
      case MasteryLevel.learning: return const Color(0xFFFFA726);
      case MasteryLevel.fair:     return const Color(0xFFFFEE58);
      case MasteryLevel.good:     return const Color(0xFF66BB6A);
      case MasteryLevel.mastered: return const Color(0xFF26A69A);
    }
  }
}
