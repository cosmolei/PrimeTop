==== 客户端-作文辅导页面架构与批改交互设计-详细设计.md | unclosed@570/592 ====
│                                  │
│  桃花开了，粉粉的，很漂亮。        │
│  ─── 🔴 "描述较笼统，建议增加细节  │
│   如颜色深浅、花瓣形态" ────────── │
│                                  │
│  微风拂过，花瓣像蝴蝶一样翩翩起舞。 │
│  ─── 🟢 "比喻生动，画面感强" ───── │
│                                  │
│  [点击批注展开详细说明]            │
├──────────────────────────────────┤
│  📊 批注统计                     │
│  优点 12处 | 问题 5处 | 建议 8处   │
│  错别字 2处 | 标点 1处 | 好词 6处   │
└──────────────────────────────────┘

==== 客户端-同步课堂学习页面架构与章节导航交互设计-详细设计.md | unclosed@938/973 ====
  final ScrollController _scrollController = ScrollController();

  /// 学习计时器
  final Stopwatch _studyStopwatch = Stopwatch();

  /// 已完成的内容块ID集合
  final Set<String> _completedBlocks = {};

  /// 模式切换动画控制器
  late final AnimationController _modeSwitchController;

  @override
  void initState() {
    super.

==== 客户端-学习规划页面架构与任务管理交互设计-详细设计.md | unclosed@877/916 ====
│  ┌─ GoalProgressCard ─────────────┐  │
│  │  🎯 期末考试数学90+            │  │
│  │  距截止还剩 43 天             │  │
│  │  进度 35% ███████░░░░░░░       │  │
│  │  ⚠️ 二次函数掌握度偏低         │  │
│  │               [查看目标 →]     │  │
│  └───────────────────────────────┘  │
│                                      │
│  ┌─ QuickActions ─────────────────┐  │
│  │  [+ 新目标]  [📝 生成计划]    │  │
│  └───────────────────────────────┘  │
│                                      │
│  ┌─ RecentReviewSummary ──────────┐  │
│  │  📊 上周复盘 · 完成

==== 客户端-学生班级与作业提交页面架构与交互设计-详细设计.md | unclosed@670/688 ====
{
  "code": 0,
  "data": {
    "homeworkId": "hw_001",
    "submissionId": "sub_abc123",
    "submitTime": "2026-06-13T10:45:00.000Z",
    "gradingStatus": "teacherGraded",
    "totalScore": 90,
    "maxScore": 100,
    "classAvgScore": 82.5,
    "classRank": 8,
    "classTotalStudents": 42,
    "timeSpent": "3分15秒",
    "teacherComment": "整体不错

==== 客户端-学科上下文与学习环境管理-详细设计.md | unclosed@717/727 ====

### 5.1 AI 对话模块

```dart
class AIConversationPage extends StatefulWidget {
  // ...
}

class _AIConversationPageState extends State<AIConversationPage> {
  late SubjectContext _context;
  
  @override
  void initState() {
    super.

==== 客户端-学科工具箱与参考资料速查页面架构与交互设计.md | unclosed@533/559 ====
│  │ 📖   │  │ ⚡   │  │ 🧪   │   │
│  │英语  │  │物理  │  │化学  │   │
│  │词典  │  │常数  │  │元素  │   │
│  │3200词│  │85项  │  │118个 │   │
│  └──────┘  └──────┘  └──────┘   │
│                                  │
│  ┌──────┐  ┌──────┐  ┌──────┐   │
│  │ 🕰️   │  │ 🌍   │  │ ✍️   │   │
│  │历史  │  │地理  │  │作文  │   │
│  │年表  │  │知识  │  │素材  │   │
│  └──────┘  └──────┘  └──────┘   │
│                                  │
├──────────────────────────────────┤
│  ⭐ 我的收藏 (最近3条)  查看全部 >│  收藏快

==== 客户端-成就勋章与奖励展示组件-详细设计.md | unclosed@720/908 ====
      isScrollControlled: true,
      backgroundColor: Colors.transparent,
      builder: (_) => BadgeDetailSheet(badge: widget.badge),
    );
  }

  Color _tierColor(int tier) {
    switch (tier) {
      case 1: return const Color(0xFFCD7F32); // 铜
      case 2: return const Color(0xFFC0C0C0); // 银
      case 3: return const Color(0xFFFFD700); // 金
      case 4: return const Color(0xFFE5E4E2); // 铂金
      case 5: return const Color(0xFFB9F2FF); // 钻石
      default: return Colors.grey

==== 客户端-教师端页面架构与班级管理交互设计.md | unclosed@795/805 ====

**页面布局：**

```
┌──────────────────────────────────────────────────────────────┐
│  初二(3)班 · 学生名册                           [+ 添加学生]  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  👥 35人  |  已绑定家长 28人  |  本周活跃 30人                  │
│                                                              │
│  筛选: [全部标签 ▼]  [全部状态 ▼]     🔍 搜索学生              │
│                                                              │
│  ☐  ┌────────────────────────────────────────────────────┐  │
│  │  │ ☐  王小明  #01  ┃  数学  ┃  完成

==== 客户端-数据导出与账户注销页面架构与交互设计.md | unclosed@839/850 ====
### 6.4 完成态布局

```
┌──────────────────────────────────┐
│  ←  数据导出                     │
├──────────────────────────────────┤
│                                  │
│         ✅ 导出完成              │
│                                  │
│  文件大小：23.5 MB               │
│  包含模块：4 个                  │
│  完成时间：2026-06-06 07:28      │
│                                  │
│  ⚠️

==== 客户端-文科背诵页面架构与交互设计-详细设计.md | unclosed@570/577 ====
- 支持语音跟读：录音 → ASR 识别 → 对比原文 → 标注差异
- 跟读对比结果实时显示：逐字标注 ✅ 正确 / ❌ 错误 / ⚠️ 漏字
- 用户自评"已记住"后进入下一段

#### 3.5.4 Step 3：关键词提示回忆

```
┌──────────────────────────────────┐
│ Step 3/4 · 关键词回忆  第 1/2 段  │
├──────────────────────────────────┤
│                                  │
│  ┌────────────────────────────┐  │
│  │  床前____光，疑是____霜。  │  │
│  │  (填空部分高亮显示)         │  │

==== 客户端-物理实验模拟与力学分析交互组件-详细设计.md | unclosed@696/760 ====
    // 灯泡: 圆 + 交叉线
    canvas.drawCircle(Offset.zero, 12, paint);
    canvas.drawLine(const Offset(-8, -8), const Offset(8, 8), paint);
    canvas.drawLine(const Offset(-8, 8), const Offset(8, -8), paint);
    
    // 灯泡通电发光效果
    if (comp.current > 0) {
      final glowPaint = Paint()
        ..color = Colors.yellow.withOpacity(0.6)
        ..maskFilter = const MaskFilter.blur(BlurStyle.normal, 8);
      canvas.drawCircle(Offset.zero, 16, glowPaint);
    }
  }
}

==== 客户端-理科解题页面架构与交互设计.md | unclosed@703/946 ====
                      fontSize: 18,
                      fontWeight: FontWeight.w700,
                    ),
                  ),
                  const SizedBox(height: 16),
                  // 公式渲染
                  Container(
                    width: double.infinity,
                    padding: const EdgeInsets.all(16),
                    decoration: BoxDecoration(
                      color: Theme.of(context).colorScheme.surfaceContainerHighest,
                      borderRadius: BorderRadius.circular(12),
                    ),
                    child

==== 客户端-知识图谱浏览与关联学习页面架构与交互设计-详细设计.md | unclosed@710/795 ====
  }

  Color _cartoonMasteryColor(MasteryLevel mastery) {
    switch (mastery) {
      case MasteryLevel.unknown:  return const Color(0xFFB0BEC5);
      case MasteryLevel.weak:     return const Color(0xFFFF7043);
      case MasteryLevel.learning: return const Color(0xFFFFA726);
      case MasteryLevel.fair:     return const Color(0xFFFFEE58);
      case MasteryLevel.good:     return const Color(0xFF66BB6A);
      case MasteryLevel.mastered: return const Color(0xFF26A69A);
    }
  }
}


==== 客户端-管理后台Web前端架构与开发规范-详细设计.md | unclosed@579/621 ====
}

export const usePermissionStore = create<PermissionState>((set) => ({
  menus: [],
  permissionCodes: new Set<string>(),
  loaded: false,

  loadMenus: async () => {
    const [menuRes, permRes] = await Promise.all([
      getMenuTree(),
      getPermissionCodes(),
    ]);
    set({
      menus

==== 客户端-练习测评答题页面架构与交互流程设计-详细设计.md | unclosed@654/849 ====
      child: SafeArea(
        child: Padding(
          padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
          child: Row(
            children: [
              // 标记按钮
              IconButton(
                icon: Icon(
                  session.markedIndices.contains(currentIndex)
                    ? Icons.bookmark
                    : Icons.bookmark_border,
                  color: session.markedIndices.contains(currentIndex)
                    ? theme.accentColor
                    : null

==== 客户端-统一图片加载缓存框架与智能预加载引擎-详细设计.md | unclosed@1000/1040 ====
        _metrics = metrics;

  /// 加载图片
  ///
  /// [url] 图片 URL
  /// [transform] 期望的变换参数（会被自适应引擎调整）
  /// [module] 业务模块标识
  Future<ImageLoadResult> load({
    required String url,
    ImageTransform transform = ImageTransform.standard,
    required String module,
  }) async {
    final stopwatch = Stopwatch()..start();


==== 客户端-统一对话框与模态弹出层调度管理系统-详细设计.md | unclosed@1336/1356 ====
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return MaterialApp(
      navigatorKey: navigatorKey,
      title: '启硕 PrimeTop',
      theme: AppTheme.lightTheme,
      darkTheme: AppTheme.darkTheme,
      home: const SplashPage(),
      builder: (context, child) {
        // 初始化 OverlayManager
        WidgetsBinding.instance.addPostFrameCallback((_) {
          ref.read(overlayManagerProvider);
        });
        return child ??

==== 客户端-编程练习页面架构与代码编辑器交互组件-详细设计.md | unclosed@1216/1257 ====
    final resp = await _client.get('$_baseUrl/api/v1/it/tasks', query: {
      'stage': stage,
      if (knowledgePointId != null) 'kpId': knowledgePointId,
      if (difficulty != null) 'difficulty': difficulty,
      'page': page.toString(),
      'pageSize': pageSize.toString(),
    });
    return PaginatedResult.fromJson(
      resp.data,
      (json) => ProgrammingTaskSummary.fromJson(json),
    );
  }

  /// 获取题目详情

==== 客户端-解题步骤分步动画展示与推导过程可视化引擎-详细设计.md | unclosed@917/975 ====
      StepComplexity.simple => 0.8,
      StepComplexity.medium => 1.0,
      StepComplexity.complex => 1.5,
    };
    
    // 动画偏好系数
    final prefMultiplier = switch (pref) {
      AnimationPreference.full => 1.0,
      AnimationPreference.reduced => 0.6,
      AnimationPreference.minimal => 0.3,
      AnimationPreference.off => 0.0,
    };
    
    final eff

==== 客户端全局异常处理与安全恢复机制-详细设计.md | unclosed@1793/1856 ====
    expect(await detector.getRecentCrashCount(), equals(3));
  });

  test('ErrorMessageFactory produces age-appropriate messages', () {
    final msg = ErrorMessageFactory.create(
      ExceptionType.network,
      Severity.high,
      AgeGroup.preschool,
    );
    expect(msg, contains('网络'));
    expect(msg, isNot(contains('HTTP'))); // 不含技术术语
  });
}
```}

==== 客户端动态化运营活动页面与JSON驱动渲染引擎-详细设计.md | unclosed@722/770 ====
    // 2. 解析 Schema
    final parseResult = _parser.parse(schema);

    // 3. 评估条件规则，过滤组件
    final filteredNodes = _ruleEvaluator.filter(
      parseResult.body,
      context: _buildEvaluationContext(variables),
    );

    // 4. 解析样式
    final styledNodes = _styleResolver.resolve(filteredNodes);

    // 5. 渲染为 Widget 树
    final widget = _renderNode

==== 客户端安全防护体系-详细设计.md | unclosed@829/936 ====
            false
        }
    }

    // ── 调试器检测 ──

    private fun detectDebugger(): Boolean {
        return Debug.isDebuggerConnected() || checkTracerPid()
    }

    /** 检查 /proc/self/status 中的 TracerPid */
    private fun checkTracerPid(): Boolean {
        return try {
            val reader

==== 客户端应用锁与生物识别认证系统-详细设计.md | unclosed@769/851 ====
    }
    return setPin(newPin);
  }

  /// 检查是否已设置 PIN
  Future<bool> isPinSet() async {
    final hash = await _secureStorage.read(key: AppLockStorageKeys.pinHash);
    return hash != null;
  }

  // ─── 私有方法 ───

  String _generateSalt() {
    final random

==== 客户端性能监控与用户体验度量系统-详细设计.md | unclosed@774/832 ====
    }
  }

  /// 事件聚合: 同一秒内相同类型的指标取均值/最大值
  List<PerfEvent> _aggregateEvents(List<PerfEvent> events) {
    final Map<String, List<PerfEvent>> groups = {};
    
    for (final event in events) {
      // 按秒级时间+类型+名称分组
      final key = '${event.timestamp ~/ 1000}_${event.type}_${event.name}';
      groups.putIfAbsent(key, () => []).add(event);
    }

    return groups.entries.map

==== 客户端手势交互与动画系统-详细设计.md | unclosed@843/855 ====

```dart
/// 骨架屏加载效果
class ShimmerEffect extends StatefulWidget {
  final Widget child;
  final Color baseColor;
  final Color highlightColor;
  final Duration duration;

  const ShimmerEffect({
    super.key,
    required this.child,
    this.baseColor = const Color(0xFFE0E0E0),
    this.highlightColor = const

==== 数据迁移与Schema版本管理-详细设计.md | unclosed@1508/1535 ====
---

## 13. 注意事项与最佳实践

1. **禁止修改已发布迁移**：一旦迁移脚本合并到 main 分支，绝不修改。需要修正时，新建迁移脚本。
2. **每个迁移只做一件事**：一个迁移脚本只包含一个逻辑变更，避免混合多个不相关变更。
3. **先加后删**：添加新列/表后至少过一个版本再删除旧列/表，确保新旧代码兼容。
4. **大表操作分批**：百万行以上表的 UPDATE/DELETE 必须分批执行，每批 5000 行，间隔 100ms。
5. **索引使用 CONCURRENTLY**：所有索引创建/重建使用 `CONCURRENTLY` 避免锁表。
6. **downgrade 必须完整**：每个 `upgrade()` 必须有对应的 `downgrade()`，生产环境可能需要回滚。
7. **迁移测试必须覆盖**：每个迁移脚本至少包含 upgrade、downgrade、幂等性三个测试。
8. **生产迁移必须备份**：任何生产环境迁移执行前必须确认备份可用。
9. **避免数据丢失**：删除列/表前确认没有代码引用，且数据已归档或不再需要。
10. **使用环境变量**：数据库连接信息不硬编码，通过环境变量或配置中心注入。

==== 服务端-AI对话安全审计与敏感内容自动上报服务-详细设计.md | unclosed@557/628 ====
└───────┬───────────┘
        │
        ├─── 异步 ───┐
        │             ▼
        │     ┌───────────────┐
        │     │ 审计日志记录    │
        │     │ 用户画像更新    │
        │     │ 工单/通知发送    │
        │     └───────────────┘
        │
        ▼
   返回扫描结果给调用方
   (AI对话引擎根据结果
    决定是否放行/替换/拦截)

==== 服务端-AI对话追问拓展与多轮交互响应编排引擎-详细设计.md | unclosed@585/623 ====
            "4. 适当使用对比或类比"
        ),
        "requires_rag": False,
        "max_output_tokens": 800,
    },
    
    FollowUpAction.GIVE_EXAMPLE: {
        "strategy_tag": "follow_up_example",
        "system_instruction": (
            "你是一个善于举例的老师。学生希望看到具体的例子来帮助理解。\n"
            "要求：\n"
            "1. 提供 2-3 个由浅入深的例子\n"
            "2. 第一个例子用最简单、最生活化的场景\n"
            "3. 逐步增加复杂度，最后一个贴近考试题目

==== 服务端-AI辅导对话嵌入式即时练习与实时理解度检测引擎-详细设计.md | unclosed@678/702 ====
        prior_mastery: float,
        student_profile: StudentProfile
    ) -> ComprehensionAssessment:
        """
        贝叶斯理解度估计

        P(understands | evidence) = P(evidence | understands) * P(understands)
                                     / P(evidence)

        简化为 Beta 分布共轭先验更新:
        - 先验: Beta(α, β)，其中 α/(α+β) = prior_mastery
        - 练习正确: α += w_practice
        - 练习错误: β += w_practice
        - 对话信号: 根据

==== 服务端-AI辅导对话情感感知与自适应回应策略引擎-详细设计.md | unclosed@614/673 ====

    private void applyConfusedStrategy(AdaptiveStrategyParams params,
                                        int intensity,
                                        int consecutiveNeg,
                                        StudentProfile student) {
        params.setToneStyle(ToneStyle.CALM);
        params.setIncreaseStepBreakdown(true);
        params.setProactiveHintsEnabled(true);

        if (intensity >= 3) {
            params.setUseSimplifiedExplanation(true);
            params.setEncouragementFrequency(2); // 每 2 轮鼓励一次
            params.setPromptInstructionOverride(
                "学生表示不太明白。请用更简单

==== 服务端-AI辅导对话知识点掌握度实时更新与能力维度同步引擎-详细设计.md | unclosed@518/579 ====
            // 章节知识点给较低的基础分，除非已在对话中明确出现
            for (String kpId : chapterKPs) {
                kpScores.putIfAbsent(kpId, 0.2);
            }
        }
        
        // 4. 过滤：只保留相关性 >= 0.3 的知识点
        return kpScores.entrySet().stream()
            .filter(e -> e.getValue() >= 0.3)
            .map(e -> new WeightedKnowledgePoint(e.getKey(), e.getValue()))
            .sorted(Comparator.comparing(WeightedKnowledgePoint::getWeight).reversed())
            .collect(Collectors.toList());
    }
}

==== 服务端-中小学语文阅读理解专项训练与文本分析智能评估引擎-详细设计.md | unclosed@549/566 ====
┌──────────────────┐
│  自动难度评估     │ ← PassageDifficultyAssessor
│  + 特征提取       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  AI 自动标注      │ ← 调用 LLM 提取主题、修辞手法、知识点
│  主题/知识点/修辞 │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  AI 出题预生成    │ ← 基于

==== 服务端-作文素材库管理与主题素材智能推荐服务-详细设计.md | unclosed@608/628 ====
        ThemeAnalysisResult result = new ThemeAnalysisResult();
        
        // 1. 关键词直接匹配主题词表
        List<ThemeMatch> directMatches = matchThemesByKeywords(
            mergeKeywords(essayTitle, keywords)
        );
        
        // 2. 语义相似度匹配（通过向量库）
        List<ThemeMatch> semanticMatches = matchThemesBySemantic(essayTitle);
        
        // 3. 合并去重，按匹配分数排序
        List<ThemeMatch> merged = mergeAndDeduplicate(directMatches, semanticMatches);
        
        // 4. 取 Top-3 主

==== 服务端-化学学科智能辅导引擎-详细设计.md | unclosed@456/525 ====
          "products": [
            { "formula": "FeSO₄", "coefficient": 1, "state": "(aq)" },
            { "formula": "Cu", "coefficient": 1, "state": "(s)" }
          ],
          "type": "displacement",
          "balanced": true,
          "conditions": []
        }
      },
      {
        "stepId": 5,
        "type": "summary",
        "title": "知识总结",
        "content": "本题核心知识点：\n1. 金属活动性顺序：K Ca Na Mg Al Zn **Fe** Sn Pb (H) **Cu** Hg Ag Pt Au\n2. 置换反应：A + BC → AC + B（活动性 A > B）\n3. 常见离子颜色：Cu²⁺ 蓝色、Fe²⁺ 浅绿色、Fe³

==== 服务端-地理学科空间思维训练与交互式地图学习智能辅助引擎-详细设计.md | unclosed@506/657 ====
    user_id         BIGINT NOT NULL COMMENT '学生用户ID',
    region_uuid     VARCHAR(64) NOT NULL COMMENT '区域UUID',
    cognition_level DECIMAL(5,2) DEFAULT 0 COMMENT '认知水平 0-100',
    interaction_count INT DEFAULT 0 COMMENT '交互次数',
    correct_rate    DECIMAL(5,4) DEFAULT 0 COMMENT '相关题目正确率',
    last_active_at  DATETIME COMMENT '最近活跃时间',
    weak_points     JSON COMMENT '该区域下的薄弱知识点',
    metadata        JSON COMMENT '扩展数据',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_user_region (user_id, region_uuid),
    INDEX idx_user (user_id),
    INDEX

==== 服务端-学习会话状态快照与跨设备无缝接续恢复引擎-详细设计.md | unclosed@517/539 ====
          "module": "practice",
          "params": { "exam_id": "exam_2026_072_mid_math", "subject": "math" }
        },
        "last_device_id": "dev_a1b2c3d4",
        "last_device_name": "张三的iPhone",
        "last_device_type": "PHONE",
        "last_active_at": "2026-07-07T13:25:00Z",
        "is_current_device": false,
        "snapshot_summary": {
          "progress_text": "第12/25题",
          "elapsed_text": "已用时15分50秒",
          "has_unsaved_draft": true
        },
         

==== 服务端-学习内容推荐排序融合层与多目标加权决策引擎-详细设计.md | unclosed@804/849 ====
            # R5: 疲劳度检测（用户疲劳时降低高认知负荷内容权重）
            if context.learningState.fatigueLevel > 0.7:
                if item.candidate.cognitiveLevel in ['ANALYZE', 'EVALUATE', 'CREATE']:
                    item.final_score *= 0.6
                    rules_applied.append('FATIGUE_HIGH_COGNITION_PENALTY')
            
            # === 软规则（调权） ===
            
            # R6: 知识连续性加权（刚学完的章节相关内容加权）
            if item.candidate.chapterId == context.currentChapterId:
                item.final_score *= 1.15
                rules_applied.append('KNOWLEDGE_CONTINUITY_BOOST')
            
            # R7

==== 服务端-学习反思智能引导与元认知能力培养辅助引擎-详细设计.md | unclosed@623/648 ====
    },
    "subject_scores": {
      "MATH": 0.61,
      "CHINESE": 0.70,
      "ENGLISH": 0.68
    },
    "statistics": {
      "total_reflections": 52,
      "avg_response_quality": 0.68,
      "streak_days": 3,
      "last_reflection_at": "2026-06-16T14:30:00Z"
    },
    "radar_chart": {
      "labels": ["计划能力", "监控能力", "评估能力", "调

==== 服务端-学习状态实时感知与会话质量动态评估引擎-详细设计.md | unclosed@539/549 ====

#### 3.2.1 活跃会话上下文

```
Key:   lssa:session:{sessionId}:ctx
Type:  Hash
TTL:   3h（会话超时自动过期）

Fields:
  studentId        → 学生ID
  source           → 学习模式
  currentState     → 当前状态
  stateSince       → 当前状态开始时间
  previou

==== 服务端-学习行为预测与学习需求智能预判引擎-详细设计.md | unclosed@1017/1018 ====
    ],
    "summary": {
      "totalPredictions": 5,
      "highConfidenceCount": 3,
      "urgentActions": 2
    }
  }
}
```

### 5.2 预测信号订阅（Kafka）

```
Topic: prime

==== 服务端-学习计划动态调整与智能重排服务-详细设计.md | unclosed@640/705 ====
            reduction_ratio = self._calc_reduction_ratio(context)
            actions.append(AdjustmentAction(
                action_type=ActionType.ADJUST_DAILY_LOAD,
                new_task_params={'daily_task_count': context.current_daily_count * reduction_ratio},
                description=f'根据你最近的完成情况，每日任务量调整为 {int(context.current_daily_count * reduction_ratio)} 项',
                internal_reason=f'时间适配分={scores.time_fitness_score}, 压缩比={reduction_ratio}',
                impact_score=70
            ))

        # 情况2：有大量积压任务 → 延期低优先级任务
        overdue_tasks = context.get_overdue_tasks()
        if overdue_tasks:
            low_priority_overdue = [t for t in overdue_tasks if t.priority <= 2]
            if low_priority

==== 服务端-学习计划执行障碍智能诊断与个性化克服策略推荐引擎-详细设计.md | unclosed@1137/1139 ====
            
            # 插入到计划前部
            await self.plan_service.insert_tasks_before(
                plan_id=diagnosis.plan_id,
                tasks=micro_tasks,
                pause_original=payload.get("pauseOriginalPlan", True)
            )
```

### 6.2 启发式分析器

```python
class HeuristicAnalyzer:
    """启发式模式分析器 - 识

==== 服务端-学习计划模板库与场景化计划方案智能推荐引擎-详细设计.md | unclosed@910/963 ====
                    currentDate = currentDate.plusDays(1);
                    continue;
                }
                
                // 生成正常学习日任务
                DailyTaskPlan dailyPlan = buildDailyPlan(
                    currentDate, phase, dayInPhase, template, request, profile);
                dailyPlans.add(dailyPlan);
                
                currentDate = currentDate.plusDays(1);
            }
        }
        
        // Step 4: 生

==== 服务端-学段衔接知识桥梁课程与过渡期学习指导引擎-详细设计.md | unclosed@582/648 ====
      "phase": 3,
      "name": "衔接检测期",
      "weeks": "8-9",
      "focus": "综合检测与新学期准备",
      "tasks": [
        {
          "week": 8,
          "task_type": "comprehensive_test",
          "estimated_hours": 2.0
        },
        {
          "week": 9,
          "task_type": "learning_habits",
          "topic": "初中学习

==== 服务端-学生AI学习伙伴角色个性化与对话风格自适应引擎-详细设计.md | unclosed@729/838 ====
        
        // 学习风格匹配
        if ("visual".equals(style) && traits.personality.metaphor_usage == concrete) {
            score += 0.15;
        }
        if ("analytical".equals(style) && traits.personality.directness > 0.7) {
            score += 0.15;
        }
        if ("social".equals(style) && traits.personality.warmth > 0.7) {
            score += 0.15;
        }
        
        // 性格标签匹配
        if (personalityTags.contains("enc

==== 服务端-学生AI辅助依赖度智能监测与自主学习能力培养引导引擎-详细设计.md | unclosed@1002/1061 ====
                    "redirectToSimilarQuestion": True
                },
                "independenceChallenge": {
                    "dailyIndependentGoal": 3,
                    "rewardMultiplier": 2.0
                }
            }
        },
        "CRITICAL": {
            "action": "STRUCTURED_WEANING",
            "config": {
                "aiUsageLimit": {
                    "dailyMaxActions": 8,
                 

==== 服务端-学生个性化推送时间智能决策与通知触达时机优化引擎-详细设计.md | unclosed@448/511 ====
            )
            adjusted_score = sampled_rate * context_weight
            
            # 探索/利用判定
            total_samples = alpha + beta - 2.0  # 减去先验
            is_exploring = total_samples < self.MIN_SAMPLES_FOR_EXPLOIT
            
            scores.append(SlotScore(
                slot_index=slot_idx,
                day_of_week=dow,
                hour=hour,
                raw_score=sampled_rate,
                context_adjusted=adjusted_score,
               

==== 服务端-学生作业全生命周期管理与智能催交提醒调度引擎-详细设计.md | unclosed@518/550 ====
    // 4. 学生行为因子
    private StudentActivityPattern pattern;  // 学生活跃时段模式
    
    // 5. 负荷因子
    private int concurrentAssignments;       // 当前并发作业数
    private String loadLevel;               // LIGHT/NORMAL/HEAVY/OVERLOADED
}

/**
 * 学生活跃时段模式
 */
public class StudentActivityPattern {
    // 各时段活跃概率 [0-1]，基于过去30天行为统计
    private Map<HourBucket, Double> hourlyActiveProb

==== 服务端-学生学习卡点智能诊断与突破辅导策略推荐引擎-详细设计.md | unclosed@1135/1449 ====
            
            // 低年级学生：提升成功体验策略优先级
            if (profile.getGradeLevel() <= GradeLevel.G3 &&
                s.getStrategyType() == BreakthroughStrategyType.SUCCESS_EXPERIENCE) {
                s.setPriority(s.getPriority() + 15);
            }
            
            // 高自信学生：可以更直接指出错误
            if (profile.getSubjectConfidence(s.getSubject()) > 0.7 &&
                s.getStrategyType() == BreakthroughStrategyType.COGNITIVE_CONFLICT) {
                s.setPriority(s.getPriority() + 10);
            }
            
            // 低自

==== 服务端-学生学习过程质量实时感知与低效学习行为智能干预引擎-详细设计.md | unclosed@1661/1664 ====
└───┬────┘ └────────┘ └──────────┘
    │
    ▼
┌──────────┐
│ EVALUATED│ (效果已评估)
└──────────┘
```

### 10.2 行为模式标签流转

```
新用户
    │
    ├─ 观察

==== 服务端-学生用户学习内容推荐候选集生成与多路召回融合引擎-详细设计.md | unclosed@668/747 ====
            if (weakKps != null && !weakKps.isEmpty()) {
                List<KpRelation> prereqs = graphService.findRelations(
                    weakKps, RelationType.PREREQUISITE, perHop);
                for (KpRelation rel : prereqs) {
                    List<String> contentIds = ckpService.getContentByKnowledgePoint(
                        rel.getTargetKpId(), request.getStage());
                    for (String contentId : contentIds) {
                        if (request.getExcludeContentIds().contains(contentId)) continue;
                        if (candidates.stream().anyMatch(c -> c.getContentId().equals(contentId))) 
                            continue;
                        candidates.add(RecallCandidate.builder()
                            .contentId(contentId)
                            .sourceChannel(RecallChannelEnum.KG)
                            .channelScore(0

==== 服务端-学生用户学习设备使用场景识别与多终端体验智能适配引擎-详细设计.md | unclosed@1058/1084 ====
---

## 16. 关联文档

| 文档 | 关系 |
| --- | --- |
| 客户端-用户偏好设置与学习环境个性化系统 | 本引擎的个性化覆写消费用户偏好 |
| 服务端-实时用户Presence服务与多端活跃状态同步引擎 | Presence 提供设备在线状态，本引擎提供场景策略 |
| 用户设备管理与多端登录策略 | 设备注册和绑定管理 |
| 客户端-学习计时器与番茄钟专注模式 | 消费本引擎的 breakIntervalMinutes 和 enableFocusMode |
| 护眼模式与视觉健康管理系统 | 消费本引擎的 timeSlot 判定，联动护眼策略 |
| 服务端-学习时机感知与场景化内容推荐编排引擎 | 推荐编排消费本引擎的 contentFormatPriority |
| 服务端-多源通知智能合并与用户通知疲劳度管控引擎 | 通知合并消费本引擎的 notificationPolicy |
| 客户端-系统资源压力感知与智能降级调度引擎 | 客户端降级时参考本引擎的弱网/低电量策略 |

==== 服务端-学生考试备考状态智能评估与备考节奏动态调控引擎-详细设计.md | unclosed@498/547 ====
          "lateNightRatio": 0.20,
          "engagementTrend": "DECLINING",
          "efficiencyIndex": 0.68
        },
        "recommendedActions": [
          "将每日学习时间回调至计划值(3.5小时)",
          "安排1天休息日恢复精力",
          "优先复习高性价比考点"
        ],
        "createdAt": "2026-04-15T08:00:00Z",
        "status": "PENDING"
      }
    ],
    "

==== 服务端-学生考试焦虑智能识别与心理调适辅助干预引擎-详细设计.md | unclosed@500/511 ====

**响应:**
```json
{
  "code": 0,
  "data": {
    "needIntervention": true,
    "urgency": "medium",
    "recommendedStrategies": [
      {
        "strategyId": 7,
        "strategyName": "考前策略指导",
        "priority": 1,
        "reason": "距考试5天，学生焦虑评

==== 服务端-学生解题过程分步评分与过程性评价服务-详细设计.md | unclosed@570/594 ====
│  ② 步骤分割与标准化                                                │
│  ┌─────────────┐                                                  │
│  │ 步骤分割     │──→ 自动分割步骤 → 步骤编号 → 格式标准化          │
│  │ (若未预分割) │    (基于换行/编号/逻辑连接词)                      │
│  └─────────────┘                                                  │
│        │                                                          │
│        ▼                                                          │
│  ③ 获取参照解题过程                                                │
│  ┌─────────────┐                                                  │
│  │ 标准答案获取 │──→ 调用理科解题步骤推导服务获取标准解法            │
│  │             │──→ 获取题目元数据(知识点/题型/难度/评分标准)       │
│  └─────────────┘                                                  │
│        │                                                          │
│        ▼                                                         

==== 服务端-学生课外阅读分级推荐与阅读素养发展追踪评估引擎-详细设计.md | unclosed@1503/1518 ====
public class ReadingQuizGenerator {
    
    /**
     * 为书籍生成阅读理解测试题
     */
    public List<ReadingQuiz> generateForBook(BookInfo book, int questionCount) {
        List<ReadingQuiz> quizzes = new ArrayList<>();
        
        // 获取书籍关键章节内容
        List<BookChapter> keyChapters = chapterService.getKeyChapters(book.getId());
        
        for (BookChapter chapter : keyChapters) {
            String prompt = String.format(
                """

==== 服务端-小学科学探究式学习智能辅导与虚拟实验引导引擎-详细设计.md | unclosed@572/626 ====
      "fields": [
        { "key": "switch_on_result", "label": "开关闭合时", "type": "text" },
        { "key": "switch_off_result", "label": "开关断开时", "type": "text" }
      ]
    },
    {
      "stageIndex": 3,
      "title": "思考与总结",
      "guidance": "思考：要让灯泡发光，电路需要满足什么条件？如果断开一处导线，灯泡还会亮吗？",
      "interactiveType": "reflection_questions",
      "questions": [
        "电路中不可缺少的部分有哪些？",
        "开关的作用是什么？",
        "如果再加一

==== 服务端-幼儿端儿童语音识别适配与语音交互安全引擎-详细设计.md | unclosed@1057/1061 ====
| RECOGNIZING | ASR 推理中 | 15s | 降级通用模型 |
| EVALUATING | 发音评估中 | 10s | 跳过评估 |
| SAFETY_CHECK | 安全检查中 | 2s | 放行（fail-open） |
| COMPLETED | 成功完成 | - | - |
| BLOCKED | 安全拦截 | - | - |
| FAILED | 系统错误 | - | - |

### 6.3 模型降级链路

```text
首选: child_acoustic_v3_preschool (WER=8.2%)
  │
  ├─ 不可用/超时 →
  │   次}

==== 服务端-教材修订内容变更检测与平台内容影响评估引擎-详细设计.md | unclosed@528/620 ====
    }

    // 未匹配的新知识点 → 新增
    for (int j = 0; j < newPoints.size(); j++) {
        if (!matchedNew.contains(j)) {
            changes.add(buildAddedChange(newPoints.get(j)));
        }
    }

    return changes;
}

private static final double KNOWLEDGE_MATCH_THRESHOLD = 0.70;
private static final double KNOWLEDGE_WEAK_THRESHOLD = 0.50;

==== 服务端-教育AI代码执行沙箱与智能计算辅助引擎-详细设计.md | unclosed@1408/1433 ====
        // 邮箱
        Pattern.compile("[\\w.-]+@[\\w.-]+\\.\\w+"),
        // IP 地址
        Pattern.compile("\\b(?:\\d{1,3}\\.){3}\\d{1,3}\\b"),
        // 密钥/Token
        Pattern.compile("(?i)(?:key|token|secret|password|api[_-]?key)\\s*[=:]\\s*\\S+"),
        // 文件系统路径（可能的敏感路径）
        Pattern.compile("/(?:etc|root|home|var|proc|sys)/[^\\s]+")
    );

    /**
     * 过滤输出中的敏感信息
     */
    public static String filterOutput}

==== 服务端-教育内容多模态语义对齐与跨模态智能检索推荐引擎-详细设计.md | unclosed@533/560 ====
         │
         ▼
┌──────────────────┐
│ 3. 投影对齐      │  非文本模态向量投影到统一空间
│    Project Align │  image_vec(768d) → aligned_vec(1024d)
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ 4. 多路召回                          │
│    Multi-way Recall                  │
│  ┌─────────┬─────────┬─────────┐    │
│  │对齐空间 │文本专列 │图像专列 │    │
│  │Ann查

==== 服务端-教育内容学习目标自动提取与课标核心素养对齐引擎-详细设计.md | unclosed@835/836 ====
    "objectiveType": "...",
    "bloomLevel": "...",
    "isPrimary": false,
    "knowledgePoints": ["关联知识点ID"],
    "inferenceBasis": "推断依据说明",
    "confidenceScore": 0.0-1.0
  }
]
```

#### 4.2.3 素养维度对齐Prompt模板

```text
你是一名课程标

==== 服务端-教育内容学生阅读行为智能分析与个性化阅读体验优化引擎-详细设计.md | unclosed@1003/1040 ====
        if (recentEvents.size() < 10) {
            return FatigueAssessment.normal(0.2);
        }
        
        // 信号1：滚动速度趋势（加速 = 急于看完）
        double scrollSpeedTrend = calculateScrollSpeedTrend(recentEvents);
        
        // 信号2：停留时长趋势（递减 = 疲劳）
        double dwellTrend = calculateDwellTrend(recentEvents);
        
        // 信号3：互动行为衰减
        double interactionDecay = calculateInteractionDecay(recentEvents);
        
        // 信号4：会话时长过

==== 服务端-教育内容智能查重与知识库冗余内容净化引擎-详细设计.md | unclosed@516/534 ====
│  │ 指纹生成  │──▶│  相似性检测   │──▶│  冗余聚类分析    │    │
│  │ Pipeline  │   │  Engine      │   │  & Grouping     │    │
│  └──────────┘   └──────┬───────┘   └────────┬─────────┘    │
│       ▲                 │                    │              │
│       │           ┌─────┴──────┐      ┌──────┴───────┐     │
│       │           │  匹配片段   │      │  净化工作流   │     │
│       │           │  定位对齐   │      │  & 审核面板   │     │
│       │           └────────────┘      └──────────────┘     │
│       │                                                      │
│  ┌────┴────────────────────────────────────────────────┐   │
│  │              触发机制                                 │   │
│  │  ┌──────────┐  ┌────────────┐  ┌──────────────┐   │   │
│  │  │ 增量查重  │  │ 定时全量扫描│  │ 手动触发扫描  │   │   │
│  │  │(入库前)   │  │ (每日凌晨)  │  │ (运营后台)   │   │

==== 服务端-教育内容知识切片与微课智能分段生成引擎-详细设计.md | unclosed@1481/1508 ====
public enum SliceType {
    INTRO,        // 引入/导入
    KNOWLEDGE,    // 知识讲解
    EXAMPLE,      // 例题演示
    PRACTICE,     // 练习
    SUMMARY,      // 总结回顾
    EXTENSION     // 拓展延伸
}

// 内容类型
public enum RawContentType {
    TEXT,         // 纯文本/Markdown
    PDF,          // PDF文档
    VIDEO,        // 视频内容}

==== 服务端-教育内容知识图谱自动构建与实体关系抽取引擎-详细设计.md | unclosed@735/761 ====
// 3. 是否存在反向关系？
//    ├─ (entityB, relation, entityA) 存在？
//    │    ├─ relation 可逆（如 related_to, same_as）→ 合并
//    │    └─ relation 有向（如 prerequisite）→ 标记冲突，路由审核
//    └─ 否 → 创建新关系
//
// 4. 是否存在传递性冲突？
//    ├─ A prerequisite B, B prerequisite C, 但 C prerequisite A → 环检测
//    └─ 发现环 → 标记冲突，路由审核
//
// 5. 置信度阈值
//    ├─ confidence ≥ 0.85 → auto_approved
//    ├─ 0.60 ≤ confidence < 0.85 → pending_review
//    └─ confidence < 0.60 → candidate（仅存

==== 服务端-教育大模型RLHF反馈数据管线与模型持续优化闭环引擎-详细设计.md | unclosed@511/525 ====
{
  "code": 0,
  "data": {
    "job_id": "train_job_001",
    "status": "TRAINING",
    "progress": 0.45,
    "current_epoch": 2,
    "total_epochs": 3,
    "current_step": 1350,
    "total_steps": 3000,
    "latest_metrics": {
      "loss": 0.234,
      "rewards/accuracies": 0.78,
      "rewards/margins": 1.23

==== 服务端-教育平台学习策略智能编排引擎-详细设计.md | unclosed@518/532 ====
输入: 学生ID + 知识点/内容列表 + 学习上下文
  │
  ├─→ 1. 加载学生认知特征画像
  │     (工作记忆容量, 遗忘率, 交错耐受度, ...)
  │
  ├─→ 2. 加载知识点属性
  │     (难度, 认知层级, 前置依赖, 内容类型)
  │
  ├─→ 3. 规则过滤: 排除不适用的策略
  │     (学段限制, 学科限制, 学生特征限制)
  │
  ├─→ 4. 候选策略评分
  │     对每个候选策略:
  │     - 历

==== 服务端-教育平台搜索智能联想与实时热搜推荐引擎-详细设计.md | unclosed@1155/1716 ====
| 考试服务 | GET /api/v1/exam/upcoming | 获取近期考试安排 |
| 敏感词服务 | POST /api/v1/sensitive/check | 搜索词安全过滤 |

---

## 15. 版本演进路线

| 版本 | 功能范围 | 预估时间 |
|------|---------|---------|
| v1.0 (MVP) | 基础联想（Trie 前缀匹配）+ 全局热搜 Top-10 + 搜索历史 | 2 周 |
| v1.5 | 拼音匹配 + 分学段/学科热搜 + 个性化推荐（基础版） | 2 周 |
| v2.0 | 语义向量召回 + 协同过滤推荐 + 搜索趋势分析看板 | 3 周 |
| v2.5 | 搜索词智能扩展 + 关联词图谱 + 考试季节性加权 | 2 周 |
| v3.0 | AI 驱动的搜索意图预判 + 多模态搜索联想 | 待定 |

==== 服务端-教育平台统一风控决策中心与用户安全画像引擎-详细设计.md | unclosed@762/890 ====
                .build();
        } else if (unfreezeTime != null) {
            // 冻结已过期，清理缓存
            frozenCache.remove(request.getUserId());
        }

        return null; // 通过快速检查
    }

    /**
     * 综合评分计算
     */
    private int calculateFinalScore(UserSecurityProfile profile,
                                    

==== 服务端-教育数据匿名化与去标识化处理管线引擎-详细设计.md | unclosed@645/709 ====
        # 使用 log-linear 模型估计: risk = 1 / group_size
        individual_risks = data.merge(
            qi_groups.reset_index().rename(columns={0: '_group_size'}),
            on=self.quasi_identifiers,
            how='left'
        )['_group_size'].apply(lambda s: 1.0 / s)
        
        max_individual_risk = float(individual_risks.max())
        avg_individual_risk = float(individual_risks.mean())
        high_risk_count = int((individual_risks > 0.2).sum())
        high_risk_ratio = high_risk_count / len(data)
        
        # 3. 准标识符组合唯一性
        unique_qi_combos = data[self.quasi_identifiers].drop

==== 服务端-教育知识点前置依赖检测与学习路径推荐服务-详细设计.md | unclosed@589/769 ====
            )
        
        critical_gaps = [g for g in gaps if g.mandatory]
        shallow_gaps = [g for g in gaps if g.depth == 1]
        deep_gaps = [g for g in gaps if g.depth > 1]
        
        # 按优先级排序：先深层（根因），再浅层；同层按依赖强度降序
        priority_gaps = sorted(
            gaps,
            key=lambda g: (-g.depth, -g.prerequisite_strength, g.mastery_level),
        )
        priority_gap_ids = [g.knowledge_id for g in priority_gaps]
        
        # 预估补学时长：每

==== 服务端-教育视频内容学生观看行为智能分析与个性化观看体验优化引擎-详细设计.md | unclosed@660/942 ====
                event.get('timestamp', ''),
                event.get('deviceType', ''),
                event.get('networkType', ''),
                event.get('metadata', {}).get('chapterId', ''),
                json.dumps(event.get('metadata', {}))
            ])
        self.clickhouse.execute(
            "INSERT INTO video_behavior_event VALUES",
            rows
        )

    def _finalize_session(self, state: WatchSessionState,
                          abandoned: bool = False, abandon_point: int = 0,
                          abandon_reason: str = ''):

==== 服务端-用户学习年度回顾与成长报告生成引擎-详细设计.md | unclosed@537/552 ====
┌──────────────────┐        ┌──────────────────┐
│ 灰度用户筛选       │        │ 全量用户发布       │
│ (活跃度 TOP 5%)  │        │ status=published  │
│ status=published  │        │                   │
│                   │        │ Push 推送          │
│ 端内入口展示       │        │ · 标题：你的2026   │
│ · 首页Banner      │        │ · 副标题：点击查看  │
│ · 消息中心        │        │ · 深链接直达       │
│                   │        │                   │
│ 监控指标           │        │ 端内全量展示       │
│ · 打开率           │        │ · 首页弹窗        │
│ · 分享率           │        │ · 首页Banner      │
│ · 崩溃率           │        │ · 底部Tab角标     │
│ · 加载时长         │        │ · 消息中心      

==== 服务端-用户学习激励中心与行为奖励统一规则引擎-详细设计.md | unclosed@627/628 ====
        ],
        "triggeredAt": "2026-06-17T15:30:00Z"
      }
    ]
  }
}
```

### 4.3 客户端奖励领取确认 API

#### 4.3.1 确认奖励已展示

```
POST /api/v1/incentive/rewards/ack

==== 服务端-用户学习行为异常检测与作弊行为识别服务-详细设计.md | unclosed@839/847 ====
  behavior_similarity: 0.9       -- 行为相似度阈值
```

### 4.5 虚假学习时长检测

```
规则编码: FAKE_STUDY_TIME_V1
触发条件: 学习会话结束时 / 离线分析

检测维度:
1. 心跳异常: 心跳间隔极度均匀 (非自然的人类行为节奏)
2. 交互缺失: 长时间无任何交互事件 (点击/滑动/输入)
3. 页面冻结: 页面停留在同一位置长时间不变
4. 后台活跃: APP在后

==== 服务端-用户生命周期营销自动化与智能触达策略编排引擎-详细设计.md | unclosed@621/636 ====
                    │   步骤调度器扫描      │
                    │   (每30秒一轮)        │
                    └──────────┬───────────┘
                               │
                    ┌──────────▼───────────┐
                    │  取出到期步骤          │
                    │  Redis ZSet:          │
                    │  key=pulse:step:due   │
                    │  score=executeAt      │
                    └──────────┬───────────┘
                               │
              ┌────────────────┼────────────────┐
              │                │                │
              

==== 服务端-用户转化漏斗分析与增长归因引擎-详细设计.md | unclosed@600/649 ====
        {"touchpoint_module": "study_plan", "attributed_conversions": 105.1, "percentage": 12.0},
        {"touchpoint_module": "sync_class", "attributed_conversions": 70.1, "percentage": 8.0},
        {"touchpoint_module": "exam_prep", "attributed_conversions": 28.8, "percentage": 3.3}
      ]
    },
    "consensus_ranking": [
      {"rank": 1, "touchpoint_module": "ai_tutor", "avg_rank_score": 1.0},
      {"rank": 2, "touchpoint_module": "photo_search", "avg_rank_score": 2.0},
      {"rank": 3, "touchpoint_module": "mistake_book", "avg_rank_score": 3.25},
      {"rank": 4, "touchpoint_module": "study_plan", "avg_rank_score": 4.0},
      {"rank": 5, "touchpoint_module": "sync_class", "avg_rank_score": 4.25},
      {"rank": 6, "touchpoint_module": "exam_prep", "avg_rank_score": 5.5}
    ],
    "insight": "AI辅导功能是最大的转化驱动力（多模型一致排名第1

==== 服务端-统一通知消息模板引擎与多渠道内容适配渲染服务-详细设计.md | unclosed@551/553 ====
| `default(value)` | 空值默认 | `{{name \| default("同学")}}` |
| `truncate(maxLen)` | 截断+省略号 | `{{text \| truncate(50)}}` |
| `join(separator)` | 列表拼接 | `{{subjects \| join("、")}}` |
| `enumerate` | 列表编号 | `1. xxx\n2. yyy` |
| `mask` | 脱敏处理 | `{{phone \| mask}}` → `138****5678` |
| `pluralize(single, plural)` | 复数化 | `{{count}}个{{unit \| pluralize("任务", "任务")}}` |
| `upper` / `lower` | 大小写转换 | `{{code \| upper}}` |
| `safe` | 标记为安全（不转义） | `{{html_content \| safe}}` |

### 4.3 条件块示例

```
{% if task_count > 0 %}
你还

==== 服务端-题目条件变式生成与解题方法归纳引擎-详细设计.md | unclosed@692/707 ====
 * 数值变式生成器
 * 
 * 核心逻辑：
 * 1. 从题干中提取数值参数和约束
 * 2. 在约束范围内生成新数值
 * 3. 重新计算答案
 * 4. 校验新答案的合理性
 */
public class NumericVariationGenerator {
    
    public VariationResult generate(Question source, VariationConfig config) {
        // Step 1: 提取数值参数
        List<NumericParam> params = extractNumericParams(source.getStem());
        // 例

==== 服务端-题目答案正确性自动验算与解析一致性校验管线引擎-详细设计.md | unclosed@885/891 ====
     |
     v (否)
   任务完成 -> 统计汇总 -> 通知创建者
```

### 6.2 批量验算控制器

```python
class BatchVerificationController:
    """批量验算控制器"""
    
    BATCH_SIZE = 50
    AI_RATE_LIMIT = 3  # AI策略最大并发
    CALC_RATE_LIMIT = 10  # 计算

==== 服务端API接口安全防护与渗透防御体系-详细设计.md | unclosed@746/749 ====
                    }
                });
            }
        };
    }
}
```

### 5.3 命令注入防御

```java
/**
 * 安全的系统命令执行工具
 * 原则：禁止直接执行用户输入拼

==== 服务端分布式唯一ID生成与序号管理服务-详细设计.md | unclosed@691/810 ====
            else:
                code = base62_encode(random_id % self.CODE_8_MAX).zfill(8)[-8:]

            if not existence_check_fn(code):
                return code

        raise RuntimeError(f"短码生成失败: 重试 {max_retries * 2} 次后仍有冲突")

    @staticmethod
    def _obfuscate(value: int) -> int:
        """简单混淆，使连续输入产生不连续输出"""
        # MurmurHash 风格的位混合
        value ^= value >> 16
        value = (value * ShortCodeGenerator.OBFUSCATION_SALT) & 0xFFFFFFFF

==== 服务端数据库读写分离与分库分表演进策略-详细设计.md | unclosed@733/787 ====
        self.steps.append(SagaStep(name=name, execute=execute, compensate=compensate))
        return self

    async def execute(self) -> bool:
        """执行 Saga"""
        logger.info(f"Saga [{self.name}] 开始执行, 共 {len(self.steps)} 步")

        for step in self.steps:
            step.status = SagaStepStatus.EXECUTING
            try:
                await step.execute()
                step.status = SagaStepStatus.COMPLETED
                self.completed_steps.append(step)
                logger.info(f"Saga [{self.name}] 步

==== 服务端统一数据对账与跨服务一致性校验引擎-详细设计.md | unclosed@537/644 ====
                "field": "status",
                "source": source["status"],
                "target": target["status"],
                "expected_target": expected_target_status
            }

        return item

    def auto_fix(self, discrepancy: ReconItem) -> Optional[Dict]:
        """支付差异自动修复策略"""
        if discrepancy.discrepancy_type == "value_mismatch":
            diff_amount = abs(
                Decimal(discrepancy.source_value["amount"]) -
                Decimal(discrepancy.target_value["amount"])

==== 服务端通用数据导入服务与批量操作引擎-详细设计.md | unclosed@779/929 ====
            for f in fields:
                if mapped.get(f) is None:
                    errors.append(ValidationError(
                        row_number=row_number, error_type="validation_error",
                        error_code="FIELD_REQUIRED", field_name=f,
                        field_value=None, error_message=message
                    ))

        # unique 和 relation_exists 在 BatchProcessor 中做批量校验（需查库）
        return errors


class BatchValidator:
    """批次级批量校验（需

==== 生产事故应急响应与OnCall值班调度体系-详细设计.md | unclosed@935/936 ====

## 15. 附录 B：与相关文档的衔接清单

| 衔接点 | 方向 | 契约 |
| --- | --- | --- |
| Alertmanager → 本服务 | 入 | Webhook + HMAC；label 约定 `severity/team/component/playbook_hint` |
| 自愈引擎 → 本服务 | 入 | `POST /internal/webhooks/selfheal`，`need_human=true` 或 playbook 执行失败 |
| SLO 平台 → 本服务 | 入 | burn rate 超阈事件（fast/slow burn 两档分别映射 SEV2/SEV3） |
| 本服务 → 通知中心 | 出 | SEV1/2 用户公告（系统公告/Banner），幂等键 `incident-{id}-update-{n}` |
| 本服务 → 复盘引擎 | 出 | `RESOLVED` 后创建复盘任务；回写 `postmortem_id` |
| 本服务 → 客服知识库 | 出 | 故障公告同步（坐席/AI 客服口径） |
| 运维工作台 → 本服务 | UI | 排班/值班台/事故/状态页四个管理页面挂载 |
```


==== 用户协议与法律文档管理服务-详细设计.md | unclosed@536/655 ====
        scene: ConsentScene,
        client_info: ClientInfo,
    ) -> None:
        """用户拒绝同意（记录拒绝行为）"""
        # 对于必要文档，拒绝 = 无法使用 APP
        # 此处仅记录，由前端处理退出逻辑

        doc = await self._doc_repo.get_by_type_version(doc_type, doc_version)

        await self._consent_repo.create(
            user_id=user_id,
            doc_type=doc_type,
            doc_version=doc_version,
            doc_id=doc.id

==== 用户引导与新手教程系统-详细设计.md | unclosed@813/820 ====
### 4.1 概述

除了首次引导，用户首次使用各功能模块时，触发轻量级的场景化教程。每个教程包含 2-5 步操作引导。

### 4.2 教程注册表

```typescript
/** 功能教程定义 */
interface FeatureTutorial {
  id: string;
  featureName: string;
  triggerCondition: TutorialTrigger;
  steps: TutorialStep[];
  priority: number;        // 展示优先级（同页

==== 用户额度与API调用管控系统-详细设计.md | unclosed@683/707 ====
        "retry_after_sec": 28800,
        "upgrade_suggestion": {
            "tier": "monthly",
            "tier_display": "月度会员",
            "new_limit": 100,
            "price": "¥29.9/月",
            "upgrade_url": "/api/v1/subscription/plans"
        },
        "add_on_available": [
            {
                "product_id": "addon_ai_qa_10",
                "name": "AI问答10次加油包",
                "price": "¥2.9",
                "purchase_url": "/api/v1/store/addon/

==== 端到端流程设计-学习计时器与番茄钟专注模式完整链路-详细设计.md | unclosed@561/577 ====
    user_id             VARCHAR(36),                        -- NULL表示系统预设
    name                VARCHAR(64)     NOT NULL,
    mode_type           VARCHAR(32)     NOT NULL,           -- POMODORO / CUSTOM / COUNTDOWN
    
    focus_seconds       INT             NOT NULL,
    break_seconds       INT             NOT NULL DEFAULT 0,
    long_break_seconds  INT             NOT NULL DEFAULT 0,
    long_break_interval SMALLINT        NOT NULL DEFAULT 4, -- 每N个周期一次长休息
    
    is_system           BOOLEAN         NOT NULL DEFAULT FALSE,
    sort_order          SMALLINT        NOT NULL DEFAULT 0,
    
    created_at          TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ     NOT NULL DEFAULT NOW()

==== 端到端流程设计-拼音识字启蒙完整链路-详细设计.md | unclosed@597/607 ====

### 4.1 业务流程

```
┌───────────────────────────────────────────────────────────────┐
│                   汉字识记完整流程                               │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  用户进入识字入口                                              │
│       │                                                       │
│       ▼                                                       │
│  ┌─────────────┐                                              │
│  │ 字库选择     │ ←── 按教材 / 按主题 / 按偏旁 / 智能推荐      │
│  └──────┬──────┘                                              │

==== 端到端流程设计-数据导出与账户注销完整链路-详细设计.md | unclosed@774/840 ====
 │                        │                        │                     │────────┐           │                │
 │                        │                        │                     │◀───────┘           │                │
 │  ◀── 推送"导出完成" ──│                         │                     │                   │                │
 │                        │                        │                     │                   │                │
 │  点击下载               │                        │                     │                   │                │
 │───────────────────────▶│  GET /download          │                     │                   │                │
 │                        │───────────────────────▶│  生成预签名URL       │                   │                │
 │                        │                        │──────────────────▶  │                   │                │
 │                        │                        │                     │  生成URL           │                │
 │                        │                        │                     │─────────────────────────────────────▶│
 │                        │                        │                     │◀────────────────── presigned URL ──│
 │                        │                        │◀─ 302重定向 ────────│                   │                │
 │                        │◀─ 302 ────────────────│                     │                   │                │
 │  ◀── 下载文件 ──────────│                        │                     │                   │                │                        

==== 端到端流程设计-自适应学习与个性化推荐完整链路-详细设计.md | unclosed@490/563 ====
        profile.user_id,
        limit=5
    )
    if recent_mistakes:
        candidates.append(Candidate(
            type='mistake_retry',
            priority_boost=40,
            reason=f'有{len(recent_mistakes)}道错题待复习',
            metadata={'count': len(recent_mistakes)}
        ))
    
    # 场景相关策略
    if scene == RecommendScene.AFTER_PRACTICE:
        # 练习后推荐：基于刚才做错的

==== 管理后台-运营活动与营销推广配置工作台-详细设计.md | unclosed@555/557 ====
    "per_user_limit": 1,
    "valid_type": "FIXED",
    "valid_start": "2026-07-01 00:00:00",
    "valid_end": "2026-07-31 23:59:59",
    "distribution": "MANUAL",
    "campaign_id": 10086
}
```

#### 优惠券操作

```
POST /coupons/batches/:id/distribute    # 发放到指定用户群
POST /coupons/batches/:id/disable       |

==== 英语语法专项训练系统-详细设计.md | unclosed@631/637 ====
### 5.3 AI 语法讲解接口

#### POST `/api/v1/grammar/ai/explain

请求 AI 对某题/某语法点进行详细讲解。

**请求：**
```json
{
  "question_id": 2001,                // 题目ID（与kp_id二选一）
  "knowledge_point_id": null,         // 知识点ID
  "user_answer": "go",                // 用户答案（错题讲解时传入）
  "explanation_depth": "detailed",    // brief/detailed/deep
  "include_contrast": true            // 是否包含易混对比

==== 设备指纹与反作弊风控引擎-详细设计.md | unclosed@746/759 ====
```
用户发起支付
    │
    ▼
设备环境检查 ──▶ 新设备? ──▶ 短信验证
    │
    ▼
支付历史检查 ──▶ 多次退款? ──▶ 拒绝 + 运营审核
    │
    ▼
IP/代理检查 ──▶ 命中黑名单? ──▶ 拒绝
    │
    ▼
支付工具检查 ──▶ 同工具>5账号? ──▶ 限