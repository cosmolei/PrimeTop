# 服务端-练习测评会话服务与答题流程状态机 - 详细设计

> **版本**: v1.0 | **日期**: 2026-06-06 | **状态**: 初稿
> **优先级**: P0 | **关联模块**: 练习与测评系统、题目服务、学习记录服务、学情分析
> **关联文档**: 练习与测评系统-详细设计、服务端-题目智能推荐与个性化练习编排服务、互动练习与即时反馈引擎、服务端-练习答题实时批改与知识点掌握度增量更新服务、客户端-练习测评答题页面架构与交互流程设计、客户端-断点续学与学习上下文恢复

---

## 1. 文档定位

### 1.1 为什么需要这份文档

PrimeTop 的练习测评功能是核心学习闭环的关键环节。当用户进入一次练习/考试时，系统需要管理从**会话创建 → 答题交互 → 批改评分 → 结果汇总 → 学习记录沉淀**的完整生命周期。

现有文档已分别覆盖：
- **题目智能推荐与编排**：如何选择合适的题目组成一套练习
- **实时批改与评分**：如何对提交的答案进行判分
- **客户端答题页面**：前端如何展示题目、收集答案
- **学习记录追踪**：如何持久化学习数据

但缺少一份专注于**练习会话本身的生命周期管理**的文档，回答以下问题：

1. 一次练习会话从创建到结束经过哪些状态？状态流转规则是什么？
2. 答题计时器如何在服务端与客户端之间保持同步？
3. 用户中途退出（切后台、杀进程、断网）后如何恢复会话？
4. 多设备同时操作同一会话时如何保证数据一致性？
5. 练习会话的题目队列如何管理（顺序、跳过、标记）？
6. 每道题的答题过程如何记录（草稿、修改、犹豫时长）？
7. 会话完成后如何批量触发下游任务（错题收录、掌握度更新、报告生成）？

### 1.2 适用读者

- **服务端开发工程师**：实现练习会话服务、状态机、计时器和恢复机制
- **客户端开发工程师**：理解会话协议、计时器同步策略和断点恢复流程
- **测试工程师**：设计会话生命周期测试用例、异常恢复测试场景

### 1.3 设计目标

| 目标 | 说明 | 指标 |
| --- | --- | --- |
| 会话可靠性 | 会话创建后不丢失，支持断点恢复 | 恢复成功率 ≥ 99.9% |
| 计时准确 | 服务端计时与客户端显示偏差可控 | 偏差 ≤ 2s |
| 并发安全 | 多设备操作同一会话不产生脏数据 | 零数据冲突 |
| 状态一致 | 会话状态在任何异常下都能正确恢复 | 无死锁、无悬空状态 |
| 低延迟 | 创建会话、提交答案等核心操作快速响应 | P99 ≤ 200ms |
| 可观测 | 会话全生命周期可追踪、可回溯 | 100% 链路追踪覆盖 |

---

## 2. 核心数据模型

### 2.1 练习会话（PracticeSession）

```sql
-- 练习会话主表
CREATE TABLE practice_sessions (
    -- 主键与标识
    id                  BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '会话ID',
    session_uuid        CHAR(36) NOT NULL UNIQUE COMMENT '会话UUID（外部暴露）',
    
    -- 用户信息
    user_id             BIGINT NOT NULL COMMENT '用户ID',
    student_profile_id  BIGINT NOT NULL COMMENT '学生档案ID（快照）',
    
    -- 会话配置
    session_type        TINYINT NOT NULL COMMENT '会话类型: 1=同步练习 2=专项训练 3=模拟考试 4=错题重练 5=智能推荐练习 6=每日任务练习',
    source_entry        VARCHAR(32) NOT NULL COMMENT '入口来源: sync_classroom/topic_drill/mock_exam/mistake_review/smart_recommend/daily_task',
    
    -- 学科与教材上下文
    subject_code        VARCHAR(16) NOT NULL COMMENT '学科编码: CHN/MATH/ENG/PHY/CHE/BIO/HIS/GEO/POL',
    grade_level         TINYINT NOT NULL COMMENT '年级层级: 1=幼儿 2=小学 3=初中 4=高中',
    grade               TINYINT NOT NULL COMMENT '具体年级: 0-12',
    semester            VARCHAR(8) NOT NULL COMMENT '学期: 2026-S1/2026-S2',
    textbook_edition    VARCHAR(32) NOT NULL COMMENT '教材版本: PEP/BJB/SJB/FLTRP',
    
    -- 题目配置
    total_question_count INT NOT NULL COMMENT '题目总数',
    question_source_type VARCHAR(16) NOT NULL COMMENT '题目来源: curated/ai_generated/mixed',
    
    -- 计时配置
    time_limit_seconds  INT NULL COMMENT '总时间限制（秒），NULL=不限时',
    per_question_limit  INT NULL COMMENT '单题时间限制（秒），NULL=不限时',
    
    -- 状态管理
    status              TINYINT NOT NULL DEFAULT 1 COMMENT '会话状态: 1=CREATED 2=ACTIVE 3=PAUSED 4=COMPLETED 5=EXPIRED 6=ABANDONED',
    status_reason       VARCHAR(64) NULL COMMENT '状态变更原因',
    
    -- 计时信息
    started_at          DATETIME(3) NULL COMMENT '首次进入答题时间',
    last_active_at      DATETIME(3) NULL COMMENT '最近一次活动时间',
    elapsed_seconds     INT NOT NULL DEFAULT 0 COMMENT '累计答题耗时（秒）',
    paused_at           DATETIME(3) NULL COMMENT '最近一次暂停时间',
    total_paused_seconds INT NOT NULL DEFAULT 0 COMMENT '累计暂停时长（秒）',
    completed_at        DATETIME(3) NULL COMMENT '完成时间',
    expired_at          DATETIME(3) NULL COMMENT '过期时间（自动计算）',
    
    -- 答题进度
    current_index       INT NOT NULL DEFAULT 0 COMMENT '当前答题位置（0-based）',
    answered_count      INT NOT NULL DEFAULT 0 COMMENT '已作答题目数',
    correct_count       INT NOT NULL DEFAULT 0 COMMENT '正确题目数',
    marked_for_review   INT NOT NULL DEFAULT 0 COMMENT '标记待检查题目数',
    
    -- 成绩统计（完成后填充）
    total_score         DECIMAL(5,2) NULL COMMENT '总分',
    max_score           DECIMAL(5,2) NULL COMMENT '满分',
    accuracy_rate       DECIMAL(5,4) NULL COMMENT '正确率',
    avg_time_per_q      DECIMAL(6,2) NULL COMMENT '平均每题耗时（秒）',
    
    -- 会话级上下文
    chapter_id          BIGINT NULL COMMENT '关联章节ID（同步练习时）',
    topic_id            BIGINT NULL COMMENT '关联专题ID（专项训练时）',
    plan_id             BIGINT NULL COMMENT '关联学习计划ID（每日任务时）',
    mock_exam_id        BIGINT NULL COMMENT '关联模拟试卷ID（模拟考试时）',
    
    -- 元数据
    device_info         JSON NULL COMMENT '设备信息快照',
    app_version         VARCHAR(16) NOT NULL COMMENT '客户端版本',
    
    -- 审计字段
    created_at          DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at          DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    version             INT NOT NULL DEFAULT 1 COMMENT '乐观锁版本号',
    
    -- 索引
    INDEX idx_user_status (user_id, status),
    INDEX idx_user_created (user_id, created_at DESC),
    INDEX idx_expired (status, expired_at) WHERE status IN (1, 2, 3),
    INDEX idx_chapter (chapter_id, status),
    INDEX idx_plan (plan_id, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='练习会话主表';
```

### 2.2 会话题目（SessionQuestion）

```sql
-- 会话题目表：会话中每道题的实例
CREATE TABLE session_questions (
    id                  BIGINT PRIMARY KEY AUTO_INCREMENT,
    session_id          BIGINT NOT NULL COMMENT '关联 practice_sessions.id',
    question_uuid       CHAR(36) NOT NULL UNIQUE COMMENT '题目实例UUID',
    
    -- 题目引用
    question_id         BIGINT NOT NULL COMMENT '题库中的题目ID',
    question_snapshot   JSON NOT NULL COMMENT '题目快照（题干、选项、答案、解析等）',
    question_version    INT NOT NULL DEFAULT 1 COMMENT '题目快照版本',
    
    -- 排序与分组
    sequence_index      INT NOT NULL COMMENT '题目序号（0-based）',
    group_label         VARCHAR(32) NULL COMMENT '分组标签（如阅读理解的同一篇文章下的题）',
    group_sequence      INT NULL COMMENT '组内序号',
    
    -- 分值
    score_value         DECIMAL(5,2) NOT NULL DEFAULT 1.00 COMMENT '本题分值',
    
    -- 答题状态
    answer_status       TINYINT NOT NULL DEFAULT 0 COMMENT '0=NOT_VIEWED 1=VIEWED 2=ANSWERING 3=ANSWERED 4=REVIEWED 5=MARKED_CORRECT 6=MARKED_WRONG',
    
    -- 答题内容
    user_answer         JSON NULL COMMENT '用户答案（结构化）',
    submitted_at        DATETIME(3) NULL COMMENT '提交时间',
    
    -- 批改结果
    is_correct          TINYINT NULL COMMENT '0=错误 1=正确 NULL=未批改',
    auto_score          DECIMAL(5,2) NULL COMMENT '自动评分',
    manual_score        DECIMAL(5,2) NULL COMMENT '人工评分（主观题）',
    final_score         DECIMAL(5,2) NULL COMMENT '最终得分',
    grading_detail      JSON NULL COMMENT '批改详情（步骤分、错误定位等）',
    graded_at           DATETIME(3) NULL COMMENT '批改时间',
    
    -- 计时
    first_viewed_at     DATETIME(3) NULL COMMENT '首次查看时间',
    answer_elapsed_ms   INT NULL COMMENT '答题耗时（毫秒）',
    
    -- 标记
    is_marked_review    TINYINT NOT NULL DEFAULT 0 COMMENT '是否标记待检查',
    is_skipped          TINYINT NOT NULL DEFAULT 0 COMMENT '是否跳过',
    
    -- 知识点关联
    knowledge_point_ids JSON NULL COMMENT '关联知识点ID列表',
    difficulty_level    TINYINT NULL COMMENT '题目难度: 1-5',
    
    -- 乐观锁
    version             INT NOT NULL DEFAULT 1,
    created_at          DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at          DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    
    INDEX idx_session_seq (session_id, sequence_index),
    INDEX idx_question (question_id),
    UNIQUE INDEX uk_session_seq (session_id, sequence_index)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='会话题目实例表';
```

### 2.3 答题过程记录（AnswerEvent）

```sql
-- 答题过程事件表：记录答题过程中的细粒度行为
CREATE TABLE answer_events (
    id                  BIGINT PRIMARY KEY AUTO_INCREMENT,
    event_uuid          CHAR(36) NOT NULL UNIQUE,
    session_id          BIGINT NOT NULL,
    session_question_id BIGINT NOT NULL COMMENT '关联 session_questions.id',
    
    -- 事件信息
    event_type          VARCHAR(32) NOT NULL COMMENT 'VIEW/INPUT_CHANGE/OPTION_SELECT/VOICE_INPUT/CALC_SCRATCH/ERASE/UNDO/CONFIRM/SKIP/FLAG/UNFLAG/NAVIGATE',
    event_data          JSON NULL COMMENT '事件数据（如当前输入值）',
    client_timestamp    BIGINT NOT NULL COMMENT '客户端事件时间戳（毫秒）',
    server_timestamp    DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) COMMENT '服务端接收时间',
    
    -- 上下文
    sequence_index      INT NOT NULL COMMENT '当时在答第几题',
    time_offset_ms      INT NOT NULL COMMENT '距会话开始的毫秒偏移',
    
    -- 索引
    INDEX idx_session_q (session_id, session_question_id),
    INDEX idx_session_time (session_id, time_offset_ms)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='答题过程事件表';
```

### 2.4 Redis 缓存结构

```python
# ====== 会话热点数据缓存（Redis Hash） ======

# Key: practice:session:{session_uuid}
# 用途：会话实时状态，替代高频数据库查询
{
    "id": "1234567890",                       # 数据库主键ID
    "user_id": "100001",
    "status": "2",                            # 状态码
    "current_index": "5",                     # 当前题号
    "answered_count": "3",                    # 已答题数
    "elapsed_seconds": "420",                 # 累计耗时
    "last_active_at": "2026-06-06T00:15:30",  # 最近活动时间
    "server_start_ts": "1749153600000",       # 服务端开始计时时间戳
    "pause_accumulated_ms": "15000",          # 累计暂停毫秒
    "version": "7",                           # 乐观锁版本
}
# TTL: 从创建起 48h（超过48h未完成的会话自动清理）

# ====== 单题实时答案缓存（Redis Hash） ======

# Key: practice:answer:{session_uuid}:{seq_index}
# 用途：答题过程中的中间状态，避免频繁写入DB
{
    "answer_status": "2",                     # ANSWERING
    "user_answer": "{\"options\":[\"A\",\"C\"]}", # 当前答案
    "first_viewed_at": "2026-06-06T00:12:00",
    "view_duration_ms": "18000",              # 查看时长
    "input_count": "3",                       # 输入修改次数
    "is_marked_review": "0",
}
# TTL: 随会话 TTL

# ====== 会话计时器（Redis Sorted Set + Key） ======

# Key: practice:timers:active
# 用途：跟踪所有活跃会话的截止时间，用于定时检查超时
# Score = 截止时间戳（毫秒），Member = session_uuid
# ZADD practice:timers:active <deadline_ts> <session_uuid>

# Key: practice:timer:{session_uuid}
# 用途：单会话计时器详情
{
    "time_limit_seconds": "1800",             # 总时限
    "server_start_ts": "1749153600000",       # 开始时间戳
    "pause_accumulated_ms": "15000",          # 累计暂停
    "per_question_limit": "120",              # 单题时限（秒）
    "current_q_started_ts": "1749154230000",  # 当前题目开始时间戳
}
# TTL: 随会话 TTL

# ====== 会话分布式锁 ======

# Key: practice:lock:{session_uuid}
# 用途：防止多设备同时操作同一会话
# SET practice:lock:{session_uuid} <device_id> NX EX 30
# 每次操作前获取锁，操作后释放
```

### 2.5 数据类型定义（TypeScript/Python 风格）

```python
from enum import IntEnum
from dataclasses import dataclass, field
from typing import Optional, List, Dict, Any
from datetime import datetime

# ====== 枚举定义 ======

class SessionType(IntEnum):
    """练习会话类型"""
    SYNC_PRACTICE = 1         # 同步课堂练习
    TOPIC_DRILL = 2           # 专项训练
    MOCK_EXAM = 3             # 模拟考试
    MISTAKE_REVIEW = 4        # 错题重练
    SMART_RECOMMEND = 5       # 智能推荐练习
    DAILY_TASK = 6            # 每日任务练习

class SessionStatus(IntEnum):
    """会话状态"""
    CREATED = 1               # 已创建，未开始
    ACTIVE = 2                # 答题中
    PAUSED = 3                # 已暂停
    COMPLETED = 4             # 已完成
    EXPIRED = 5               # 已超时
    ABANDONED = 6             # 已放弃

class AnswerStatus(IntEnum):
    """单题答题状态"""
    NOT_VIEWED = 0            # 未查看
    VIEWED = 1                # 已查看
    ANSWERING = 2             # 答题中
    ANSWERED = 3              # 已提交
    REVIEWED = 4              # 已检查
    MARKED_CORRECT = 5        # 批改正确
    MARKED_WRONG = 6          # 批改错误

class AnswerEventType(str):
    """答题过程事件类型"""
    VIEW = "VIEW"                  # 查看题目
    INPUT_CHANGE = "INPUT_CHANGE"  # 输入变化
    OPTION_SELECT = "OPTION_SELECT" # 选择选项
    VOICE_INPUT = "VOICE_INPUT"     # 语音输入
    CALC_SCRATCH = "CALC_SCRATCH"   # 草稿计算
    ERASE = "ERASE"                 # 擦除
    UNDO = "UNDO"                   # 撤销
    CONFIRM = "CONFIRM"             # 确认提交
    SKIP = "SKIP"                   # 跳过
    FLAG = "FLAG"                   # 标记
    UNFLAG = "UNFLAG"               # 取消标记
    NAVIGATE = "NAVIGATE"           # 导航切换题目

# ====== 核心数据结构 ======

@dataclass
class QuestionSnapshot:
    """题目快照（存储在 session_questions.question_snapshot）"""
    question_id: int
    question_type: str          # single_choice/multi_choice/fill_blank/short_answer/essay/calculation/reading_comp
    stem: str                   # 题干（Markdown + LaTeX）
    options: Optional[List[Dict[str, str]]]  # 选项 [{"label":"A","content":"..."}]
    correct_answer: Any         # 标准答案（加密存储）
    explanation: str            # 解析（Markdown + LaTeX）
    difficulty: int             # 1-5
    knowledge_points: List[Dict[str, Any]]  # [{kp_id, kp_name, chapter_path}]
    hint_tiers: List[str]       # 提示层级（渐进提示）
    solution_steps: List[str]   # 解题步骤
    estimated_time_seconds: int # 建议用时
    media: Optional[List[Dict]] # 图片、音频等媒体资源

@dataclass
class UserAnswer:
    """用户答案结构"""
    question_type: str
    # 选择题
    selected_options: Optional[List[str]] = None     # ["A", "C"]
    # 填空题
    fill_blanks: Optional[List[str]] = None          # ["答案1", "答案2"]
    # 主观题
    text_content: Optional[str] = None               # 文本答案
    # 图片答案
    image_ids: Optional[List[str]] = None            # 上传的图片ID
    # 语音答案
    voice_id: Optional[str] = None                   # 语音文件ID
    # 通用
    is_confirmed: bool = False                       # 是否已确认提交
    confidence_level: Optional[int] = None           # 自信度 1-5

@dataclass
class GradingResult:
    """批改结果"""
    is_correct: Optional[bool]        # None=需人工批改
    auto_score: Optional[float]       # 自动评分
    step_scores: Optional[List[float]] # 分步得分
    error_analysis: Optional[str]     # 错误分析
    knowledge_gaps: List[str]         # 暴露的知识点缺口
    ai_feedback: Optional[str]        # AI 个性化反馈
    reference_answer: Optional[str]   # 参考答案展示
```

---

## 3. 会话状态机设计

### 3.1 会话级别状态机

```
                                    ┌──────────┐
                                    │ ABANDONED│ ← 用户主动放弃
                                    └──────────┘
                                          ▲
    ┌─────────┐    startSession    ┌───────┴──┐    submitAll / timeout    ┌───────────┐
    │ CREATED │ ───────────────▶  │  ACTIVE  │ ────────────────────────▶ │ COMPLETED │
    └─────────┘                    └─────┬────┘                           └───────────┘
                                         │                                      │
                                         │ pauseSession                         │
                                         ▼                                      │
                                   ┌──────────┐                                │
                                   │  PAUSED  │ ────── resumeSession ─────────▶│
                                   └──────────┘                                │
                                        │                                      │
                                        │ 超过24h未恢复                          │
                                        ▼                                      │
                                   ┌──────────┐                                │
                                   │ EXPIRED  │ ───────────────────────────────│
                                   └──────────┘                                │
                                                                               │
                                    ┌──────────┐                               │
                                    │ ARCHIVED │ ◀────── 归档清理 ──────────────┘
                                    └──────────┘
```

### 3.2 状态流转规则

```python
# 状态转换定义
SESSION_TRANSITIONS = {
    # (current_status, action) -> (new_status, preconditions, side_effects)
    
    (SessionStatus.CREATED, "start"): {
        "target": SessionStatus.ACTIVE,
        "preconditions": [
            "session not expired (created_at + 48h > now)",
            "user has active quota",
        ],
        "side_effects": [
            "set started_at = now()",
            "set last_active_at = now()",
            "set server_start_ts in Redis",
            "add to active timers sorted set",
            "set all session_questions[0] answer_status = VIEWED",
            "emit SessionStartedEvent",
        ],
    },
    
    (SessionStatus.ACTIVE, "pause"): {
        "target": SessionStatus.PAUSED,
        "preconditions": [
            "session_type != MOCK_EXAM",  # 模拟考试不允许暂停
        ],
        "side_effects": [
            "set paused_at = now()",
            "record pause timestamp in Redis",
            "remove from active timers sorted set",
            "flush pending answer cache to DB",
            "emit SessionPausedEvent",
        ],
    },
    
    (SessionStatus.PAUSED, "resume"): {
        "target": SessionStatus.ACTIVE,
        "preconditions": [
            "paused_at + 24h > now()",  # 暂停超过24h则过期
        ],
        "side_effects": [
            "accumulate paused duration",
            "set paused_at = NULL",
            "re-add to active timers sorted set (adjust deadline)",
            "emit SessionResumedEvent",
        ],
    },
    
    (SessionStatus.ACTIVE, "complete"): {
        "target": SessionStatus.COMPLETED,
        "preconditions": [
            "all questions answered OR user explicitly submits all",
        ],
        "side_effects": [
            "set completed_at = now()",
            "calculate final scores",
            "remove from active timers sorted set",
            "flush all answer cache to DB",
            "trigger async: grading pipeline",
            "trigger async: knowledge mastery update",
            "trigger async: mistake detection",
            "trigger async: learning record creation",
            "trigger async: session summary report generation",
            "emit SessionCompletedEvent",
        ],
    },
    
    (SessionStatus.ACTIVE, "timeout"): {
        "target": SessionStatus.EXPIRED,
        "preconditions": [
            "elapsed_seconds >= time_limit_seconds",
        ],
        "side_effects": [
            "auto-submit all unanswered questions",
            "set expired_at = now()",
            "same as complete side_effects",
            "emit SessionExpiredEvent",
        ],
    },
    
    # 任何非终态 → 放弃
    (SessionStatus.CREATED, "abandon"): {"target": SessionStatus.ABANDONED},
    (SessionStatus.ACTIVE, "abandon"): {"target": SessionStatus.ABANDONED},
    (SessionStatus.PAUSED, "abandon"): {"target": SessionStatus.ABANDONED},
}

def validate_transition(
    current: SessionStatus,
    action: str,
    session: PracticeSession,
) -> tuple[bool, Optional[str]]:
    """验证状态转换是否合法"""
    key = (current, action)
    if key not in SESSION_TRANSITIONS:
        return False, f"不允许的操作：在 {current.name} 状态下执行 {action}"
    
    transition = SESSION_TRANSITIONS[key]
    
    # 检查前置条件
    for precondition in transition.get("preconditions", []):
        result = evaluate_precondition(precondition, session)
        if not result.ok:
            return False, result.message
    
    return True, None
```

### 3.3 单题级别状态机

```
┌─────────────┐    viewQuestion    ┌──────────┐   startAnswering   ┌───────────┐
│ NOT_VIEWED  │ ────────────────▶ │  VIEWED  │ ────────────────▶  │ ANSWERING │
└─────────────┘                    └──────────┘                    └─────┬─────┘
                                                                        │
                                        ┌───────────────────────────────┤
                                        │                               │
                                        │ confirmAnswer                 │ skipQuestion
                                        ▼                               │
                                  ┌───────────┐                         │
                                  │  ANSWERED  │ ◀───────────────────────┘
                                  └─────┬─────┘
                                        │
                              ┌─────────┼──────────┐
                              │                    │
                              ▼                    ▼
                    ┌─────────────────┐  ┌─────────────────┐
                    │ MARKED_CORRECT  │  │  MARKED_WRONG   │
                    └─────────────────┘  └─────────────────┘
                              │                    │
                              └─────────┬──────────┘
                                        │ (optional)
                                        ▼
                                  ┌───────────┐
                                  │  REVIEWED  │ (用户标记已检查)
                                  └───────────┘
```

---

## 4. API 接口设计

### 4.1 创建练习会话

```
POST /api/v1/practice/sessions
Authorization: Bearer <jwt>
X-Request-Id: <trace_id>
```

**请求体**：

```typescript
interface CreatePracticeSessionRequest {
  /** 会话类型 */
  session_type: 'sync_practice' | 'topic_drill' | 'mock_exam' 
              | 'mistake_review' | 'smart_recommend' | 'daily_task';
  
  /** 学科 */
  subject_code: string;
  
  /** 创建来源上下文 */
  context: {
    /** 来源入口 */
    source_entry: string;
    /** 章节ID（同步练习时必填） */
    chapter_id?: string;
    /** 专题ID（专项训练时必填） */
    topic_id?: string;
    /** 模拟试卷ID（模拟考试时必填） */
    mock_exam_id?: string;
    /** 学习计划ID（每日任务时必填） */
    plan_id?: string;
    /** 错题筛选条件（错题重练时） */
    mistake_filter?: {
      knowledge_point_ids?: string[];
      error_types?: string[];
      date_range?: { start: string; end: string };
      count?: number;  // 默认10
    };
  };
  
  /** 自定义配置 */
  config?: {
    /** 题目数量 */
    question_count?: number;  // 默认根据类型决定
    /** 难度范围 */
    difficulty_range?: [number, number];  // [1, 5]
    /** 题型过滤 */
    question_types?: string[];
    /** 是否限时 */
    time_limit_minutes?: number;
    /** 是否允许跳题 */
    allow_skip?: boolean;  // 默认true，模拟考试可能为false
    /** 是否允许返回修改 */
    allow_go_back?: boolean;  // 默认true
    /** 是否顺序答题 */
    sequential_only?: boolean;  // 默认false
    /** 答案展示时机 */
    show_answer_mode: 'immediate' | 'after_each' | 'after_all';  // 立即/每题后/全部交卷后
  };
}