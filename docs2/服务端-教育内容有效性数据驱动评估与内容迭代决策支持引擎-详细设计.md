# 服务端-教育内容有效性数据驱动评估与内容迭代决策支持引擎-详细设计

## 1. 模块概述

### 1.1 功能定位

教育内容有效性数据驱动评估引擎（Content Effectiveness Evaluation & Decision Engine，简称 CEE）是 PrimeTop 内容运营体系的核心分析中枢。与已有的「教育内容质量自动评估」模块不同——后者关注 **内容本身的静态质量**（格式是否规范、知识点是否准确、语言是否通顺），本引擎聚焦于 **内容投放后的实际效果**：

- 学生看完这个知识点讲解后，相关题目正确率提升了多少？
- 这套微课视频的完播率是多少？在哪个时间点集中流失？
- 哪些内容的消费显著推动了会员转化？
- 哪些内容长期无人问津，应该下线或重制？

本引擎从 **内容视角**（而非用户视角）出发，建立"内容曝光 → 用户消费 → 学习行为变化 → 业务价值产出"的全链路评估闭环，为内容团队的生产排期、迭代优化、上下架决策提供量化依据。

### 1.2 与已有模块的边界

| 已有模块 | 关注点 | 本模块差异 |
| --- | --- | --- |
| 教育内容质量自动评估与低质内容智能检测 | AI 对内容本身进行格式/准确性/适龄性评分 | 本模块基于 **真实用户消费数据** 评估，而非内容本身的静态属性 |
| 学习效果量化追踪与教育价值归因分析 | 追踪 **用户** 的学习效果和归因 | 本模块追踪 **内容** 的效果，回答"这个内容对多少学生有效" |
| 练习答题行为数据采集与学习过程分析引擎 | 采集和分析 **答题行为** | 本模块关联内容消费到答题效果变化 |
| 管理后台数据分析看板与运营驾驶舱服务 | 全局运营数据可视化 | 本模块提供 **内容维度的深度分析引擎**，为看板提供数据源 |
| 教学内容用户纠错反馈与内容质量持续迭代引擎 | 基于 **用户纠错反馈** 迭代内容 | 本模块基于 **全量消费行为数据** 评估，覆盖无反馈提交的沉默大多数 |

### 1.3 核心能力总览

```
┌─────────────────────────────────────────────────────────────┐
│                  内容有效性评估引擎 (CEE)                      │
├─────────────┬──────────────┬──────────────┬─────────────────┤
│  数据采集层  │  效果分析层    │  决策支持层    │   服务输出层     │
├─────────────┼──────────────┼──────────────┼─────────────────┤
│ 消费事件追踪 │ 内容吸引力评分│ 内容ROI分析   │ RESTful API     │
│ 学习增量度量 │ 内容理解度评分│ 内容缺口检测  │ GraphQL 查询    │
│ 业务归因标记 │ 内容留存评分  │ 上下架建议    │ Webhook 回调    │
│ 外部数据接入 │ 内容转化评分  │ 生产排期建议  │ 批量报告导出     │
└─────────────┴──────────────┴──────────────┴─────────────────┘
```

---

## 2. 核心数据模型

### 2.1 内容消费事件表 `content_consumption_events`

记录每次用户与内容的交互行为，是所有效果分析的基础数据。

```sql
CREATE TABLE content_consumption_events (
    id              BIGINT       PRIMARY KEY AUTO_INCREMENT,
    event_id        VARCHAR(64)  NOT NULL COMMENT '事件唯一ID（防重）',
    user_id         BIGINT       NOT NULL COMMENT '用户ID',
    content_id      BIGINT       NOT NULL COMMENT '内容ID',
    content_type    VARCHAR(32)  NOT NULL COMMENT '内容类型: LESSON/EXERCISE/EXPLANATION/VIDEO/AUDIO/FLASHCARD/MICRO_COURSE',
    subject_code    VARCHAR(16)  NOT NULL COMMENT '学科编码',
    chapter_id      BIGINT       NULL     COMMENT '章节ID',
    knowledge_point_ids JSON     NULL     COMMENT '关联知识点ID列表',
    
    -- 消费行为字段
    event_type      VARCHAR(32)  NOT NULL COMMENT 'VIEW/START/PROGRESS/COMPLETE/ABANDON/REPLAY/BOOKMARK/SHARE',
    progress_pct    DECIMAL(5,2) NULL     COMMENT '消费进度百分比 0-100',
    duration_ms     INT          NULL     COMMENT '本次消费停留时长(毫秒)',
    total_duration_ms INT        NULL     COMMENT '该内容累计消费时长(毫秒)',
    replay_count    INT          DEFAULT 0 COMMENT '重播次数',
    abandon_point   VARCHAR(64)  NULL     COMMENT '放弃位置标记(如视频时间点/章节段落号)',
    
    -- 上下文字段
    source          VARCHAR(32)  NULL     COMMENT '来源: RECOMMEND/SEARCH/CHAPTER/ERROR_REVIEW/PLAN/AI_SUGGEST',
    session_id      VARCHAR(64)  NULL     COMMENT '学习会话ID',
    device_type     VARCHAR(16)  NULL     COMMENT '设备类型',
    app_version     VARCHAR(32)  NULL     COMMENT 'APP版本',
    
    -- 效果标记字段（异步回填）
    has_assessment  TINYINT      DEFAULT 0 COMMENT '是否有后续测评 0/1',
    assessment_score_delta DECIMAL(5,2) NULL COMMENT '消费前后测评得分变化',
    assessment_time_delta INT   NULL     COMMENT '消费后到测评的时间间隔(秒)',
    
    created_at      DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    
    UNIQUE KEY uk_event_id (event_id),
    KEY idx_user_content (user_id, content_id),
    KEY idx_content_type_date (content_id, content_type, created_at),
    KEY idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='内容消费事件表';
```

**分表策略**：按月水平分表 `content_consumption_events_YYYYMM`，保留 13 个月热数据，超期数据归档到 ClickHouse。

### 2.2 内容效果指标聚合表 `content_effectiveness_metrics`

按内容维度聚合的效果指标，由定时任务每日凌晨 T+1 计算。

```sql
CREATE TABLE content_effectiveness_metrics (
    id                  BIGINT       PRIMARY KEY AUTO_INCREMENT,
    content_id          BIGINT       NOT NULL COMMENT '内容ID',
    content_type        VARCHAR(32)  NOT NULL COMMENT '内容类型',
    stat_date           DATE         NOT NULL COMMENT '统计日期',
    
    -- 曝光与消费指标
    total_impressions   INT          DEFAULT 0 COMMENT '总曝光次数',
    unique_exposed_users INT         DEFAULT 0 COMMENT '去重曝光用户数',
    total_consumptions  INT          DEFAULT 0 COMMENT '总消费启动次数',
    unique_consumers    INT          DEFAULT 0 COMMENT '去重消费用户数',
    avg_duration_ms     INT          DEFAULT 0 COMMENT '平均消费时长(毫秒)',
    median_duration_ms  INT          DEFAULT 0 COMMENT '中位数消费时长',
    avg_progress_pct    DECIMAL(5,2) DEFAULT 0 COMMENT '平均消费完成进度%',
    completion_rate     DECIMAL(5,4) DEFAULT 0 COMMENT '完成率: 完成次数/启动次数',
    abandonment_rate    DECIMAL(5,4) DEFAULT 0 COMMENT '放弃率',
    avg_replay_count    DECIMAL(3,1) DEFAULT 0 COMMENT '平均重播次数',
    
    -- 学习效果指标
    avg_score_delta     DECIMAL(5,2) NULL COMMENT '平均测评得分提升',
    positive_delta_ratio DECIMAL(5,4) DEFAULT 0 COMMENT '正向提升比例(得分提升>5分的人数占比)',
    avg_knowledge_mastery_delta DECIMAL(5,4) DEFAULT 0 COMMENT '知识点掌握度平均提升',
    assessment_sample_size INT      DEFAULT 0 COMMENT '测评样本量',
    
    -- 业务价值指标
    membership_convert_count INT    DEFAULT 0 COMMENT '消费后7日内会员转化人数',
    membership_convert_rate DECIMAL(5,4) DEFAULT 0 COMMENT '会员转化率',
    share_count         INT          DEFAULT 0 COMMENT '分享次数',
    favorite_count      INT          DEFAULT 0 COMMENT '收藏次数',
    feedback_count      INT          DEFAULT 0 COMMENT '反馈(纠错/点赞)次数',
    positive_feedback_ratio DECIMAL(5,4) DEFAULT 0 COMMENT '正面反馈比例',
    
    -- 综合评分
    attractiveness_score DECIMAL(5,2) DEFAULT 0 COMMENT '吸引力评分 0-100',
    comprehension_score DECIMAL(5,2) DEFAULT 0 COMMENT '理解促进评分 0-100',
    retention_score     DECIMAL(5,2) DEFAULT 0 COMMENT '记忆留存评分 0-100',
    conversion_score    DECIMAL(5,2) DEFAULT 0 COMMENT '转化贡献评分 0-100',
    overall_cee_score   DECIMAL(5,2) DEFAULT 0 COMMENT '综合有效性评分 0-100',
    performance_trend   VARCHAR(8)   DEFAULT 'STABLE' COMMENT 'UP/DOWN/STABLE/NEW',
    
    -- 元数据
    calculation_version INT         DEFAULT 1 COMMENT '计算版本号(算法迭代用)',
    data_quality_flag   VARCHAR(16) DEFAULT 'NORMAL' COMMENT 'NORMAL/LOW_SAMPLE/STALE/ANOMALY',
    created_at          DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at          DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    
    UNIQUE KEY uk_content_date (content_id, stat_date),
    KEY idx_date_score (stat_date, overall_cee_score DESC),
    KEY idx_type_date (content_type, stat_date),
    KEY idx_trend (performance_trend, stat_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='内容效果指标聚合表';
```

### 2.3 内容A/B实验表 `content_ab_experiments`

```sql
CREATE TABLE content_ab_experiments (
    id                BIGINT       PRIMARY KEY AUTO_INCREMENT,
    experiment_name   VARCHAR(128) NOT NULL COMMENT '实验名称',
    experiment_code   VARCHAR(64)  NOT NULL COMMENT '实验唯一编码',
    content_id        BIGINT       NOT NULL COMMENT '实验内容ID',
    variant_type      VARCHAR(16)  NOT NULL COMMENT 'CONTROL/VARIANT_A/VARIANT_B',
    variant_content_snapshot JSON  NULL     COMMENT '变体内容快照(用于回溯)',
    
    hypothesis        TEXT         NULL     COMMENT '实验假设',
    success_metrics   JSON         NOT NULL COMMENT '成功指标配置 [{"metric":"completion_rate","target":0.65,"direction":"higher"}]',
    segmentation_rules JSON        NULL     COMMENT '分流规则(学段/年级等)',
    
    status            VARCHAR(16)  DEFAULT 'DRAFT' COMMENT 'DRAFT/RUNNING/PAUSED/COMPLETED/CANCELLED',
    traffic_pct       DECIMAL(5,2) DEFAULT 0 COMMENT '流量占比%',
    start_time        DATETIME     NULL,
    end_time          DATETIME     NULL,
    min_sample_size   INT          DEFAULT 1000 COMMENT '最小样本量',
    significance_level DECIMAL(4,3) DEFAULT 0.05 COMMENT '显著性水平',
    
    result_summary    JSON         NULL COMMENT '实验结果摘要(计算后回填)',
    winning_variant   VARCHAR(16)  NULL COMMENT '获胜变体',
    is_significant    TINYINT      DEFAULT 0 COMMENT '结果是否显著',
    
    created_by        BIGINT       NOT NULL COMMENT '创建人',
    created_at        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    
    UNIQUE KEY uk_experiment_code (experiment_code),
    KEY idx_content_status (content_id, status),
    KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='内容A/B实验表';
```

### 2.4 内容缺口分析表 `content_gap_analysis`

```sql
CREATE TABLE content_gap_analysis (
    id                  BIGINT       PRIMARY KEY AUTO_INCREMENT,
    analysis_date       DATE         NOT NULL,
    knowledge_point_id  BIGINT       NULL     COMMENT '知识点ID',
    chapter_id          BIGINT       NULL     COMMENT '章节ID',
    subject_code        VARCHAR(16)  NOT NULL COMMENT '学科',
    grade_code          VARCHAR(16)  NOT NULL COMMENT '年级',
    
    -- 需求指标
    search_volume       INT          DEFAULT 0 COMMENT '相关搜索量(近30天)',
    search_unique_users INT          DEFAULT 0 COMMENT '搜索去重用户数',
    zero_result_rate    DECIMAL(5,4) DEFAULT 0 COMMENT '零结果率(搜索无匹配内容)',
    question_volume     INT          DEFAULT 0 COMMENT 'AI问答中相关提问量',
    
    -- 供给指标
    existing_content_count INT       DEFAULT 0 COMMENT '现有内容数量',
    avg_cee_score       DECIMAL(5,2) DEFAULT 0 COMMENT '现有内容平均有效性评分',
    high_quality_ratio  DECIMAL(5,4) DEFAULT 0 COMMENT '高质量内容占比(CEE>70)',
    
    -- 缺口判定
    gap_level           VARCHAR(16)  DEFAULT 'NONE' COMMENT 'NONE/LOW/MEDIUM/HIGH/CRITICAL',
    gap_type            VARCHAR(32)  NULL     COMMENT 'QUANTITY_GAP/QUALITY_GAP/COVERAGE_GAP/FRESHNESS_GAP',
    recommended_action  VARCHAR(32)  NULL     COMMENT 'CREATE_NEW/IMPROVE_EXISTING/URGENT_FILL',
    estimated_priority  INT          DEFAULT 0 COMMENT '建议优先级 1-10',
    
    -- 产出建议
    suggested_content_types JSON     NULL     COMMENT '建议内容类型 ["VIDEO","EXERCISE","EXPLANATION"]',
    suggested_difficulty VARCHAR(16) NULL     COMMENT '建议难度: EASY/MEDIUM/HARD',
    reference_content_ids JSON      NULL     COMMENT '可参考的高效内容ID',
    
    created_at          DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    
    UNIQUE KEY uk_date_kp_grade (analysis_date, knowledge_point_id, grade_code),
    KEY idx_gap_level (gap_level, estimated_priority DESC),
    KEY idx_subject_grade (subject_code, grade_code, gap_level)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='内容缺口分析表';
```

### 2.5 内容生命周期决策记录表 `content_lifecycle_decisions`

```sql
CREATE TABLE content_lifecycle_decisions (
    id                BIGINT       PRIMARY KEY AUTO_INCREMENT,
    content_id        BIGINT       NOT NULL,
    decision_type     VARCHAR(16)  NOT NULL COMMENT 'PROMOTE/REVISE/DEPRECATE/REPRODUCE/ARCHIVE',
    decision_reason   TEXT         NOT NULL COMMENT '决策依据说明',
    
    trigger_metrics   JSON         NULL     COMMENT '触发指标快照 {"cee_score":42,"completion_rate":0.23,...}',
    trigger_rule_id   VARCHAR(64)  NULL     COMMENT '触发的自动规则ID',
    is_auto_triggered TINYINT      DEFAULT 0 COMMENT '是否自动触发',
    
    assigned_to       BIGINT       NULL     COMMENT '指派处理人',
    status            VARCHAR(16)  DEFAULT 'PENDING' COMMENT 'PENDING/IN_PROGRESS/DONE/REJECTED',
    due_date          DATE         NULL,
    resolved_at       DATETIME     NULL,
    resolution_notes  TEXT         NULL,
    
    created_at        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    
    KEY idx_content_status (content_id, status),
    KEY idx_status_type (status, decision_type),
    KEY idx_assigned (assigned_to, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='内容生命周期决策记录';
```

---

## 3. 效果评估指标体系

### 3.1 指标分层架构

```
                    ┌─────────────────┐
                    │  综合有效性评分  │  overall_cee_score (0-100)
                    │   一级指标       │
                    └────────┬────────┘
           ┌───────────────┼───────────────┐
     ┌─────┴─────┐   ┌─────┴─────┐   ┌─────┴─────┐   ┌──────────┐
     │ 吸引力评分 │   │ 理解促进  │   │ 记忆留存  │   │ 转化贡献  │
     │  二级指标  │   │  二级指标  │   │  二级指标  │   │  二级指标  │
     └─────┬─────┘   └─────┬─────┘   └─────┬─────┘   └─────┬────┘
           │                │                │                │
      三级原子指标      三级原子指标      三级原子指标      三级原子指标
```

### 3.2 详细指标定义

#### 3.2.1 吸引力评分（Attractiveness Score）

衡量内容吸引用户消费的能力。

| 三级指标 | 计算方式 | 权重 |
| --- | --- | --- |
| 消费转化率 | 消费启动数 / 曝光数 | 0.30 |
| 完播/完成率 | 完成消费数 / 消费启动数 | 0.25 |
| 均消费时长比 | 实际平均时长 / 预期消费时长 | 0.20 |
| 重播率 | 有重播行为用户数 / 消费用户数 | 0.10 |
| 分享率 | 分享次数 / 消费用户数 | 0.10 |
| 收藏率 | 收藏次数 / 消费用户数 | 0.05 |

**计算公式**：

```text
attractiveness_score = (
    normalize(consumption_conversion_rate) * 0.30 +
    normalize(completion_rate) * 0.25 +
    normalize(duration_ratio) * 0.20 +
    normalize(replay_rate) * 0.10 +
    normalize(share_rate) * 0.10 +
    normalize(favorite_rate) * 0.05
) * 100
```

`normalize()` 函数对每个指标做 Min-Max 归一化到 [0, 1]，参考基线由同类内容（同类型+同学科+同年级）的 P75 分位数确定。

#### 3.2.2 理解促进评分（Comprehension Score）

衡量内容对学生理解知识点的帮助程度。

| 三级指标 | 计算方式 | 权重 |
| --- | --- | --- |
| 平均测评增量 | 消费后测评得分 - 消费前测评得分 | 0.35 |
| 正向提升比例 | 得分提升>5分人数 / 总测评人数 | 0.25 |
| 知识点掌握度增量 | 消费后掌握度 - 消费前掌握度 | 0.25 |
| 同类题正确率变化 | 消费后同类题正确率 - 消费前 | 0.15 |

**前置条件**：需要有足够的样本量（≥30 人同时有消费前和消费后测评数据）。

#### 3.2.3 记忆留存评分（Retention Score）

衡量内容帮助学生长期记忆的效果。

| 三级指标 | 计算方式 | 权重 |
| --- | --- | --- |
| 7日复访率 | 7日内重新消费/复习该内容的用户比例 | 0.30 |
| 14日知识保持率 | 14日后同类题正确率 / 首次正确率 | 0.35 |
| 错题重做通过率 | 关联错题在该内容消费后重做通过的比例 | 0.20 |
| 间隔复习完成率 | 消费该内容后触发的复习任务完成率 | 0.15 |

#### 3.2.4 转化贡献评分（Conversion Score）

衡量内容对业务价值的贡献。

| 三级指标 | 计算方式 | 权重 |
| --- | --- | --- |
| 会员转化率 | 消费后7日会员转化人数 / 消费人数 | 0.35 |
| 正面反馈比 | (点赞数 - 差评数) / 总反馈数 | 0.25 |
| 推荐触发率 | 被用户主动推荐/分享给其他用户的次数 | 0.20 |
| 续费相关消费 | 续费用户中消费过该内容的比例 | 0.20 |

### 3.3 综合有效性评分

```text
overall_cee_score = 
    attractiveness_score * 0.20 +
    comprehension_score * 0.35 +
    retention_score     * 0.25 +
    conversion_score    * 0.20
```

> 理解促进评分权重最高（0.35），因为教育产品的核心价值在于学习效果。

### 3.4 评分等级与建议

| CEE Score | 等级 | 含义 | 建议动作 |
| --- | --- | --- | --- |
| 85-100 | S | 卓越效果 | 标杆内容，推荐为参考模板，加大分发 |
| 70-84 | A | 优秀效果 | 继续保持，可考虑作为推荐素材 |
| 55-69 | B | 中等效果 | 局部优化（提升完播率/补充练习等） |
| 40-54 | C | 偏弱效果 | 需要修订，分析薄弱维度并改进 |
| 0-39 | D | 低效内容 | 建议重制或下线 |

---

## 4. 核心流程设计

### 4.1 消费事件采集流程

```
客户端                           服务端                        数据层
  │                                │                             │
  │  1. 用户消费内容               │                             │
  │  (观看视频/阅读讲解/做练习)     │                             │
  │                                │                             │
  │  2. 上报消费事件               │                             │
  │  POST /api/v1/cee/track       │                             │
  │ ──────────────────────────────>│                             │
  │                                │  3. 校验+写入消费事件表      │
  │                                │ ───────────────────────────>│
  │                                │                             │
  │                                │  4. 投递Kafka消息             │
  │                                │ ───────────────────────────>│
  │                                │                             │
  │  5. 返回成功                   │                             │
  │ <──────────────────────────────│                             │
  │                                │                             │
  │  6. 后续答题/测评行为           │                             │
  │  POST /api/v1/cee/track       │                             │
  │ ──────────────────────────────>│                             │
  │                                │  7. 关联内容消费+回填效果标记 │
  │                                │ ───────────────────────────>│
```

**关键设计要点**：

1. **事件批量上报**：客户端每 30 秒或离开内容页面时批量上报消费事件，减少请求频率
2. **幂等保障**：`event_id` 由客户端生成（`userId + contentId + timestamp + random`），服务端去重
3. **弱网兜底**：客户端本地 SQLite 缓存，网络恢复后补报
4. **效果回填**：测评得分变化通过异步任务关联回填，不阻塞主链路

### 4.2 效果指标计算流程（T+1 离线批处理）

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  Step 1      │    │  Step 2      │    │  Step 3      │    │  Step 4      │
│  数据准备     │───>│  指标计算     │───>│  评分合成     │───>│  决策触发     │
└──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
 │                    │                    │                    │
 │ 从ClickHouse       │ 按内容ID聚合        │ 计算四维评分        │ 检查规则阈值
 │ 拉取昨日全量       │ 原始指标            │ 加权合成综合分      │ 触发生命周期
 │ 消费+测评数据      │                    │                    │ 决策记录
```

#### Step 1: 数据准备（每日 01:00 触发）

```java
/**
 * 每日效果数据准备任务
 * 从消费事件表和测评记录表中提取原始数据，写入临时计算表
 */
@Scheduled(cron = "0 0 1 * * ?")
public void prepareDailyEffectivenessData() {
    LocalDate statDate = LocalDate.now().minusDays(1);
    
    // 1. 提取昨日活跃内容列表（有消费事件的内容）
    List<Long> activeContentIds = consumptionEventMapper
        .findActiveContentIds(statDate);
    
    // 2. 对每个内容，拉取关联数据
    for (Long contentId : activeContentIds) {
        ContentEffectRawData raw = ContentEffectRawData.builder()
            .consumptionStats(fetchConsumptionStats(contentId, statDate))
            .assessmentPairs(fetchAssessmentPairs(contentId, statDate))  // 消费前后的测评配对
            .behaviorStats(fetchBehaviorStats(contentId, statDate))     // 分享/收藏/反馈
            .conversionStats(fetchConversionStats(contentId, statDate)) // 会员转化
            .build();
        
        // 写入临时表
        effectRawDataMapper.insert(raw);
    }
    
    log.info("数据准备完成: date={}, activeContents={}", statDate, activeContentIds.size());
}
```

#### Step 2: 指标聚合计算（每日 02:00 触发）

```java
@Scheduled(cron = "0 0 2 * * ?")
public void calculateEffectivenessMetrics() {
    LocalDate statDate = LocalDate.now().minusDays(1);
    
    List<ContentEffectRawData> rawDataList = effectRawDataMapper.findByDate(statDate);
    
    List<ContentEffectivenessMetrics> results = new ArrayList<>();
    
    for (ContentEffectRawData raw : rawDataList) {
        ContentEffectivenessMetrics metrics = new ContentEffectivenessMetrics();
        metrics.setContentId(raw.getContentId());
        metrics.setContentType(raw.getContentType());
        metrics.setStatDate(statDate);
        
        // ---- 曝光与消费指标 ----
        ConsumptionStats cs = raw.getConsumptionStats();
        metrics.setTotalImpressions(cs.getTotalImpressions());
        metrics.setUniqueExposedUsers(cs.getUniqueExposedUsers());
        metrics.setTotalConsumptions(cs.getTotalConsumptions());
        metrics.setUniqueConsumers(cs.getUniqueConsumers());
        metrics.setAvgDurationMs(cs.getAvgDurationMs());
        metrics.setMedianDurationMs(cs.getMedianDurationMs());
        metrics.setAvgProgressPct(cs.getAvgProgressPct());
        metrics.setCompletionRate(cs.getCompletionRate());
        metrics.setAbandonmentRate(1.0 - cs.getCompletionRate());
        metrics.setAvgReplayCount(cs.getAvgReplayCount());
        
        // ---- 学习效果指标 ----
        List<AssessmentPair> pairs = raw.getAssessmentPairs();
        if (pairs.size() >= MIN_ASSESSMENT_SAMPLE) { // 最小样本量 30
            double avgDelta = pairs.stream()
                .mapToDouble(AssessmentPair::getScoreDelta)
                .average().orElse(0);
            double positiveRatio = pairs.stream()
                .filter(p -> p.getScoreDelta() > 5.0)
                .count() / (double) pairs.size();
            double avgMasteryDelta = pairs.stream()
                .mapToDouble(AssessmentPair::getKnowledgeMasteryDelta)
                .average().orElse(0);
            
            metrics.setAvgScoreDelta(BigDecimal.valueOf(avgDelta));
            metrics.setPositiveDeltaRatio(BigDecimal.valueOf(positiveRatio));
            metrics.setAvgKnowledgeMasteryDelta(BigDecimal.valueOf(avgMasteryDelta));
            metrics.setAssessmentSampleSize(pairs.size());
        } else {
            metrics.setDataQualityFlag("LOW_SAMPLE");
        }
        
        // ---- 业务价值指标 ----
        BehaviorStats bs = raw.getBehaviorStats();
        metrics.setShareCount(bs.getShareCount());
        metrics.setFavoriteCount(bs.getFavoriteCount());
        metrics.setFeedbackCount(bs.getFeedbackCount());
        metrics.setPositiveFeedbackRatio(bs.getPositiveFeedbackRatio());
        
        ConversionStats conv = raw.getConversionStats();
        metrics.setMembershipConvertCount(conv.getMembershipConvertCount());
        metrics.setMembershipConvertRate(conv.getMembershipConvertRate());
        
        // ---- 标记数据质量 ----
        if (metrics.getUniqueConsumers() < 50) {
            metrics.setDataQualityFlag("LOW_SAMPLE");
        } else if (isDataAnomal