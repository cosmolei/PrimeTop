# 客户端-统一图片加载缓存框架与智能预加载引擎 - 详细设计

> **模块版本**：v1.0 | **最后更新**：2026-06-30
> **原始需求来源**：`docs/design/启硕-PrimeTop-全学段AI辅助学习软件项目设计文档.md` §8.3, §9.1, §13.1
> **依赖文档**：
> - `客户端架构与前端框架-详细设计.md`（Flutter 框架基础）
> - `客户端网络请求治理与弱网适配方案-详细设计.md`（网络层）
> - `客户端本地持久层与数据管理-详细设计.md`（本地存储）
> - `服务端-CDN内容分发网络与多区域边缘缓存策略-详细设计.md`（CDN 对接）
> - `客户端资源包下载管理器-详细设计.md`（离线资源包）
> - `客户端性能监控与用户体验度量系统-详细设计.md`（性能埋点）

---

## 1. 概述

### 1.1 模块定位

统一图片加载缓存框架（PrimeImageLoader）是 PrimeTop Flutter 客户端的**核心视觉基础设施**，为 AI 对话配图、教材插图、题目图形、知识卡片、用户头像、运营 Banner、学情报告图表等全场景提供统一的图片加载、解码、缓存、预加载和展示能力。

**核心职责：**
- 多级缓存（内存 → 磁盘 → CDN → 源站）的透明管理
- 智能预加载：基于学习内容序列提前加载后续图片
- 自适应质量控制：根据网络环境和设备性能动态调整
- 内存安全：在低端设备上自动降级，防止 OOM
- 与富文本渲染引擎、学科内容组件无缝集成

**不在本模块范围内：**
- 图片拍摄与压缩 → 见 `客户端多媒体选取与处理服务-详细设计.md`
- OCR 识别 → 见 `图片处理与OCR识别管线-详细设计.md`
- 富文本中的图文混排 → 见 `富文本与学科内容渲染引擎-详细设计.md`
- CDN 分发架构（服务端） → 见 `服务端-CDN内容分发网络与多区域边缘缓存策略-详细设计.md`

### 1.2 设计目标

| 目标 | 指标 | 说明 |
|------|------|------|
| 缓存命中率 | ≥ 85% | 二次访问同图片命中缓存 |
| 图片加载时间（WiFi） | < 300ms | 从请求到首帧渲染 |
| 图片加载时间（弱网） | < 2s | 3G/边缘网络下 |
| 内存占用 | < 80MB | 图片缓存峰值，低端设备（≤4GB RAM） |
| 磁盘缓存 | ≤ 500MB | 默认上限，用户可配置 |
| OOM 崩溃率 | < 0.01% | 因图片加载导致的崩溃 |
| 预加载命中率 | ≥ 70% | 预加载的图片在 5min 内被使用 |

### 1.3 与其他模块的关系

```text
┌──────────────────────────────────────────────────────────────┐
│                    业务层（图片消费方）                        │
│  AI对话 │ 同步课堂 │ 拍题答疑 │ 学情报告 │ 运营Banner │ 头像 │
└──────────────────┬───────────────────────────────────────────┘
                   │ PrimeImage.network() / PrimeImage.asset()
                   ▼
┌──────────────────────────────────────────────────────────────┐
│              PrimeImageLoader（本模块）                        │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐ │
│  │内存缓存  │  │ 磁盘缓存  │  │网络请求层 │  │ 智能预加载器  │ │
│  │(LRU+弱引用)│  │(LRU+TTL) │  │(CDN+重试)│  │(序列预测)    │ │
│  └─────────┘  └──────────┘  └──────────┘  └──────────────┘ │
│  ┌──────────────────────────────────────────────────────────┐│
│  │              图片解码管线（Decoder Pipeline）              ││
│  │  WebP → PNG → JPEG → GIF → SVG → BMP → HEIF(仅iOS)       ││
│  └──────────────────────────────────────────────────────────┘│
│  ┌──────────────────┐  ┌──────────────────────────────────┐  │
│  │  自适应质量控制   │  │    内存安全监控器（Memory Guard）   │  │
│  │  (网络+设备感知)  │  │    OOM 预警 + 自动缩容            │  │
│  └──────────────────┘  └──────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
         │                    │                      │
         ▼                    ▼                      ▼
┌──────────────┐  ┌────────────────┐  ┌────────────────────┐
│  CDN 节点     │  │ 本地文件系统     │  │  性能监控系统        │
│  (源站回源)   │  │ (临时+持久目录)  │  │  (埋点+ Crash 上报) │
└──────────────┘  └────────────────┘  └────────────────────┘
```

---

## 2. 数据模型

### 2.1 缓存键设计

每张图片在缓存系统中通过 **CacheKey** 唯一标识。CacheKey 由 URL + 变换参数生成哈希：

```dart
/// 缓存键生成规则
///
/// 格式: <url_hash>__<transform_hash>
/// 示例: a1b2c3d4__w400_q80_webp
class CacheKey {
  /// 原始 URL（含 CDN 查询参数）
  final String sourceUrl;

  /// 请求的变换参数
  final ImageTransform transform;

  /// 生成的缓存键
  late final String key = _computeKey();

  String _computeKey() {
    // URL hash: SHA-256 取前 16 字符
    final urlHash = sha256.convert(utf8.encode(sourceUrl)).toString().substring(0, 16);
    // Transform hash: 由宽度_高度_质量_格式组成
    final tHash = '${transform.targetWidth ?? 'auto'}_'
        '${transform.targetHeight ?? 'auto'}_'
        '${transform.quality}_'
        '${transform.format.name}';
    return '${urlHash}__$tHash';
  }

  /// 磁盘文件路径
  String get diskPath {
    // 分桶: 取 hash 前两位作为一级目录，避免单目录文件过多
    final bucket = key.substring(0, 2);
    return 'image_cache/$bucket/$key.${transform.format.extension}';
  }
}
```

### 2.2 图片变换参数

```dart
/// 图片变换描述符
///
/// 描述加载图片时需要的变换操作，用于生成变换后缓存
class ImageTransform {
  /// 目标宽度（px），null 表示自适应
  final int? targetWidth;

  /// 目标高度（px），null 表示自适应
  final int? targetHeight;

  /// 质量等级（1-100）
  final int quality;

  /// 期望的图片格式
  final ImageFormat format;

  /// 圆角半径（px），0 表示无圆角
  final double borderRadius;

  /// 是否模糊处理（用于占位图）
  final bool blur;

  /// 模糊半径
  final double blurRadius;

  /// 裁剪模式
  final CropMode cropMode;

  /// 背景色（用于 PNG 透明背景合成，仅 JPEG/WebP）
  final int? backgroundColor;

  const ImageTransform({
    this.targetWidth,
    this.targetHeight,
    this.quality = 80,
    this.format = ImageFormat.webp,
    this.borderRadius = 0,
    this.blur = false,
    this.blurRadius = 10,
    this.cropMode = CropMode.none,
    this.backgroundColor,
  });

  /// 预设：高清模式（WiFi + 高端设备）
  static const hd = ImageTransform(quality: 90, format: ImageFormat.webp);

  /// 预设：标准模式（默认）
  static const standard = ImageTransform(quality: 75, format: ImageFormat.webp);

  /// 预设：省流模式（弱网/低端设备）
  static const lite = ImageTransform(quality: 50, format: ImageFormat.webp);

  /// 预设：缩略图（列表/头像）
  static const thumbnail = ImageTransform(
    targetWidth: 200,
    quality: 60,
    format: ImageFormat.webp,
  );

  /// 预设：占位符（BlurHash）
  static const placeholder = ImageTransform(
    quality: 20,
    blur: true,
    blurRadius: 20,
    format: ImageFormat.jpeg,
  );
}

enum ImageFormat {
  webp('webp', '.webp'),
  jpeg('jpeg', '.jpg'),
  png('png', '.png'),
  gif('gif', '.gif'),
  svg('svg', '.svg'),
  bmp('bmp', '.bmp');

  final String name;
  final String extension;
  const ImageFormat(this.name, this.extension);
}

enum CropMode {
  none,       // 不裁剪
  center,     // 居中裁剪
  smart,      // 智能裁剪（基于显著区域）
  face,       // 人脸优先裁剪（用于头像）
}
```

### 2.3 缓存元数据

```dart
/// 磁盘缓存元数据（存储在 SQLite / Hive 中）
class CacheEntry {
  /// 缓存键
  final String key;

  /// 原始 URL
  final String sourceUrl;

  /// 本地文件路径（相对路径）
  final String filePath;

  /// 文件大小（字节）
  final int fileSize;

  /// 图片宽度（px）
  final int width;

  /// 图片高度（px）
  final int height;

  /// 图片格式
  final String format;

  /// 创建时间
  final DateTime createdAt;

  /// 最后访问时间
  final DateTime lastAccessedAt;

  /// 访问次数
  final int hitCount;

  /// 过期时间（null 表示永不过期）
  final DateTime? expiresAt;

  /// 关联的业务模块（用于分模块清理）
  final String module;

  /// BlurHash 值（用于快速占位渲染）
  final String? blurHash;

  /// 图片平均主色调（用于占位背景色）
  final int? dominantColor;

  CacheEntry({
    required this.key,
    required this.sourceUrl,
    required this.filePath,
    required this.fileSize,
    required this.width,
    required this.height,
    required this.format,
    required this.createdAt,
    required this.lastAccessedAt,
    this.hitCount = 0,
    this.expiresAt,
    required this.module,
    this.blurHash,
    this.dominantColor,
  });

  /// 转为 JSON
  Map<String, dynamic> toJson() => {
    'key': key,
    'source_url': sourceUrl,
    'file_path': filePath,
    'file_size': fileSize,
    'width': width,
    'height': height,
    'format': format,
    'created_at': createdAt.toIso8601String(),
    'last_accessed_at': lastAccessedAt.toIso8601String(),
    'hit_count': hitCount,
    'expires_at': expiresAt?.toIso8601String(),
    'module': module,
    'blur_hash': blurHash,
    'dominant_color': dominantColor,
  };

  factory CacheEntry.fromJson(Map<String, dynamic> json) => CacheEntry(
    key: json['key'] as String,
    sourceUrl: json['source_url'] as String,
    filePath: json['file_path'] as String,
    fileSize: json['file_size'] as int,
    width: json['width'] as int,
    height: json['height'] as int,
    format: json['format'] as String,
    createdAt: DateTime.parse(json['created_at'] as String),
    lastAccessedAt: DateTime.parse(json['last_accessed_at'] as String),
    hitCount: json['hit_count'] as int? ?? 0,
    expiresAt: json['expires_at'] != null
        ? DateTime.parse(json['expires_at'] as String)
        : null,
    module: json['module'] as String,
    blurHash: json['blur_hash'] as String?,
    dominantColor: json['dominant_color'] as int?,
  );
}
```

### 2.4 SQLite 缓存索引表

```sql
-- 图片缓存元数据表
CREATE TABLE IF NOT EXISTS image_cache_entries (
    key               TEXT PRIMARY KEY,
    source_url        TEXT NOT NULL,
    file_path         TEXT NOT NULL,
    file_size         INTEGER NOT NULL DEFAULT 0,
    width             INTEGER NOT NULL DEFAULT 0,
    height            INTEGER NOT NULL DEFAULT 0,
    format            TEXT NOT NULL DEFAULT 'webp',
    created_at        TEXT NOT NULL,    -- ISO8601
    last_accessed_at  TEXT NOT NULL,    -- ISO8601
    hit_count         INTEGER NOT NULL DEFAULT 0,
    expires_at        TEXT,             -- ISO8601, NULL = never
    module            TEXT NOT NULL DEFAULT 'general',
    blur_hash         TEXT,
    dominant_color    INTEGER
);

-- 按最后访问时间索引（LRU 清理用）
CREATE INDEX IF NOT EXISTS idx_cache_last_accessed
    ON image_cache_entries(last_accessed_at ASC);

-- 按模块索引（分模块清理用）
CREATE INDEX IF NOT EXISTS idx_cache_module
    ON image_cache_entries(module);

-- 按 URL 反查（去重 / 更新用）
CREATE INDEX IF NOT EXISTS idx_cache_source_url
    ON image_cache_entries(source_url);

-- 按文件大小索引（空间清理用）
CREATE INDEX IF NOT EXISTS idx_cache_file_size
    ON image_cache_entries(file_size DESC);

-- 预加载任务表
CREATE TABLE IF NOT EXISTS image_preload_tasks (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    url               TEXT NOT NULL,
    transform_json    TEXT NOT NULL,
    priority          INTEGER NOT NULL DEFAULT 0,   -- 0=低 1=中 2=高
    module            TEXT NOT NULL DEFAULT 'general',
    session_id        TEXT,                          -- 关联的学习会话
    expected_use_time TEXT,                          -- 预期使用时间 ISO8601
    status            TEXT NOT NULL DEFAULT 'pending', -- pending/loading/done/failed/expired
    created_at        TEXT NOT NULL,
    completed_at      TEXT,
    retry_count       INTEGER NOT NULL DEFAULT 0,
    UNIQUE(url, transform_json)
);

CREATE INDEX IF NOT EXISTS idx_preload_status_priority
    ON image_preload_tasks(status, priority DESC);
CREATE INDEX IF NOT EXISTS idx_preload_session
    ON image_preload_tasks(session_id);
```

### 2.5 预加载策略配置

```dart
/// 预加载场景配置
class PreloadConfig {
  /// 是否启用预加载
  final bool enabled;

  /// 最大并发预加载数
  final int maxConcurrent;

  /// 单次预加载任务最大数量
  final int maxBatchSize;

  /// 预加载图片最大总大小（字节）
  final int maxTotalBytes;

  /// 仅在 WiFi 下预加载
  final bool wifiOnly;

  /// 预加载图片质量（低于正常加载质量以节省带宽）
  final int preloadQuality;

  /// 预加载过期时间（分钟）
  final int expireMinutes;

  /// 各场景配置
  static const Map<PreloadScene, PreloadConfig> defaults = {
    // 同步课堂：预加载下一章节插图
    PreloadScene.textbookChapter: PreloadConfig(
      enabled: true,
      maxConcurrent: 3,
      maxBatchSize: 10,
      maxTotalBytes: 20 * 1024 * 1024, // 20MB
      wifiOnly: true,
      preloadQuality: 60,
      expireMinutes: 30,
    ),
    // 知识卡片浏览：预加载后续 3-5 张卡片图
    PreloadScene.knowledgeCard: PreloadConfig(
      enabled: true,
      maxConcurrent: 2,
      maxBatchSize: 5,
      maxTotalBytes: 5 * 1024 * 1024, // 5MB
      wifiOnly: true,
      preloadQuality: 50,
      expireMinutes: 15,
    ),
    // 练习测评：预加载题目配图
    PreloadScene.exercise: PreloadConfig(
      enabled: true,
      maxConcurrent: 2,
      maxBatchSize: 8,
      maxTotalBytes: 10 * 1024 * 1024, // 10MB
      wifiOnly: false, // 练习场景允许移动数据预加载
      preloadQuality: 60,
      expireMinutes: 60,
    ),
    // 学情报告：预加载图表渲染数据
    PreloadScene.report: PreloadConfig(
      enabled: false, // 报告通常即用即加载
      maxConcurrent: 1,
      maxBatchSize: 3,
      maxTotalBytes: 3 * 1024 * 1024,
      wifiOnly: true,
      preloadQuality: 70,
      expireMinutes: 10,
    ),
  };

  const PreloadConfig({
    required this.enabled,
    required this.maxConcurrent,
    required this.maxBatchSize,
    required this.maxTotalBytes,
    required this.wifiOnly,
    required this.preloadQuality,
    required this.expireMinutes,
  });
}

enum PreloadScene {
  textbookChapter,
  knowledgeCard,
  exercise,
  report,
  aiConversation,
  banner,
}
```

---

## 3. 架构设计

### 3.1 整体架构

```text
┌─────────────────────────────────────────────────────────────────────┐
│                         PrimeImage Widget                            │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌────────────────┐  │
│  │ Placeholder│  │  Error    │  │  Loaded   │  │  Fade Transition│  │
│  │(BlurHash/  │  │(重试按钮/  │  │(Image.memory│  │(动画渐显)       │  │
│  │ Shimmer/   │  │ 缺省图)    │  │ /Image.file)│  │                │  │
│  │ Skeleton)  │  │           │  │              │  │                │  │
│  └───────────┘  └───────────┘  └───────────┘  └────────────────┘  │
└────────────────────────┬────────────────────────────────────────────┘
                         │ ImageRequest
                         ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    ImageLoadController                               │
│  1. 生成 CacheKey & ImageTransform                                   │
│  2. 查内存缓存 → 命中？返回 MemoryImage                              │
│  3. 查磁盘缓存 → 命中？读取文件 + 解码 + 写入内存缓存 → 返回           │
│  4. 发起网络请求（带 CDN 签名 + 重试 + 超时）                         │
│  5. 下载完成 → 写入磁盘缓存 → 解码 → 写入内存缓存 → 返回               │
│  6. 提取 BlurHash + dominantColor → 存入缓存元数据                    │
└────────────────────────┬────────────────────────────────────────────┘
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  MemoryCache │ │  DiskCache   │ │ NetworkLayer │
│              │ │              │ │              │
│ LRU Map      │ │ SQLite 索引  │ │ HTTP + CDN   │
│ + Weak Ref   │ │ + 文件系统    │ │ 签名URL      │
│ 容量: ~50MB  │ │ 容量: ~300MB  │ │ 重试×3       │
│ 条目: ~200   │ │ 条目: ~2000   │ │ 超时: 15s    │
└──────────────┘ └──────────────┘ └──────────────┘
          │              │              │
          └──────────────┴──────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    辅助系统                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │ MemoryGuard  │  │  Preloader   │  │ AdaptEngine  │             │
│  │ 内存压力监控  │  │ 预加载调度器  │  │ 自适应引擎    │             │
│  │ 自动缩容     │  │ 序列预测     │  │ 网络+设备感知  │             │
│  └──────────────┘  └──────────────┘  └──────────────┘             │
│  ┌──────────────┐  ┌──────────────┐                               │
│  │  Metrics     │  │  Cleanup     │                               │
│  │ 埋点上报     │  │ 定时清理      │                               │
│  └──────────────┘  └──────────────┘                               │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 内存缓存设计

```dart
/// 内存缓存：LRU + 弱引用双层设计
///
/// 设计思路：
/// - 强引用层（Strong Cache）：最近高频使用的解码后图片，LRU 淘汰
/// - 弱引用层（Weak Cache）：被淘汰但可能还有引用的图片，GC 可回收
/// - 两层协作保证：热数据快速命中，冷数据不 OOM
class MemoryImageCache {
  /// 强引用 LRU 缓存
  final LinkedHashMap<String, _CacheRecord> _strongCache = LinkedHashMap<>();

  /// 弱引用缓存（被 LRU 淘汰后移入此处）
  final Map<String, WeakReference<_CacheRecord>> _weakCache = {};

  /// 弱引用的清理队列（Map 被回收后清理 _weakCache 中的空引用）
  final Expando<_CleanupToken> _cleanupTokens = Expando();

  /// 最大内存占用（字节）
  final int maxBytes;

  /// 当前内存占用（字节）
  int _currentBytes = 0;

  /// 最大缓存条目数
  final int maxEntries;

  static const int _defaultMaxBytes = 50 * 1024 * 1024; // 50MB
  static const int _defaultMaxEntries = 200;

  MemoryImageCache({
    this.maxBytes = _defaultMaxBytes,
    this.maxEntries = _defaultMaxEntries,
  });

  /// 获取图片
  ui.Image? get(String key) {
    // 1. 查强引用层
    final strong = _strongCache[key];
    if (strong != null) {
      // LRU: 移到末尾（最近使用）
      _strongCache.remove(key);
      _strongCache[key] = strong;
      strong.lastAccessed = DateTime.now();
      return strong.image;
    }

    // 2. 查弱引用层
    final weak = _weakCache[key];
    if (weak != null) {
      final record = weak.target;
      if (record != null) {
        // 从弱引用"复活"到强引用
        _promoteToStrong(key, record);
        return record.image;
      } else {
        // 弱引用已被 GC 回收
        _weakCache.remove(key);
      }
    }
    return null;
  }

  /// 写入图片
  void put(String key, ui.Image image, {required int estimatedBytes}) {
    // 如果已存在，先移除旧的
    final existing = _strongCache.remove(key);
    if (existing != null) {
      _currentBytes -= existing.estimatedBytes;
    }

    // 创建新记录
    final record = _CacheRecord(
      image: image,
      estimatedBytes: estimatedBytes,
      createdAt: DateTime.now(),
      lastAccessed: DateTime.now(),
    );

    _strongCache[key] = record;
    _currentBytes += estimatedBytes;

    // 淘汰超出容量的旧条目
    _evictIfNeeded();
  }

  /// LRU 淘汰
  void _evictIfNeeded() {
    while (_currentBytes > maxBytes || _strongCache.length > maxEntries) {
      if (_strongCache.isEmpty) break;

      // 移除最旧的（LinkedHashMap 迭代顺序 = 插入顺序，第一个即最久未使用）
      final oldestKey = _strongCache.keys.first;
      final evicted = _strongCache.remove(oldestKey)!;
      _currentBytes -= evicted.estimatedBytes;

      // 移入弱引用层（如果还有其他 Widget 引用它，不会被立即回收）
      _weakCache[oldestKey] = WeakReference(evicted);
    }
  }

  /// 从弱引用提升到强引用
  void _promoteToStrong(String key, _CacheRecord record) {
    _weakCache.remove(key);
    _strongCache[key] = record;
    _currentBytes += record.estimatedBytes;
    _evictIfNeeded();
  }

  /// 清空所有缓存（内存压力大时调用）
  void clearAll() {
    // 将所有强引用降级为弱引用
    for (final entry in _strongCache.entries) {
      _weakCache[entry.key] = WeakReference(entry.value);
    }
    _strongCache.clear();
    _currentBytes = 0;
  }

  /// 按模块清除（切换页面时释放旧页面图片）
  void clearByModule(String module) {
    // 由于 CacheKey 不含 module 信息，需外部传入需清除的 key 列表
    // 通常由 PageLifecycle 调用
  }

  /// 获取当前状态
  MemoryCacheStats get stats => MemoryCacheStats(
    strongEntries: _strongCache.length,
    weakEntries: _weakCache.length,
    currentBytes: _currentBytes,
    maxBytes: maxBytes,
    utilization: _currentBytes / maxBytes,
  );
}

/// 缓存记录内部类
class _CacheRecord {
  final ui.Image image;
  final int estimatedBytes;
  final DateTime createdAt;
  DateTime lastAccessed;

  _CacheRecord({
    required this.image,
    required this.estimatedBytes,
    required this.createdAt,
    required this.lastAccessed,
  });
}

/// 内存缓存统计
class MemoryCacheStats {
  final int strongEntries;
  final int weakEntries;
  final int currentBytes;
  final int maxBytes;
  final double utilization;

  const MemoryCacheStats({
    required this.strongEntries,
    required this.weakEntries,
    required this.currentBytes,
    required this.maxBytes,
    required this.utilization,
  });
}
```

### 3.3 磁盘缓存设计

```dart
/// 磁盘缓存：SQLite 索引 + 文件系统存储
class DiskImageCache {
  /// 缓存根目录
  final String cacheDir;

  /// SQLite 数据库
  final Database db;

  /// 最大磁盘占用（字节）
  final int maxBytes;

  /// 当前磁盘占用（字节）
  int _currentBytes = 0;

  /// 写入锁（防止同一图片并发写入）
  final Map<String, Future<void>> _writeLocks = {};

  static const int _defaultMaxBytes = 300 * 1024 * 1024; // 300MB

  DiskImageCache({
    required this.cacheDir,
    required this.db,
    this.maxBytes = _defaultMaxBytes,
  });

  /// 初始化：读取已有缓存统计
  Future<void> init() async {
    final result = await db.rawQuery(
      'SELECT SUM(file_size) as total FROM image_cache_entries',
    );
    _currentBytes = Sqflite.firstIntValue(result) ?? 0;

    // 启动时检查并清理过期缓存
    await _cleanExpired();
  }

  /// 读取图片文件
  Future<File?> get(String key) async {
    // 查询缓存索引
    final rows = await db.query(
      'image_cache_entries',
      where: 'key = ?',
      whereArgs: [key],
      limit: 1,
    );

    if (rows.isEmpty) return null;

    final entry = CacheEntry.fromJson(rows.first]);

    // 检查过期
    if (entry.expiresAt != null && DateTime.now().isAfter(entry.expiresAt!)) {
      await _removeEntry(key);
      return null;
    }

    // 检查文件是否存在
    final file = File(p.join(cacheDir, entry.filePath));
    if (!await file.exists()) {
      // 文件丢失（可能被系统清理），删除索引
      await _removeEntry(key);
      return null;
    }

    // 更新访问记录
    await db.update(
      'image_cache_entries',
      {
        'last_accessed_at': DateTime.now().toIso8601String(),
        'hit_count': entry.hitCount + 1,
      },
      where: 'key = ?',
      whereArgs: [key],
    );

    return file;
  }

  /// 写入图片文件
  Future<void> put({
    required String key,
    required String sourceUrl,
    required List<int> data,
    required String module,
    int? width,
    int? height,
    String format = 'webp',
    String? blurHash,
    int? dominantColor,
    Duration? ttl,
  }) async {
    // 检查是否已存在
    final existing = await db.query(
      'image_cache_entries',
      where: 'key = ?',
      whereArgs: [key],
      limit: 1,
    );
    if (existing.isNotEmpty) return;

    // 防止并发写入
    if (_writeLocks.containsKey(key)) {
      return _writeLocks[key]!;
    }

    final completer = Completer<void>();
    _writeLocks[key] = completer.future;

    try {
      // 计算文件路径
      final bucket = key.substring(0, 2);
      final fileName = '$key.$format';
      final relativePath = p.join('image_cache', bucket, fileName);
      final fullPath = p.join(cacheDir, relativePath);

      // 确保目录存在
      final dir = Directory(p.dirname(fullPath));
      if (!await dir.exists()) {
        await dir.create(recursive: true);
      }

      // 写入文件
      final file = File(fullPath);
      await file.writeAsBytes(data, flush: true);

      // 写入索引
      final now = DateTime.now();
      final entry = CacheEntry(
        key: key,
        sourceUrl: sourceUrl,
        filePath: relativePath,
        fileSize: data.length,
        width: width ?? 0,
        height: height ?? 0,
        format: format,
        createdAt: now,
        lastAccessedAt: now,
        expiresAt: ttl != null ? now.add(ttl) : null,
        module: module,
        blurHash: blurHash,
        dominantColor: dominantColor,
      );

      await db.insert(
        'image_cache_entries',
        entry.toJson(),
        conflictAlgorithm: ConflictAlgorithm.ignore,
      );

      _currentBytes += data.length;

      // 触发异步清理（非阻塞）
      _evictIfNeeded();
    } finally {
      completer.complete();
      _writeLocks.remove(key);
    }
  }

  /// LRU 清理
  Future<void> _evictIfNeeded() async {
    if (_currentBytes <= maxBytes) return;

    // 按最后访问时间升序查询需要清理的条目
    final candidates = await db.rawQuery(
      'SELECT key, file_path, file_size FROM image_cache_entries '
      'ORDER BY last_accessed_at ASC LIMIT ?',
      [100],
    );

    for (final row in candidates) {
      if (_currentBytes <= maxBytes * 0.9) break; // 清理到 90% 以下停止

      final key = row['key'] as String;
      final filePath = row['file_path'] as String;
      final fileSize = row['file_size'] as int;

      // 删除文件
      final file = File(p.join(cacheDir, filePath));
      if (await file.exists()) {
        await file.delete();
      }

      // 删除索引
      await db.delete(
        'image_cache_entries',
        where: 'key = ?',
        whereArgs: [key],
      );

      _currentBytes -= fileSize;
    }
  }

  /// 清理过期缓存
  Future<void> _cleanExpired() async {
    final now = DateTime.now().toIso8601String();
    final expired = await db.query(
      'image_cache_entries',
      where: 'expires_at IS NOT NULL AND expires_at < ?',
      whereArgs: [now],
    );

    for (final row in expired) {
      await _removeEntry(row['key'] as String);
    }
  }

  /// 移除单个缓存条目
  Future<void> _removeEntry(String key) async {
    final rows = await db.query(
      'image_cache_entries',
      where: 'key = ?',
      whereArgs: [key],
      limit: 1,
    );
    if (rows.isEmpty) return;

    final entry = CacheEntry.fromJson(rows.first);
    final file = File(p.join(cacheDir, entry.filePath));
    if (await file.exists()) {
      await file.delete();
    }
    await db.delete(
      'image_cache_entries',
      where: 'key = ?',
      whereArgs: [key],
    );
    _currentBytes -= entry.fileSize;
  }

  /// 按模块清理
  Future<int> clearByModule(String module) async {
    final rows = await db.query(
      'image_cache_entries',
      where: 'module = ?',
      whereArgs: [module],
    );

    int cleared = 0;
    for (final row in rows) {
      final entry = CacheEntry.fromJson(row);
      final file = File(p.join(cacheDir, entry.filePath));
      if (await file.exists()) await file.delete();
      _currentBytes -= entry.fileSize;
      cleared++;
    }

    await db.delete(
      'image_cache_entries',
      where: 'module = ?',
      whereArgs: [module],
    );

    return cleared;
  }

  /// 清空全部缓存
  Future<void> clearAll() async {
    final dir = Directory(p.join(cacheDir, 'image_cache'));
    if (await dir.exists()) {
      await dir.delete(recursive: true);
    }
    await db.delete('image_cache_entries');
    _currentBytes = 0;
  }

  /// 获取缓存统计
  Future<DiskCacheStats> getStats() async {
    final totalResult = await db.rawQuery(
      'SELECT COUNT(*) as count, SUM(file_size) as total, '
      'AVG(hit_count) as avg_hits FROM image_cache_entries',
    );
    final row = totalResult.first;
    return DiskCacheStats(
      totalEntries: (row['count'] as int?) ?? 0,
      totalBytes: (row['total'] as int?) ?? 0,
      avgHitCount: (row['avg_hits'] as num?)?.toDouble() ?? 0,
      maxBytes: maxBytes,
    );
  }
}

class DiskCacheStats {
  final int totalEntries;
  final int totalBytes;
  final double avgHitCount;
  final int maxBytes;

  const DiskCacheStats({
    required this.totalEntries,
    required this.totalBytes,
    required this.avgHitCount,
    required this.maxBytes,
  });

  double get utilization => maxBytes > 0 ? totalBytes / maxBytes : 0;
}
```

---

## 4. 核心组件设计

### 4.1 ImageLoadController - 加载控制器

```dart
/// 图片加载控制器
///
/// 负责协调内存缓存 → 磁盘缓存 → 网络请求的完整加载流程
class ImageLoadController {
  final MemoryImageCache _memoryCache;
  final DiskImageCache _diskCache;
  final NetworkImageLoader _networkLoader;
  final ImageDecoder _decoder;
  final AdaptEngine _adaptEngine;
  final MetricsCollector _metrics;

  /// 进行中的加载请求（去重）
  final Map<String, Future<ImageLoadResult>> _pending = {};

  ImageLoadController({
    required MemoryImageCache memoryCache,
    required DiskImageCache diskCache,
    required NetworkImageLoader networkLoader,
    required ImageDecoder decoder,
    required AdaptEngine adaptEngine,
    required MetricsCollector metrics,
  })  : _memoryCache = memoryCache,
        _diskCache = diskCache,
        _networkLoader = networkLoader,
        _decoder = decoder,
        _adaptEngine = adaptEngine,
        _metrics = metrics;

  /// 加载图片
  ///
  /// [url] 图片 URL
  /// [transform] 期望的变换参数（会被自适应引擎调整）
  /// [module] 业务模块标识
  Future<ImageLoadResult> load({
    required String url,
    ImageTransform transform = ImageTransform.standard,
    required String module,
  }) async {
    final stopwatch = Stopwatch()..start();
