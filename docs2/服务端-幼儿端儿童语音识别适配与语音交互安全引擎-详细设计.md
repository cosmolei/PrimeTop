# 服务端-PrimeTop 教育平台幼儿端儿童语音识别适配与语音交互安全引擎-详细设计

> **文档版本**：v1.0  
> **创建日期**：2026-08-10  
> **关联模块**：拼音识字启蒙、幼儿启蒙、发音陪练、语音服务(ASR-TTS)  
> **适用学段**：幼儿（3-6 岁）、小学低年级（6-8 岁）

---

## 1. 概述

### 1.1 功能定位

PrimeTip 平台覆盖幼儿至高中全学段，其中幼儿启蒙与小学低年级模块（拼音学习、识字朗读、英语口语启蒙、听写默写）高度依赖语音交互。然而，主流 ASR（自动语音识别）引擎均基于成人语料训练，对儿童语音的识别准确率显著下降（通常 WER 高出 2-3 倍）。

本模块作为**语音服务的垂直领域增强层**，构建在通用 `语音服务(ASR-TTS)` 之上，为幼儿与低龄用户提供：

1. **儿童声学模型适配**：针对儿童声道的物理特征优化识别准确率
2. **儿童发音容错处理**：识别儿童常见的发音替代、音节缺失、含糊发音
3. **语音交互安全**：确保儿童语音数据的隐私保护与合规存储
4. **家庭环境降噪**：针对家庭场景（电视声、家人对话、玩具声）的鲁棒降噪
5. **语音内容安全过滤**：防止儿童在语音交互中接触不适宜内容

### 1.2 设计目标

| 指标 | 目标值 |
|------|--------|
| 儿童语音识别 WER（字错率） | ≤ 12%（普通话清晰发音） |
| 儿童语音识别 WER（拼音单音） | ≤ 8% |
| 端到端语音交互延迟 | ≤ 1500ms（采集→识别→反馈） |
| 家庭环境噪声鲁棒性 | SNR ≥ 10dB 时 WER 恶化 ≤ 3% |
| 儿童语音数据留存合规 | 满足《未成年人保护法》《儿童个人信息网络保护规定》 |
| 并发语音会话支持 | ≥ 500 路（单节点） |

### 1.3 适用场景

| 场景 | 学段 | 交互形式 |
|------|------|----------|
| 拼音发音陪练 | 幼儿~小学低年级 | 单音节/词组跟读 |
| 课文朗读评估 | 小学 | 整段/整篇朗读 |
| 英语单词发音 | 幼儿~小学 | 单词跟读 |
| 语音背诵检测 | 小学~初中 | 背诵段落检测 |
| 语音提问 | 全学段 | 自然语言问答（需成人模式降级） |
| 听写默写 | 小学 | 听写词语/默写句子 |

---

## 2. 系统架构

### 2.1 整体架构

```text
┌──────────────────────────────────────────────────────────────────┐
│                        客户端（Flutter APP）                       │
│  ┌──────────┐  ┌──────────────┐  ┌──────────┐  ┌──────────────┐ │
│  │ 录音采集  │→│ 前端降噪(VAD) │→│ 音频编码  │→│  上传/流式传输 │ │
│  └──────────┘  └──────────────┘  └──────────┘  └──────────────┘ │
│  ┌──────────┐  ┌──────────────┐  ┌──────────┐  ┌──────────────┐ │
│  │ 反馈渲染  │←│  TTS 播放    │←│ 结果解析  │←│  响应接收     │ │
│  └──────────┘  └──────────────┘  └──────────┘  └──────────────┘ │
└──────────────────────────┬───────────────────────────────────────┘
                           │ HTTPS / WSS
┌──────────────────────────┴───────────────────────────────────────┐
│                         API 网关层                                 │
│  鉴权 → 限流 → 年龄路由 → 请求转发                                 │
└──────────────────────────┬───────────────────────────────────────┘
                           │
┌──────────────────────────┴───────────────────────────────────────┐
│                  儿童语音适配引擎 (ChildVoice Gateway)             │
│                                                                   │
│  ┌────────────┐  ┌──────────────┐  ┌───────────────┐            │
│  │ 年龄检测   │→│ 模型路由选择  │→│ ASR 推理调度   │            │
│  │ AgeRouter  │  │ ModelRouter  │  │ InferenceEng  │            │
│  └────────────┘  └──────────────┘  └───────────────┘            │
│                                                                   │
│  ┌────────────┐  ┌──────────────┐  ┌───────────────┐            │
│  │ 发音评估   ││ 后处理修正   │→│ 内容安全过滤   │            │
│  │ PronEval   │  │ PostCorrect  │  │ SafetyFilter  │            │
│  └────────────┘  └──────────────┘  └───────────────┘            │
│                                                                   │
│  ┌────────────┐  ┌──────────────┐                               │
│  │ 音频预处理 │  │ 数据脱敏存储  │                               │
│  │ PreProcess │  │ DataPrivacy  │                               │
│  └────────────┘  └──────────────┘                               │
└──────────────────────────┬───────────────────────────────────────┘
                           │
┌──────────────────────────┴───────────────────────────────────────┐
│                      底层 ASR / TTS 基础设施                       │
│                                                                   │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐                 │
│  │ 儿童声学   │  │ 成人声学   │  │ 通用声学   │                 │
│  │ 模型(微调) │  │ 模型(基线) │  │ 模型(兜底) │                 │
│  └────────────┘  └────────────┘  └────────────┘                 │
│                                                                   │
│  ┌────────────┐  ┌────────────┐                                  │
│  │ 儿童声纹   │  │ TTS 引擎   │                                  │
│  │ 检测模型   │  │ (多音色)   │                                  │
│  └────────────┘  └────────────┘                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 与现有模块的关系

```
语音服务(ASR-TTS)-详细设计          ← 基础 ASR/TTS 能力提供方
    ↑ 依赖
    │
儿童语音适配引擎 (本文档)            ← 年龄感知 + 儿童模型 + 安全增强
    ↑ 被依赖
    │
├── 拼音识字启蒙-详细设计             ← 调用拼音发音评估
├── 发音陪练服务-详细设计             ← 调用发音质量评分
├── 语文朗读评估系统-详细设计         ← 调用朗读流畅度评估
├── 文科背诵-详细设计                 ← 调用背诵准确度检测
└── 幼儿启蒙-详细设计                 ← 调用语音交互能力
```

---

## 3. 核心数据结构

### 3.1 儿童语音会话表 `child_voice_session`

```sql
CREATE TABLE child_voice_session (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    session_id      VARCHAR(64) NOT NULL UNIQUE COMMENT '会话唯一标识',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    user_age        INT NOT NULL COMMENT '用户年龄（冗余，加速路由）',
    user_grade      VARCHAR(20) NOT NULL COMMENT '用户学段',
    scenario        VARCHAR(40) NOT NULL COMMENT '场景: pinyin/read_aloud/oral_english/recite/dictation/voice_qa',
    
    -- 音频信息
    audio_format    VARCHAR(10) NOT NULL DEFAULT 'opus' COMMENT '音频编码格式',
    sample_rate     INT NOT NULL DEFAULT 16000 COMMENT '采样率',
    duration_ms     INT NOT NULL COMMENT '音频时长(毫秒)',
    audio_size_kb   INT NOT NULL COMMENT '音频文件大小(KB)',
    
    -- 处理结果
    asr_text        TEXT COMMENT 'ASR 识别文本',
    asr_confidence  DECIMAL(5,4) COMMENT '识别置信度 0-1',
    asr_model       VARCHAR(40) COMMENT '使用的 ASR 模型',
    asr_wer         DECIMAL(5,4) COMMENT '与标准文本对比的字错率(有标准文本时)',
    
    -- 评估结果
    pronunciation_score  DECIMAL(5,2) COMMENT '发音评分 0-100',
    fluency_score        DECIMAL(5,2) COMMENT '流畅度评分 0-100',
    accuracy_score       DECIMAL(5,2) COMMENT '准确度评分 0-100',
    
    -- 安全
    safety_flag     TINYINT NOT NULL DEFAULT 0 COMMENT '安全标记: 0=安全,1=需关注,2=拦截',
    safety_reason   VARCHAR(200) COMMENT '安全原因',
    
    -- 隐私
    audio_stored    TINYINT NOT NULL DEFAULT 0 COMMENT '是否留存原始音频: 0=否(即时销毁),1=是(有家长授权)',
    text_stored     TINYINT NOT NULL DEFAULT 1 COMMENT '是否留存识别文本',
    retention_days  INT COMMENT '留存天数(有授权时)',
    
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    expires_at      DATETIME COMMENT '数据过期时间(自动清理)',
    
    INDEX idx_user (user_id, created_at),
    INDEX idx_scenario (scenario, created_at),
    INDEX idx_expires (expires_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='儿童语音会话记录';
```

### 3.2 儿童发音基线表 `child_pronunciation_baseline`

```sql
CREATE TABLE child_pronunciation_baseline (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    age_stage       VARCHAR(20) NOT NULL COMMENT '年龄段: toddler/preschool/early_primary',
    
    -- 声母准确率（按声母分组统计）
    initial_accuracy JSON COMMENT '{"b":0.85,"p":0.72,"m":0.90,...}',
    -- 韵母准确率
    final_accuracy   JSON COMMENT '{"a":0.88,"o":0.75,"e":0.80,...}',
    -- 声调准确率
    tone_accuracy    JSON COMMENT '{"tone1":0.90,"tone2":0.85,"tone3":0.70,"tone4":0.80}',
    
    -- 常见错误模式
    common_errors    JSON COMMENT '[{"pattern":"zh→z","frequency":0.3},{"pattern":"sh→s","frequency":0.25}]',
    
    -- 更新统计
    total_samples    INT NOT NULL DEFAULT 0 COMMENT '累计评估样本数',
    last_updated     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_at       DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_user (user_id),
    INDEX idx_age (age_stage)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='儿童发音基线画像';
```

### 3.3 儿童 ASR 模型注册表 `child_asr_model_registry`

```sql
CREATE TABLE child_asr_model_registry (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    model_code      VARCHAR(40) NOT NULL UNIQUE COMMENT '模型编码',
    model_name      VARCHAR(100) NOT NULL COMMENT '模型名称',
    model_type      VARCHAR(20) NOT NULL COMMENT 'acoustic/language/combined',
    
    -- 适用范围
    age_min         INT NOT NULL COMMENT '适用最小年龄',
    age_max         INT NOT NULL COMMENT '适用最大年龄',
    scenarios       JSON COMMENT '适用场景列表',
    dialect_support JSON COMMENT '支持的方言补偿: ["sichuan","cantonese","northeast"]',
    
    -- 性能指标
    wer_general     DECIMAL(5,4) COMMENT '通用场景 WER',
    wer_clean       DECIMAL(5,4) COMMENT '清晰发音 WER',
    wer_noisy       DECIMAL(5,4) COMMENT '嘈杂环境 WER',
    avg_latency_ms  INT COMMENT '平均推理延迟',
    
    -- 部署信息
    endpoint_url    VARCHAR(255) COMMENT '推理服务地址',
    model_version   VARCHAR(20) COMMENT '模型版本',
    is_active       TINYINT NOT NULL DEFAULT 1 COMMENT '是否启用',
    is_default      TINYINT NOT NULL DEFAULT 0 COMMENT '是否默认模型',
    
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='儿童 ASR 模型注册表';
```

### 3.4 儿童语音内容安全规则表 `child_voice_safety_rule`

```sql
CREATE TABLE child_voice_safety_rule (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    rule_code       VARCHAR(40) NOT NULL UNIQUE COMMENT '规则编码',
    rule_name       VARCHAR(100) NOT NULL COMMENT '规则名称',
    category        VARCHAR(30) NOT NULL COMMENT '分类: content/privacy/abuse/time_limit',
    
    -- 规则条件
    condition_type  VARCHAR(20) NOT NULL COMMENT '条件类型: keyword/regex/semantic/duration',
    condition_value TEXT NOT NULL COMMENT '条件值',
    
    -- 规则动作
    action          VARCHAR(20) NOT NULL COMMENT '动作: warn/block/mask/escalate',
    action_params   JSON COMMENT '动作参数',
    
    -- 适用范围
    age_min         INT COMMENT '适用最小年龄(空=全部)',
    age_max         INT COMMENT '适用最大年龄(空=全部)',
    
    priority        INT NOT NULL DEFAULT 100 COMMENT '优先级(越小越高)',
    is_active       TINYINT NOT NULL DEFAULT 1,
    
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='儿童语音内容安全规则';
```

---

## 4. 核心算法设计

### 4.1 年龄感知模型路由（Age-Aware Model Router）

根据用户年龄、场景、网络条件，动态选择最优 ASR 模型。

```python
class ChildVoiceModelRouter:
    """儿童语音 ASR 模型智能路由"""
    
    # 年龄段→模型优先级映射
    AGE_MODEL_PRIORITY = {
        "toddler": {          # 3-4 岁
            "pinyin": ["child_acoustic_v3_toddler", "child_acoustic_v2", "general_v4"],
            "read_aloud": ["child_acoustic_v3_toddler", "child_acoustic_v2", "general_v4"],
            "voice_qa": ["child_acoustic_v2", "general_v4"],  # 幼儿QA降级到通用
        },
        "preschool": {        # 5-6 岁
            "pinyin": ["child_acoustic_v3_preschool", "child_acoustic_v2", "general_v4"],
            "read_aloud": ["child_acoustic_v3_preschool", "child_acoustic_v2", "general_v4"],
            "oral_english": ["child_en_v2", "general_en_v3"],
            "voice_qa": ["child_acoustic_v2", "general_v4"],
        },
        "early_primary": {   # 7-8 岁
            "pinyin": ["child_acoustic_v3_primary", "general_v4"],
            "read_aloud": ["child_acoustic_v3_primary", "general_v4"],
            "dictation": ["general_v4"],  # 已可使用通用模型
            "voice_qa": ["general_v4"],
        },
        "standard": {        # 9 岁+，使用通用模型
            "_all": ["general_v4"],
        },
    }
    
    def select_model(
        self,
        user_age: int,
        scenario: str,
        network_quality: str,  # good/medium/poor
        device_capability: str,  # high/medium/low
    ) -> str:
        """选择最优 ASR 模型"""
        
        age_stage = self._get_age_stage(user_age)
        model_list = self._get_model_candidates(age_stage, scenario)
        
        # 根据网络和设备条件筛选
        if network_quality == "poor" or device_capability == "low":
            # 弱网/低端设备：优先本地轻量模型或快速端模型
            model_list = self._prefer_fast_models(model_list)
        
        # 检查模型健康状态
        for model_code in model_list:
            if self._is_model_healthy(model_code):
                return model_code
        
        # 全部不可用时降级到通用模型
        return "general_v4"
    
    def _get_age_stage(self, age: int) -> str:
        if age <= 4:
            return "toddler"
        elif age <= 6:
            return "preschool"
        elif age <= 8:
            return "early_primary"
        else:
            return "standard"
```

### 4.2 儿童声纹检测（Child Voiceprint Detection）

判断输入音频是否来自儿童，用于动态路由决策。

```python
class ChildVoiceprintDetector:
    """
    基于 F0（基频）+ MFCC + 共振峰的轻量级儿童声检测。
    儿童基频范围: 250-400Hz（男童 250-340Hz，女童 280-400Hz）
    成人基频范围: 80-200Hz（男 80-150Hz，女 150-250Hz）
    """
    
    CHILD_F0_RANGE = (220, 450)   # 儿童基频范围(Hz)，放宽边界
    ADULT_F0_RANGE = (70, 260)    # 成人基频范围(Hz)
    
    # 儿童共振峰特征（第一共振峰 F1 普遍高于成人）
    CHILD_F1_MIN = 600  # Hz
    
    def detect(self, audio_samples, sample_rate=16000) -> dict:
        """
        检测音频是否为儿童语音。
        
        Returns:
            {
                "is_child": bool,
                "confidence": float,  # 0-1
                "estimated_age_group": str,  # toddler/preschool/early_primary/older/adult
                "f0_mean": float,
                "f0_std": float,
            }
        """
        # 1. 提取基频 F0
        f0 = self._extract_f0(audio_samples, sample_rate)
        f0_mean = np.mean(f0[f0 > 0]) if np.any(f0 > 0) else 0
        
        # 2. 提取 MFCC 特征
        mfcc = self._extract_mfcc(audio_samples, sample_rate)
        
        # 3. 提取共振峰
        formants = self._extract_formants(audio_samples, sample_rate)
        f1_mean = np.mean([f[0] for f in formants if len(f) > 0]) if formants else 0
        
        # 4. 规则判断
        f0_in_child_range = self.CHILD_F0_RANGE[0] <= f0_mean <= self.CHILD_F0_RANGE[1]
        f1_is_high = f1_mean >= self.CHILD_F1_MIN
        
        # 5. 模型判断（轻量 SVM 分类器）
        features = np.concatenate([
            [f0_mean, np.std(f0[f0 > 0]) if np.any(f0 > 0) else 0],
            np.mean(mfcc, axis=0)[:13],
            [f1_mean]
        ])
        model_score = self._svm_classifier.predict_proba(features.reshape(1, -1))[0]
        
        # 6. 融合决策
        is_child = f0_in_child_range and model_score[1] > 0.5
        confidence = model_score[1] if is_child else model_score[0]
        
        # 7. 年龄段估计
        if is_child:
            if f0_mean > 350:
                estimated_age = "toddler"
            elif f0_mean > 300:
                estimated_age = "preschool"
            elif f0_mean > 250:
                estimated_age = "early_primary"
            else:
                estimated_age = "older"
        else:
            estimated_age = "adult"
        
        return {
            "is_child": is_child,
            "confidence": round(confidence, 4),
            "estimated_age_group": estimated_age,
            "f0_mean": round(f0_mean, 1),
            "f0_std": round(np.std(f0[f0 > 0]) if np.any(f0 > 0) else 0, 1),
        }
```

### 4.3 儿童发音评估算法

#### 4.3.1 拼音单音评估

```python
class PinyinPronunciationEvaluator:
    """
    拼音单音/词组发音质量评估。
    
    评估维度：
    1. 声母准确度（声母是否发准）
    2. 韵母准确度（韵母是否发准）
    3. 声调准确度（声调是否正确）
    4. 整体相似度（与标准音的声学距离）
    """
    
    # 声母分组（按发音部位）
    INITIAL_GROUPS = {
        "bilabial": ["b", "p", "m"],           # 双唇音
        "labiodental": ["f"],                    # 唇齿音
        "alveolar": ["d", "t", "n", "l"],       # 舌尖中音
        "retroflex": ["zh", "ch", "sh", "r"],   # 舌尖后音
        "alveolar_sibilant": ["z", "c", "s"],   # 舌尖前音
        "palatal": ["j", "q", "x"],             # 舌面音
        "velar": ["g", "k", "h"],               # 舌根音
    }
    
    # 儿童常见声母混淆（用于容错判断）
    COMMON_CONFUSIONS = {
        ("zh", "z"), ("ch", "c"), ("sh", "s"),   # 平翘舌不分
        ("r", "l"),                                # r/l 混淆
        ("n", "l"),                                # n/l 混淆（南方常见）
        ("h", "f"),                                # h/f 混淆
        ("j", "z"), ("q", "c"), ("x", "s"),      # 面音与齿音混淆
    }
    
    def evaluate(
        self,
        audio_samples: np.ndarray,
        target_pinyin: str,   # 如 "mā" 或 "zhōng"
        sample_rate: int = 16000,
        user_age: int = 5,
    ) -> dict:
        """
        评估拼音发音。
        
        Returns:
            {
                "overall_score": float,      # 综合评分 0-100
                "initial_score": float,      # 声母评分
                "final_score": float,        # 韵母评分  
                "tone_score": float,         # 声调评分
                "issues": list,              # 具体问题列表
                "is_common_error": bool,     # 是否属于年龄常见错误
                "encouragement": str,        # 鼓励语
            }
        """
        # 解析目标拼音
        initial, final, tone = self._parse_pinyin(target_pinyin)
        
        # 提取标准音频特征（预录制的标准发音库）
        ref_features = self._get_reference_features(target_pinyin)
        
        # 提取用户音频特征
        user_features = self._extract_detailed_features(audio_samples, sample_rate)
        
        # 分维度评分
        initial_score = self._score_initial(user_features, ref_features, initial, user_age)
        final_score = self._score_final(user_features, ref_features, final, user_age)
        tone_score = self._score_tone(user_features, ref_features, tone, user_age)
        
        # 综合评分（加权平均，声母和韵母权重更高）
        weights = {"initial": 0.35, "final": 0.35, "tone": 0.30}
        if user_age <= 4:
            # 幼儿声调容错更高，降低声调权重
            weights = {"initial": 0.40, "final": 0.40, "tone": 0.20}
        
        overall = (
            initial_score * weights["initial"]
            + final_score * weights["final"]
            + tone_score * weights["tone"]
        )
        
        # 检测是否为年龄常见错误
        is_common = self._is_age_appropriate_error(
            initial, user_features, user_age
        )
        
        # 如果是常见错误，适度宽容（加分上限5分）
        if is_common and overall >= 60:
            overall = min(100, overall + 3)
        
        # 生成具体问题反馈
        issues = self._identify_issues(
            initial, final, tone,
            initial_score, final_score, tone_score,
            user_features, ref_features, user_age
        )
        
        # 生成鼓励语
        encouragement = self._generate_encouragement(overall, user_age)
        
        return {
            "overall_score": round(overall, 1),
            "initial_score": round(initial_score, 1),
            "final_score": round(final_score, 1),
            "tone_score": round(tone_score, 1),
            "issues": issues,
            "is_common_error": is_common,
            "encouragement": encouragement,
        }
    
    def _is_age_appropriate_error(
        self, target_initial: str, user_features: dict, age: int
    ) -> bool:
        """判断是否为该年龄段常见发音错误"""
        detected_initial = user_features.get("detected_initial", "")
        
        for correct, confused in self.COMMON_CONFUSIONS:
            if target_initial == correct and detected_initial == confused:
                if age <= 5:
                    return True  # 5岁以下完全容错
                elif age <= 7 and (correct, confused) in [
                    ("zh", "z"), ("ch", "c"), ("sh", "s"), ("r", "l")
                ]:
                    return True  # 7岁以下平翘舌容错
        return False
```

#### 4.3.2 朗读流畅度评估

```python
class ReadingFluencyEvaluator:
    """
    整段/整篇朗读流畅度评估。
    
    评估维度：
    1. 语速（字/分钟）
    2. 停顿合理性（段落间、标点处停顿 vs 不当停顿）
    3. 回读次数（读错后重新读）
    4. 音节清晰度
    5. 整体流畅度
    """
    
    # 年龄段参考语速（字/分钟）
    REFERENCE_SPEED = {
        (6, 7): {"min": 80, "ideal": 120, "max": 160},
        (7, 8): {"min": 100, "ideal": 140, "max": 180},
        (8, 9): {"min": 120, "ideal": 160, "max": 200},
        (9, 99): {"min": 140, "ideal": 180, "max": 240},
    }
    
    def evaluate(
        self,
        audio_samples: np.ndarray,
        reference_text: str,
        alignment_result: dict,  # 来自 ASR 的强制对齐结果
        sample_rate: int = 16000,
        user_age: int = 7,
    ) -> dict:
        """
        评估朗读流畅度。
        
        Args:
            alignment_result: 强制对齐输出，包含每个字的起止时间
        
        Returns:
            {
                "fluency_score": float,
                "speed_score": float,
                "pause_score": float,
                "clarity_score": float,
                "regression_count": int,    # 回读次数
                "words_per_minute": float,
                "details": {...}
            }
        """
        # 1. 计算语速
        total_chars = len(reference_text)
        duration_sec = len(audio_samples) / sample_rate
        wpm = (total_chars / duration_sec) * 60
        
        speed_ref = self._get_speed_reference(user_age)
        speed_score = self._score_speed(wpm, speed_ref)
        
        # 2. 停顿分析
        pauses = self._detect_pauses(alignment_result, audio_samples, sample_rate)
        appropriate_pauses = self._count_appropriate_pauses(pauses, reference_text)
        inappropriate_pauses = len(pauses) - appropriate_pauses
        pause_score = self._score_pauses(
            appropriate_pauses, inappropriate_pauses, total_chars
        )
        
        # 3. 回读检测
        regressions = self._detect_regressions(alignment_result, reference_text)
        
        # 4. 音节清晰度
        clarity = self._assess_syllable_clarity(alignment_result, audio_samples)
        
        # 5. 整体流畅度（综合评分）
        fluency_score = (
            speed_score * 0.25
            + pause_score * 0.35
            + clarity["score"] * 0.25
            + self._regression_penalty(regressions, total_chars) * 0.15
        )
        
        return {
            "fluency_score": round(fluency_score, 1),
            "speed_score": round(speed_score, 1),
            "pause_score": round(pause_score, 1),
            "clarity_score": round(clarity["score"], 1),
            "regression_count": len(regressions),
            "words_per_minute": round(wpm, 1),
            "details": {
                "total_pauses": len(pauses),
                "appropriate_pauses": appropriate_pauses,
                "inappropriate_pauses": inappropriate_pauses,
                "unclear_syllables": clarity["unclear"],
            }
        }
```

### 4.4 家庭环境智能降噪

```python
class HomeEnvironmentDenoiser:
    """
    针对家庭场景的智能降噪。
    常见家庭噪声：电视/手机媒体声、家人交谈、玩具声、宠物声、厨房声、空调声。
    """
    
    # 噪声类型→降噪策略
    DENOISE_STRATEGY = {
        "tv_media": {"method": "spectral_subtraction", "aggression": 0.7},
        "human_speech": {"method": "deep_denoiser", "model": "denoise_v2"},
        "toys": {"method": "highpass_filter", "cutoff": 200},
        "hvac": {"method": "stationary_noise", "aggression": 0.5},
        "transient": {"method": "transient_suppress", "threshold": 0.3},
    }
    
    def process(
        self,
        audio_samples: np.ndarray,
        sample_rate: int = 16000,
    ) -> tuple[np.ndarray, dict]:
        """
        智能降噪处理。
        
        Returns:
            (cleaned_audio, metadata)
            metadata = {
                "noise_types_detected": list,
                "snr_before": float,
                "snr_after": float,
                "processing_ms": int,
            }
        """
        # 1. 噪声分类（轻量 CNN 分类器）
        noise_types = self._classify_noise(audio_samples, sample_rate)
        
        # 2. SNR 估计
        snr_before = self._estimate_snr(audio_samples, sample_rate)
        
        # 3. 根据噪声类型选择策略
        import time
        start = time.time()
        
        cleaned = audio_samples.copy().astype(np.float32)
        
        for noise_type in noise_types:
            strategy = self.DENOISE_STRATEGY.get(noise_type)
            if not strategy:
                continue
            
            cleaned = self._apply_denoise(
                cleaned, sample_rate, strategy
            )
        
        # 4. 语音增强（提升儿童语音清晰度）
        cleaned = self._enhance_child_voice(cleaned, sample_rate)
        
        processing_ms = int((time.time() - start) * 1000)
        snr_after = self._estimate_snr(cleaned, sample_rate)
        
        return cleaned, {
            "noise_types_detected": noise_types,
            "snr_before": round(snr_before, 1),
            "snr_after": round(snr_after, 1),
            "processing_ms": processing_ms,
        }
    
    def _enhance_child_voice(self, audio, sr):
        """儿童语音增强：提升高频（儿童语音高频能量更丰富）"""
        # 高频提升（儿童语音特征频段 300-4000Hz）
        from scipy.signal import butter, sosfilt
        sos = butter(2, [300, 4000], btype='bandpass', fs=sr, output='sos')
        enhanced = sosfilt(sos, audio)
        # 混合原始与增强（避免过度处理）
        return 0.7 * audio + 0.3 * enhanced
```

---

## 5. API 接口设计

### 5.1 儿童语音识别接口

#### `POST /api/v1/child-voice/recognize`

提交儿童语音进行识别和评估。

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| audio | binary | 是 | 音频文件（opus/wav/mp3，≤ 60s） |
| user_id | int | 是 | 用户 ID |
| scenario | string | 是 | 场景：`pinyin`/`read_aloud`/`oral_english`/`recite`/`dictation`/`voice_qa` |
| target_text | string | 否 | 标准文本（有对照文本时用于评估准确度） |
| target_pinyin | string | 否 | 目标拼音（拼音场景） |
| enable_evaluation | bool | 否 | 是否返回发音评估（默认 true） |
| store_audio | bool | 否 | 是否留存原始音频（默认 false） |

**请求头**：

```
Authorization: Bearer <token>
Content-Type: multipart/form-data
X-User-Age: 5
X-Device-Capability: high
X-Network-Quality: good
```

**响应（200）**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "session_id": "cvs_20260810_abc123def456",
    "recognition": {
      "text": " zhōng guó rén mín",
      "confidence": 0.9234,
      "model_used": "child_acoustic_v3_preschool",
      "wer": 0.0,
      "is_child_voice": true,
      "age_group_detected": "preschool"
    },
    "evaluation": {
      "overall_score": 87.5,
      "initial_score": 90.0,
      "final_score": 85.0,
      "tone_score": 87.5,
      "issues": [
        {
          "type": "tone",
          "target": "zhōng (1声)",
          "actual": "接近4声",
          "severity": "minor",
          "suggestion": "注意第一声要平，声音不要往下走哦"
        }
      ],
      "is_common_error": false,
      "encouragement": "读得真棒！声母和韵母都很准，声调再稳一点就完美啦！⭐"
    },
    "safety": {
      "flag": "safe",
      "checked_rules": ["content_filter", "duration_limit", "privacy_policy"]
    },
    "audio_meta": {
      "duration_ms": 2400,
      "stored": false,
      "denoise_applied": true,
      "noise_types": ["tv_media"],
      "snr_before": 8.2,
      "snr_after": 21.5
    }
  }
}
```

**错误码**：

| 错误码 | HTTP状态 | 说明 |
|--------|----------|------|
| 40001 | 400 | 音频格式不支持 |
| 40002 | 400 | 音频时长超限（>60s） |
| 40003 | 400 | 音频损坏或无有效语音 |
| 40301 | 403 | 用户不同意语音数据协议 |
| 40302 | 403 | 未成年人未获得家长授权 |
| 42901 | 429 | 语音识别次数达到日限额 |
| 50001 | 500 | ASR 服务不可用（已降级） |
| 50002 | 500 | 评估模型不可用（仅返回识别结果） |

### 5.2 流式语音识别接口

#### `WSS /api/v1/child-voice/stream/recognize`

建立 WebSocket 连接进行实时流式语音识别，适用于朗读、背诵等长文本场景。

**连接参数（Query String）**：

| 参数 | 必填 | 说明 |
|------|------|------|
| user_id | 是 | 用户 ID |
| scenario | 是 | 场景 |
| target_text | 否 | 对照文本 |
| sample_rate | 否 | 采样率（默认 16000） |

**通信协议**：

```text
Client → Server: 二进制音频帧（Opus 编码，每帧 20ms）
Client → Server: {"type": "end"} — 结束录音
Server → Client: {"type": "partial", "text": "...", "confidence": 0.85} — 中间结果
Server → Client: {"type": "final", "text": "...", "confidence": 0.92} — 最终结果
Server → Client: {"type": "evaluation", ...} — 评估结果（如果启用）
Server → Client: {"type": "safety_warning", "action": "mask"} — 安全告警
```

**客户端示例（Dart/Flutter）**：

```dart
class ChildVoiceStreamClient {
  final String baseUrl = 'wss://api.primetop.edu/api/v1/child-voice/stream/recognize';
  
  /// 启动流式语音识别
  Future<void> startStream({
    required int userId,
    required String scenario,
    String? targetText,
    required VoiceStreamCallback callback,
  }) async {
    final params = {
      'user_id': userId.toString(),
      'scenario': scenario,
      if (targetText != null) 'target_text': targetText,
      'sample_rate': '16000',
    };
    
    final uri = Uri.parse('$baseUrl?${_buildQuery(params)}');
    final channel = WebSocketChannel.connect(uri);
    
    // 发送音频流
    await for (final audioChunk in _audioStreamController.stream) {
      channel.sink.add(audioChunk); // 二进制音频帧
    }
    
    // 发送结束信号
    channel.sink.add(jsonEncode({'type': 'end'}));
    
    // 接收结果
    channel.stream.listen((message) {
      final data = jsonDecode(message);
      switch (data['type']) {
        case 'partial':
          callback.onPartial(data['text'], data['confidence']);
          break;
        case 'final':
          callback.onFinal(data['text'], data['confidence']);
          break;
        case 'evaluation':
          callback.onEvaluation(data);
          break;
        case 'safety_warning':
          callback.onSafetyWarning(data);
          break;
      }
    });
  }
}

abstract class VoiceStreamCallback {
  void onPartial(String text, double confidence);
  void onFinal(String text, double confidence);
  void onEvaluation(Map<String, dynamic> evaluation);
  void onSafetyWarning(Map<String, dynamic> warning);
}
```

### 5.3 发音练习接口

#### `POST /api/v1/child-voice/practice`

提交发音练习，获取详细评估和反馈。

**请求**：

```json
{
  "user_id": 10086,
  "practice_type": "pinyin",
  "items": [
    {
      "target": "mā",
      "audio_ref": "audio_upload_id_xxx"
    },
    {
      "target": "mēn",
      "audio_ref": "audio_upload_id_yyy"
    }
  ],
  "session_context": {
    "lesson_id": "pinyin_lesson_03",
    "unit": "声母 m"
  }
}
```

**响应（200）**：

```json
{
  "code": 0,
  "data": {
    "session_id": "cps_20260810_xyz789",
    "results": [
      {
        "target": "mā",
        "score": 92.0,
        "initial_score": 95.0,
        "final_score": 90.0,
        "tone_score": 91.0,
        "passed": true,
        "issues": [],
        "reward": "⭐",
        "audio_playback_url": null
      },
      {
        "target": "mēn",
        "score": 68.5,
        "initial_score": 85.0,
        "final_score": 55.0,
        "tone_score": 65.0,
        "passed": false,
        "issues": [
          {
            "type": "final",
            "description": "韵母 en 发音不够饱满",
            "suggestion": "嘴巴微微张开，嘴角向两边咧，发出'e'的声音后轻轻闭唇发'n'"
          }
        ],
        "reward": null,
        "audio_playback_url": "https://cdn.primetop.edu/audio/ref/men_standard.mp3"
      }
    ],
    "summary": {
      "total": 2,
      "passed": 1,
      "avg_score": 80.25,
      "stars_earned": 1,
      "encouragement": "很棒哦！'mā' 读得特别准！'mēn' 再试几次一定可以的！加油！💪"
    },
    "baseline_update": {
      "initial_m": 0.90,
      "final_en": 0.55,
      "needs_review": ["final_en"]
    }
  }
}
```

### 5.4 儿童语音日限额查询

#### `GET /api/v1/child-voice/quota`

```json
{
  "code": 0,
  "data": {
    "user_id": 10086,
    "daily_limit": 50,
    "daily_used": 12,
    "daily_remaining": 38,
    "reset_at": "2026-08-11T00:00:00+08:00",
    "membership_bonus": 0,
    "is_member": false
  }
}
```

---

## 6. 状态流转设计

### 6.1 语音会话状态机

```text
                    ┌──────────┐
    POST /recognize │ CREATED  │
         ─────────→ └────┬─────┘
                         │ 音频上传完成
                         ▼
                   ┌──────────────┐
                   │  PREPROCESS  │
                   │ (降噪+VAD)   │
                   └──────┬───────┘
                          │ 音频有效
                          ▼
                   ┌──────────────┐    不通过    ┌─────────────┐
                   │ AGE_ROUTING  │────────────→│ FALLBACK    │
                   │ (年龄路由)   │             │ (通用模型)   │
                   └──────┬───────┘             └──────┬──────┘
                          │ 通过                       │
                          ▼                            │
                   ┌──────────────┐                   │
                   │ RECOGNIZING  │←──────────────────┘
                   │ (ASR 推理)   │
                   └──────┬───────┘
                          │ 推理完成
                          ▼
                   ┌──────────────┐
                   │ EVALUATING   │  (enable_evaluation=true)
                   │ (发音评估)   │
                   └──────┬───────┘
                          │
                          ▼
                   ┌──────────────┐
                   │ SAFETY_CHECK │
                   │ (安全过滤)   │
                   └──────┬───────┘
                    ┌─────┴──────┐
                    │安全        │不安全
                    ▼            ▼
              ┌──────────┐  ┌───────────┐
              │ COMPLETED │  │ BLOCKED   │
              │ (返回结果)│  │ (拦截/脱敏) │
              └──────────┘  └───────────┘
```

### 6.2 状态定义

| 状态 | 说明 | 超时 | 超时处理 |
|------|------|------|----------|
| CREATED | 会话已创建，等待音频 | 10s | 自动取消 |
| PREPROCESS | 音频预处理中 | 3s | 跳过降噪 |
| AGE_ROUTING | 模型路由中 | 1s | 使用默认模型 |
| RECOGNIZING | ASR 推理中 | 15s | 降级通用模型 |
| EVALUATING | 发音评估中 | 10s | 跳过评估 |
| SAFETY_CHECK | 安全检查中 | 2s | 放行（fail-open） |
| COMPLETED | 成功完成 | - | - |
| BLOCKED | 安全拦截 | - | - |
| FAILED | 系统错误 | - | - |

### 6.3 模型降级链路

```text
首选: child_acoustic_v3_preschool (WER=8.2%)
  │
  ├─ 不可用/超时 →
  │   次}