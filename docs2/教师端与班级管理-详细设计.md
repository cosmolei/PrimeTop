# 教师端与班级管理 - 详细设计文档

> 版本：1.0  
> 日期：2026-05-20  
> 状态：预研设计（V2.0 规划）  
> 模块优先级：P3（中长期扩展）

---

## 1. 模块概述

### 1.1 功能定位

教师端与班级管理模块为 PrimeTop 的 V2.0 扩展功能，面向学校教师和教培机构讲师，提供班级创建、学生管理、作业布置与批改、班级学情分析、家校沟通等核心能力。该模块需与现有学生端、家长端、内容服务、AI 服务和数据分析体系深度集成。

### 1.2 设计目标

1. **低门槛接入**：教师无需复杂培训即可快速上手，核心操作 ≤ 3 步完成
2. **与学生端无缝衔接**：教师发布的作业、通知直接推送到学生 APP
3. **与家长端联动**：学情报告、班级通知自动同步至家长中心
4. **与现有模块解耦**：通过事件驱动和 API 集成，不侵入现有核心代码
5. **支持多场景**：公立学校班级、培训机构小班、一对一辅导

### 1.3 核心用户角色

| 角色 | 说明 | 典型场景 |
|------|------|----------|
| 班主任 | 管理本班学生、查看全科成绩 | 日常班级管理、家校沟通 |
| 科任教师 | 管理所教学科的学生 | 布置作业、查看学科数据 |
| 教研组长 | 管理年级/学科组 | 年级横向对比、教研资源共享 |
| 机构讲师 | 培训机构教师 | 小班管理、学员进度跟踪 |

---

## 2. 功能清单

### 2.1 P3 核心功能（V2.0 首发）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 教师注册与认证 | 手机号注册、教师资格认证、学校关联 | P3-0 |
| 班级创建与管理 | 创建班级、生成邀请码、学生加入审批 | P3-0 |
| 学生名册管理 | 查看班级学生列表、学科分组、标签管理 | P3-0 |
| 作业布置 | 从题库选题、自定义题目、设置截止时间 | P3-0 |
| 作业完成情况 | 查看提交状态、批改结果统计 | P3-0 |
| 班级学情看板 | 全班各学科掌握度、薄弱知识点分布 | P3-0 |
| 班级通知 | 发布通知、家长确认回执 | P3-1 |
| 个体学情详情 | 查看单个学生学习轨迹、错题分布 | P3-1 |

### 2.2 P3 扩展功能（V2.x 迭代）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| AI 辅助出题 | 根据教学进度自动生成练习题 | P3-2 |
| 教研资源共享 | 学科组共享教案、课件、题集 | P3-2 |
| 年级数据对比 | 年级横向对比分析 | P3-2 |
| 自定义报告模板 | 按学校要求定制学情报告格式 | P3-2 |
| 家校互动 | 教师与家长一对一消息 | P3-2 |
| 教学日历 | 排课、考试安排、假期管理 | P3-3 |

---

## 3. 数据模型

### 3.1 核心实体 ER 图

```
┌──────────────┐     ┌───────────────────┐     ┌──────────────────┐
│   Teacher    │────<│  TeacherClass     │>────│     Class        │
│              │     │  (教师-班级关联)   │     │                  │
└──────┬───────┘     └───────────────────┘     └───────┬──────────┘
       │                                              │
       │         ┌───────────────────┐                │
       │         │  ClassStudent     │                │
       │         │  (班级-学生关联)   │>───────────────┤
       │         └───────┬───────────┘                │
       │                 │                            │
       │                 v                            │
       │         ┌───────────────────┐                │
       │         │  StudentProfile   │                │
       │         │  (已有实体)        │                │
       │         └───────────────────┘                │
       │                                              │
       │<──────────────────────────────────────────────┤
       │         ┌───────────────────┐                 │
       ├────────>│    Assignment     │>────────────────┤
       │         │    (作业/任务)     │                 │
       │         └───────┬───────────┘                 │
       │                 │                             │
       │                 v                             │
       │         ┌───────────────────┐                 │
       │         │  AssignmentTask   │                 │
       │         │  (学生作业实例)    │                 │
       │         └───────────────────┘                 │
       │                                              │
       │         ┌───────────────────┐                 │
       └────────>│  ClassNotice     │>────────────────┘
                 │  (班级通知)        │
                 └───────────────────┘
```

### 3.2 DDL 定义

#### 3.2.1 教师表 teacher

```sql
CREATE TABLE teacher (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id         BIGINT NOT NULL COMMENT '关联 users 表（教师也是用户）',
    real_name       VARCHAR(50) NOT NULL COMMENT '真实姓名',
    phone           VARCHAR(20) NOT NULL COMMENT '手机号',
    school_name     VARCHAR(200) COMMENT '所属学校/机构名称',
    school_code     VARCHAR(50) COMMENT '学校代码（用于官方学校匹配）',
    title           VARCHAR(50) COMMENT '职称：高级教师/一级教师/...',
    subject_ids     JSON COMMENT '所教学科 ID 列表 [1,3,5]',
    cert_no         VARCHAR(100) COMMENT '教师资格证编号',
    cert_status     TINYINT NOT NULL DEFAULT 0 COMMENT '认证状态：0-未认证 1-审核中 2-已认证 3-认证失败',
    cert_verified_at DATETIME COMMENT '认证通过时间',
    avatar_url      VARCHAR(500) COMMENT '头像',
    bio             VARCHAR(500) COMMENT '个人简介',
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-禁用 1-正常',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_user_id (user_id),
    UNIQUE KEY uk_phone (phone),
    INDEX idx_school_code (school_code),
    INDEX idx_cert_status (cert_status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师信息表';
```

#### 3.2.2 班级表 class

```sql
CREATE TABLE class (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    class_name      VARCHAR(100) NOT NULL COMMENT '班级名称，如"高一(3)班"',
    class_code      VARCHAR(20) NOT NULL COMMENT '班级邀请码，6位字母数字',
    school_name     VARCHAR(200) COMMENT '学校名称',
    grade_id        INT COMMENT '年级 ID（关联 grade 表）',
    subject_id      INT COMMENT '学科 ID（单科班级时填写）',
    class_type      TINYINT NOT NULL DEFAULT 1 COMMENT '班级类型：1-行政班(全科) 2-教学班(单科) 3-辅导班(机构)',
    academic_year   VARCHAR(10) COMMENT '学年，如 2025-2026',
    semester        TINYINT COMMENT '学期：1-上 2-下',
    description     VARCHAR(500) COMMENT '班级描述',
    avatar_url      VARCHAR(500) COMMENT '班级头像',
    max_students    INT NOT NULL DEFAULT 200 COMMENT '最大学生数',
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-已归档 1-活跃 2-已毕业',
    created_by      BIGINT NOT NULL COMMENT '创建者教师 ID',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    archived_at     DATETIME COMMENT '归档时间',
    
    UNIQUE KEY uk_class_code (class_code),
    INDEX idx_grade (grade_id),
    INDEX idx_created_by (created_by),
    INDEX idx_status (status),
    INDEX idx_school_grade (school_name, grade_id, academic_year)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级表';
```

#### 3.2.3 教师-班级关联表 teacher_class

```sql
CREATE TABLE teacher_class (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    teacher_id      BIGINT NOT NULL,
    class_id        BIGINT NOT NULL,
    role            TINYINT NOT NULL DEFAULT 2 COMMENT '角色：1-班主任 2-科任教师 3-助教',
    subject_id      INT COMMENT '负责学科（科任教师时填写）',
    joined_at       DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '0-退出 1-正常',
    
    UNIQUE KEY uk_teacher_class (teacher_id, class_id, subject_id),
    INDEX idx_class_id (class_id),
    INDEX idx_teacher_id (teacher_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师班级关联表';
```

#### 3.2.4 班级-学生关联表 class_student

```sql
CREATE TABLE class_student (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    class_id        BIGINT NOT NULL,
    student_id      BIGINT NOT NULL COMMENT '关联 student_profile.id',
    student_no      VARCHAR(50) COMMENT '班内学号',
    group_name      VARCHAR(50) COMMENT '分组名称（如：A组/B组）',
    tags            JSON COMMENT '学生标签 ["需要关注","进步明显"]',
    join_method     TINYINT NOT NULL DEFAULT 1 COMMENT '加入方式：1-邀请码 2-教师邀请 3-系统导入',
    join_status     TINYINT NOT NULL DEFAULT 0 COMMENT '0-待审核 1-已通过 2-已拒绝',
    joined_at       DATETIME COMMENT '通过审核时间',
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '0-已退出 1-正常',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_class_student (class_id, student_id),
    INDEX idx_student_id (student_id),
    INDEX idx_join_status (class_id, join_status),
    INDEX idx_class_group (class_id, group_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级学生关联表';
```

#### 3.2.5 作业/任务表 assignment

```sql
CREATE TABLE assignment (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    class_id        BIGINT NOT NULL COMMENT '目标班级',
    teacher_id      BIGINT NOT NULL COMMENT '发布教师',
    title           VARCHAR(200) NOT NULL COMMENT '作业标题',
    description     TEXT COMMENT '作业说明',
    assignment_type TINYINT NOT NULL COMMENT '类型：1-题库选题 2-AI生成 3-自定义题目 4-综合练习',
    subject_id      INT NOT NULL COMMENT '学科',
    content         JSON NOT NULL COMMENT '作业内容（结构化，见下方 JSON Schema）',
    knowledge_point_ids JSON COMMENT '关联知识点 ID 列表',
    difficulty      TINYINT COMMENT '难度：1-基础 2-中等 3-提高',
    total_score     DECIMAL(5,1) COMMENT '总分（如果适用）',
    
    -- 时间控制
    publish_at      DATETIME NOT NULL COMMENT '发布时间（定时发布）',
    deadline_at     DATETIME COMMENT '截止时间（NULL 表示不限时）',
    time_limit_min  INT COMMENT '限时（分钟），NULL 表示不限时',
    
    -- 完成规则
    allow_late_submit TINYINT NOT NULL DEFAULT 0 COMMENT '是否允许补交：0-否 1-是',
    late_submit_deadline DATETIME COMMENT '补交截止时间',
    show_answer_after TINYINT NOT NULL DEFAULT 1 COMMENT '提交后是否显示答案：0-不显示 1-立即显示 2-截止后显示',
    
    -- 状态
    status          TINYINT NOT NULL DEFAULT 0 COMMENT '0-草稿 1-已发布 2-已截止 3-已批改 4-已归档',
    published_at    DATETIME COMMENT '实际发布时间',
    
    -- 统计（冗余字段，异步更新）
    total_students  INT NOT NULL DEFAULT 0 COMMENT '应提交人数',
    submitted_count INT NOT NULL DEFAULT 0 COMMENT '已提交人数',
    graded_count    INT NOT NULL DEFAULT 0 COMMENT '已批改人数',
    avg_score       DECIMAL(5,2) COMMENT '平均分',
    completion_rate DECIMAL(5,4) COMMENT '完成率',
    
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_class (class_id),
    INDEX idx_teacher (teacher_id),
    INDEX idx_status_publish (status, publish_at),
    INDEX idx_deadline (deadline_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='作业/任务表';
```

#### 3.2.6 作业内容 JSON Schema

```json
{
    "assignment_content": {
        "type": "object",
        "required": ["sections"],
        "properties": {
            "sections": {
                "type": "array",
                "items": {
                    "type": "object",
                    "required": ["title", "items"],
                    "properties": {
                        "title": { "type": "string", "description": "大题标题，如'一、选择题'" },
                        "description": { "type": "string" },
                        "items": {
                            "type": "array",
                            "items": {
                                "oneOf": [
                                    {
                                        "type": "object",
                                        "description": "题库题目引用",
                                        "required": ["question_id"],
                                        "properties": {
                                            "question_id": { "type": "integer" },
                                            "score": { "type": "number" }
                                        }
                                    },
                                    {
                                        "type": "object",
                                        "description": "AI 生成题目参数",
                                        "required": ["prompt", "knowledge_point_ids"],
                                        "properties": {
                                            "prompt": { "type": "string" },
                                            "knowledge_point_ids": { "type": "array", "items": { "type": "integer" } },
                                            "difficulty": { "type": "integer" },
                                            "count": { "type": "integer" },
                                            "score_per_question": { "type": "number" }
                                        }
                                    },
                                    {
                                        "type": "object",
                                        "description": "自定义题目",
                                        "required": ["content"],
                                        "properties": {
                                            "content": { "type": "string", "description": "题干（支持 LaTeX）" },
                                            "options": { "type": "array", "items": { "type": "string" } },
                                            "answer": { "type": "string" },
                                            "explanation": { "type": "string" },
                                            "question_type": { "type": "string", "enum": ["choice", "fill", "short_answer", "essay"] },
                                            "score": { "type": "number" }
                                        }
                                    }
                                ]
                            }
                        }
                    }
                }
            }
        }
    }
}
```

#### 3.2.7 学生作业实例表 assignment_task

```sql
CREATE TABLE assignment_task (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    assignment_id   BIGINT NOT NULL,
    student_id      BIGINT NOT NULL,
    class_id        BIGINT NOT NULL,
    
    -- 提交信息
    status          TINYINT NOT NULL DEFAULT 0 COMMENT '0-未开始 1-进行中 2-已提交 3-已批改 4-超时未交',
    started_at      DATETIME COMMENT '开始时间',
    submitted_at    DATETIME COMMENT '提交时间',
    time_spent_sec  INT COMMENT '耗时（秒）',
    
    -- 答题内容
    answers         JSON COMMENT '答题内容，见下方 Schema',
    score           DECIMAL(5,1) COMMENT '得分',
    auto_score      DECIMAL(5,1) COMMENT '自动批改得分',
    manual_score    DECIMAL(5,1) COMMENT '人工批改得分（教师手动调整）',
    
    -- AI 批改辅助
    ai_feedback     JSON COMMENT 'AI 批改反馈',
    
    -- 教师批改
    graded_by       BIGINT COMMENT '批改教师 ID',
    graded_at       DATETIME COMMENT '批改时间',
    teacher_comment TEXT COMMENT '教师评语',
    
    -- 错题自动收录
    mistakes_generated TINYINT NOT NULL DEFAULT 0 COMMENT '是否已生成错题记录：0-否 1-是',
    
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_assignment_student (assignment_id, student_id),
    INDEX idx_student (student_id),
    INDEX idx_class_status (class_id, status),
    INDEX idx_graded (graded_by, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学生作业实例表';
```

#### 3.2.8 答题内容 JSON Schema

```json
{
    "answers": {
        "type": "array",
        "items": {
            "type": "object",
            "required": ["section_index", "item_index"],
            "properties": {
                "section_index": { "type": "integer" },
                "item_index": { "type": "integer" },
                "question_id": { "type": "integer", "description": "题库题目 ID（如果是题库题）" },
                "answer": { 
                    "description": "学生答案",
                    "oneOf": [
                        { "type": "string" },
                        { "type": "array", "items": { "type": "string" } }
                    ]
                },
                "images": { 
                    "type": "array", 
                    "items": { "type": "string" },
                    "description": "手写拍照上传的图片 URL 列表"
                },
                "is_correct": { "type": "boolean" },
                "score": { "type": "number" },
                "auto_result": {
                    "type": "object",
                    "properties": {
                        "is_correct": { "type": "boolean" },
                        "score": { "type": "number" },
                        "feedback": { "type": "string" }
                    }
                }
            }
        }
    }
}
```

#### 3.2.9 班级通知表 class_notice

```sql
CREATE TABLE class_notice (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    class_id        BIGINT NOT NULL,
    teacher_id      BIGINT NOT NULL,
    title           VARCHAR(200) NOT NULL,
    content         TEXT NOT NULL,
    notice_type     TINYINT NOT NULL DEFAULT 1 COMMENT '1-普通通知 2-作业提醒 3-考试通知 4-家长信',
    priority        TINYINT NOT NULL DEFAULT 1 COMMENT '1-普通 2-重要 3-紧急',
    target_audience TINYINT NOT NULL DEFAULT 1 COMMENT '1-全班学生 2-全班家长 3-学生和家长',
    require_confirm TINYINT NOT NULL DEFAULT 0 COMMENT '是否需要确认回执',
    confirm_deadline DATETIME COMMENT '确认截止时间',
    
    -- 附件
    attachments     JSON COMMENT '附件列表 [{"name":"","url":"","size":0}]',
    
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '0-已撤回 1-已发布 2-已过期',
    published_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    -- 统计
    confirm_count   INT NOT NULL DEFAULT 0 COMMENT '已确认人数',
    read_count      INT NOT NULL DEFAULT 0 COMMENT '已读人数',
    
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_class (class_id),
    INDEX idx_teacher (teacher_id),
    INDEX idx_status_time (status, published_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级通知表';
```

#### 3.2.10 通知确认表 class_notice_confirm

```sql
CREATE TABLE class_notice_confirm (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    notice_id       BIGINT NOT NULL,
    user_id         BIGINT NOT NULL COMMENT '确认人 user_id（学生或家长）',
    user_type       TINYINT NOT NULL COMMENT '1-学生 2-家长',
    confirmed_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_notice_user (notice_id, user_id, user_type),
    INDEX idx_notice_id (notice_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通知确认记录表';
```

---

## 4. API 接口设计

### 4.1 接口规范

所有接口遵循 `API网关与通用接口规范-详细设计.md` 中的统一规范：
- 基础路径：`/api/v1/teacher/`
- 认证方式：Bearer Token (JWT)
- 响应格式：`{ "code": 0, "message": "success", "data": {...} }`
- 错误码段：`50xxx`

### 4.2 教师认证相关

#### POST /api/v1/teacher/register
教师注册（在已有用户账号基础上申请教师身份）

```json
// Request
{
    "real_name": "张老师",
    "phone": "13800138000",
    "school_name": "北京市第一中学",
    "school_code": "BJ001",
    "title": "高级教师",
    "subject_ids": [1, 3],        // 语文、英语
    "cert_no": "T20251100001",    // 教师资格证号（可选）
    "cert_images": ["https://..."] // 资格证照片（可选）
}

// Response
{
    "code": 0,
    "data": {
        "teacher_id": 1001,
        "cert_status": 0,  // 待认证
        "message": "教师身份申请已提交，预计1-3个工作日审核"
    }
}
```

#### GET /api/v1/teacher/profile
获取教师信息

#### PUT /api/v1/teacher/profile
更新教师信息

### 4.3 班级管理

#### POST /api/v1/teacher/classes
创建班级

```json
// Request
{
    "class_name": "高一(3)班",
    "class_type": 1,            // 行政班
    "school_name": "北京市第一中学",
    "grade_id": 10,             // 高一
    "subject_id": null,         // 全科
    "academic_year": "2025-2026",
    "semester": 1,
    "description": "高一3班数学课代表群",
    "max_students": 60
}

// Response
{
    "code": 0,
    "data": {
        "class_id": 2001,
        "class_code": "A3K9F2",   // 6位邀请码
        "qr_code_url": "https://cdn.primetop.com/qr/class_2001.png",
        "created_at": "2026-05-20T10:00:00+08:00"
    }
}
```

#### GET /api/v1/teacher/classes
获取我管理的班级列表

```
Query: ?status=1&page=1&page_size=20&class_type=1
```

#### GET /api/v1/teacher/classes/{classId}
获取班级详情（含学生数、最近作业、统计概要）

#### PUT /api/v1/teacher/classes/{classId}
更新班级信息

#### POST /api/v1/teacher/classes/{classId}/archive
归档班级（毕业/学期结束）

#### POST /api/v1/teacher/classes/{classId}/refresh-code
刷新邀请码

### 4.4 学生管理

#### GET /api/v1/teacher/classes/{classId}/students
获取班级学生列表

```
Query: ?page=1&page_size=50&group=A组&tag=需要关注&join_status=1&sort=student_no
```

```json
// Response
{
    "code": 0,
    "data": {
        "total": 45,
        "items": [
            {
                "student_id": 3001,
                "real_name": "王小明",
                "student_no": "01",
                "group_name": "A组",
                "tags": ["进步明显"],
                "avatar_url": "...",
                "joined_at": "2026-03-01",
                "last_active_at": "2026-05-19",
                "study_stats": {
                    "total_study_minutes": 1200,
                    "question_count": 156,
                    "mistake_count": 23,
                    "avg_accuracy": 0.82
                }
            }
        ],
        "groups": ["A组", "B组", "C组"],
        "pending_count": 3   // 待审核人数
    }
}
```

#### POST /api/v1/teacher/classes/{classId}/students/approve
批量审批学生加入

```json
{
    "student_ids": [3001, 3002, 3003],
    "action": "approve"  // approve | reject
}
```

#### PUT /api/v1/teacher/classes/{classId}/students/{studentId}
更新学生信息（学号、分组、标签）

#### DELETE /api/v1/teacher/classes/{classId}/students/{studentId}
移除学生

#### POST /api/v1/teacher/classes/{classId}/students/import
批量导入学生

```json
{
    "students": [
        {"name": "张三", "student_no": "01", "group": "A组"},
        {"name": "李四", "student_no": "02", "group": "B组"}
    ]
}
// 系统通过姓名+班级匹配已有的 PrimeTop 学生账号
```

### 4.5 作业管理

#### POST /api/v1/teacher/assignments
创建作业

```json
{
    "class_id": 2001,
    "title": "第三章 一元二次方程 综合练习",
    "description": "请认真完成，注意审题",
    "assignment_type": 1,
    "subject_id": 3,  // 数学
    "content": {
        "sections": [
            {
                "title": "一、选择题（每题5分）",
                "items": [
                    {"question_id": 50001, "score": 5},
                    {"question_id": 50002, "score": 5},
                    {"question_id": 50003, "score": 5}
                ]
            },
            {
                "title": "二、解答题",
                "items": [
                    {
                        "content": "解方程 $x^2 - 5x + 6 = 0$，并写出解题过程。",
                        "answer": "$x_1=2, x_2=3$",
                        "explanation": "使用因式分解法...",
                        "question_type": "short_answer",
                        "score": 10
                    }
                ]
            }
        ]
    },
    "knowledge_point_ids": [301, 305],
    "difficulty": 2,
    "publish_at": "2026-05-21T08:00:00+08:00",
    "deadline_at": "2026-05-22T22:00:00+08:00",
    "allow_late_submit": true,
    "late_submit_deadline": "2026-05-23T22:00:00+08:00",
    "show_answer_after": 2,  // 截止后显示
    "time_limit_min": 60
}
```

#### GET /api/v1/teacher/assignments
获取作业列表

```
Query: ?class_id=2001&status=1&page=1&page_size=20
```

#### GET /api/v1/teacher/assignments/{assignmentId}
获取作业详情（含统计）

#### PUT /api/v1/teacher/assignments/{assignmentId}
更新作业（仅草稿状态可修改）

#### DELETE /api/v1/teacher/assignments/{assignmentId}
删除作业（仅草稿状态）

#### POST /api/v1/teacher/assignments/{assignmentId}/publish
立即发布（修改 publish_at 为当前时间）

### 4.6 作业批改

#### GET /api/v1/teacher/assignments/{assignmentId}/tasks
获取学生提交列表

```
Query: ?status=2&page=1&page_size=50&sort=submit_time
```

```json
// Response
{
    "code": 0,
    "data": {
        "total": 42,
        "submitted_count": 38,
        "unsubmitted_count": 4,
        "avg_score": 78.5,
        "items": [
            {
                "task_id": 4001,
                "student_id": 3001,
                "student_name": "王小明",
                "status": 2,
                "submitted_at": "2026-05-21T19:30:00+08:00",
                "time_spent_sec": 2400,
                "score": null,          // 未批改
                "auto_score": 85.0,     // AI 自动判分
                "ai_feedback": {
                    "overall": "完成情况良好",
                    "weak_points": ["第二题计算过程有误"],
                    "suggested_score": 85
                }
            }
        ]
    }
}
```

#### GET /api/v1/teacher/assignments/{assignmentId}/tasks/{taskId}
获取单个学生作业详情（含答题内容）

#### POST /api/v1/teacher/assignments/{assignmentId}/tasks/{taskId}/grade
批改学生作业

```json
{
    "scores": [
        {
            "section_index": 0,
            "item_index": 0,
            "score": 5,
            "comment": "正确"
        },
        {
            "section_index": 0,
            "item_index": 1,
            "score": 3,
            "comment": "方法正确但计算有误，$x_1=2$ 正确，$x_2$ 应为3"
        }
    ],
    "total_score": 43,
    "teacher_comment": "整体完成不错，注意计算准确率",
    "mistakes_to_record": [1]  // section.item 索引，标记需要录入错题本的题目
}
```

#### POST /api/v1/teacher/assignments/{assignmentId}/tasks/batch-grade
批量快速批改（选择题等自动批改结果确认）

```json
{
    "task_ids": [4001, 4002, 4003],
    "confirm_auto_score": true,  // 确认使用 AI 自动批改分数
    "teacher_comment_template": "已完成批改"
}
```

### 4.7 班级学情分析

#### GET /api/v1/teacher/classes/{classId}/analytics/overview
班级学情概览

```json
// Response
{
    "code": 0,
    "data": {
        "class_name": "高一(3)班",
        "total_students": 45,
        "period": "2026-05-13 ~ 2026-05-19",
        "study_stats": {
            "avg_daily_minutes": 35.2,
            "active_students": 42,
            "active_rate": 0.933
        },
        "subject_stats": [
            {
                "subject_id": 1,
                "subject_name": "语文",
                "avg_accuracy": 0.85,
                "weak_knowledge_points": [
                    {"id": 101, "name": "文言文翻译", "avg_accuracy": 0.52, "affecte