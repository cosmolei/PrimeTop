# 客户端-成就勋章与奖励展示组件 详细设计

## 1. 模块概述

### 1.1 功能定位

成就勋章与奖励展示组件是面向学生端的激励可视化层，负责将服务端的成就数据（徽章、等级、积分、连续天数、里程碑等）转化为富有吸引力的视觉呈现和交互体验，驱动学生的学习动力和持续活跃。

### 1.2 设计原则

| 原则 | 说明 |
|------|------|
| 即时反馈 | 成就解锁后 200ms 内触发视觉反馈 |
| 分龄适配 | 幼儿→卡通/大图标；高中→简洁/数据化 |
| 离线可用 | 本地缓存成就数据，弱网可展示 |
| 低侵入 | 不干扰学习流程，以弹窗/角标等轻量方式展示 |
| 可分享 | 关键成就可生成分享海报 |

### 1.3 依赖模块

| 依赖 | 说明 |
|------|------|
| 学生成就与学习里程碑追踪引擎 | 提供 achievement、milestone API |
| 积分与虚拟经济体系 | 提供 points、balance、transaction API |
| 运营活动与成长激励体系 | 提供 check-in、challenge、rank API |
| 客户端状态管理架构 | Riverpod Provider 体系 |
| 客户端主题引擎与动态外观系统 | 分龄主题适配 |
| 客户端本地持久层与数据管理 | Hive 本地缓存 |
| 客户端动画与手势交互系统 | 动画基础设施 |

---

## 2. 数据结构定义

### 2.1 客户端数据模型

```dart
/// 成就徽章
@HiveType(typeId: 42)
class AchievementBadge extends HiveObject {
  @HiveField(0)
  final String badgeId;            // 徽章唯一标识 "badge_math_streak_30"

  @HiveField(1)
  final String category;           // 分类: learning / social / milestone / special

  @HiveField(2)
  final String name;               // 名称 "数学达人"

  @HiveField(3)
  final String description;        // 描述 "连续30天完成数学练习"

  @HiveField(4)
  final int tier;                  // 等级: 1=铜, 2=银, 3=金, 4=铂金, 5=钻石

  @HiveField(5)
  final String iconAsset;          // 图标资源路径

  @HiveField(6)
  final String? unlockedAt;        // 解锁时间 ISO8601，null 表示未解锁

  @HiveField(7)
  final int currentProgress;       // 当前进度值

  @HiveField(8)
  final int targetProgress;        // 目标进度值

  @HiveField(9)
  final bool isSecret;             // 是否为隐藏成就（未解锁前显示问号）

  @HiveField(10)
  final bool isNew;                // 是否新解锁（用于红点提示）

  @HiveField(11)
  final String? sharePosterTemplate; // 分享海报模板ID

  bool get isUnlocked => unlockedAt != null;
  double get progressRatio => targetProgress > 0 
      ? (currentProgress / targetProgress).clamp(0.0, 1.0) 
      : 0.0;
  String get tierLabel => const ['铜', '银', '金', '铂金', '钻石'][tier - 1];
}

/// 用户等级信息
@HiveType(typeId: 43)
class UserLevelInfo extends HiveObject {
  @HiveField(0)
  final int level;                 // 当前等级 1-100

  @HiveField(1)
  final int currentExp;            // 当前经验值

  @HiveField(2)
  final int nextLevelExp;          // 升级所需经验值

  @HiveField(3)
  final String title;              // 等级称号 "学习新星"

  @HiveField(4)
  final String? titleIcon;         // 称号图标

  @HiveField(5)
  final int totalBadges;           // 拥有徽章总数

  @HiveField(6)
  final int totalBadgesGold;       // 金色以上徽章数

  double get expRatio => nextLevelExp > 0 
      ? (currentExp / nextLevelExp).clamp(0.0, 1.0) 
      : 0.0;
}

/// 连续打卡数据
@HiveType(typeId: 44)
class StreakData extends HiveObject {
  @HiveField(0)
  final int currentStreak;         // 当前连续天数

  @HiveField(1)
  final int longestStreak;         // 历史最长连续天数

  @HiveField(2)
  final List<bool> weeklyCheckIn;  // 本周7天打卡状态 [Mon..Sun]

  @HiveField(3)
  final List<MonthlyDayStatus> monthlyDays; // 当月每日状态

  @HiveField(4)
  final String? lastCheckInDate;   // 最后打卡日期 yyyy-MM-dd

  @HiveField(5)
  final bool isCheckedInToday;     // 今日是否已打卡

  bool get isAtRisk => !isCheckedInToday; // 今日未打卡（即将断签风险）
}

/// 月度单日状态
@HiveType(typeId: 45)
class MonthlyDayStatus extends HiveObject {
  @HiveField(0)
  final int day;                   // 日期 1-31

  @HiveField(1)
  final bool checkedIn;            // 是否打卡

  @HiveField(2)
  final int studyMinutes;          // 学习分钟数

  @HiveField(3)
  final int expEarned;             // 获得经验值
}

/// 积分展示数据
@HiveType(typeId: 46)
class PointsDisplayData extends HiveObject {
  @HiveField(0)
  final int balance;               // 当前积分余额

  @HiveField(1)
  final int totalEarned;           // 累计获得

  @HiveField(2)
  final int totalSpent;            // 累计消耗

  @HiveField(3)
  final List<PointsRecord> recentRecords; // 最近交易记录（展示用）

  @HiveField(4)
  final int todayEarned;           // 今日获得

  @HiveField(5)
  final int? nextMilestone;        // 下一里程碑积分（如10000分）
}

/// 积分交易记录
@HiveType(typeId: 47)
class PointsRecord extends HiveObject {
  @HiveField(0)
  final String id;

  @HiveField(1)
  final int amount;                // 正数=获得, 负数=消耗

  @HiveField(2)
  final String reason;             // 原因: "完成每日任务" / "兑换主题"

  @HiveField(3)
  final String createdAt;          // ISO8601

  @HiveField(4)
  final String? relatedId;         // 关联资源ID
}

/// 排行榜条目
class LeaderboardEntry {
  final int rank;
  final String userId;
  final String nickname;
  final String? avatarUrl;
  final int score;                 // 积分/学习时长/徽章数
  final int level;
  final bool isSelf;               // 是否当前用户

  const LeaderboardEntry({
    required this.rank,
    required this.userId,
    required this.nickname,
    this.avatarUrl,
    required this.score,
    required this.level,
    this.isSelf = false,
  });
}

/// 成就解锁通知
class AchievementUnlockEvent {
  final String badgeId;
  final String name;
  final String description;
  final int tier;
  final String iconAsset;
  final int bonusExp;              // 解锁奖励经验
  final int bonusPoints;           // 解锁奖励积分
  final bool canShare;             // 是否可分享
}

/// 等级升级事件
class LevelUpEvent {
  final int oldLevel;
  final int newLevel;
  final String newTitle;
  final List<String> unlockedFeatures; // 新解锁的功能/权益
  final int bonusPoints;
}
```

### 2.2 服务端 API 接口

#### 2.2.1 获取成就徽章列表

```
GET /api/v1/achievements/badges
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| category | string | 否 | 按分类筛选: learning/social/milestone/special |
| unlocked_only | bool | 否 | 仅返回已解锁的 |
| page_token | string | 否 | 分页游标 |
| page_size | int | 否 | 每页数量，默认 20 |

**响应：**

```json
{
  "code": 0,
  "data": {
    "badges": [
      {
        "badge_id": "badge_math_streak_30",
        "category": "learning",
        "name": "数学达人",
        "description": "连续30天完成数学练习",
        "tier": 3,
        "icon_asset": "badges/math_streak_gold.webp",
        "unlocked_at": "2026-05-20T14:30:00+08:00",
        "current_progress": 30,
        "target_progress": 30,
        "is_secret": false,
        "is_new": false,
        "share_poster_template": "achievement_gold"
      }
    ],
    "total_count": 156,
    "unlocked_count": 23,
    "next_page_token": "eyJvZmZzZXQiOjIwfQ"
  }
}
```

#### 2.2.2 获取用户等级与经验

```
GET /api/v1/achievements/level
```

**响应：**

```json
{
  "code": 0,
  "data": {
    "level": 15,
    "current_exp": 2340,
    "next_level_exp": 3000,
    "title": "学习新星",
    "title_icon": "titles/learning_star.webp",
    "total_badges": 23,
    "total_badges_gold": 5
  }
}
```

#### 2.2.3 获取打卡连续数据

```
GET /api/v1/achievements/streak
```

**响应：**

```json
{
  "code": 0,
  "data": {
    "current_streak": 14,
    "longest_streak": 28,
    "weekly_check_in": [true, true, true, false, true, true, false],
    "monthly_days": [
      {"day": 1, "checked_in": true, "study_minutes": 45, "exp_earned": 30},
      {"day": 2, "checked_in": true, "study_minutes": 30, "exp_earned": 20}
    ],
    "last_check_in_date": "2026-05-26",
    "is_checked_in_today": true
  }
}
```

#### 2.2.4 执行打卡

```
POST /api/v1/achievements/check-in
```

**响应：**

```json
{
  "code": 0,
  "data": {
    "success": true,
    "current_streak": 15,
    "bonus_exp": 20,
    "bonus_points": 10,
    "streak_milestone": null,
    "new_badges": []
  }
}
```

#### 2.2.5 获取积分信息

```
GET /api/v1/points/balance
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| include_recent | bool | 否 | 包含最近交易记录，默认 true |
| recent_limit | int | 否 | 最近记录条数，默认 10 |

**响应：**

```json
{
  "code": 0,
  "data": {
    "balance": 1280,
    "total_earned": 5600,
    "total_spent": 4320,
    "today_earned": 40,
    "next_milestone": 10000,
    "recent_records": [
      {
        "id": "ptx_20260527_001",
        "amount": 10,
        "reason": "每日打卡",
        "created_at": "2026-05-27T08:00:00+08:00",
        "related_id": null
      }
    ]
  }
}
```

#### 2.2.6 获取排行榜

```
GET /api/v1/achievements/leaderboard
```

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| type | string | 是 | 排行类型: exp/badges/streak/points |
| period | string | 是 | 周期: daily/weekly/monthly/all |
| subject | string | 否 | 按学科筛选（仅 exp 类型） |
| page_size | int | 否 | 默认 50 |

**响应：**

```json
{
  "code": 0,
  "data": {
    "type": "exp",
    "period": "weekly",
    "entries": [
      {
        "rank": 1,
        "user_id": "u_12345",
        "nickname": "小明",
        "avatar_url": "https://cdn.example.com/avatars/12345.webp",
        "score": 560,
        "level": 22,
        "is_self": false
      }
    ],
    "my_rank": {
      "rank": 45,
      "score": 120,
      "level": 15
    },
    "updated_at": "2026-05-27T09:00:00+08:00"
  }
}
```

#### 2.2.7 标记成就已读

```
POST /api/v1/achievements/badges/{badge_id}/read
```

**响应：**

```json
{
  "code": 0,
  "data": {
    "success": true
  }
}
```

#### 2.2.8 领取里程碑奖励

```
POST /api/v1/achievements/milestones/{milestone_id}/claim
```

**响应：**

```json
{
  "code": 0,
  "data": {
    "success": true,
    "rewards": {
      "points": 100,
      "exp": 50,
      "items": [
        {"type": "avatar_frame", "id": "frame_gold_star", "name": "金星头像框"}
      ]
    }
  }
}
```

---

## 3. 状态管理架构

### 3.1 Provider 体系

```dart
/// ============ 成就数据 Provider ============

/// 成就徽章列表（按分类分组）
@riverpod
class AchievementBadges extends _$AchievementBadges {
  @override
  FutureOr<Map<String, List<AchievementBadge>>> build() async {
    // 先读本地缓存
    final cached = await _loadFromHive();
    if (cached != null) {
      // 后台刷新
      ref.read(achievementRefreshProvider.notifier).trigger();
      return cached;
    }
    return await _fetchFromServer();
  }

  Future<Map<String, List<AchievementBadge>>> _fetchFromServer() async {
    final api = ref.read(apiClientProvider);
    final response = await api.get('/api/v1/achievements/badges', 
      queryParameters: {'page_size': 200});
    
    final badges = (response.data['badges'] as List)
        .map((b) => AchievementBadge.fromJson(b))
        .toList();

    // 按分类分组
    final grouped = <String, List<AchievementBadge>>{};
    for (final badge in badges) {
      grouped.putIfAbsent(badge.category, () => []).add(badge);
    }

    // 写入 Hive 缓存
    await _saveToHive(grouped);
    return grouped;
  }

  /// 标记徽章已读
  Future<void> markAsRead(String badgeId) async {
    final api = ref.read(apiClientProvider);
    await api.post('/api/v1/achievements/badges/$badgeId/read');
    
    // 更新本地状态
    state = AsyncData(state.value!.map((category, badges) =>
      MapEntry(category, badges.map((b) => 
        b.badgeId == badgeId ? b.copyWith(isNew: false) : b
      ).toList())
    ));
  }
}

/// 用户等级信息
@riverpod
class UserLevel extends _$UserLevel {
  @override
  FutureOr<UserLevelInfo> build() async {
    final box = await Hive.openBox<UserLevelInfo>('achievement_cache');
    final cached = box.get('user_level');
    if (cached != null) {
      _backgroundRefresh();
      return cached;
    }
    return await _fetch();
  }

  Future<UserLevelInfo> _fetch() async {
    final api = ref.read(apiClientProvider);
    final response = await api.get('/api/v1/achievements/level');
    final info = UserLevelInfo.fromJson(response.data);
    final box = await Hive.openBox<UserLevelInfo>('achievement_cache');
    await box.put('user_level', info);
    return info;
  }

  void _backgroundRefresh() async {
    try {
      final fresh = await _fetch();
      state = AsyncData(fresh);
    } catch (_) {}
  }
}

/// 打卡数据
@riverpod
class StreakInfo extends _$StreakInfo {
  @override
  FutureOr<StreakData> build() async {
    final box = await Hive.openBox<StreakData>('achievement_cache');
    final cached = box.get('streak');
    if (cached != null) {
      _backgroundRefresh();
      return cached;
    }
    return await _fetch();
  }

  Future<StreakData> _fetch() async {
    final api = ref.read(apiClientProvider);
    final response = await api.get('/api/v1/achievements/streak');
    final data = StreakData.fromJson(response.data);
    final box = await Hive.openBox<StreakData>('achievement_cache');
    await box.put('streak', data);
    return data;
  }

  /// 执行打卡
  Future<CheckInResult> checkIn() async {
    final api = ref.read(apiClientProvider);
    final response = await api.post('/api/v1/achievements/check-in');
    final result = CheckInResult.fromJson(response.data);
    
    // 刷新数据
    state = AsyncData(await _fetch());
    return result;
  }
}

/// 积分信息
@riverpod
class PointsInfo extends _$PointsInfo {
  @override
  FutureOr<PointsDisplayData> build() async {
    final api = ref.read(apiClientProvider);
    final response = await api.get('/api/v1/points/balance',
      queryParameters: {'include_recent': true, 'recent_limit': 10});
    return PointsDisplayData.fromJson(response.data);
  }
}

/// 排行榜
@riverpod
class Leaderboard extends _$Leaderboard {
  @override
  FutureOr<List<LeaderboardEntry>> build({
    required String type,
    required String period,
    String? subject,
  }) async {
    final api = ref.read(apiClientProvider);
    final response = await api.get('/api/v1/achievements/leaderboard',
      queryParameters: {
        'type': type,
        'period': period,
        if (subject != null) 'subject': subject,
        'page_size': 50,
      });
    return (response.data['entries'] as List)
        .map((e) => LeaderboardEntry.fromJson(e))
        .toList();
  }
}

/// 成就解锁弹窗队列
@riverpod
class AchievementPopupQueue extends _$AchievementPopupQueue {
  @override
  List<AchievementUnlockEvent> build() => [];

  void enqueue(AchievementUnlockEvent event) {
    state = [...state, event];
  }

  void dequeue() {
    if (state.isNotEmpty) {
      state = state.sublist(1);
    }
  }
}

/// 后台刷新触发器
@riverpod
class AchievementRefresh extends _$AchievementRefresh {
  @override
  bool build() => false;

  void trigger() {
    state = true;
    // 延迟执行，避免并发
    Future.delayed(const Duration(seconds: 2), () {
      ref.invalidate(achievementBadgesProvider);
      ref.invalidate(userLevelProvider);
      ref.invalidate(streakInfoProvider);
      state = false;
    });
  }
}
```

### 3.2 SSE 实时推送集成

```dart
/// 成就相关 SSE 事件监听
class AchievementSSEHandler {
  final Ref _ref;

  void handleEvent(SSEEvent event) {
    switch (event.eventType) {
      case 'achievement.unlocked':
        final data = AchievementUnlockEvent.fromJson(event.data);
        // 加入弹窗队列
        _ref.read(achievementPopupQueueProvider.notifier).enqueue(data);
        // 刷新徽章列表
        _ref.invalidate(achievementBadgesProvider);
        _ref.invalidate(userLevelProvider);
        break;

      case 'level.up':
        final data = LevelUpEvent.fromJson(event.data);
        _showLevelUpAnimation(data);
        _ref.invalidate(userLevelProvider);
        break;

      case 'check_in.reminder':
        // 不在成就组件处理，由通知系统处理
        break;

      case 'milestone.reached':
        final milestone = event.data['milestone_id'] as String;
        _ref.invalidate(userLevelProvider);
        break;
    }
  }

  void _showLevelUpAnimation(LevelUpEvent event) {
    // 全屏升级动画，见 4.5 节
    _ref.read(overlayManagerProvider).show(
      LevelUpOverlay(event: event),
    );
  }
}
```

---

## 4. 核心组件设计

### 4.1 徽章卡片组件（BadgeCard）

```dart
/// 单个徽章卡片
class BadgeCard extends ConsumerStatefulWidget {
  final AchievementBadge badge;
  final VoidCallback? onTap;
  final double size; // 基准尺寸，用于网格适配

  const BadgeCard({
    super.key,
    required this.badge,
    this.onTap,
    this.size = 80,
  });

  @override
  ConsumerState<BadgeCard> createState() => _BadgeCardState();
}

class _BadgeCardState extends ConsumerState<BadgeCard>
    with SingleTickerProviderStateMixin {
  late AnimationController _glowController;
  late Animation<double> _glowAnimation;

  @override
  void initState() {
    super.initState();
    _glowController = AnimationController(
      vsync: this,
      duration: const Duration(milliseconds: 1500),
    )..repeat(reverse: true);
    _glowAnimation = Tween<double>(begin: 0.3, begin: 1.0).animate(
      CurvedAnimation(parent: _glowController, curve: Curves.easeInOut),
    );
  }

  @override
  void dispose() {
    _glowController.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final theme = ref.watch(ageAppThemeProvider);
    final isUnlocked = widget.badge.isUnlocked;
    final isSecret = widget.badge.isSecret && !isUnlocked;

    return GestureDetector(
      onTap: widget.onTap ?? () => _showBadgeDetail(context),
      child: AnimatedContainer(
        duration: const Duration(milliseconds: 300),
        width: widget.size,
        height: widget.size,
        decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(theme.cornerRadiusMd),
          color: isUnlocked
              ? _tierColor(widget.badge.tier).withOpacity(0.1)
              : theme.colorScheme.surfaceVariant,
          border: Border.all(
            color: isUnlocked
                ? _tierColor(widget.badge.tier)
                : theme.colorScheme.outline.withOpacity(0.3),
            width: isUnlocked ? 2 : 1,
          ),
          boxShadow: isUnlocked && widget.badge.tier >= 4
              ? [
                  BoxShadow(
                    color: _tierColor(widget.badge.tier).withOpacity(0.3),
                    blurRadius: 12 * _glowAnimation.value,
                    spreadRadius: 2,
                  ),
                ]
              : null,
        ),
        child: Stack(
          clipBehavior: Clip.none,
          children: [
            // 图标区域
            Center(
              child: isSecret
                  ? Icon(Icons.lock_outline, size: widget.size * 0.35,
                      color: theme.colorScheme.outline)
                  : isUnlocked
                      ? _buildBadgeIcon(context)
                      : _buildLockedIcon(context),
            ),

            // 进度条（未解锁时）
            if (!isUnlocked && !isSecret)
              Positioned(
                left: 8,
                right: 8,
                bottom: 6,
                child: _buildProgressBar(context),
              ),

            // 新解锁红点
            if (widget.badge.isNew)
              Positioned(
                top: -2,
                right: -2,
                child: Container(
                  width: 12,
                  height: 12,
                  decoration: BoxDecoration(
                    color: theme.colorScheme.error,
                    shape: BoxShape.circle,
                    border: Border.all(color: theme.colorScheme.surface, width: 2),
                  ),
                ),
              ),
          ],
        ),
      ),
    );
  }

  Widget _buildBadgeIcon(BuildContext context) {
    // 加载徽章图标，支持 Lottie 动画图标
    final asset = widget.badge.iconAsset;
    if (asset.endsWith('.json')) {
      return Lottie.asset(
        'assets/animations/$asset',
        width: widget.size * 0.5,
        height: widget.size * 0.5,
      );
    }
    return Image.asset(
      'assets/images/$asset',
      width: widget.size * 0.5,
      height: widget.size * 0.5,
      errorBuilder: (_, __, ___) => Icon(
        Icons.emoji_events,
        size: widget.size * 0.45,
        color: _tierColor(widget.badge.tier),
      ),
    );
  }

  Widget _buildLockedIcon(BuildContext context) {
    final theme = ref.watch(ageAppThemeProvider);
    return Opacity(
      opacity: 0.4,
      child: Image.asset(
        'assets/images/${widget.badge.iconAsset}',
        width: widget.size * 0.5,
        height: widget.size * 0.5,
        color: theme.colorScheme.onSurface.withOpacity(0.3),
        colorBlendMode: BlendMode.srcIn,
        errorBuilder: (_, __, ___) => Icon(
          Icons.emoji_events_outlined,
          size: widget.size * 0.45,
        ),
      ),
    );
  }

  Widget _buildProgressBar(BuildContext context) {
    final theme = ref.watch(ageAppThemeProvider);
    return ClipRRect(
      borderRadius: BorderRadius.circular(2),
      child: LinearProgressIndicator(
        value: widget.badge.progressRatio,
        backgroundColor: theme.colorScheme.outline.withOpacity(0.15),
        valueColor: AlwaysStoppedAnimation(
          _tierColor(widget.badge.tier).withOpacity(0.7),
        ),
        minHeight: 3,
      ),
    );
  }

  void _showBadgeDetail(BuildContext context) {
    showModalBottomSheet(
      context: context,
      isScrollControlled: true,
      backgroundColor: Colors.transparent,
      builder: (_) => BadgeDetailSheet(badge: widget.badge),
    );
  }

  Color _tierColor(int tier) {
    switch (tier) {
      case 1: return const Color(0xFFCD7F32); // 铜
      case 2: return const Color(0xFFC0C0C0); // 银
      case 3: return const Color(0xFFFFD700); // 金
      case 4: return const Color(0xFFE5E4E2); // 铂金
      case 5: return const Color(0xFFB9F2FF); // 钻石
      default: return Colors.grey