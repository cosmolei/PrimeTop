# 服务端通用数据导入服务与批量操作引擎 - 详细设计

## 1. 概述

### 1.1 背景与目标

PrimeTop 作为全学段 AI 辅助学习平台，运营初期及日常运营中存在大量批量数据处理需求：

| 场景 | 数据量 | 来源格式 | 触发方式 |
|------|--------|----------|----------|
| 题库批量导入 | 1000~50000 条/次 | Excel/CSV/JSON | 管理后台运营人员 |
| B端机构学生批量注册 | 100~5000 人/次 | Excel/CSV | 机构管理员 |
| 教材知识点批量导入 | 500~20000 条/次 | Excel/JSON | 内容运营 |
| 错题/学习记录迁移 | 1000~100000 条/次 | JSON/CSV | 系统迁移 |
| 题目标签批量更新 | 500~10000 条/次 | Excel/CSV | 运营后台 |
| 内容批量发布/下架 | 100~5000 条/次 | JSON (API) | 管理后台 |
| 用户权益批量发放 | 100~10000 人/次 | Excel/JSON | 运营活动 |

**目标**：构建一套通用的数据导入与批量操作引擎，支持多格式解析、异步处理、进度追踪、错误汇总、回滚机制，让各业务模块通过声明式配置即可接入，无需重复开发。

### 1.2 设计原则

1. **声明式配置**：业务模块通过 YAML/数据类声明导入规则，无需编写解析逻辑
2. **异步处理**：大数量导入走 Celery 异步队列，不阻塞 API 请求
3. **原子性与幂等**：支持事务级别回滚和幂等重试
4. **错误友好**：逐行校验，收集全部错误而非首错即停
5. **进度可观测**：实时进度查询、WebSocket 推送
6. **安全可控**：文件大小/行数限制、权限校验、操作审计

### 1.3 术语定义

| 术语 | 含义 |
|------|------|
| ImportJob | 一次导入任务实例 |
| ImportTemplate | 导入模板定义（字段映射、校验规则） |
| Batch | 导入任务中的一次批量处理单元（默认 100 条） |
| ImportRow | 单行数据记录，含原始值、校验结果、处理状态 |
| DryRun | 试运行模式，仅校验不实际写入 |

---

## 2. 整体架构

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                    管理后台 / B端管理台                      │
│  ┌──────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │ 模板下载  │  │ 文件上传     │  │  进度查看/错误下载  │  │
│  └──────────┘  └──────┬───────┘  └────────▲──────────┘  │
└────────────────────────┼──────────────────┼─────────────┘
                         │                  │
┌────────────────────────┼──────────────────┼─────────────┐
│                    API 服务层                            │
│  ┌──────────┐  ┌───────▼───────┐  ┌───────┴──────────┐  │
│  │TemplateAPI│  │ ImportAPI    │  │ ProgressAPI      │  │
│  └──────────┘  └───────┬───────┘  └──────────────────┘  │
└────────────────────────┼────────────────────────────────┘
                         │
┌────────────────────────┼────────────────────────────────┐
│              数据导入引擎 (ImportEngine)                  │
│  ┌─────────┐  ┌───────▼───────┐  ┌──────────────────┐  │
│  │ 模板注册表│  │ 导入调度器    │  │  进度追踪器      │  │
│  └─────────┘  └───────┬───────┘  └──────────────────┘  │
│  ┌─────────┐  ┌───────▼───────┐  ┌──────────────────┐  │
│  │ 文件解析器│  │ 校验引擎      │  │  错误收集器      │  │
│  └─────────┘  └───────┬───────┘  └──────────────────┘  │
│  ┌─────────┐  ┌───────▼───────┐  ┌──────────────────┐  │
│  │ 类型转换器│  │ 批量写入器    │  │  回滚管理器      │  │
│  └─────────┘  └───────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────┼────────────────────────────────┐
│                  Celery 异步队列                         │
│  ┌──────────┐  ┌───────▼───────┐  ┌──────────────────┐  │
│  │ 解析任务  │  │ 批次处理任务   │  │  后处理任务      │  │
│  └──────────┘  └───────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────┼────────────────────────────────┐
│                    存储层                                │
│  ┌──────┐  ┌──────────┐  ┌─────────┐  ┌─────────────┐  │
│  │ MySQL│  │Redis(进度)│  │MinIO(文件)│ │ClickHouse   │  │
│  │ 业务表│  │          │  │         │  │(导入日志)    │  │
│  └──────┘  └──────────┘  └─────────┘  └─────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 核心流程

```
上传文件 → 创建ImportJob → 异步解析 → 分批校验 → 分批写入 → 生成报告
                                         ↓              ↓
                                    错误收集 ←──── 失败回滚
```

---

## 3. 数据模型

### 3.1 import_template（导入模板定义表）

```sql
CREATE TABLE import_template (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    template_code   VARCHAR(64) NOT NULL UNIQUE COMMENT '模板编码: question_bank_import',
    template_name   VARCHAR(128) NOT NULL COMMENT '模板名称',
    description     VARCHAR(512) COMMENT '模板说明',
    target_module   VARCHAR(64) NOT NULL COMMENT '目标模块: question/student/knowledge_point/content',
    target_table    VARCHAR(128) COMMENT '目标数据表',
    handler_class   VARCHAR(256) NOT NULL COMMENT '业务处理器类路径',
    field_mappings  JSON NOT NULL COMMENT '字段映射配置',
    validation_rules JSON COMMENT '校验规则配置',
    unique_keys     JSON COMMENT '去重键定义',
    default_values  JSON COMMENT '默认值配置',
    file_max_size_mb INT NOT NULL DEFAULT 20 COMMENT '文件最大体积(MB)',
    row_limit       INT NOT NULL DEFAULT 50000 COMMENT '单次最大行数',
    required_permissions JSON COMMENT '所需权限编码列表',
    template_file_url VARCHAR(512) COMMENT '模板文件下载地址',
    is_active       TINYINT(1) NOT NULL DEFAULT 1,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_target_module (target_module)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='导入模板定义表';
```

### 3.2 import_job（导入任务表）

```sql
CREATE TABLE import_job (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    job_no          VARCHAR(32) NOT NULL UNIQUE COMMENT '任务编号: IMP-20260525-XXXXX',
    template_id     BIGINT NOT NULL,
    template_code   VARCHAR(64) NOT NULL,
    status          VARCHAR(20) NOT NULL DEFAULT 'pending'
        COMMENT 'pending/parsing/validating/importing/completed/partial_completed/failed/cancelled/rolling_back/rolled_back',
    file_url        VARCHAR(512) NOT NULL,
    file_name       VARCHAR(256) NOT NULL,
    file_size       BIGINT NOT NULL DEFAULT 0,
    file_hash       VARCHAR(64),
    total_rows      INT NOT NULL DEFAULT 0,
    processed_rows  INT NOT NULL DEFAULT 0,
    success_rows    INT NOT NULL DEFAULT 0,
    failed_rows     INT NOT NULL DEFAULT 0,
    skipped_rows    INT NOT NULL DEFAULT 0,
    error_file_url  VARCHAR(512),
    dry_run         TINYINT(1) NOT NULL DEFAULT 0,
    options         JSON COMMENT '导入选项',
    celery_task_id  VARCHAR(128),
    operator_id     BIGINT NOT NULL,
    operator_type   VARCHAR(20) NOT NULL,
    started_at      DATETIME,
    completed_at    DATETIME,
    error_summary   JSON,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_status (status),
    INDEX idx_template_code (template_code),
    INDEX idx_operator (operator_id, operator_type),
    INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='导入任务表';
```

### 3.3 import_batch（导入批次表）

```sql
CREATE TABLE import_batch (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    job_id          BIGINT NOT NULL,
    batch_index     INT NOT NULL,
    status          VARCHAR(20) NOT NULL DEFAULT 'pending',
    row_start       INT NOT NULL,
    row_end         INT NOT NULL,
    success_count   INT NOT NULL DEFAULT 0,
    failed_count    INT NOT NULL DEFAULT 0,
    skipped_count   INT NOT NULL DEFAULT 0,
    celery_task_id  VARCHAR(128),
    error_details   JSON,
    started_at      DATETIME,
    completed_at    DATETIME,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_job_id (job_id),
    UNIQUE KEY uk_job_batch (job_id, batch_index)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='导入批次表';
```

### 3.4 import_error_detail（导入错误明细表）

```sql
CREATE TABLE import_error_detail (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    job_id          BIGINT NOT NULL,
    batch_id        BIGINT,
    row_number      INT NOT NULL COMMENT '行号(1-based)',
    row_data        JSON,
    error_type      VARCHAR(32) NOT NULL
        COMMENT 'parse_error/validation_error/duplicate_error/write_error/ref_error',
    error_code      VARCHAR(32),
    field_name      VARCHAR(64),
    field_value     VARCHAR(256),
    error_message   VARCHAR(1024) NOT NULL,
    severity        VARCHAR(10) NOT NULL DEFAULT 'error' COMMENT 'error/warning',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_job_id (job_id),
    INDEX idx_error_type (error_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='导入错误明细表';
```

### 3.5 import_rollback_log（回滚日志表）

```sql
CREATE TABLE import_rollback_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    job_id          BIGINT NOT NULL,
    target_table    VARCHAR(128) NOT NULL,
    affected_ids    JSON NOT NULL COMMENT '受影响记录ID列表',
    rollback_sql    TEXT COMMENT '回滚SQL',
    rolled_back     TINYINT(1) NOT NULL DEFAULT 0,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_job_id (job_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='导入回滚日志表';
```

### 3.6 batch_operation（通用批量操作表）

```sql
CREATE TABLE batch_operation (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    operation_no    VARCHAR(32) NOT NULL UNIQUE COMMENT 'BAT-20260525-XXXXX',
    operation_type  VARCHAR(32) NOT NULL COMMENT 'batch_update/batch_delete/batch_grant/batch_publish',
    target_module   VARCHAR(64) NOT NULL,
    payload         JSON NOT NULL COMMENT '操作参数(含filter和updates)',
    status          VARCHAR(20) NOT NULL DEFAULT 'pending'
        COMMENT 'pending/running/completed/partial_completed/failed/cancelled/rolling_back',
    total_count     INT NOT NULL DEFAULT 0,
    processed_count INT NOT NULL DEFAULT 0,
    success_count   INT NOT NULL DEFAULT 0,
    failed_count    INT NOT NULL DEFAULT 0,
    dry_run         TINYINT(1) NOT NULL DEFAULT 0,
    celery_task_id  VARCHAR(128),
    operator_id     BIGINT NOT NULL,
    operator_type   VARCHAR(20) NOT NULL,
    started_at      DATETIME,
    completed_at    DATETIME,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_status (status),
    INDEX idx_module (target_module),
    INDEX idx_operator (operator_id, operator_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通用批量操作表';
```

### 3.7 Redis 数据结构

```
# 任务进度 (Hash)  TTL: 24h
import:progress:{job_id}
  ├── total_rows / processed_rows / success_rows / failed_rows / skipped_rows
  ├── current_batch / total_batches
  ├── status / eta_seconds / updated_at

# 去重缓存 (Set) — TTL: 24h
import:dedup:{job_id}

# 任务锁 (String) — TTL: 30s（自动续期）
import:lock:{job_id}

# 并发限制 (Semaphore) — TTL: 无（手动管理）
import:concurrent:{operator_id}  → 当前该操作者并行任务数，上限 3
```

---

## 4. API 接口设计

### 4.1 模板管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/admin/import/templates` | 获取可用模板列表 |
| GET | `/api/v1/admin/import/templates/{templateCode}` | 获取模板详情(含字段说明) |
| GET | `/api/v1/admin/import/templates/{templateCode}/download` | 下载模板文件 |

### 4.2 导入任务

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/admin/import/jobs` | 创建导入任务(上传文件) |
| GET | `/api/v1/admin/import/jobs` | 查询任务列表(分页) |
| GET | `/api/v1/admin/import/jobs/{jobId}` | 获取任务详情(含实时进度) |
| POST | `/api/v1/admin/import/jobs/{jobId}/cancel` | 取消进行中的任务 |
| POST | `/api/v1/admin/import/jobs/{jobId}/rollback` | 回滚已完成任务 |

#### POST /api/v1/admin/import/jobs

```
Request (multipart/form-data):
  file: 上传文件
  body: {"templateCode":"question_bank_import","dryRun":false,"options":{"updateOnDuplicate":true,"batchSize":200}}

Response 202:
{
  "code": 0,
  "data": {
    "jobId": 12345,
    "jobNo": "IMP-20260525-00001",
    "status": "pending",
    "fileName": "初中数学题库-七年级.xlsx",
    "fileSize": 2048576,
    "createdAt": "2026-05-25T14:30:00+08:00"
  }
}
```

#### GET /api/v1/admin/import/jobs/{jobId}

```json
{
  "code": 0,
  "data": {
    "jobId": 12345,
    "status": "importing",
    "totalRows": 5000,
    "progress": {
      "processedRows": 3500,
      "successRows": 3420,
      "failedRows": 60,
      "skippedRows": 20,
      "currentBatch": 17,
      "totalBatches": 25,
      "percentage": 70.0,
      "etaSeconds": 95,
      "speed": 16.5
    },
    "errorSummary": {
      "totalErrors": 60,
      "byType": {"validation_error": 35, "duplicate_error": 20, "ref_error": 5},
      "topErrors": [
        {"errorCode": "FIELD_REQUIRED", "message": "题目内容不能为空", "count": 18},
        {"errorCode": "DUPLICATE_QUESTION", "message": "题目重复", "count": 20}
      ]
    }
  }
}
```

### 4.3 错误报告

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/admin/import/jobs/{jobId}/errors` | 分页查询错误明细 |
| GET | `/api/v1/admin/import/jobs/{jobId}/errors/download` | 下载错误报告(Excel) |

### 4.4 通用批量操作

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/admin/batch/operations` | 发起批量操作 |
| GET | `/api/v1/admin/batch/operations` | 查询批量操作列表 |
| GET | `/api/v1/admin/batch/operations/{operationId}` | 查询批量操作进度 |
| POST | `/api/v1/admin/batch/operations/{operationId}/cancel` | 取消批量操作 |
| POST | `/api/v1/admin/batch/operations/{operationId}/rollback` | 回滚批量操作 |

```json
// 批量更新题目状态
POST /api/v1/admin/batch/operations
{
  "operationType": "batch_update",
  "targetModule": "question",
  "payload": {
    "filter": {"subjectId": 1, "gradeIds": [7, 8], "status": "draft"},
    "updates": {"status": "published", "publishedAt": "2026-06-01T00:00:00+08:00"}
  },
  "options": {"dryRun": true, "batchSize": 500}
}

// 批量发放权益
{
  "operationType": "batch_grant",
  "targetModule": "membership",
  "payload": {
    "userIds": [1001, 1002, 1003],
    "grant": {"membershipType": "monthly", "durationDays": 7, "reason": "春节活动赠送"}
  }
}
```

---

## 5. 状态机设计

### 5.1 ImportJob 状态流转

```
                    ┌──────────────────────────────────┐
                    │           cancelled               │
                    └──────────────▲───────────────────┘
                                   │ (用户主动取消)
                    ┌──────────────┤
                    │              │
  ┌─────────┐   ┌──▼───┐   ┌─────▼────┐   ┌───────────┐
  │ pending │──►│parsing│──►│validating│──►│ importing │
  └─────────┘   └──┬───┘   └────┬─────┘   └─────┬─────┘
                   │              │               │
                   │ 解析失败       │ 校验全失败     │ 全部成功
                   │              │               ▼
                   │              │         ┌──────────┐
                   │              │         │completed │
                   │              │         └────┬─────┘
                   │              │              │ 回滚
                   ▼              ▼              ▼
                ┌──────────────────────┐   ┌───────────────┐
                │       failed         │   │ rolling_back   │
                └──────────────────────┘   └───────┬───────┘
                                                   │
                                                   ▼
                                           ┌──────────────┐
                                           │ rolled_back  │
                                           └──────────────┘

  部分成功时:
  importing ──► partial_completed (success_rows > 0 && failed_rows > 0)
                 partial_completed 可回滚 (仅回滚成功写入的数据)
```

**状态定义**：

| 状态 | 含义 | 可转换到 |
|------|------|----------|
| `pending` | 已创建，等待解析 | parsing, cancelled |
| `parsing` | 正在解析文件 | validating, failed, cancelled |
| `validating` | 正在校验数据 | importing, failed, cancelled |
| `importing` | 正在分批写入 | completed, partial_completed, failed, cancelled |
| `completed` | 全部成功写入 | rolling_back |
| `partial_completed` | 部分成功、部分失败 | rolling_back |
| `failed` | 失败（解析/校验阶段全失败） | — |
| `cancelled` | 用户手动取消 | — |
| `rolling_back` | 正在回滚 | rolled_back, failed |
| `rolled_back` | 已回滚完成 | — |

### 5.2 ImportBatch 状态流转

```
pending ──► running ──┬──► completed (全部成功)
                      ├──► partial (部分失败)
                      └──► failed (全部失败)
```

### 5.3 BatchOperation 状态流转

```
pending ──► running ──┬──► completed
                      ├──► partial_completed
                      ├──► failed
                      └──► cancelled
                                  │
                              rolling_back ──► rolled_back
```

---

## 6. 模板注册与字段映射

### 6.1 field_mappings JSON 结构

```json
{
  "fields": [
    {
      "source": "题目内容",
      "target": "content",
      "type": "string",
      "required": true,
      "maxLength": 5000,
      "transform": null,
      "description": "题目正文，支持LaTeX公式"
    },
    {
      "source": "学科",
      "target": "subject_code",
      "type": "enum",
      "required": true,
      "enumMap": {
        "数学": "math", "语文": "chinese", "英语": "english",
        "物理": "physics", "化学": "chemistry", "生物": "biology",
        "历史": "history", "地理": "geography", "政治": "politics"
      },
      "description": "学科名称，自动映射为系统编码"
    },
    {
      "source": "难度",
      "target": "difficulty",
      "type": "integer",
      "required": false,
      "defaultValue": 3,
      "range": {"min": 1, "max": 5},
      "description": "1-5级难度，默认3"
    },
    {
      "source": "选项A",
      "target": "options",
      "type": "json_array_append",
      "jsonPath": "$.options",
      "required": false
    },
    {
      "source": "知识点标签",
      "target": "knowledge_point_ids",
      "type": "relation_resolve",
      "relationModule": "knowledge_point",
      "matchField": "name",
      "createIfNotFound": false,
      "required": false
    }
  ]
}
```

### 6.2 validation_rules JSON 结构

```json
{
  "rowRules": [
    {
      "type": "required",
      "fields": ["content", "subject_code"],
      "message": "题目内容和学科为必填项"
    },
    {
      "type": "unique",
      "fields": ["content"],
      "scope": ["subject_code", "grade_id"],
      "message": "同学科同年级下存在重复题目",
      "dedupStrategy": "skip"
    },
    {
      "type": "relation_exists",
      "fields": ["subject_code"],
      "lookupTable": "subject",
      "lookupColumn": "code",
      "message": "学科编码不存在"
    }
  ],
  "crossRowRules": [
    {
      "type": "content_dedup",
      "fields": ["content"],
      "similarityThreshold": 0.95,
      "message": "与第{n}行内容高度相似"
    }
  ],
  "maxErrors": 500,
  "stopOnError": false
}
```

### 6.3 模板注册示例

```python
# services/import_engine/templates/question_import.py

class QuestionBankImportHandler(BaseImportHandler):
    """题库导入处理器"""

    template_code = "question_bank_import"
    template_name = "题库批量导入"
    target_module = "question"
    target_table = "question"

    field_mappings = {
        "fields": [
            {"source": "题目内容", "target": "content", "type": "string", "required": True, "maxLength": 5000},
            {"source": "题型", "target": "question_type", "type": "enum", "required": True,
             "enumMap": {"单选": "single_choice", "多选": "multi_choice", "填空": "fill_blank",
                         "判断": "true_false", "简答": "short_answer", "计算": "calculation"}},
            {"source": "学科", "target": "subject_code", "type": "enum", "required": True,
             "enumMap": {"数学": "math", "语文": "chinese", "英语": "english"}},
            {"source": "年级", "target": "grade_id", "type": "grade_resolve", "required": True},
            {"source": "难度", "target": "difficulty", "type": "integer", "defaultValue": 3,
             "range": {"min": 1, "max": 5}},
            {"source": "答案", "target": "answer", "type": "string", "required": False},
            {"source": "解析", "target": "explanation", "type": "string", "required": False},
        ]
    }

    validation_rules = {
        "rowRules": [
            {"type": "required", "fields": ["content", "question_type", "subject_code"]},
            {"type": "unique", "fields": ["content"], "scope": ["subject_code", "grade_id"],
             "dedupStrategy": "skip"},
        ],
        "maxErrors": 500,
    }

    async def before_batch_write(self, rows: list[dict]) -> list[dict]:
        """写入前业务处理：生成题目hash、补充结构化字段"""
        for row in rows:
            row["content_hash"] = hashlib.md5(row["content"].encode()).hexdigest()
            row["status"] = "draft"
            row["source"] = "import"
        return rows

    async def after_batch_write(self, inserted_ids: list[int], rows: list[dict]):
        """写入后：触发题目向量化、ES索引同步"""
        from services.question.service import trigger_vectorization
        await trigger_vectorization(inserted_ids)
```

---

## 7. 核心服务实现

### 7.1 文件解析器（FileParser）

```python
# services/import_engine/parsers.py

from abc import ABC, abstractmethod
from pathlib import Path
import json
import csv
import io
import openpyxl
from typing import AsyncIterator

class BaseParser(ABC):
    """解析器基类"""

    @abstractmethod
    async def parse(self, file_path: str, **kwargs) -> AsyncIterator[list[dict]]:
        """流式解析文件，按 chunk_size 批量返回行"""
        ...

    @abstractmethod
    def supported_extensions(self) -> list[str]:
        ...


class ExcelParser(BaseParser):
    """Excel (.xlsx/.xls) 解析器"""

    def supported_extensions(self) -> list[str]:
        return [".xlsx", ".xls"]

    async def parse(self, file_path: str, chunk_size: int = 200, **kwargs) -> AsyncIterator[list[dict]]:
        wb = openpyxl.load_workbook(file_path, read_only=True, data_only=True)
        ws = wb.active

        headers = None
        chunk = []

        for row_idx, row in enumerate(ws.iter_rows(values_only=True), start=1):
            if row_idx == 1:
                # 首行作为表头
                headers = [str(cell).strip() if cell else f"col_{i}" for i, cell in enumerate(row)]
                continue

            # 跳过空行
            if all(cell is None for cell in row):
                continue

            record = {}
            for i, cell in enumerate(row):
                if i < len(headers):
                    record[headers[i]] = cell
            record["_row_number"] = row_idx
            chunk.append(record)

            if len(chunk) >= chunk_size:
                yield chunk
                chunk = []

        if chunk:
            yield chunk

        wb.close()


class CSVParser(BaseParser):
    """CSV 解析器（支持 GBK/UTF-8 自动检测）"""

    def supported_extensions(self) -> list[str]:
        return [".csv"]

    def _detect_encoding(self, file_path: str) -> str:
        """检测文件编码：尝试 UTF-8，失败则用 GBK"""
        try:
            with open(file_path, "r", encoding="utf-8") as f:
                f.read(1024)
            return "utf-8"
        except UnicodeDecodeError:
            return "gbk"

    async def parse(self, file_path: str, chunk_size: int = 200, **kwargs) -> AsyncIterator[list[dict]]:
        encoding = self._detect_encoding(file_path)
        delimiter = kwargs.get("delimiter", ",")

        with open(file_path, "r", encoding=encoding) as f:
            reader = csv.DictReader(f, delimiter=delimiter)
            chunk = []
            for row_idx, record in enumerate(reader, start=2):  # 第1行是表头
                record["_row_number"] = row_idx
                chunk.append(record)
                if len(chunk) >= chunk_size:
                    yield chunk
                    chunk = []
            if chunk:
                yield chunk


class JSONParser(BaseParser):
    """JSON 解析器（支持数组和换行分隔的 NDJSON）"""

    def supported_extensions(self) -> list[str]:
        return [".json", ".jsonl"]

    async def parse(self, file_path: str, chunk_size: int = 200, **kwargs) -> AsyncIterator[list[dict]]:
        ext = Path(file_path).suffix

        if ext == ".jsonl":
            # NDJSON：每行一个 JSON 对象
            chunk = []
            with open(file_path, "r", encoding="utf-8") as f:
                for row_idx, line in enumerate(f, start=1):
                    line = line.strip()
                    if not line:
                        continue
                    record = json.loads(line)
                    record["_row_number"] = row_idx
                    chunk.append(record)
                    if len(chunk) >= chunk_size:
                        yield chunk
                        chunk = []
                if chunk:
                    yield chunk
        else:
            # 标准JSON数组
            with open(file_path, "r", encoding="utf-8") as f:
                data = json.load(f)

            if isinstance(data, list):
                for i in range(0, len(data), chunk_size):
                    chunk = data[i:i + chunk_size]
                    for j, record in enumerate(chunk):
                        record["_row_number"] = i + j + 1
                    yield chunk
            elif isinstance(data, dict) and "data" in data:
                # 兼容 {"data": [...]} 格式
                records = data["data"]
                for i in range(0, len(records), chunk_size):
                    chunk = records[i:i + chunk_size]
                    for j, record in enumerate(chunk):
                        record["_row_number"] = i + j + 1
                    yield chunk
            else:
                raise ParseError("JSON格式不支持：期望数组或含data字段的对象")


# 解析器注册表
PARSER_REGISTRY: dict[str, BaseParser] = {
    ".xlsx": ExcelParser(),
    ".xls": ExcelParser(),
    ".csv": CSVParser(),
    ".json": JSONParser(),
    ".jsonl": JSONParser(),
}

def get_parser(file_path: str) -> BaseParser:
    ext = Path(file_path).suffix.lower()
    parser = PARSER_REGISTRY.get(ext)
    if not parser:
        raise ParseError(f"不支持的文件格式: {ext}")
    return parser
```

### 7.2 校验引擎（ValidationEngine）

```python
# services/import_engine/validators.py

from dataclasses import dataclass
from typing import Any

@dataclass
class ValidationError:
    row_number: int
    error_type: str          # validation_error / duplicate_error / ref_error
    error_code: str
    field_name: str | None
    field_value: str | None
    error_message: str
    severity: str = "error"  # error / warning


class ValidationEngine:
    """逐行校验引擎"""

    def __init__(self, field_mappings: dict, validation_rules: dict):
        self.fields = field_mappings["fields"]
        self.rules = validation_rules.get("rowRules", [])
        self.max_errors = validation_rules.get("maxErrors", 500)
        self._error_count = 0

    def validate_row(self, row: dict, row_number: int) -> list[ValidationError]:
        """校验单行数据，返回错误列表（收集全部而非首错即停）"""
        errors = []
        if self._error_count >= self.max_errors:
            return errors

        # 1. 字段映射与类型转换
        mapped = self._map_fields(row, row_number, errors)

        # 2. 执行规则校验
        for rule in self.rules:
            if self._error_count >= self.max_errors:
                break
            rule_errors = self._apply_rule(rule, mapped, row_number)
            errors.extend(rule_errors)
            self._error_count += len([e for e in rule_errors if e.severity == "error"])

        return errors

    def _map_fields(self, row: dict, row_number: int, errors: list) -> dict:
        """字段映射 + 类型转换"""
        result = {}
        for field_def in self.fields:
            source = field_def["source"]
            target = field_def["target"]
            value = row.get(source)

            # 默认值
            if value is None or str(value).strip() == "":
                if "defaultValue" in field_def:
                    result[target] = field_def["defaultValue"]
                    continue
                if field_def.get("required"):
                    errors.append(ValidationError(
                        row_number=row_number, error_type="validation_error",
                        error_code="FIELD_REQUIRED", field_name=source,
                        field_value=None, error_message=f"字段 '{source}' 不能为空"
                    ))
                    continue
                result[target] = None
                continue

            # 类型转换
            field_type = field_def.get("type", "string")
            try:
                result[target] = self._convert_type(value, field_type, field_def)
            except (ValueError, TypeError) as e:
                errors.append(ValidationError(
                    row_number=row_number, error_type="validation_error",
                    error_code="TYPE_MISMATCH", field_name=source,
                    field_value=str(value)[:256],
                    error_message=f"字段 '{source}' 类型转换失败: {e}"
                ))

            # 范围校验
            if "range" in field_def and result.get(target) is not None:
                r = field_def["range"]
                val = result[target]
                if isinstance(val, (int, float)):
                    if "min" in r and val < r["min"]:
                        errors.append(ValidationError(
                            row_number=row_number, error_type="validation_error",
                            error_code="VALUE_TOO_SMALL", field_name=source,
                            field_value=str(val), error_message=f"值不能小于 {r['min']}"
                        ))
                    if "max" in r and val > r["max"]:
                        errors.append(ValidationError(
                            row_number=row_number, error_type="validation_error",
                            error_code="VALUE_TOO_LARGE", field_name=source,
                            field_value=str(val), error_message=f"值不能大于 {r['max']}"
                        ))

            # 长度校验
            if "maxLength" in field_def and result.get(target) is not None:
                if len(str(result[target])) > field_def["maxLength"]:
                    errors.append(ValidationError(
                        row_number=row_number, error_type="validation_error",
                        error_code="VALUE_TOO_LONG", field_name=source,
                        field_value=str(result[target])[:50] + "...",
                        error_message=f"长度超过限制 {field_def['maxLength']} 字符"
                    ))

        return result

    def _convert_type(self, value: Any, field_type: str, field_def: dict) -> Any:
        """类型转换"""
        if field_type == "string":
            return str(value).strip()
        elif field_type == "integer":
            return int(float(value))
        elif field_type == "float":
            return float(value)
        elif field_type == "enum":
            enum_map = field_def.get("enumMap", {})
            str_val = str(value).strip()
            if str_val in enum_map:
                return enum_map[str_val]
            raise ValueError(f"'{str_val}' 不在可选值 {list(enum_map.keys())} 中")
        elif field_type == "boolean":
            return str(value).strip().lower() in ("是", "true", "1", "yes")
        else:
            return value

    def _apply_rule(self, rule: dict, mapped: dict, row_number: int) -> list[ValidationError]:
        """应用单条校验规则"""
        errors = []
        rule_type = rule["type"]
        fields = rule.get("fields", [])
        message = rule.get("message", "校验失败")

        if rule_type == "required":
            for f in fields:
                if mapped.get(f) is None:
                    errors.append(ValidationError(
                        row_number=row_number, error_type="validation_error",
                        error_code="FIELD_REQUIRED", field_name=f,
                        field_value=None, error_message=message
                    ))

        # unique 和 relation_exists 在 BatchProcessor 中做批量校验（需查库）
        return errors


class BatchValidator:
    """批次级批量校验（需