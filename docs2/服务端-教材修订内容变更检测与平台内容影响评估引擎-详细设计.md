# 服务端-教材修订内容变更检测与平台内容影响评估引擎-详细设计

## 1. 模块概述

### 1.1 功能定位

本引擎负责在教材新版本发布时，自动完成以下核心任务：

1. **结构化对比**：对新旧版教材进行章节、知识点、考点的细粒度差异比对
2. **变更分类**：将差异归类为新增、删除、修改、调序、重命名等变更类型
3. **影响评估**：扫描全平台与旧版教材关联的所有内容（题目、课程、解析、考点梳理等），评估受影响范围
4. **任务生成**：自动生成内容更新工单，推送至内容运营团队

### 1.2 与现有模块的边界

| 现有模块 | 覆盖范围 | 本引擎差异 |
| --- | --- | --- |
| 教育内容版本管理与教材同步更新引擎 | 版本元数据管理、发布同步 | 不做内容级别的差异检测和影响分析 |
| 教材内容生命周期与版本管理引擎 | 内容从创建到下架的全生命周期 | 不处理版本间内容变更对比 |
| 服务端-教材版本间知识点映射与学生进度迁移引擎 | **跨出版社**版本间的知识点映射（如人教版↔苏教版） | 本引擎处理**同一出版社**新旧版次间的变更检测 |
| 服务端-教材课标对齐度分析与内容覆盖率统计服务 | 教材与课程标准的对齐度 | 不处理版次间的增量变更 |

### 1.3 设计目标

| 目标 | 指标 |
| --- | --- |
| 变更检测准确率 | 章节结构变更 ≥ 98%，知识点变更 ≥ 90% |
| 单教材对比耗时 | ≤ 5 分钟（一本完整教材约 200-400 页） |
| 影响评估扫描范围 | 覆盖全平台所有与该教材版本绑定的内容资源 |
| 影响报告生成耗时 | ≤ 15 分钟 |
| 自动任务生成覆盖率 | 高优先级变更（知识点删除/修改）自动生成任务 ≥ 95% |

### 1.4 核心使用场景

```
场景1：教材年度修订
人教版数学七年级上册 2024版 → 2025版
→ 自动检测新增/删除/修改的知识点
→ 评估平台已有题目、课程、解析的影响
→ 生成内容更新工单

场景2：课标改革导致教材改版
义务教育数学课程标准更新
→ 多家出版社同步修订
→ 批量检测所有受影响教材
→ 生成跨教材的影响评估报告

场景3：区域教材版本切换
某省份更换教材版本
→ 对比新旧版本的差异
→ 评估学生历史学习数据的影响
→ 协调学生进度迁移
```

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     触发层 (Trigger Layer)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 手动触发对比  │  │ 定时巡检任务  │  │ 外部事件回调  │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
└─────────┼─────────────────┼─────────────────┼───────────────┘
          └─────────────────┼─────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   对比分析层 (Diff Engine)                    │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │
│  │ 结构对比器  │  │ 知识点对比器│  │ 考点对比器  │            │
│  │ StructureDiff│ │ KnowledgeDiff│ │ ExamPointDiff│           │
│  └──────┬─────┘  └──────┬─────┘  └──────┬─────┘            │
│         └───────────────┼───────────────┘                    │
│                         ▼                                    │
│              ┌────────────────┐                              │
│              │  变更合并与分类  │                              │
│              │ ChangeAggregator│                             │
│              └───────┬────────┘                              │
└──────────────────────┼──────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                 影响评估层 (Impact Assessment)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 内容资源扫描  │  │ 关联关系分析  │  │ 风险评级计算  │      │
│  │ContentScanner │ │RelationAnalyzer│ │RiskGrader    │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         └─────────────────┼─────────────────┘                │
│                           ▼                                  │
│               ┌────────────────┐                             │
│               │  影响报告生成    │                             │
│               │ ReportGenerator │                            │
│               └───────┬────────┘                             │
└───────────────────────┼─────────────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                   任务调度层 (Task Dispatch)                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 工单自动生成  │  │ 优先级排序    │  │ 状态追踪      │      │
│  │TaskGenerator  │ │PrioritySorter │ │StatusTracker  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 服务间交互关系

```
                    ┌────────────────┐
                    │  教材内容管理    │
                    │  ContentService │
                    └───────┬────────┘
                            │ 教材结构化数据
                            ▼
┌────────────────┐  ┌────────────────┐  ┌────────────────┐
│  知识点服务      │  │  变更检测引擎    │  │  题库服务       │
│  KnowledgeSvc   │◄─┤  RevisionDiff   │─►│  QuestionSvc   │
└────────────────┘  │  Engine         │  └────────────────┘
                    └───────┬────────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
┌────────────────┐ ┌────────────────┐ ┌────────────────┐
│  课程内容服务    │ │  考点服务       │ │  工单服务       │
│  CourseSvc      │ │  ExamPointSvc  │ │  TaskSvc       │
└────────────────┘ └────────────────┘ └────────────────┘
```

---

## 3. 数据模型

### 3.1 核心实体 ER 图

```
┌─────────────────────┐       ┌──────────────────────────┐
│  textbook_revision   │       │  revision_diff_record     │
│─────────────────────│       │──────────────────────────│
│  id (PK)            │◄──────│  id (PK)                 │
│  textbook_id        │  1:N  │  revision_id (FK)        │
│  old_edition_id     │       │  diff_type               │
│  new_edition_id     │       │  scope                   │
│  status             │       │  status                  │
│  triggered_by       │       │  started_at              │
│  triggered_at       │       │  completed_at            │
│  completed_at       │       │  summary_json            │
│  report_url         │       │  error_detail            │
└─────────────────────┘       └──────────┬───────────────┘
                                         │ 1:N
                                         ▼
┌──────────────────────────┐  ┌─────────────────────────────┐
│  content_impact_record    │  │  change_detail              │
│──────────────────────────│  │─────────────────────────────│
│  id (PK)                 │  │  id (PK)                    │
│  diff_record_id (FK)     │  │  diff_record_id (FK)        │
│  content_type            │  │  change_category            │
│  content_id              │  │  target_type                │
│  content_title           │  │  target_id                  │
│  target_entity_type      │  │  target_name                │
│  target_entity_id        │  │  old_value                  │
│  impact_level            │  │  new_value                  │
│  impact_description      │  │  similarity_score           │
│  suggested_action        │  │  ai_analysis                │
│  status                  │  │  position_old               │
│  assigned_to             │  │  position_new               │
│  resolved_at             │  │  metadata                   │
└──────────────────────────┘  └─────────────────────────────┘
```

### 3.2 数据库表定义

#### 3.2.1 textbook_revision — 教材修订对比任务

```sql
CREATE TABLE textbook_revision (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    textbook_id     BIGINT NOT NULL COMMENT '教材ID',
    old_edition_id  BIGINT NOT NULL COMMENT '旧版次ID',
    new_edition_id  BIGINT NOT NULL COMMENT '新版次ID',
    status          VARCHAR(32) NOT NULL DEFAULT 'PENDING'
                    COMMENT 'PENDING/RUNNING/ANALYZING/COMPLETED/FAILED',
    triggered_by    BIGINT NOT NULL COMMENT '触发人ID',
    trigger_source  VARCHAR(32) NOT NULL DEFAULT 'MANUAL'
                    COMMENT 'MANUAL/SCHEDULED/EXTERNAL',
    priority        TINYINT NOT NULL DEFAULT 2 COMMENT '1=紧急 2=普通 3=低',
    started_at      DATETIME,
    completed_at    DATETIME,
    report_url      VARCHAR(512) COMMENT '报告文件URL',
    total_changes   INT DEFAULT 0 COMMENT '变更总数',
    total_impacts   INT DEFAULT 0 COMMENT '受影响内容总数',
    error_detail    TEXT,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_textbook (textbook_id),
    INDEX idx_status (status),
    INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教材修订对比任务';
```

#### 3.2.2 revision_diff_record — 版本差异记录

```sql
CREATE TABLE revision_diff_record (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    revision_id     BIGINT NOT NULL COMMENT '关联textbook_revision.id',
    diff_type       VARCHAR(32) NOT NULL
                    COMMENT 'STRUCTURE/KNOWLEDGE/EXAMPOINT/CONTENT',
    scope           VARCHAR(128) NOT NULL COMMENT '对比范围描述，如"第三章 二次方程"',
    status          VARCHAR(32) NOT NULL DEFAULT 'PENDING'
                    COMMENT 'PENDING/RUNNING/COMPLETED/SKIPPED/FAILED',
    started_at      DATETIME,
    completed_at    DATETIME,
    summary_json    JSON COMMENT '差异摘要，结构化JSON',
    total_changes   INT DEFAULT 0,
    error_detail    TEXT,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_revision (revision_id),
    INDEX idx_type (diff_type),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='版本差异记录';
```

#### 3.2.3 change_detail — 变更明细

```sql
CREATE TABLE change_detail (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    diff_record_id  BIGINT NOT NULL COMMENT '关联revision_diff_record.id',
    change_category VARCHAR(32) NOT NULL
                    COMMENT 'ADDED/REMOVED/MODIFIED/REORDERED/RENAMED/MERGED/SPLIT',
    target_type     VARCHAR(32) NOT NULL
                    COMMENT 'CHAPTER/SECTION/KNOWLEDGE_POINT/EXAM_POINT/TOPIC',
    target_id       BIGINT COMMENT '目标实体ID（新版中的ID）',
    target_name     VARCHAR(256) NOT NULL COMMENT '目标名称',
    old_name        VARCHAR(256) COMMENT '旧版中的名称（如有修改）',
    old_value       JSON COMMENT '旧值（结构化）',
    new_value       JSON COMMENT '新值（结构化）',
    similarity_score DECIMAL(5,4) DEFAULT NULL COMMENT '相似度评分 0-1',
    ai_analysis     TEXT COMMENT 'AI辅助变更分析说明',
    position_old    INT COMMENT '旧版中的序号位置',
    position_new    INT COMMENT '新版中的序号位置',
    metadata        JSON COMMENT '扩展元数据',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_diff (diff_record_id),
    INDEX idx_category (change_category),
    INDEX idx_target_type (target_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='变更明细';
```

#### 3.2.4 content_impact_record — 内容影响记录

```sql
CREATE TABLE content_impact_record (
    id                  BIGINT PRIMARY KEY AUTO_INCREMENT,
    diff_record_id      BIGINT NOT NULL COMMENT '关联revision_diff_record.id',
    change_detail_id    BIGINT COMMENT '关联change_detail.id',
    content_type        VARCHAR(32) NOT NULL
                        COMMENT 'QUESTION/COURSE/ANALYSIS/EXAM_PAPER/LESSON/NOTE',
    content_id          BIGINT NOT NULL COMMENT '受影响内容资源ID',
    content_title       VARCHAR(512) COMMENT '内容标题/摘要',
    target_entity_type  VARCHAR(32) NOT NULL
                        COMMENT 'CHAPTER/KNOWLEDGE_POINT/EXAM_POINT',
    target_entity_id    BIGINT NOT NULL COMMENT '关联的旧版实体ID',
    impact_level        VARCHAR(16) NOT NULL
                        COMMENT 'CRITICAL/HIGH/MEDIUM/LOW',
    impact_description  TEXT NOT NULL COMMENT '影响描述',
    suggested_action    VARCHAR(32) NOT NULL
                        COMMENT 'UPDATE/DELETE/REMAP/REVIEW/MANUAL_CHECK',
    status              VARCHAR(32) NOT NULL DEFAULT 'PENDING'
                        COMMENT 'PENDING/ASSIGNED/IN_PROGRESS/RESOLVED/WONT_FIX',
    assigned_to         BIGINT COMMENT '处理人ID',
    resolved_at         DATETIME,
    resolution_note     TEXT,
    created_at          DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at          DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_diff (diff_record_id),
    INDEX idx_content (content_type, content_id),
    INDEX idx_level (impact_level),
    INDEX idx_status (status),
    INDEX idx_assignee (assigned_to)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='内容影响记录';
```

### 3.3 核心领域模型 (Java)

```java
/**
 * 变更类别枚举
 */
public enum ChangeCategory {
    ADDED("新增"),
    REMOVED("删除"),
    MODIFIED("修改"),
    REORDERED("调序"),
    RENAMED("重命名"),
    MERGED("合并"),
    SPLIT("拆分");

    private final String description;
    ChangeCategory(String desc) { this.description = desc; }
}

/**
 * 变更目标类型
 */
public enum TargetType {
    CHAPTER("章"),
    SECTION("节"),
    KNOWLEDGE_POINT("知识点"),
    EXAM_POINT("考点"),
    TOPIC("专题");

    private final String description;
    TargetType(String desc) { this.description = desc; }
}

/**
 * 影响等级
 */
public enum ImpactLevel {
    CRITICAL("严重", 1),  // 知识点被删除，关联题目将无法使用
    HIGH("高", 2),        // 知识点内容修改，关联题目解析可能错误
    MEDIUM("中", 3),      // 章节结构调整，需要更新关联关系
    LOW("低", 4);         // 措辞修改、调序等，影响较小

    private final String description;
    private final int severity;
    ImpactLevel(String desc, int severity) {
        this.description = desc;
        this.severity = severity;
    }
}

/**
 * 建议动作
 */
public enum SuggestedAction {
    UPDATE("更新内容"),
    DELETE("删除失效内容"),
    REMAP("重新映射知识点"),
    REVIEW("人工审核"),
    MANUAL_CHECK("手动检查");

    private final String description;
    SuggestedAction(String desc) { this.description = desc; }
}

/**
 * 对比维度
 */
public enum DiffType {
    STRUCTURE("章节结构对比"),
    KNOWLEDGE("知识点对比"),
    EXAMPOINT("考点对比"),
    CONTENT("内容文本对比");

    private final String description;
    DiffType(String desc) { this.description = desc; }
}

/**
 * 教材修订对比任务状态机
 */
public enum RevisionStatus {
    PENDING,     // 待处理
    RUNNING,     // 对比进行中
    ANALYZING,   // 影响分析中
    COMPLETED,   // 已完成
    FAILED;      // 失败
}
```

---

## 4. 核心引擎设计

### 4.1 教材结构对比器 (StructureDiffComparator)

#### 4.1.1 算法设计

教材结构对比采用 **改进的树编辑距离 (Tree Edit Distance)** 算法，结合语义相似度进行节点匹配。

```
输入:
  oldTree: 旧版教材目录树 (Chapter → Section → Subsection)
  newTree: 新版教材目录树

步骤:
  1. 对两棵树进行层级展开，提取每个节点
  2. 计算节点间的语义相似度矩阵 (使用章节标题的编辑距离 + 语义向量余弦相似度)
  3. 使用动态规划求最优匹配 (匈牙利算法)
  4. 对匹配节点递归对比子节点
  5. 未匹配的旧节点 → REMOVED
  6. 未匹配的新节点 → ADDED
  7. 匹配但序号变化的节点 → REORDERED
  8. 匹配但标题变化的节点 → RENAMED 或 MODIFIED

输出:
  List<ChangeDetail> 结构变更列表
```

#### 4.1.2 语义相似度计算

```java
/**
 * 计算两个章节标题的相似度
 * 综合编辑距离和语义向量相似度
 *
 * @param title1 旧版标题
 * @param title2 新版标题
 * @return 相似度 [0, 1]
 */
public double calculateTitleSimilarity(String title1, String title2) {
    if (title1 == null || title2 == null) return 0.0;
    if (title1.equals(title2)) return 1.0;

    // 1. 编辑距离相似度 (权重 0.4)
    double editDistSim = calculateEditDistanceSimilarity(title1, title2);

    // 2. 语义向量余弦相似度 (权重 0.4)
    double semanticSim = calculateSemanticSimilarity(title1, title2);

    // 3. 关键词重合度 (权重 0.2)
    double keywordOverlap = calculateKeywordOverlap(title1, title2);

    return 0.4 * editDistSim + 0.4 * semanticSim + 0.2 * keywordOverlap;
}

/**
 * 匹配阈值判定
 */
private static final double MATCH_THRESHOLD = 0.75;

/**
 * 判定两个节点是否为同一实体
 */
public boolean isSameEntity(double similarity) {
    return similarity >= MATCH_THRESHOLD;
}
```

#### 4.1.3 树对比核心逻辑

```java
/**
 * 对比两棵教材目录树
 */
public List<ChangeDetail> diffChapterTrees(
        List<ChapterNode> oldChapters,
        List<ChapterNode> newChapters) {

    List<ChangeDetail> changes = new ArrayList<>();

    // 1. 构建相似度矩阵
    int oldSize = oldChapters.size();
    int newSize = newChapters.size();
    double[][] simMatrix = new double[oldSize][newSize];

    for (int i = 0; i < oldSize; i++) {
        for (int j = 0; j < newSize; j++) {
            simMatrix[i][j] = calculateChapterSimilarity(
                oldChapters.get(i), newChapters.get(j));
        }
    }

    // 2. 匈牙利算法求最优匹配
    int[] oldToNewMatch = hungarianAlgorithm.findMaxWeightMatching(simMatrix);

    // 3. 生成变更明细
    Set<Integer> matchedNew = new HashSet<>();

    for (int i = 0; i < oldSize; i++) {
        int j = oldToNewMatch[i];
        if (j >= 0 && simMatrix[i][j] >= MATCH_THRESHOLD) {
            // 匹配成功
            matchedNew.add(j);
            ChapterNode oldCh = oldChapters.get(i);
            ChapterNode newCh = newChapters.get(j);

            // 检查标题变更
            if (!oldCh.getTitle().equals(newCh.getTitle())) {
                changes.add(buildRenameChange(oldCh, newCh));
            }

            // 检查位置变更
            if (i != j) {
                changes.add(buildReorderChange(oldCh, newCh, i, j));
            }

            // 递归对比子章节
            changes.addAll(diffChapterTrees(
                oldCh.getChildren(), newCh.getChildren()));

            // 对比关联的知识点
            changes.addAll(diffKnowledgePoints(
                oldCh.getKnowledgePoints(),
                newCh.getKnowledgePoints()));

        } else {
            // 旧节点在新版中被删除
            changes.add(buildRemovedChange(oldChapters.get(i)));
        }
    }

    // 检查新增节点
    for (int j = 0; j < newSize; j++) {
        if (!matchedNew.contains(j)) {
            changes.add(buildAddedChange(newChapters.get(j)));
        }
    }

    return changes;
}
```

### 4.2 知识点对比器 (KnowledgeDiffComparator)

#### 4.2.1 对比策略

知识点对比比章节结构对比更复杂，因为知识点可能：
- 内容修改但名称不变
- 名称修改但内容不变
- 被拆分为多个知识点
- 多个知识点被合并为一个
- 从一个章节移动到另一个章节

```java
/**
 * 知识点对比核心逻辑
 */
public List<ChangeDetail> diffKnowledgePoints(
        List<KnowledgePoint> oldPoints,
        List<KnowledgePoint> newPoints) {

    List<ChangeDetail> changes = new ArrayList<>();

    // 1. 构建知识点相似度矩阵
    //    综合考虑：名称相似度 + 描述相似度 + 关联章节相似度
    double[][] simMatrix = buildKnowledgeSimMatrix(oldPoints, newPoints);

    // 2. 最优匹配
    int[] matches = hungarianAlgorithm.findMaxWeightMatching(simMatrix);

    // 3. 分类处理
    Set<Integer> matchedNew = new HashSet<>();
    Map<Integer, List<Integer>> potentialSplits = new HashMap<>(); // 旧知识点可能被拆分
    Map<Integer, List<Integer>> potentialMerges = new HashMap<>();  // 多个旧知识点可能被合并

    for (int i = 0; i < oldPoints.size(); i++) {
        int j = matches[i];
        if (j >= 0 && simMatrix[i][j] >= KNOWLEDGE_MATCH_THRESHOLD) {
            // 匹配成功 — 检查是否为修改
            matchedNew.add(j);
            KnowledgePoint oldKp = oldPoints.get(i);
            KnowledgePoint newKp = newPoints.get(j);

            if (!oldKp.getContent().equals(newKp.getContent())) {
                double contentSim = calculateContentSimilarity(
                    oldKp.getContent(), newKp.getContent());
                if (contentSim < 0.95) {
                    changes.add(buildModifiedChange(oldKp, newKp, contentSim));
                }
            }

            // 检查所属章节是否变化
            if (!oldKp.getChapterId().equals(newKp.getChapterId())) {
                changes.add(buildMoveChange(oldKp, newKp));
            }

        } else {
            // 检查是否被拆分：一个旧知识点匹配多个新知识点
            List<Integer> splitCandidates = findMultiMatches(
                i, simMatrix, KNOWLEDGE_WEAK_THRESHOLD);
            if (splitCandidates.size() > 1) {
                potentialSplits.put(i, splitCandidates);
                matchedNew.addAll(splitCandidates);
                changes.add(buildSplitChange(
                    oldPoints.get(i),
                    splitCandidates.stream().map(newPoints::get).toList()));
            }
        }
    }

    // 检查合并：多个旧知识点匹配同一个新知识点
    for (int j = 0; j < newPoints.size(); j++) {
        if (matchedNew.contains(j)) continue;
        List<Integer> mergeCandidates = new ArrayList<>();
        for (int i = 0; i < oldPoints.size(); i++) {
            if (matches[i] < 0 && simMatrix[i][j] >= KNOWLEDGE_WEAK_THRESHOLD) {
                mergeCandidates.add(i);
            }
        }
        if (mergeCandidates.size() > 1) {
            changes.add(buildMergeChange(
                mergeCandidates.stream().map(oldPoints::get).toList(),
                newPoints.get(j)));
            matchedNew.add(j);
        }
    }

    // 未匹配的旧知识点 → 删除
    for (int i = 0; i < oldPoints.size(); i++) {
        if (matches[i] < 0 && !potentialSplits.containsKey(i)) {
            changes.add(buildRemovedChange(oldPoints.get(i)));
        }
    }

    // 未匹配的新知识点 → 新增
    for (int j = 0; j < newPoints.size(); j++) {
        if (!matchedNew.contains(j)) {
            changes.add(buildAddedChange(newPoints.get(j)));
        }
    }

    return changes;
}

private static final double KNOWLEDGE_MATCH_THRESHOLD = 0.70;
private static final double KNOWLEDGE_WEAK_THRESHOLD = 0.50;