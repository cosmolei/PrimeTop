# 客户端-教师端页面架构与班级管理交互设计

> 版本：1.0  
> 日期：2026-06-07  
> 状态：详细设计（V2.0 规划）  
> 模块优先级：P3（中长期扩展）  
> 关联文档：`教师端与班级管理-详细设计.md`、`端到端流程设计-教师端班级管理与作业发布完整链路-详细设计.md`、`客户端架构与前端框架-详细设计.md`、`客户端组件库与设计系统-详细设计.md`

---

## 1. 模块概述

### 1.1 功能定位

教师端页面架构是 PrimeTop V2.0 面向教师用户的客户端（Web 管理后台 + 小程序）页面体系，负责：
- 教师注册认证与身份管理
- 班级创建、邀请码分发、学生名册管理
- 作业选题、发布、批改与统计
- 班级学情看板与个体学情详情
- 班级通知发布与家长回执追踪
- 教师个人中心与多班级切换

### 1.2 技术选型

| 维度 | 方案 | 说明 |
|------|------|------|
| Web 端 | React 18 + Ant Design Pro | 管理后台场景，信息密度高，PC 大屏为主 |
| 小程序端 | Taro 3 + 自定义组件库 | 移动端轻量操作，查看+简单操作 |
| 状态管理 | Zustand（Web）+ Taro 自有状态（小程序） | 轻量、与现有客户端技术栈统一思路 |
| 路由 | React Router v6（Web）+ Taro Router（小程序） | 路由守卫控制认证和权限 |
| 图表 | ECharts（Web）+ ECharts-Taro（小程序） | 学情看板、答题统计图表 |
| 网络层 | Axios + SWR（Web）+ Taro.request（小程序） | 请求封装、缓存、重试 |

### 1.3 前置依赖

| 依赖 | 说明 |
|------|------|
| 客户端组件库与设计系统 | 通用组件、主题、图标 |
| 客户端路由与深链接系统 | 页面路由、深链接跳转 |
| 客户端网络请求治理 | 请求封装、错误处理、弱网适配 |
| 客户端状态管理架构 | 全局状态（用户信息、班级上下文） |
| 客户端页面状态模式 | 骨架屏、空态、错误态、加载态 |
| 客户端通用列表组件与分页加载引擎 | 学生列表、作业列表等通用列表 |
| 服务端统一认证授权与令牌管理体系 | 教师登录、Token 管理 |
| 服务端-统一响应封装与分页查询规范 | API 响应格式约定 |

---

## 2. 整体页面架构

### 2.1 页面树

```
教师端
├── /login                         # 教师登录页
├── /register                      # 教师注册页
├── /certification                 # 教师资格认证页
├── /main                          # 主框架（侧边栏 + 内容区）
│   ├── /main/dashboard            # 教师工作台首页
│   ├── /main/classes              # 班级管理
│   │   ├── /main/classes/create   # 创建班级
│   │   ├── /main/classes/:id      # 班级详情
│   │   │   ├── .../roster         # 学生名册
│   │   │   ├── .../assignments    # 作业列表
│   │   │   ├── .../analytics      # 班级学情看板
│   │   │   └── .../notices        # 班级通知
│   │   └── /main/classes/:id/students/:sid  # 个体学情详情
│   ├── /main/assignments          # 作业管理（跨班级视图）
│   │   ├── /main/assignments/create  # 创建作业
│   │   ├── /main/assignments/:id     # 作业详情与批改
│   │   └── /main/assignments/:id/grading  # 批改界面
│   ├── /main/analytics            # 教学数据分析
│   ├── /main/notices              # 通知管理
│   └── /main/profile              # 个人中心
│       ├── .../settings           # 账号设置
│       └── .../cert-status        # 认证状态
└── /invite/:code                  # 学生加入班级（深链接落地页）
```

### 2.2 Web 端布局架构

```
┌──────────────────────────────────────────────────────────────┐
│  顶部栏：Logo | 当前班级选择器 | 全局搜索 | 通知铃铛 | 头像 ▼  │
├───────────┬──────────────────────────────────────────────────┤
│           │                                                  │
│  侧边栏    │              内容区                               │
│           │                                                  │
│  ┌─────┐  │  ┌──────────────────────────────────────────┐   │
│  │ 📊  │  │  │                                          │   │
│  │工作台│  │  │         页面内容                          │   │
│  ├─────┤  │  │                                          │   │
│  │ 👥  │  │  │                                          │   │
│  │班级  │  │  │                                          │   │
│  │管理  │  │  │                                          │   │
│  ├─────┤  │  │                                          │   │
│  │ 📝  │  │  │                                          │   │
│  │作业  │  │  │                                          │   │
│  │管理  │  │  │                                          │   │
│  ├─────┤  │  │                                          │   │
│  │ 📈  │  │  │                                          │   │
│  │教学  │  │  │                                          │   │
│  │分析  │  │  │                                          │   │
│  ├─────┤  │  │                                          │   │
│  │ 📢  │  │  │                                          │   │
│  │通知  │  │  │                                          │   │
│  │管理  │  │  │                                          │   │
│  ├─────┤  │  └──────────────────────────────────────────┘   │
│  │ ⚙️  │  │                                                  │
│  │个人  │  │                                                  │
│  │中心  │  │                                                  │
│  └─────┘  │                                                  │
├───────────┴──────────────────────────────────────────────────┤
│  底部状态栏：在线状态 | 当前学期 | 版本号                       │
└──────────────────────────────────────────────────────────────┘
```

### 2.3 小程序端布局架构

```
┌────────────────────────┐
│  顶部导航栏（页面标题）   │
├────────────────────────┤
│                        │
│    页面内容区域          │
│    （可滚动）            │
│                        │
│                        │
├────────────────────────┤
│  TabBar                │
│  ┌────┬────┬────┬────┐ │
│  │工作台│班级│作业│我的│ │
│  └────┴────┴────┴────┘ │
└────────────────────────┘
```

### 2.4 全局状态结构

```typescript
// types/teacher-store.ts

/** 教师全局状态 */
interface TeacherGlobalState {
  // 认证
  auth: {
    token: string | null;
    refreshToken: string | null;
    certStatus: 'pending' | 'verified' | 'rejected' | 'none';
    teacherId: string | null;
    name: string | null;
    school: string | null;
    subjects: string[];             // 所教学科列表
  };

  // 班级上下文
  classContext: {
    currentClassId: string | null;  // 当前选中班级
    classList: ClassBrief[];
    loading: boolean;
  };

  // 通知
  notifications: {
    unreadCount: number;
    lastSyncAt: string | null;
  };

  // UI 状态
  ui: {
    sidebarCollapsed: boolean;
    activeModule: string;
  };
}

/** 班级简要信息 */
interface ClassBrief {
  id: string;
  name: string;
  subject: string;
  grade: string;
  studentCount: number;
  semester: string;
  isArchived: boolean;
}

/** 班级上下文切换 Action */
interface TeacherActions {
  switchClass: (classId: string) => Promise<void>;
  refreshClassList: () => Promise<void>;
  updateUnreadCount: () => Promise<void>;
  logout: () => void;
}
```

---

## 3. 页面详细设计

### 3.1 教师登录与注册

#### 3.1.1 登录页 `/login`

**布局：**
```
┌──────────────────────────────────────────────┐
│                                              │
│            ┌─────────────────┐               │
│            │   PrimeTop Logo │               │
│            │   教师端         │               │
│            └─────────────────┘               │
│                                              │
│   ┌──────────────────────────────────┐       │
│   │ 📱 手机号                        │       │
│   └──────────────────────────────────┘       │
│   ┌──────────────────────────────────┐       │
│   │ 🔒 密码           [👁 显示]      │       │
│   └──────────────────────────────────┘       │
│                                              │
│   ┌──────────────────────────────────┐       │
│   │          登  录                  │       │
│   └──────────────────────────────────┘       │
│                                              │
│   忘记密码？  |  注册教师账号                   │
│                                              │
│   ─────── 其他登录方式 ───────                │
│        [企业微信]  [钉钉]                      │
│                                              │
└──────────────────────────────────────────────┘
```

**关键交互：**

```typescript
// pages/login/LoginPage.tsx

interface LoginFormData {
  phone: string;
  password: string;
}

const LoginPage: React.FC = () => {
  const [form, setForm] = useState<LoginFormData>({ phone: '', password: '' });
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleLogin = async () => {
    // 1. 前端校验
    if (!validatePhone(form.phone)) {
      setError('请输入正确的手机号');
      return;
    }
    if (form.password.length < 8) {
      setError('密码长度不能少于8位');
      return;
    }

    setLoading(true);
    setError(null);

    try {
      // 2. 调用登录接口
      const result = await authApi.teacherLogin({
        phone: form.phone,
        password: form.password,
        clientType: 'web_teacher',  // 区分教师端
      });

      // 3. 存储 Token
      authStorage.setTokens({
        accessToken: result.accessToken,
        refreshToken: result.refreshToken,
      });

      // 4. 加载教师档案
      const profile = await teacherApi.getProfile();
      teacherStore.auth = {
        token: result.accessToken,
        refreshToken: result.refreshToken,
        certStatus: profile.certStatus,
        teacherId: profile.teacherId,
        name: profile.name,
        school: profile.school,
        subjects: profile.subjects,
      };

      // 5. 根据认证状态路由
      if (profile.certStatus === 'none') {
        navigate('/certification');
      } else {
        navigate('/main/dashboard');
      }

    } catch (err) {
      if (err.code === 'AUTH_003') {
        setError('手机号或密码错误');
      } else if (err.code === 'AUTH_010') {
        setError('该账号未注册为教师，请先注册');
      } else {
        setError('登录失败，请稍后重试');
      }
    } finally {
      setLoading(false);
    }
  };

  // ... render
};
```

**状态码处理：**

| 错误码 | 场景 | 处理 |
|--------|------|------|
| AUTH_003 | 手机号或密码错误 | 提示用户重新输入 |
| AUTH_005 | 账号被冻结 | 提示联系客服 |
| AUTH_010 | 非教师账号 | 引导注册教师身份 |
| AUTH_011 | 账号待审核 | 跳转认证状态页 |

#### 3.1.2 注册页 `/register`

**注册流程状态机：**

```
                   ┌──────────┐
          ┌───────>│ 输入手机号 │<──────┐
          │        └─────┬────┘       │
          │              │ 下一步      │ 重新发送
          │        ┌─────▼────┐       │
          │        │ 输入验证码 │───────┘
          │        └─────┬────┘
          │              │ 验证成功
          │        ┌─────▼────┐
          │        │ 设置密码   │
          │        └─────┬────┘
          │              │
          │        ┌─────▼────┐
          │        │ 填写基本信息│
          │        │ 姓名/学校  │
          │        │ 学科/年级  │
          │        └─────┬────┘
          │              │
          │        ┌─────▼────┐
          │        │ 上传资格证 │── 验证码过期
          │        │ (可选)     │───────────┘
          │        └─────┬────┘
          │              │ 提交
          │        ┌─────▼────┐
          │        │ 注册完成   │
          │        │ → 跳转认证 │
          │        └──────────┘
          └── 重试
```

**数据结构定义：**

```typescript
// types/teacher-registration.ts

/** 教师注册请求 */
interface TeacherRegisterRequest {
  phone: string;
  verificationCode: string;
  password: string;
  profile: {
    realName: string;
    gender: 'male' | 'female';
    schoolName: string;
    schoolId?: string;            // 从学校库匹配
    teachingSubjects: {           // 所教学科（可多个）
      subject: string;            // 学科代码，如 "MATH"
      grades: string[];           // 所教年级，如 ["7", "8"]
    }[];
    title?: string;               // 职称：初级/中级/高级/特级
    yearsOfExperience?: number;
  };
  certification?: {
    certificateType: string;      // 教师资格证类型
    certificateNumber: string;
    certificateImageUrls: string[];
  };
}

/** 注册步骤枚举 */
type RegisterStep = 'phone' | 'verify' | 'password' | 'profile' | 'cert' | 'done';

/** 注册步骤状态 */
interface RegistrationState {
  currentStep: RegisterStep;
  phone: string;
  verificationSid: string | null;
  password: string;
  profile: Partial<TeacherRegisterRequest['profile']>;
  certification: Partial<TeacherRegisterRequest['certification']>;
  submitting: boolean;
  error: string | null;
}
```

---

### 3.2 教师工作台首页 `/main/dashboard`

#### 3.2.1 页面布局

```
┌──────────────────────────────────────────────────────────────┐
│  顶部栏                                                       │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  👋 早上好，张老师                                        │ │
│  │  🏫 XX中学 · 数学 · 初二(3)班                    [切换▼] │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─ 今日待办 ──────────────────────────────────────────────┐ │
│  │                                                         │ │
│  │  📝 待批改作业    3 份                                    │ │
│  │  ├─ 初二(3)班 · 第八章测试     28/35 已提交  [去批改 >]   │ │
│  │  ├─ 初二(5)班 · 周末作业       42/45 已提交  [去批改 >]   │ │
│  │  └─ 初二(3)班 · 函数专题练习   15/35 已提交  [查看 >]     │
│  │                                                         │ │
│  │  📢 待处理通知    2 条                                    │ │
│  │  └─ 3位家长确认了家长会通知  [查看详情 >]                   │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─ 数据概览 ─────────────────────────────────────────────┐ │
│  │                                                         │ │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐               │ │
│  │  │  3   │  │ 112  │  │ 78%  │  │ 15   │               │ │
│  │  │管理班级│  │学生总数│  │本周活跃│  │薄弱预警│               │ │
│  │  └──────┘  └──────┘  └──────┘  └──────┘               │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─ 班级薄弱知识点 TOP 5 ─────────────────────────────────┐ │
│  │                                                         │ │
│  │  ┌───────────────────────────────────────────────────┐ │ │
│  │  │  📊 横向条形图                                      │ │ │
│  │  │  二次函数图像变换  ████████████████  62% 掌握       │ │ │
│  │  │  全等三角形判定    ██████████████    68% 掌握       │ │ │
│  │  │  因式分解-提公因式  ██████████████   71% 掌握       │ │ │
│  │  │  一元二次方程应用题  ████████████     73% 掌握      │ │ │
│  │  │  相似三角形性质    ████████████      76% 掌握       │ │ │
│  │  └───────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─ 最近作业完成率趋势 ───────────────────────────────────┐ │
│  │                                                         │ │
│  │  ┌───────────────────────────────────────────────────┐ │ │
│  │  │  📈 折线图（最近4次作业）                            │ │ │
│  │  │  100% ┤                                            │ │ │
│  │  │   80% ┤    ●───●                                   │ │ │
│  │  │   60% ┤         ●───●                              │ │ │
│  │  │   40% ┤                   ●                        │ │ │
│  │  │        第八章  函数专题  周末作业  综合测试            │ │ │
│  │  └───────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

#### 3.2.2 数据加载策略

```typescript
// pages/dashboard/useDashboardData.ts

interface DashboardData {
  todayTasks: {
    pendingGrading: GradingTask[];
    pendingNotices: NoticeTask[];
  };
  overview: {
    classCount: number;
    studentTotal: number;
    weeklyActiveRate: number;
    weakPointAlerts: number;
  };
  weakKnowledgePoints: WeakKnowledgePoint[];
  assignmentTrend: AssignmentTrendPoint[];
}

/** 自定义 Hook：首页数据加载 */
function useDashboardData(classId: string | null) {
  const [data, setData] = useState<DashboardData | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  // 首次加载：并行请求所有数据
  useEffect(() => {
    let cancelled = false;

    async function loadAll() {
      setLoading(true);
      try {
        const [tasks, overview, weakPoints, trend] = await Promise.all([
          teacherApi.getTodayTasks(classId),
          teacherApi.getOverview(classId),
          teacherApi.getWeakKnowledgePoints(classId, { top: 5 }),
          teacherApi.getAssignmentTrend(classId, { limit: 4 }),
        ]);

        if (!cancelled) {
          setData({
            todayTasks: tasks,
            overview,
            weakKnowledgePoints: weakPoints,
            assignmentTrend: trend,
          });
        }
      } catch (err) {
        if (!cancelled) setError(err as Error);
      } finally {
        if (!cancelled) setLoading(false);
      }
    }

    loadAll();
    return () => { cancelled = true; };
  }, [classId]);

  // 自动刷新：每 60s 轮询待办数量（轻量）
  useEffect(() => {
    const timer = setInterval(async () => {
      try {
        const taskCount = await teacherApi.getTaskCounts(classId);
        setData(prev => prev ? {
          ...prev,
          todayTasks: { ...prev.todayTasks, ...taskCount },
        } : prev);
      } catch { /* 静默失败 */ }
    }, 60_000);
    return () => clearInterval(timer);
  }, [classId]);

  return { data, loading, error, refetch: () => loadAll() };
}
```

#### 3.2.3 API 接口

**GET `/api/v1/teacher/dashboard/tasks`**

获取今日待办任务。

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| classId | string | 否 | 筛选指定班级，不传则聚合所有班级 |

**响应体：**

```json
{
  "code": 0,
  "data": {
    "pendingGrading": [
      {
        "assignmentId": "asg_001",
        "title": "第八章测试",
        "className": "初二(3)班",
        "submitCount": 28,
        "totalCount": 35,
        "deadline": "2026-06-08T23:59:59Z",
        "aiGradedCount": 25,
        "manualReviewCount": 3
      }
    ],
    "pendingNotices": [
      {
        "noticeId": "ntc_001",
        "title": "家长会通知",
        "confirmCount": 3,
        "totalCount": 35,
        "unconfirmedParents": ["张三家长", "李四家长"]
      }
    ]
  }
}
```

**GET `/api/v1/teacher/dashboard/overview`**

获取数据概览。

**GET `/api/v1/teacher/dashboard/weak-points`**

获取班级薄弱知识点排行。

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| classId | string | 否 | 筛选班级 |
| top | int | 否 | 返回数量，默认 5 |
| subject | string | 否 | 筛选学科 |

**响应体：**

```json
{
  "code": 0,
  "data": [
    {
      "knowledgePointId": "kp_2831",
      "name": "二次函数图像变换",
      "masteryRate": 0.62,
      "errorCount": 89,
      "affectedStudentCount": 18,
      "relatedChapter": "八年级下册 · 第二十一章",
      "trend": "down"
    }
  ]
}
```

---

### 3.3 班级管理 `/main/classes`

#### 3.3.1 班级列表页

**布局：**

```
┌──────────────────────────────────────────────────────────────┐
│  班级管理                                      [+ 创建班级]  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  筛选: [全部学科 ▼]  [全部年级 ▼]  [本学期 ▼]    🔍 搜索班级  │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  初二(3)班                                    [···]     │ │
│  │  数学 · 人教版 · 2025-2026学年下学期                      │ │
│  │  👥 35人  |  📝 12次作业  |  📈 平均掌握度 76%            │ │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐               │ │
│  │  │ 名册  │  │ 作业  │  │ 学情  │  │ 通知  │               │ │
│  │  └──────┘  └──────┘  └──────┘  └──────┘               │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  初二(5)班                                    [···]     │ │
│  │  数学 · 人教版 · 2025-2026学年下学期                      │ │
│  │  👥 45人  |  📝 10次作业  |  📈 平均掌握度 71%            │ │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐               │ │
│  │  │ 名册  │  │ 作业  │  │ 学情  │  │ 通知  │               │ │
│  │  └──────┘  └──────┘  └──────┘  └──────┘               │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  ── 已归档 ──────────────────────────────────────────────── │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  初一(2)班                              [已归档]  [恢复] │ │
│  │  数学 · 人教版 · 2024-2025学年上学期                      │ │
│  │  👥 40人  |  📝 20次作业  |  📈 平均掌握度 82%            │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**API 接口：**

**GET `/api/v1/teacher/classes`**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| subject | string | 否 | 学科筛选 |
| grade | string | 否 | 年级筛选 |
| semester | string | 否 | 学期筛选 |
| includeArchived | bool | 否 | 是否包含已归档班级，默认 false |
| keyword | string | 否 | 搜索班级名称 |
| page | int | 否 | 页码，默认 1 |
| pageSize | int | 否 | 每页数量，默认 20 |

**响应体：**

```json
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": "cls_001",
        "name": "初二(3)班",
        "subject": "MATH",
        "subjectName": "数学",
        "grade": "8",
        "gradeName": "初二",
        "textbookVersion": "PEP",
        "semester": "2025-2026-2",
        "studentCount": 35,
        "assignmentCount": 12,
        "avgMasteryRate": 0.76,
        "isArchived": false,
        "createdAt": "2025-09-01T08:00:00Z",
        "inviteCode": "ABC123",
        "inviteCodeExpiry": "2026-09-01T08:00:00Z"
      }
    ],
    "total": 3,
    "page": 1,
    "pageSize": 20
  }
}
```

#### 3.3.2 创建班级页 `/main/classes/create`

**表单字段：**

```typescript
// types/class-create.ts

interface ClassCreateForm {
  name: string;                    // 班级名称，如"初二(3)班"
  subject: string;                 // 学科代码
  grade: string;                   // 年级
  textbookVersion: string;         // 教材版本
  semester: string;                // 学期，如 "2025-2026-2"
  description?: string;            // 班级描述（可选）
  maxStudents?: number;            // 最大学生数（可选，默认50）
  settings: {
    autoApprove: boolean;          // 学生加入是否自动通过
    allowStudentInvite: boolean;   // 是否允许学生邀请同学
    homeworkDeadlineReminder: boolean; // 作业截止前提醒
    reminderHoursBefore: number;   // 提前多少小时提醒，默认 2
  };
}

/** 表单校验规则 */
const classCreateRules: ValidationRules<ClassCreateForm> = {
  name: { required: true, minLength: 2, maxLength: 30 },
  subject: { required: true },
  grade: { required: true },
  textbookVersion: { required: true },
  semester: { required: true },
  maxStudents: { min: 1, max: 200 },
  'settings.reminderHoursBefore': { min: 1, max: 72 },
};
```

**创建成功后流程：**

```
提交表单
  │
  ▼
POST /api/v1/teacher/classes
  │
  ▼ 返回 classId + inviteCode
  │
  ▼
┌────────────────────────────────────────┐
│  ✅ 班级创建成功！                       │
│                                        │
│  班级名称：初二(3)班                     │
│  邀请码：ABC123                         │
│  有效期：2026-06-07 ~ 2026-09-07        │
│                                        │
│  ┌──────┐  ┌──────┐  ┌──────┐         │
│  │复制码 │  │二维码 │  │分享链接│         │
│  └──────┘  └──────┘  └──────┘         │
│                                        │
│  学生加入方式：                          │
│  1. 在APP中输入邀请码                    │
│  2. 扫描二维码                           │
│  3. 点击分享链接                         │
│                                        │
│  [进入班级]      [继续创建]              │
└────────────────────────────────────────┘
```

#### 3.3.3 学生名册管理 `/main/classes/:id/roster`

**数据结构：**

```typescript
// types/class-roster.ts

/** 学生名册项 */
interface StudentRosterItem {
  studentId: string;
  name: string;
  avatar?: string;
  studentNumber?: string;          // 学号
  joinDate: string;
  status: 'active' | 'suspended';  // 在班/停用
  parentBound: boolean;            // 是否已绑定家长
  lastActiveAt: string;
  assignmentCompletionRate: number; // 作业完成率
  avgScore: number;                // 平均分
  tags: string[];                  // 教师自定义标签
}

/** 批量操作 */
type BatchAction = 'addTag' | 'removeTag' | 'export' | 'remove';
```

**页面布局：**

```
┌──────────────────────────────────────────────────────────────┐
│  初二(3)班 · 学生名册                           [+ 添加学生]  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  👥 35人  |  已绑定家长 28人  |  本周活跃 30人                  │
│                                                              │
│  筛选: [全部标签 ▼]  [全部状态 ▼]     🔍 搜索学生              │
│                                                              │
│  ☐  ┌────────────────────────────────────────────────────┐  │
│  │  │ ☐  王小明  #01  ┃  数学  ┃  完成